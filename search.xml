<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>算法设计</title>
    <url>/algorithm/algorithm-design.html</url>
    <content><![CDATA[<p>算法（Algorithm）是指解题方案的准确而完整的描述，是一系列解决问题的清晰指令，算法代表着用系统的方法描述解决问题的策略机制。也就是说，能够对一定规范的输入，在有限时间内获得所要求的输出。如果一个算法有缺陷，或不适合于某个问题，执行这个算法将不会解决这个问题。不同的算法可能用不同的时间、空间或效率来完成同样的任务。一个算法的优劣可以用空间复杂度与时间复杂度来衡量。</p>
<p><img src="https://s1.ax1x.com/2020/08/19/d3p62R.png" alt="算法.png"></p>
<a id="more"></a>
<h1 id="hash算法"><a class="header-anchor" href="#hash算法">¶</a>Hash算法</h1>
<p>hash算法又称散列函数是一种公开的数学函数，函数输入的信息叫做报文，匀后所得到的结果叫做散列码或者叫做消息摘要。</p>
<p>散列函数h = H(M)，具有如下特点:</p>
<ul>
<li>不同内容的报文具有不同的散列值。</li>
<li>散列函数是单向的，不可逆。</li>
<li>对于任何一个报文，无法预知它的散列码。</li>
<li>散列码具有固定长度。</li>
</ul>
<p>常见的散列函数有MD5、SHA和HMAC等。</p>
<p>MD5（Message Digest 5）是一种非常著名的散列算法，已成为国际标准。MD5散列算法对输入的任意长度的消息产生128位（16字节）长度的消息摘要，MD5算法包括以下4个步骤：</p>
<ul>
<li>
<p>按位补充数据：补充后的数据长度模512后于448，也就是说数据补位后，其位数长度只差64位(bit)就是512的整数倍。即便是这个数据的位数对512求模的结果正好是448也必须进行补位。</p>
</li>
<li>
<p>补足长度：将数据长度表示为二进制，如果长度超过64位，则获取其低64位；如果长度不足64位，则在其高位补0。然后将得到的64位报文添加在经过填充的报文后，得到的结果数据长度正好是512的整数倍。也就是说长度正好是16个(32bit) 字的整数倍。</p>
</li>
<li>
<p>初始化MD缓存器：MD5运算要用到一个128位的MD5缓存器，用来保存中间变量和最终结果。该缓存器又可看成是4个32位的寄存器A、B、C、D，初始化为 :</p>
<p>A ： 01 23 45 67</p>
<p>B： 89 ab cd ef</p>
<p>C： fe dc ba 98</p>
<p>D： 76 54 32 10</p>
</li>
<li>
<p>处理数据段：首先定义4个非线性函数F、G、H、I，对输入的报文运算以512位数据段为单位进行处理。对每个数据段都要进行4轮的逻辑处理，在4轮中分别使用4个不同的函数F、G、H、I。每一轮以ABCD和当前的512位的块为输入，处理后送入ABCD(128位)。</p>
</li>
</ul>
<p>信息摘要最终处理成以A, B, C, D 的形式输出。也就是开始于A的低位在前的顺序字节，结束于D的高位在前的顺序字节。</p>
<h1 id="算法复杂度"><a class="header-anchor" href="#算法复杂度">¶</a>算法复杂度</h1>
<p>算法复杂度分为时间复杂度和空间复杂度。其作用： 时间复杂度是指执行算法所需要的计算工作量；而空间复杂度是指执行这个算法所需要的内存空间。（算法的复杂性体运行该算法时的计算机所需资源的多少上，计算机资源最重要的是时间和空间（即寄存器）资源，因此复杂度分为时间和空间复杂度。）</p>
<h2 id="时间复杂度"><a class="header-anchor" href="#时间复杂度">¶</a>时间复杂度</h2>
<p>算法的时间复杂度是一个函数，它定性<strong>描述该算法的运行时间</strong>。这是一个代表算法输入值的字符串的长度的函数。时间复杂度常用大O符号表述，不包括这个函数的低阶项和首项系数。使用这种方式时，时间复杂度可被称为是渐近的，亦即考察输入值大小趋近无穷时的情况。</p>
<p>常见时间复杂度：</p>
<ul>
<li>常数阶O(1)</li>
<li>对数阶O(logN)</li>
<li>线性阶O(n)</li>
<li>线性对数阶O(nlogN)</li>
<li>平方阶O(n²)</li>
<li>立方阶O(n³)</li>
<li>K次方阶O(n^k)</li>
<li>指数阶(2^n)</li>
</ul>
<h2 id="空间复杂度"><a class="header-anchor" href="#空间复杂度">¶</a>空间复杂度</h2>
<p>空间复杂度(Space Complexity)是对一个算法在运行过程中临时占用存储空间大小的量度，记做S(n)=O(f(n))。</p>
<h1 id="二分查找"><a class="header-anchor" href="#二分查找">¶</a>二分查找</h1>
<p>二分查找也称折半查找（Binary Search），它是一种效率较高的查找方法。对数据存储有如下要求：</p>
<ul>
<li>必须采用顺序存储结构</li>
<li>必须按关键字大小有需排列</li>
</ul>
<p>查找流程大致如下图所示：</p>
<p><img src="https://bkimg.cdn.bcebos.com/pic/3ac79f3df8dcd1003d97f14e728b4710b9122f51?x-bce-process=image/watermark,image_d2F0ZXIvYmFpa2U5Mg==,g_7,xp_5,yp_5" alt="二分查找.png"></p>
<p>空间复杂度：O(1)</p>
<p>时间复杂度：O(log<sub>2</sub><sup>n</sup>)，最差情况查找的数据为最大或者最小，查找次数最多O(logn)</p>
<h1 id="排序算法"><a class="header-anchor" href="#排序算法">¶</a>排序算法</h1>
<h2 id="算法分类"><a class="header-anchor" href="#算法分类">¶</a>算法分类</h2>
<p>十种常见排序算法可以分为两大类：</p>
<ul>
<li><strong>比较类排序</strong>：通过比较来决定元素间的相对次序，由于其时间复杂度不能突破O(nlogn)，因此也称为非线性时间比较类排序。</li>
<li><strong>非比较类排序</strong>：不通过比较来决定元素间的相对次序，它可以突破基于比较排序的时间下界，以线性时间运行，因此也称为线性时间非比较类排序。</li>
</ul>
<p><img src="https://s1.ax1x.com/2020/08/19/d3SN7D.png" alt="排序算法分类.png"></p>
<h2 id="算法复杂度-v2"><a class="header-anchor" href="#算法复杂度-v2">¶</a>算法复杂度</h2>
<p><img src="https://s1.ax1x.com/2020/08/19/d3SgHS.png" alt="排序算法复杂度"></p>
<p>其中：</p>
<ul>
<li><strong>稳定</strong>：如果a原本在b前面，而a=b，排序之后a仍然在b的前面。</li>
<li><strong>不稳定</strong>：如果a原本在b的前面，而a=b，排序之后 a 可能会出现在 b 的后面。</li>
<li><strong>时间复杂度</strong>：对排序数据的总的操作次数。反映当n变化时，操作次数呈现什么规律。</li>
<li>**空间复杂度：**是指算法在计算机内执行时所需存储空间的度量，它也是数据规模n的函数。</li>
</ul>
<h2 id="逗比排序"><a class="header-anchor" href="#逗比排序">¶</a>逗比排序</h2>
<ul>
<li>睡眠排序：这是一个思想比较简单，脑洞巨大的算法 – 我们知道sleep方法可以让一个线程睡眠s毫秒，如果需要对一个有n个数的数列进行排序，我们何不为每个数创建一个线程，然后让每个线程都睡眠该数的时间，那么对于越小的数，其睡眠时间越短，越大的数，其睡眠时间越长，最后就使得所有元素完成“排序”了。</li>
<li>猴子排序（随机排序）：猴子排序引用了无限猴子定理：即一只猴子随机不停的敲击键盘，如果时间足够长，那么它总有可能会打出特定的文本，比如莎士比亚全集？，算法通过不停的随机排列数组，直到数组有序。</li>
</ul>
<h1 id="动态规划"><a class="header-anchor" href="#动态规划">¶</a>动态规划</h1>
<p>动态规划（英语：Dynamic programming，简称DP）是一种在数学、管理科学、计算机科学、经济学和生物信息学中使用的，通过把原问题分解为相对简单的子问题的方式求解复杂问题的方法。</p>
<ul>
<li>
<p>重叠子问题，保存计算结果存到表中，遇到重复子问题从表中获取，可以极大减少计算量</p>
<ul>
<li>最长公共子序列（LCS）：是一个在一个序列集合中（通常为两个序列）用来查找所有序列中最长子序列的问题。</li>
<li>Floyd-Warshall算法（英语：Floyd-Warshall algorithm），中文亦称弗洛伊德算法或佛洛依德算法，是解决任意两点间的最短路径的一种算法，可以正确处理有向图或负权（但不可存在负权回路）的最短路径问题，同时也被用于计算有向图的传递闭包。</li>
</ul>
</li>
<li>
<p>最优子结构，局部最优解–》全局最优解</p>
<ul>
<li>
<p>贪心算法</p>
<p>贪心算法（英语：greedy algorithm），又称贪婪算法，是一种在每一步选择中都采取在当前状态下最好或最优（即最有利）的选择，从而希望导致结果是最好或最优的算法。</p>
</li>
<li>
<p>01背包</p>
</li>
<li>
<p>最小生成树</p>
</li>
<li>
<p>哈弗曼编码</p>
</li>
</ul>
</li>
</ul>
<h1 id="数据结构"><a class="header-anchor" href="#数据结构">¶</a>数据结构</h1>
<h2 id="数组"><a class="header-anchor" href="#数组">¶</a>数组</h2>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f6c84433da?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="数组.png"></p>
<h2 id="链表"><a class="header-anchor" href="#链表">¶</a>链表</h2>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f6c0eddb88?imageslim" alt="链表.png"></p>
<p><strong>链表和数组对比</strong></p>
<p>链表和数组在实际的使用过程中需要根据自身的优劣势进行选择。链表和数组的异同点也是面试中高频的考察点之一。这里对单链表和数组的区别进行了对比和总结。</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f6c83bdd33?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="链表和数组对比.png"></p>
<h2 id="跳表"><a class="header-anchor" href="#跳表">¶</a>跳表</h2>
<p>从上面的对比中可以看出，链表虽然通过增加指针域提升了自由度，但是却导致数据的查询效率恶化。特别是当链表长度很长的时候，对数据的查询还得从头依次查询，这样的效率会更低。跳表的产生就是为了解决链表过长的问题，通过增加链表的多级索引来加快原始链表的查询效率。这样的方式可以让查询的时间复杂度从O(n)提升至O(logn)。</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f6bbe1231f?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="跳表.png"></p>
<p>跳表通过增加的多级索引能够实现高效的动态插入和删除，其效率和红黑树和平衡二叉树不相上下。目前redis和levelDB都有用到跳表。</p>
<p>从上图可以看出，索引级的指针域除了指向下一个索引位置的指针，还有一个down指针指向低一级的链表位置，这样才能实现跳跃查询的目的。</p>
<ul>
<li>ConcurrentSkipListMap</li>
<li>ConcurrentSkipListSet</li>
<li>redis的跳跃表(skiplist)</li>
</ul>
<h2 id="栈"><a class="header-anchor" href="#栈">¶</a>栈</h2>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f6cccaf5f0?imageslim" alt="栈.gif"></p>
<h2 id="队列"><a class="header-anchor" href="#队列">¶</a>队列</h2>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f6f03e72b3?imageslim" alt="队列.png"></p>
<h2 id="树"><a class="header-anchor" href="#树">¶</a>树</h2>
<p>树作为一种树状的数据结构，其数据节点之间的关系也如大树一样，将有限个节点根据不同层次关系进行排列，从而形成数据与数据之间的父子关系。常见的数的表示形式更接近“倒挂的树”，因为它将根朝上，叶朝下。</p>
<h3 id="二叉树"><a class="header-anchor" href="#二叉树">¶</a>二叉树</h3>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f6e74c7c8b?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="二叉树.png"></p>
<p>二叉树每个结点最多有两个子树，二叉树根据结点的排列和数量还可进一度划分：</p>
<ul>
<li><strong>完全二叉树</strong>：除了最后一层结点，其它层的结点数都达到了最大值；同时最后一层的结点都是按照从左到右依次排布。</li>
<li><strong>满二叉树</strong>：除了最后一层，其它层的结点都有两个子结点。</li>
<li><strong>二叉排序树</strong>：是一棵空树，或者：若它的左子树不空，则左子树上所有结点的值均小于它的根结点的值；若它的右子树不空，则右子树上所有结点的值均大于它的根结点的值；它的左、右子树也分别为二叉排序树。</li>
<li>平衡二叉树：</li>
</ul>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f6f5235ee6?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="平衡二叉树.png"></p>
<p>平衡二叉树的产生是为了解决二叉排序树在插入时发生线性排列的现象。由于二叉排序树本身为有序，当插入一个有序程度十分高的序列时，生成的二叉排序树会持续在某个方向的字数上插入数据，导致最终的二叉排序树会退化为链表，从而使得二叉树的查询和插入效率恶化。</p>
<h3 id="红黑树"><a class="header-anchor" href="#红黑树">¶</a>红黑树</h3>
<p>平衡二叉树（AVL）为了追求高度平衡，需要通过平衡处理使得左右子树的高度差必须小于等于1。高度平衡带来的好处是能够提供更高的搜索效率，其最坏的查找时间复杂度都是O(logN)。但是由于需要维持这份高度平衡，所付出的代价就是当对树种结点进行插入和删除时，需要经过多次旋转实现复衡。这导致AVL的插入和删除效率并不高。</p>
<p>为了解决这样的问题，能不能找一种结构能够兼顾搜索和插入删除的效率呢？这时候红黑树便申请出战了。</p>
<p>红黑树具有五个特性：</p>
<blockquote>
<ol>
<li>每个结点要么是红的要么是黑的。</li>
<li>根结点是黑的。</li>
<li>每个叶结点（叶结点即指树尾端NIL指针或NULL结点）都是黑的。</li>
<li>如果一个结点是红的，那么它的两个儿子都是黑的。</li>
<li>对于任意结点而言，其到叶结点树尾端NIL指针的每条路径都包含相同数目的黑结点。</li>
</ol>
</blockquote>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f72c8875f4?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="img">红黑树通过将结点进行红黑着色，使得原本高度平衡的树结构被稍微打乱，平衡程度降低。红黑树不追求完全平衡，只要求达到部分平衡。这是一种折中的方案，大大提高了结点删除和插入的效率。Java8中HashMap使用数组+红黑树作为底层的数据结构。</p>
<p><strong>红黑树VS平衡二叉树</strong></p>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f75866b55d?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="红黑树VS平衡二叉树.png"></p>
<h2 id="图"><a class="header-anchor" href="#图">¶</a>图</h2>
<p>图结构一般包括顶点和边，顶点通常用圆圈来表示，边就是这些圆圈之间的连线。边还可以根据顶点之间的关系设置不同的权重，默认权重相同皆为1。此外根据边的方向性，还可将图分为有向图和无向图。</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f75600f8a9?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="图.png"></p>
<h3 id="邻接矩阵"><a class="header-anchor" href="#邻接矩阵">¶</a>邻接矩阵</h3>
<p>目前常用的图存储方式为邻接矩阵，通过所有顶点的二维矩阵来存储两个顶点之间是否相连，或者存储两顶点间的边权重。</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f763cecc6e?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="img"></p>
<p>无向图的邻接矩阵是一个对称矩阵，是因为边不具有方向性，若能从此顶点能够到达彼顶点，那么彼顶点自然也能够达到此顶点。此外，由于顶点本身与本身相连没有意义，所以在邻接矩阵中对角线上皆为0。</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f764d72686?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="img"></p>
<p>有向图由于边具有方向性，因此彼此顶点之间并不能相互达到，所以其邻接矩阵的对称性不再。</p>
<p>用邻接矩阵可以直接从二维关系中获得任意两个顶点的关系，可直接判断是否相连。但是在对矩阵进行存储时，却需要完整的一个二维数组。若图中顶点数过多，会导致二维数组的大小剧增，从而占用大量的内存空间。</p>
<p>而根据实际情况可以分析得，图中的顶点并不是任意两个顶点间都会相连，不是都需要对其边上权重进行存储。那么存储的邻接矩阵实际上会存在大量的0。虽然可以通过稀疏表示等方式对稀疏性高的矩阵进行关键信息的存储，但是却增加了图存储的复杂性。</p>
<p>因此，为了解决上述问题，一种可以只存储相连顶点关系的邻接表应运而生。</p>
<h3 id="邻接表"><a class="header-anchor" href="#邻接表">¶</a>邻接表</h3>
<p>在邻接表中，图的每一个顶点都是一个链表的头节点，其后连接着该顶点能够直接达到的相邻顶点。相较于无向图，有向图的情况更为复杂，因此这里采用有向图进行实例分析。</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f7826669a9?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="img">在邻接表中，每一个顶点都对应着一条链表，链表中存储的是顶点能够达到的相邻顶点。存储的顺序可以按照顶点的编号顺序进行。比如上图中对于顶点B来说，其通过有向边可以到达顶点A和顶点E，那么其对应的邻接表中的顺序即B-&gt;A-&gt;E，其它顶点亦如此。</p>
<p>通过邻接表可以获得从某个顶点出发能够到达的顶点，从而省去了对不相连顶点的存储空间。然而，这还不够。对于有向图而言，图中有效信息除了从顶点“指出去”的信息，还包括从别的顶点“指进来”的信息。这里的“指出去”和“指进来”可以用出度和入度来表示。</p>
<blockquote>
<p>入度：有向图的某个顶点作为终点的次数和。</p>
<p>出度：有向图的某个顶点作为起点的次数和。</p>
</blockquote>
<p>由此看出，在对有向图进行表示时，邻接表只能求出图的出度，而无法求出入度。这个问题很好解决，那就是增加一个表用来存储能够到达某个顶点的相邻顶点。这个表称作逆邻接表。</p>
<h3 id="逆邻接表"><a class="header-anchor" href="#逆邻接表">¶</a>逆邻接表</h3>
<p>逆邻接表与邻接表结构类似，只不过图的顶点链接着能够到达该顶点的相邻顶点。也就是说，邻接表时顺着图中的箭头寻找相邻顶点，而逆邻接表时逆着图中的箭头寻找相邻顶点。</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f782bcfa08?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="img"></p>
<p>邻接表和逆邻接表的共同使用下，就能够把一个完整的有向图结构进行表示。可以发现，邻接表和逆邻接表实际上有一部分数据时重合的，因此可以将两个表合二为一，从而得到了所谓的十字链表。</p>
<h3 id="十字链表"><a class="header-anchor" href="#十字链表">¶</a>十字链表</h3>
<p>十字链表似乎很简单，只需要通过相同的顶点分别链向以该顶点为终点和起点的相邻顶点即可。</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f782d0df9b?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="img"></p>
<p>但这并不是最优的表示方式。虽然这样的方式共用了中间的顶点存储空间，但是邻接表和逆邻接表的链表节点中重复出现的顶点并没有得到重复利用，反而是进行了再次存储。因此，上图的表示方式还可以进行进一步优化。</p>
<p>十字链表优化后，可通过扩展的顶点结构和边结构来进行正逆邻接表的存储：（下面的弧头可看作是边的箭头那端，弧尾可看作是边的圆点那端）</p>
<blockquote>
<p><strong>data</strong>：用于存储该顶点中的数据；</p>
<p><strong>firstin指针</strong>：用于连接以当前顶点为弧头的其他顶点构成的链表，即从别的顶点指进来的顶点；</p>
<p><strong>firstout指针</strong>：用于连接以当前顶点为弧尾的其他顶点构成的链表，即从该顶点指出去的顶点；</p>
</blockquote>
<p>边结构通过存储两个顶点来确定一条边，同时通过分别代表这两个顶点的指针来与相邻顶点进行链接：</p>
<blockquote>
<p><strong>tailvex</strong>：用于存储作为弧尾的顶点的编号；</p>
<p><strong>headvex</strong>：用于存储作为弧头的顶点的编号；</p>
<p><strong>headlink</strong> <strong>指针</strong>：用于链接下一个存储作为弧头的顶点的节点；</p>
<p><strong>taillink</strong> <strong>指针</strong>：用于链接下一个存储作为弧尾的顶点的节点；</p>
</blockquote>
<p><img src="https://user-gold-cdn.xitu.io/2020/5/24/172472f787dc80e7?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="img"></p>
<p>以上图为例子，对于顶点A而言，其作为起点能够到达顶点E。因此在邻接表中顶点A要通过边<code>AE</code>（即边04）指向顶点E，顶点A的<code>firstout</code>指针需要指向边04的<code>tailvex</code>。同时，从B出发能够到达A，所以在逆邻接表中顶点A要通过边<code>AB</code>（即边10）指向B，顶点A的<code>firstin</code>指针需要指向边10的弧头，即<code>headlink</code>指针。依次类推。</p>
<p>十字链表采用了一种看起来比较繁乱的方式对边的方向性进行了表示，能够在尽可能降低存储空间的情况下增加指针保留顶点之间的方向性。具体的操作可能一时间不好弄懂，建议多看几次上图，弄清指针指向的意义，明白正向和逆向邻接表的表示。</p>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>算法</tag>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>单例模式</title>
    <url>/design-patterns/singleton-pattern.html</url>
    <content><![CDATA[<p>在GoF的23种设计模式中，单例模式是比较简单的一种。然而，有时候越是简单的东西越容易出现问题。下面就单例设计模式详细的探讨一下。<br>
所谓单例模式，简单来说，就是在整个应用中保证只有一个类的实例存在。就像是Java Web中的application，也就是提供了一个全局变量，用处相当广泛，比如保存全局数据，实现全局性的操作等。</p>
<p><img src="https://i.loli.net/2019/04/13/5cb18b0bbbd52.png" alt="d602986de5753f457ee357a85bd45209.png"></p>
<a id="more"></a>
<h1 id="最简单实现-饿汉"><a class="header-anchor" href="#最简单实现-饿汉">¶</a>最简单实现(饿汉)</h1>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Singleton singleton = <span class="keyword">new</span> Singleton();</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> singleton;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span></span>&#123;</span><br><span class="line">    </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>外部使用者如果需要使用SingletonClass的实例，只能通过getInstance()方法，并且它的构造方法是private的，这样就保证了只能有一个对象存在。</p>
<h1 id="性能优化-lazy-loaded-懒汉"><a class="header-anchor" href="#性能优化-lazy-loaded-懒汉">¶</a>性能优化–lazy loaded(懒汉)</h1>
<p>上面的代码虽然简单，但是有一个问题----无论这个类是否被使用，都会创建一个instance对象。如果这个创建很耗时，比如说链接10000次数据库（夸张一点啦…），并且这个类还不一定会被使用，那么这个创建过程就是无用的，怎么办呢？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClass</span> </span>&#123; </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> SingletonClass instance = <span class="keyword">null</span>; </span><br><span class="line">    </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SingletonClass <span class="title">getInstance</span><span class="params">()</span> </span>&#123; </span><br><span class="line">    <span class="keyword">if</span>(instance == <span class="keyword">null</span>) &#123; </span><br><span class="line">      instance = <span class="keyword">new</span> SingletonClass(); </span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">return</span> instance; </span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">private</span> <span class="title">SingletonClass</span><span class="params">()</span> </span>&#123; </span><br><span class="line">     </span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>要使用 SingletonClass，调用getInstance（）方法，第一次的时候发现instance时null,然后就创建一个对象，返回出去；第二次再使用的时候，因为这个instance是static的，共享一个内存地址的，所以instance的值已经不是null了，因此不会再创建对象，直接将其返回。这个过程就称为lazyloaded,也就是迟加载-----直到使用的时候才经行加载。</p>
<h1 id="线程安全"><a class="header-anchor" href="#线程安全">¶</a>线程安全</h1>
<p>上面的代码很清楚，也很简单。然而就像那句名言：“80%的错误是由20%的代码优化引起的”。单线程下，这段代码没什么问题，可是如果是多线程呢，麻烦就来了，我们来分析一下：</p>
<ul>
<li>线程A希望使用SingletonClass，调用getInstance()方法。因为是第一次调用，A就发现instance是null的，于是它开始创建实例，就在这个时候，CPU发生时间片切换，</li>
<li>线程B开始执行，它要使用SingletonClass，调用getInstance()方法，同样检测到instance是null——注意，这是在A检测完之后切换的，也就是说A并没有来得及创建对象——因此B开始创建。</li>
<li>B创建完成后，切换到A继续执行，因为它已经检测完了，所以A不会再检测一遍，它会直接创建对象。这样，线程A和B各自拥有一个SingletonClass的对象——单例失败！<br>
解决的办法也很简单，那就是加锁：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClass</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> SingletonClass instance = <span class="keyword">null</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">static</span> SingletonClass <span class="title">getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(instance == <span class="keyword">null</span>)&#123;</span><br><span class="line">            instance = <span class="keyword">new</span> SingletonClass();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">SingletonClass</span><span class="params">()</span></span>&#123;</span><br><span class="line">    </span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>只要getInstance（）加上同步锁，，一个线程必须等待另外一个线程创建完后才能使用这个方法，这就保证了单例的唯一性。</p>
<h1 id="双重检查锁定-double-checked-locking"><a class="header-anchor" href="#双重检查锁定-double-checked-locking">¶</a>双重检查锁定(double–checked–locking)</h1>
<p>上面这段代码毫无疑问存在性能的问题----synchronized修饰的同步块可是要比一般的代码慢上几倍的！如果存在很多次的getInstance()调用，那性能问题就不得不考虑了？！！！</p>
<p>究竟是整个方法都必须加锁，还是紧紧其中某一句加锁就足够了？我们为什么要加锁呢？分析一下lazy loaded的那种情形的原因，原因就是检测null的操作和创建对象的操作分离了，导致出现只有加同步锁才能单利的唯一性。<br>
如果这俩个操作能够原子的进行，那么单利就已经保证了。于是，我们开始修改代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClass</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> SingletonClass instance = <span class="keyword">null</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SingletonClass <span class="title">getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">synchronized</span>(SingletonClass<span class="class">.<span class="keyword">class</span>)</span>&#123;</span><br><span class="line">            <span class="keyword">if</span>(instance == <span class="keyword">null</span>)&#123;</span><br><span class="line">            instance = <span class="keyword">new</span> SingletonClass();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">SingletonClass</span><span class="params">()</span></span>&#123;</span><br><span class="line">    </span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>首先去掉 getInstance() 的同步操作，然后把同步锁加载到if语句上。但是，这样的修改起不到任何作用：因为每次调用getInstance()的时候必然要经行同步，性能的问题还是存在。如果…我们事先判断一下是不是为null在去同步呢？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClass</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> SingletonClass instance = <span class="keyword">null</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SingletonClass <span class="title">getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(instance == <span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">synchronized</span>(SingletonClass<span class="class">.<span class="keyword">class</span>)</span>&#123;</span><br><span class="line">                <span class="keyword">if</span>(instance == <span class="keyword">null</span>)&#123;</span><br><span class="line">                    instance = <span class="keyword">new</span> SingletonClass();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;    </span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">SingletonClass</span><span class="params">()</span></span>&#123;</span><br><span class="line">    </span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>首先判断instance是不是为null，如果为null在去进行同步，如果不为null，则直接返回instance对象。这就是double—checked----locking设计实现单例模式。</p>
<h1 id="并发编程-有序性"><a class="header-anchor" href="#并发编程-有序性">¶</a>并发编程–有序性</h1>
<p>并发编程中，我们通常会遇到以下三个问题：原子性问题，可见性问题，有序性问题。我们上面的单例实现是否都解决了。</p>
<ul>
<li>
<p>[X] 原子性：即一个操作或者多个操作 要么全部执行并且执行的过程不会被任何因素打断，要么就都不执行。</p>
</li>
<li>
<p>[X] 可见性：可见性是指当多个线程访问同一个变量时，一个线程修改了这个变量的值，其他线程能够立即看得到修改的值。</p>
</li>
<li>
<p>[ ] 有序性：即程序执行的顺序按照代码的先后顺序执行。</p>
</li>
</ul>
<p>编译原理里面有一个很重要的内容是编译器优化。所谓编译器优化是指，在不改变原来语义的情况下，通过调整语句顺序，来让程序运行的更快。这称为指令重排序(Instruction Reorder)。</p>
<p>初始化Singleton和将对象地址赋给instance字段的顺序是不确定的。在某个线程创建单例对象时，在构造方法被调用之前，就为该对象分配了内存空间并将分配的内存地址赋值给instance字段了，然而该对象可能还没有初始化。若紧接着另外一个线程来调用getInstance，此时instance不是null，但取到的对象还未真正初始化，程序就会出错。</p>
<p>在JDK1.5之前，volatile是个关键字，但是并没有明确的规定其用途。在JDK1.5之后，volatile关键字有了明确的语义—是禁止指令重排序优化，被volatile修饰的写变量不能和之前的读写代码调整，读变量不能和之后的读写代码调整！因此，只要我们简单的把instance加上volatile关键字就可以了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClass</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> SingletonClass instance = <span class="keyword">null</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SingletonClass <span class="title">getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(instance == <span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">synchronized</span>(SingletonClass<span class="class">.<span class="keyword">class</span>)</span>&#123;</span><br><span class="line">                <span class="keyword">if</span>(instance == <span class="keyword">null</span>)&#123;</span><br><span class="line">                    instance = <span class="keyword">new</span> SingletonClass();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;    </span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">SingletonClass</span><span class="params">()</span></span>&#123;</span><br><span class="line">    </span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="静态内部类"><a class="header-anchor" href="#静态内部类">¶</a>静态内部类</h1>
<p>代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClass</span> </span>&#123; </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClassInstance</span> </span>&#123; </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> SingletonClass instance = <span class="keyword">new</span> SingletonClass(); </span><br><span class="line">  &#125; </span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SingletonClass <span class="title">getInstance</span><span class="params">()</span> </span>&#123; </span><br><span class="line">    <span class="keyword">return</span> SingletonClassInstance.instance; </span><br><span class="line">  &#125; </span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">private</span> <span class="title">SingletonClass</span><span class="params">()</span> </span>&#123; </span><br><span class="line"></span><br><span class="line">  &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>SingletonClass没有static的属性，因此并不会被初始化。直到调用getInstance()的时候，会首先加载SingletonClassInstance类，这个类有一个static的SingletonClass实例，因此需要调用SingletonClass的构造方法，然后getInstance()将把这个内部类的instance返回给使用者。由于这个instance是static的，因此并不会构造多次。</p>
<p>由于SingletonClassInstance是私有静态内部类，所以不会被其他类知道，同样，static语义也要求不会有多个实例存在。并且，JSL规范定义，类的构造必须是原子性的，非并发的，因此不需要加同步块。同样，由于这个构造是并发的，所以getInstance()也并不需要加同步。</p>
<h1 id="序列化问题-枚举-readresolve"><a class="header-anchor" href="#序列化问题-枚举-readresolve">¶</a>序列化问题（枚举、readResolve）</h1>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClass</span> </span>&#123; </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClassInstance</span> </span>&#123; </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> SingletonClass instance = <span class="keyword">new</span> SingletonClass(); </span><br><span class="line">  &#125; </span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SingletonClass <span class="title">getInstance</span><span class="params">()</span> </span>&#123; </span><br><span class="line">    <span class="keyword">return</span> SingletonClassInstance.instance; </span><br><span class="line">  &#125; </span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">private</span> <span class="title">SingletonClass</span><span class="params">()</span> </span>&#123; </span><br><span class="line"></span><br><span class="line">  &#125; </span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">private</span> Object <span class="title">readResolve</span><span class="params">()</span></span>&#123;</span><br><span class="line">      <span class="keyword">return</span> SingletonClassInstance.instance;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>JVM从内存中反序列化地&quot;组装&quot;一个新对象时,就会自动调用这个 readResolve方法来返回我们指定好的对象了, 单例规则也就得到了保证。</p>
<h1 id="枚举"><a class="header-anchor" href="#枚举">¶</a>枚举</h1>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Singleton&#123;  </span><br><span class="line">    instance;  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">whateverMethod</span><span class="params">()</span></span>&#123;&#125;      </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用枚举除了线程安全和防止反射调用构造器之外，还提供了自动序列化机制，防止反序列化的时候创建新的对象。因此，《Effective Java》作者推荐使用的方法。</p>
<h1 id="反射问题-第二次实例化的时候-抛出异常"><a class="header-anchor" href="#反射问题-第二次实例化的时候-抛出异常">¶</a>反射问题（第二次实例化的时候，抛出异常）</h1>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClass</span> </span>&#123; </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">boolean</span> isInstanced = <span class="keyword">false</span>;</span><br><span class="line">    </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonClassInstance</span> </span>&#123; </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> SingletonClass instance = <span class="keyword">new</span> SingletonClass(); </span><br><span class="line">  &#125; </span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SingletonClass <span class="title">getInstance</span><span class="params">()</span> </span>&#123; </span><br><span class="line">    <span class="keyword">return</span> SingletonClassInstance.instance; </span><br><span class="line">  &#125; </span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">private</span> <span class="title">SingletonClass</span><span class="params">()</span> </span>&#123; </span><br><span class="line">        <span class="keyword">if</span>(!instanced)&#123;</span><br><span class="line">            instanced = <span class="keyword">true</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Exception(<span class="string">"duplicate instance create error!"</span> + SingletonClass<span class="class">.<span class="keyword">class</span>.<span class="title">getName</span>())</span>;  </span><br><span class="line">        &#125;</span><br><span class="line">  &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java设计模式</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>信息安全技术</title>
    <url>/engineering/information-security.html</url>
    <content><![CDATA[<p>现在计算机已经渗透到各行各业，在使用过程中产生了大量的电子数据，在通信时如何保证传输数据的安全，如何防止人为、病毒、自然灾害对数据的的侵害，信息安全技术显得越发重要。</p>
<p><img src="https://s1.ax1x.com/2020/08/16/dVkEzF.png" alt="信息安全.png"></p>
<a id="more"></a>
<h1 id="信息安全技术"><a class="header-anchor" href="#信息安全技术">¶</a>信息安全技术</h1>
<h2 id="加密和解密"><a class="header-anchor" href="#加密和解密">¶</a>加密和解密</h2>
<p>原则上来说，对电子数据的攻击形式分为：</p>
<ul>
<li>
<p>被动攻击：就是非法地从传输信道上截取信息，或从存储载体上偷窃、复制信息。</p>
</li>
<li>
<p>主动攻击：就是对传输或存储的数据进行恶意的删除、篡改等。</p>
</li>
</ul>
<p>对于防止数据攻击，密码技术是最有效而且经济的方法。密码体制分为堆成加密和非对称加密：</p>
<ul>
<li>对称加密
<ul>
<li>加密秘钥和解密秘钥相同</li>
<li>加密秘钥和解密秘钥可以<strong>简单相互推导</strong></li>
</ul>
</li>
<li>非对称加密
<ul>
<li>加密秘钥和解密秘钥不同</li>
<li>加密秘钥和解密秘钥<strong>不能在有限时间内相互推导</strong></li>
</ul>
</li>
</ul>
<h3 id="对称秘钥密码体制及典型算法"><a class="header-anchor" href="#对称秘钥密码体制及典型算法">¶</a>对称秘钥密码体制及典型算法</h3>
<p>对称算法（Symmetric Algorithm），又称为传统密码算法，要求发送方和接收方在安全通信前，商定一个秘钥，对称算法的安全性依赖于秘钥。</p>
<h4 id="des算法"><a class="header-anchor" href="#des算法">¶</a>DES算法</h4>
<p>DES（Data Encryption Standard，数据加密标准）是由IBM公司研制的一种加密算法，DES是一个分组加密算法，它以64位分组对数据加密，它的秘钥长度是56位（每个第8位都用作奇偶校验）。DES算法分为如下三个步骤：</p>
<ul>
<li>
<p>初始置换：X<sub>0</sub> = IP(X) = L<sub>0</sub>R<sub>0</sub>，其中X为64位的明文，L<sub>0</sub>表示X<sub>0</sub>的前32位，R<sub>0</sub>表示X<sub>0</sub>的后32位。</p>
</li>
<li>
<p>计算16迭代，设前i-1次迭代结果为X<sub>i-1 </sub>= L<sub>i-1</sub>R<sub>i-1</sub>，则第i轮迭代运算为：L<sub>i</sub> = R<sub>i-1</sub>，R<sub>i</sub> = L<sub>i-1</sub>⊕f(R<sub>i-1</sub>, K<sub>i</sub>)</p>
</li>
<li>
<p>L<sub>i-1</sub>表示X<sub>i-1</sub>的前32位</p>
</li>
<li>
<p>R<sub>i-1</sub>表示X<sub>i-1</sub>的后32位</p>
</li>
<li>
<p>⊕表示两位串的“异或”运算</p>
</li>
<li>
<p>f主要是由一个成为S盒的置换构成</p>
</li>
<li>
<p>K<sub>i</sub>是一些由初始的56位经过秘钥编排函数产生的48位长的块</p>
</li>
<li>
<p>对串L<sub>16</sub>R<sub>16</sub>作逆置换IP<sup>-1</sup>得密文y，y = IP<sup>-1</sup>(L<sub>16</sub>R<sub>16</sub>)，IP<sup>-1</sup>是IP的逆置换。</p>
</li>
</ul>
<p>DES算法的示意图如下：</p>
<p><img src="https://s1.ax1x.com/2020/09/01/dvcUzV.png" alt="DES算法.png"></p>
<h4 id="idea算法"><a class="header-anchor" href="#idea算法">¶</a>IDEA算法</h4>
<p>国际数据加密算法（International Data Encryption Standard，IDEA），IDEA分组长度为64b，秘钥长度为128b，具有如下特点：</p>
<ul>
<li>运算简单：只需要异或，模2<sup>16</sup>和模（2<sup>16</sup>+1）乘。</li>
<li>容易使用硬件或软件实现（DES算法便于用硬件实现，难以用软件实现）。</li>
<li>软件实现的IDEA算法运算速度比DES算法快，因为都是基于16b数运算，容易在16b、32b、64bCPU上实现。</li>
<li>秘钥长度为128b，穷举法攻击不现实。</li>
</ul>
<h3 id="非对称加密算法"><a class="header-anchor" href="#非对称加密算法">¶</a>非对称加密算法</h3>
<p>非对称密码体制又称为双秘钥和公钥密码体制，是于1976年由Diffie和Hellman提出的。非对称密码体制又两个不同的密码：</p>
<ul>
<li>私钥，秘密保存。</li>
<li>公钥，公开、不需要保密。</li>
</ul>
<p>其工作方式如下：</p>
<ul>
<li>公布公钥，网络或者其他可以公开的地方。</li>
<li>发送方：用发布的公钥将信息加密，然后传送给接收方</li>
<li>接收方：使用与此公钥相对应的私钥，将接收到的加密信息解密出来。</li>
</ul>
<p>RSA公开密钥密码体制的原理是：根据数论，寻求两个大素数比较简单，而将它们的乘积进行因式分解却极其困难，因此可以将乘积公开作为加密密钥。</p>
<p>RSA算法的具体描述如下：</p>
<p><img src="https://s1.ax1x.com/2020/09/01/dvc7FA.png" alt="RSA算法描述.png"></p>
<p>然而只根据n和e（注意：不是p和q）要计算出d是不可能的。因此，任何人都可对明文进行加密，但只有授权用户（知道d）才可对密文解密 。</p>
<h2 id="散列函数和数字签名"><a class="header-anchor" href="#散列函数和数字签名">¶</a>散列函数和数字签名</h2>
<h4 id="散列函数"><a class="header-anchor" href="#散列函数">¶</a>散列函数</h4>
<p>散列函数是一种公开的数学函数，函数输入的信息叫做报文，匀后所得到的结果叫做散列码或者叫做消息摘要。</p>
<p>散列函数h = H(M)，具有如下特点:</p>
<ul>
<li>不同内容的报文具有不同的散列值。</li>
<li>散列函数是单向的，不可逆。</li>
<li>对于任何一个报文，无法预知它的散列码。</li>
<li>散列码具有固定长度。</li>
</ul>
<p>常见的散列函数有MD5、SHA和HMAC等。</p>
<p>MD5（Message Digest 5）是一种非常著名的散列算法，已成为国际标准。MD5散列算法对输入的任意长度的消息产生128位（16字节）长度的消息摘要，MD5算法包括以下4个步骤：</p>
<ul>
<li>
<p>按位补充数据：补充后的数据长度模512后于448，也就是说数据补位后，其位数长度只差64位(bit)就是512的整数倍。即便是这个数据的位数对512求模的结果正好是448也必须进行补位。</p>
</li>
<li>
<p>补足长度：将数据长度表示为二进制，如果长度超过64位，则获取其低64位；如果长度不足64位，则在其高位补0。然后将得到的64位报文添加在经过填充的报文后，得到的结果数据长度正好是512的整数倍。也就是说长度正好是16个(32bit) 字的整数倍。</p>
</li>
<li>
<p>初始化MD缓存器：MD5运算要用到一个128位的MD5缓存器，用来保存中间变量和最终结果。该缓存器又可看成是4个32位的寄存器A、B、C、D，初始化为 :</p>
<p>A ： 01 23 45 67</p>
<p>B： 89 ab cd ef</p>
<p>C： fe dc ba 98</p>
<p>D： 76 54 32 10</p>
</li>
<li>
<p>处理数据段：首先定义4个非线性函数F、G、H、I，对输入的报文运算以512位数据段为单位进行处理。对每个数据段都要进行4轮的逻辑处理，在4轮中分别使用4个不同的函数F、G、H、I。每一轮以ABCD和当前的512位的块为输入，处理后送入ABCD(128位)。</p>
</li>
</ul>
<p>信息摘要最终处理成以A, B, C, D 的形式输出。也就是开始于A的低位在前的顺序字节，结束于D的高位在前的顺序字节。</p>
<h4 id="数字签名"><a class="header-anchor" href="#数字签名">¶</a>数字签名</h4>
<p>数字签名可以解决否认、伪造、篡改及冒充等问题。数字签名方案一般包括三个过程：</p>
<ul>
<li>系统的初始化</li>
<li>签名产生过程</li>
<li>签名验证过程</li>
</ul>
<p>目前采用比较多的是非对称加密技术和对称加密技术，两种方式大体工作程序是一样的，如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/08/16/dVHmjO.png" alt="数字签名.png"></p>
<h4 id="数字水印"><a class="header-anchor" href="#数字水印">¶</a>数字水印</h4>
<p>数字水印（Digital Watermarking）是实现版权保护的有效办法，是通过在原始数据中嵌入秘密信息–水印（Watermarking）来证实数据的所有权。</p>
<ul>
<li>可以是一段文字、标识或序列号</li>
<li>不可见或不可察</li>
<li>与原始数据紧密结合并隐藏其中</li>
<li>在一些不破坏源数据使用价值或商用价值的操作后仍然能够保存下来</li>
</ul>
<p>水印嵌入和提取的流程如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/08/31/dXOgxg.png" alt="数字水印"></p>
<p>数字水印主要应用领域：版权保护、加指纹、标题与注释、篡改提示、使用控制。</p>
<p>常用的数字水印算法：空域算法、变换域算法、压缩域算法、NEC算法和生理模型算法。</p>
<h2 id="秘钥分配中心与公钥基础设施"><a class="header-anchor" href="#秘钥分配中心与公钥基础设施">¶</a>秘钥分配中心与公钥基础设施</h2>
<p>在现代密码体系中，算法本身的保密性已经不重要了，对于数据的保密在很大程度上、甚至完全依赖于对密钥的保密。因此在密码系统中，如何高效地分配密钥、安全地管理密钥对保证数据安全来说至关重要。</p>
<h3 id="密钥分配中心-key-distribution-center-kdc"><a class="header-anchor" href="#密钥分配中心-key-distribution-center-kdc">¶</a>密钥分配中心（Key Distribution Center，KDC）</h3>
<p>在KDC方案中，每一个用户都只保存自己的私钥SK和KDC的公钥PK<sub>KDC</sub>，在通信时再从KDC获取其他用户的公钥或者仅仅在某一次通信中可以使用的对称秘钥加密算法的临时秘钥K。</p>
<p>在KDC方案中，假设用户A保存私钥SK<sub>A</sub>、用户B保存私钥SK<sub>B</sub>，假设用对称秘钥来加密他们的对话，秘钥的分配流程如下：</p>
<ul>
<li>A向KDC发送SK<sub>A</sub>(A,B)，请求使用SK<sub>A</sub>加密</li>
<li>KDC收到加密消息，<strong>用A的公钥来验证</strong>请求是由A发出的</li>
<li>根据某种算法生成供A、B之间会话使用的对称秘钥K</li>
<li>KDC向A返回PK<sub>A</sub>(K, PK<sub>B</sub>(A, K))，该应答是用A的公钥加密，只有A能使用SK<sub>A</sub>解密</li>
<li>A使用SK<sub>A</sub>解密得到秘钥K，并将PK<sub>B</sub>(A, K)发送给B，表示想与B进行会话</li>
<li>B用自己的私钥解密得到会话秘钥K</li>
</ul>
<h3 id="数字证书和公开密钥基础设施"><a class="header-anchor" href="#数字证书和公开密钥基础设施">¶</a>数字证书和公开密钥基础设施</h3>
<p>数字签名和公钥加密都是基于不对称加密技术，存在的问题有：</p>
<ul>
<li>如何保证公开密钥的持有者是真实的</li>
<li>大规模信息系统下公开密钥如何产生、分发和管理</li>
</ul>
<h4 id="数字证书"><a class="header-anchor" href="#数字证书">¶</a>数字证书</h4>
<p>数字证书提供了一个在公钥和拥有相应私钥的实体之间建立关系的体制。数字证书中采用公钥体制，利用一对互相匹配的秘钥进行加密、解密。每个用户自己保存私钥，用它进行解密和签名。</p>
<p>数字证书是用户在系统中作为确认身份的证据，数字证书的内容一般包括：</p>
<ul>
<li>唯一标识证书所有者的名称</li>
<li>唯一标识证书签发者的名称</li>
<li>证书所有者的公开密钥</li>
<li>证书签发者的数字签名</li>
<li>证书的有效期</li>
<li>证书的序列号</li>
</ul>
<h4 id="公钥基础设施kpi"><a class="header-anchor" href="#公钥基础设施kpi">¶</a>公钥基础设施KPI</h4>
<p>KPI（Public Key Infrastructure，公钥基础设施）为用户和应用程序提供公开密钥的管理服务。PKI的结构模型中有三类实体：</p>
<ul>
<li>管理实体：是KPI的核心，服务的提供者
<ul>
<li>CA：框架中唯一能够发布和撤销证书的实体，维护证书的生命周期</li>
<li>RA：处理用户请求，在验证了请求的有效性后，代替用户想CA提交</li>
</ul>
</li>
<li>端实体：PKI的用户，服务的使用者
<ul>
<li>持有者</li>
<li>验证者</li>
</ul>
</li>
<li>证书库：分布式的数据库，用于证书和CRL的存放和检索</li>
</ul>
<h2 id="访问控制"><a class="header-anchor" href="#访问控制">¶</a>访问控制</h2>
<p>访问控制是通过某种途径限制和允许对资源的访问能力以及范围的一种方法。访问控制技术可以通过对计算机系统的控制，自动、有效地防止对系统资源进行非法访问或者不当的使用，它是建立在身份认证的基础之上，但不能取代身份认证。</p>
<h3 id="身份认证"><a class="header-anchor" href="#身份认证">¶</a>身份认证</h3>
<p>识别用户的身份有两种不同的形式</p>
<ul>
<li>身份认证：要求对用户的所有权限角色或自身的身份进行认证</li>
<li>身份鉴定：要求对使用者本身的身份进行检查</li>
</ul>
<p>认证的方法多种多样，其安全强度也不同，具体方法可以总结为三大类：</p>
<ul>
<li><strong>用户知道什么</strong>：用户名和口令认证</li>
<li><strong>用户拥有什么</strong>：使用令牌认证</li>
<li><strong>用户是什么</strong>：生物识别与三因素认证</li>
</ul>
<p>在安全性要求较高的系统中，认证必须能对用户进行身份鉴定，要将用户知道什么、拥有什么、是什么结合起来，同时对认证用的秘钥进行保护。</p>
<h3 id="访问控制技术"><a class="header-anchor" href="#访问控制技术">¶</a>访问控制技术</h3>
<p>根据控制手段和具体目的的不同，通常将访问控制技术划分为如下几个方面：</p>
<ul>
<li>入网访问控制</li>
<li>网络权限控制</li>
<li>目录级安全控制</li>
<li>属性安全控制</li>
<li>网络服务器的安全控制</li>
</ul>
<h2 id="安全协议"><a class="header-anchor" href="#安全协议">¶</a>安全协议</h2>
<h3 id="ipsec协议"><a class="header-anchor" href="#ipsec协议">¶</a>IPSec协议</h3>
<p>IPSec在IP层上对数据包进行高强度的安全处理提供数据源验证、无连接数据完整性、抗重播和有限通信流机密性等安全服务。</p>
<p>IPSec像Internet秘钥交换（Internet Key Exchange，IKE）协议这样的秘钥管理过程和协议，通过使用两种通信安全协议来为数据提供高质量的安全性：</p>
<ul>
<li>认证头（AH）协议</li>
<li>封装安全载荷（ESP）协议</li>
</ul>
<p>IPSec协议既可以用来保护一个完整的IP载荷，也可以用来保护某个IP载荷的上层协议。这两方面的保护分别由IPSec的两种不同的“模式”来提供，如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/08/31/djenzQ.png" alt="IPSec数据结构"></p>
<h3 id="ssl协议"><a class="header-anchor" href="#ssl协议">¶</a>SSL协议</h3>
<p>SSL协议（Secure Socket Layer）是Natscape推出的一种网络安全协议，是在传输过程通信协议（TCP/IP）上实现的一种安全协议。</p>
<p>在SSL中，所有数据被封装在记录中，记录层把从上层获得的数据分成可管理的块、可选的压缩数据、应用MAC（Message Authentication Code）、加密、增加SSL首部、在TCP报文段中传输结果单元。被接收的数据通过解密、验证、解压和重新装配，然后交付给更高级的用户。SSL中两个重要的概念是SSL连接和SSL会话：</p>
<ul>
<li>SSL连接：提供恰当类型服务的传输</li>
<li>SSL会话：客户和服务器之间的关联，会话通过握手协议来创建</li>
</ul>
<p>SSL协议工作过程如下如所示：</p>
<p><img src="https://s1.ax1x.com/2020/09/01/djgxzV.png" alt="SSL协议工作示意图.png"></p>
<h2 id="数据备份"><a class="header-anchor" href="#数据备份">¶</a>数据备份</h2>
<p>作为信息安全的重要成员——数据备份却经常被人们遗忘，这样可能导致大量的有用信息被破坏，造成的后果有时是毁灭性的。</p>
<h3 id="备份类型"><a class="header-anchor" href="#备份类型">¶</a>备份类型</h3>
<p>数据备份包括一下几种类型，在不同的情况下应该根据具体情况，选出最合适的方案：</p>
<ul>
<li>完全备份：备份时间最长，恢复时间最短，操作最方便可靠。</li>
<li>差异备份：备份时间较长，占用空间较多，但恢复时间较短。</li>
<li>增量备份：备份时间较短，占用空间较少，单恢复时间较长。</li>
<li>按需备份：有很好的选择性。</li>
</ul>
<h3 id="异地备份"><a class="header-anchor" href="#异地备份">¶</a>异地备份</h3>
<p>数据异地备份是容灾系统的核心技术，通过有效的数据复制，实现远程的业务数据与本地业务数据的同步，确保一旦本地系统出现故障，远程的容灾中心能够迅速进行完整的业务接管。例如“9.11”期间，美国金融行业虽然遭受巨大的损失，但还是能够正常运行，也得益于数据的异地备份做得非常好。</p>
<h3 id="新型的备份解决方案"><a class="header-anchor" href="#新型的备份解决方案">¶</a>新型的备份解决方案</h3>
<p>一个优秀的备份解决方案应该做到：</p>
<ul>
<li>最大限度地降低对应用数据流量的影响，从而保证通信性能</li>
<li>最大限度地降低服务器的负载，保证服务器的性能</li>
<li>优化备份资源的使用，包括服务器、驱动器等</li>
</ul>
<p>下面介绍集中备份解决方案。</p>
<ul>
<li>网络备份模式：把一个磁带设备放置在LAN上，供多个服务器共享。</li>
<li>用存储网络备份：让每个应用服务器都可以通过一个专用的存储网络，直接将数据备份到某个磁带设备，而不需要经过专门的备份服务器。</li>
<li>磁带和磁带质检直接传输数据的备份：为了减轻服务器在备份时的CPU负载，需要在数据不经过服务器本身的情况下，将备份数据从磁盘发送到磁带，这是通过磁盘和磁带质检直接传输数据的机制（即SCSI扩展复制命令的方法）来实现的。</li>
</ul>
<h2 id="计算机病毒与免疫"><a class="header-anchor" href="#计算机病毒与免疫">¶</a>计算机病毒与免疫</h2>
<h3 id="计算机病毒"><a class="header-anchor" href="#计算机病毒">¶</a>计算机病毒</h3>
<p>病毒的定义最早由F.B.Cohen于1984年提出的，在他的经典文章Computer Viruses-Theory and Experiments（计算机病毒理论与时间）中，描述如下：</p>
<p>计算机病毒是这样一种程序，它通过修改其他程序使之含有该程序本身或它的一个变体。病毒具有感染力，它可借助其使用者的权限感染他们的程序，在一个计算机系统中或网络中得已繁殖、传播。每个被感染的程序也像病毒一样可以感染其他程序，从而使更多的程序受到感染。</p>
<p>病毒的特征：</p>
<ul>
<li>感染性</li>
<li>潜伏性</li>
<li>可触发性</li>
<li>破坏性</li>
<li>人为性</li>
<li>衍生性</li>
</ul>
<p>计算机病毒的分类：</p>
<ul>
<li>引导区病毒（boot sector virus）</li>
<li>文件感染病毒（file infector virus)</li>
<li>宏病毒（Marco virus）</li>
<li>特洛伊木马（Trojan/Trojan Horse)</li>
<li>蠕虫病毒（Worm）</li>
</ul>
<h3 id="计算机病毒免疫的原理"><a class="header-anchor" href="#计算机病毒免疫的原理">¶</a>计算机病毒免疫的原理</h3>
<p>计算机病毒的传染模块一般包括传染条件判断和实施传染两个部分，在病毒被激活的状态下，病毒程序通过判断传染条件的满足与否，以决定是否对目标对象进行传染。</p>
<p>从实现计算机病毒免疫的角度看病毒的传染，可以将病毒的传染分为两种：</p>
<ul>
<li>判断传染标记，如果不存在传染标记，就进行传染。</li>
<li>不判断传染标记，找到可以传染的对象就进行一次传染。</li>
</ul>
<p>目前常用的免疫方法有如下两种：</p>
<ul>
<li>针对某一种病毒进行的计算机病毒免疫：为文件添加免疫标记，标识该文件已被传染，骗过病毒
<ul>
<li>病毒变异时，免疫标记会失效</li>
<li>某些病毒免疫标志不容易仿制</li>
<li>病毒种类较多，不可能添加各种病毒的免疫标识</li>
<li>能阻止传染，但不能阻止病毒的破坏行为</li>
</ul>
</li>
<li>基于自我完整性检查的计算机病毒的免疫
<ul>
<li>只能用于文件，不能用于引导扇区</li>
<li>为可执行程序增加一个免疫外壳</li>
<li>执行时，先运行免疫外壳，检查自身的程序大小、校验生成日期和时间等信息，没有异常才执行受保护的程序。</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>系统架构设计</category>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>信息安全技术</tag>
        <tag>系统架构设计</tag>
      </tags>
  </entry>
  <entry>
    <title>时代洪流中的新工业革命：AI大模型如何重塑人类未来</title>
    <url>/ai/ai-industrial-revolution.html</url>
    <content><![CDATA[<h3 id="时代洪流中的新工业革命：ai大模型如何重塑人类未来"><a class="header-anchor" href="#时代洪流中的新工业革命：ai大模型如何重塑人类未来">¶</a>时代洪流中的新工业革命：AI大模型如何重塑人类未来</h3>
<hr>
<h4 id="引言：当智能成为新蒸汽机"><a class="header-anchor" href="#引言：当智能成为新蒸汽机">¶</a>引言：当智能成为新蒸汽机</h4>
<p>18世纪的蒸汽机解放了人类双手，电力革命点亮了现代文明，而今天，我们正站在以<strong>AI大模型</strong>为核心的第三次工业革命浪潮之巅。从GPT-4的创造性文本生成到DeepSeek以1/10参数击败GPT-4o的数学推理能力，从浙江水科“Shoot-AI”模型在文旅场景的爆发到Meta智能眼镜的实时翻译功能，这场革命不再局限于机械与能源，而是以数据和算法为燃料，重构人类社会的每一个细胞。</p>
<hr>
<h3 id="一-ai大模型：新工业革命的三大底层逻辑"><a class="header-anchor" href="#一-ai大模型：新工业革命的三大底层逻辑">¶</a>一、AI大模型：新工业革命的三大底层逻辑</h3>
<p><strong>1. 生产力跃迁：从“人力替代”到“智能涌现”</strong><br>
工业革命用机器取代体力劳动，而AI大模型正在用“认知自动化”取代脑力劳动。</p>
<ul>
<li><strong>效率革命</strong>：杭州全诊医学通过ERNIE模型将病历生成效率提升20%，宁波传媒借助AI批改工具优化作文评审流程。</li>
<li><strong>成本颠覆</strong>：DeepSeek通过强化学习与模型蒸馏技术，将训练成本降低42.5%，推理效率提升40%，让小企业也能负担高性能AI应用。</li>
<li><strong>知识民主化</strong>：开源模型如LLaMA2和DeepSeek-R1打破巨头垄断，开发者仅需1块H100芯片即可训练场景模型，技术门槛降至历史最低。</li>
</ul>
<p><strong>2. 创新范式：从“线性迭代”到“涌现式突破”</strong><br>
当模型参数量突破万亿级，AI开始展现人类未曾预设的能力：</p>
<ul>
<li><strong>跨模态创造</strong>：DALL·E 3生成的艺术品登陆苏富比拍卖，MidJourney将文字转化为电影级分镜，创作权从专业画家延伸至每个普通人。</li>
<li><strong>科学发现</strong>：AlphaFold破解2亿种蛋白质结构，DeepMind的GNoME模型发现220万种新材料，科研周期从十年缩短至数天。</li>
<li><strong>自主进化</strong>：DeepSeek通过纯强化学习实现“无监督成长”，仅凭答案正确性即可自我优化，逼近人类元认知能力。</li>
</ul>
<p><strong>3. 经济重构：从“资源占有”到“智能密度”</strong><br>
普华永道预测，到2030年AI将贡献15.7万亿美元全球经济增量，而竞争焦点已转向三大维度：</p>
<ul>
<li><strong>算力主权</strong>：美国通过《芯片法案》投入520亿美元构建AI基础设施，中国“东数西算”工程打造10大数据中心集群，算力成为国家战略资源。</li>
<li><strong>场景霸权</strong>：浙江水科以“Shoot-AI”模型切入文旅赛道，为500家景区定制个性化游览视频，单月用户突破600万，证明垂直场景的落地价值远超通用模型。</li>
<li><strong>生态博弈</strong>：OpenAI与DeepSeek分别代表闭源与开源路线，后者通过开放API和极简定价（仅为GPT-4的1%），推动AI从“金字塔垄断”走向“分布式创新”。</li>
</ul>
<hr>
<h3 id="二-拒绝ai-放弃生存权：企业与个人的终极挑战"><a class="header-anchor" href="#二-拒绝ai-放弃生存权：企业与个人的终极挑战">¶</a>二、拒绝AI=放弃生存权：企业与个人的终极挑战</h3>
<p><strong>1. 企业的生死抉择：从“+AI”到“AI原生”</strong></p>
<ul>
<li><strong>传统模式终结</strong>：依赖人工标注和云端调用的企业（如早期SaaS平台）正被淘汰，2025年“百模大战”后，存活者需具备三大能力：
<ul>
<li><strong>数据炼金术</strong>：美的集团通过AI中台将研发周期缩短50%，故障预测准确率达95%。</li>
<li><strong>端侧智能</strong>：Meta Ray-Ban眼镜集成本地化模型，实时翻译延迟低于0.5秒，用户隐私与体验兼得。</li>
<li><strong>伦理合规</strong>：欧盟《AI法案》要求算法透明性，百度的iRAG技术通过外部知识检索减少“幻觉”，成为合规标杆。</li>
</ul>
</li>
</ul>
<p><strong>2. 个体的进化焦虑：从“技能持有”到“人机共生”</strong><br>
牛津大学研究显示，47%的职业将被AI深度改造，但危机中蕴藏机遇：</p>
<ul>
<li><strong>创造力升级</strong>：设计师用Stable Diffusion生成100版初稿，再聚焦细节优化，效率提升10倍。</li>
<li><strong>认知升维</strong>：Prompt Engineering（提示词工程）成为必修课，顶尖工程师通过精准指令调动AI完成代码生成、数据分析等高阶任务。</li>
<li><strong>跨界突破</strong>：短视频创作者借助“简单AI”3步生成爆款文案，传统作家转型互动叙事设计师，AI工具让副业收入超过主业成为可能。</li>
</ul>
<hr>
<h3 id="三-决胜未来的四大行动纲领"><a class="header-anchor" href="#三-决胜未来的四大行动纲领">¶</a>三、决胜未来的四大行动纲领</h3>
<p><strong>1. 技术范式：拥抱“小模型+强化学习”革命</strong></p>
<ul>
<li><strong>低成本落地</strong>：DeepSeek-R1以7B参数实现32B模型的性能，证明“模型蒸馏+强化学习”可大幅降低算力依赖。</li>
<li><strong>端侧爆发</strong>：三星50克AR眼镜、字节“豆包”助手预示边缘计算崛起，2025年端侧AI市场规模将突破5000亿美元。</li>
</ul>
<p><strong>2. 组织变革：构建“人机双螺旋”架构</strong></p>
<ul>
<li><strong>敏捷迭代</strong>：Salesforce的AI突击队2周推出CRM Copilot，传统企业需将开发周期从“年”压缩至“周”。</li>
<li><strong>数据驱动</strong>：丰田每天处理PB级驾驶数据训练自动驾驶模型，数据治理能力决定AI上限。</li>
</ul>
<p><strong>3. 生态卡位：抢占垂直场景制高点</strong></p>
<ul>
<li><strong>文旅革命</strong>：浙江水科为景区定制AI视频，游客停留时长增加40%，二次消费提升25%。</li>
<li><strong>医疗普惠</strong>：联影智能的AI育种系统提升作物产量20%，AI诊断工具在基层医院覆盖率突破60%。</li>
</ul>
<p><strong>4. 伦理觉醒：在创新与约束间平衡</strong></p>
<ul>
<li><strong>透明性</strong>：微软AETHER小组对所有AI产品进行安全审查，避免算法偏见。</li>
<li><strong>普惠性</strong>：OpenAI向免费用户开放o3-mini模型，DeepSeek开源代码推动技术民主化。</li>
</ul>
<hr>
<h3 id="结语：重写文明的操作系统"><a class="header-anchor" href="#结语：重写文明的操作系统">¶</a>结语：重写文明的操作系统</h3>
<p>1785年，卢德分子砸毁机器，却无法阻挡纺织女工被蒸汽机取代；2025年，抗拒AI者或将重演历史。但这一次，变革不再局限于某个行业——AI大模型正在重写整个人类文明的操作系统：</p>
<ul>
<li><strong>生产维度</strong>：从“制造”到“智造”，个性化定制成本逼近大规模生产。</li>
<li><strong>认知维度</strong>：人类首次拥有超越生物脑容量的“外接智能”，知识获取效率指数级跃迁。</li>
<li><strong>文明维度</strong>：AI终端的普及让技术融入生活肌理，人类从“使用工具”进阶为“与智能体共生”。</li>
</ul>
<p>正如李彦宏所言：“未来不属于大模型，而属于百万AI原生应用。”在这场洪流中，唯一的选择是跳入驾驶舱，以代码为杠杆，撬动属于智能时代的无限可能。</p>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
      </tags>
  </entry>
  <entry>
    <title>Eclipse插件修改之旅</title>
    <url>/eclipse/eclipse-plugin-modify.html</url>
    <content><![CDATA[<p>大家有没有遇到Eclipse有些插件有问题，但是又不知道怎么修改，接下来就教大家如何修改Eclipse的插件。<br>
<img src="https://s1.ax1x.com/2020/08/16/dVSHUO.png" alt="Eclispe插件开发.png"></p>
<a id="more"></a>
<h1 id="背景"><a class="header-anchor" href="#背景">¶</a>背景</h1>
<p>最近MyEclipse老是出现内存溢出，错误信息如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">!ENTRY org.eclipse.core.jobs 4 2 2020-08-11 11:08:00.677</span><br><span class="line">!MESSAGE An internal error occurred during: &quot;JSP Index Manager: Processing Resource Events&quot;.</span><br><span class="line">!STACK 0</span><br><span class="line">java.lang.OutOfMemoryError: unable to create native thread: possibly out of memory or process/resource limits reached</span><br><span class="line">	at java.base/java.lang.Thread.start0(Native Method)</span><br><span class="line">	at java.base/java.lang.Thread.start(Thread.java:803)</span><br><span class="line">	at org.eclipse.sapphire.java.jdt.internal.JdtJavaTypeReferenceService$1.elementChanged(JdtJavaTypeReferenceService.java:91)</span><br></pre></td></tr></table></figure>
<p>第一反应应该是调MyEclipse内存，但是一看不对，报错是无法创建线程，然后用Java VisualVM工具监控，发现线程实时峰值最高达到9532，如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/08/11/aLcbnS.png" alt="线程监控.png"></p>
<p>然后dump出线程日志，发现3000+线程都阻塞的：</p>
<p><img src="https://s1.ax1x.com/2020/08/11/aLgY9I.png" alt="线程日志情况.png"></p>
<p>经查阅这些线程都是由sapphire插件创建的，插件的介绍：Create models that reference Java types in Eclipse Java projects.</p>
<p>找到对应的org.eclipse.sapphire.java.jdt_9.1.1.201712191343.jar反编译看看：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Thread thread = <span class="keyword">new</span> Thread()</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (!value.disposed() &amp;&amp; !value.root().disposed())</span><br><span class="line">      &#123;</span><br><span class="line">        JdtJavaTypeReferenceService.<span class="keyword">null</span>.<span class="keyword">this</span>.<span class="keyword">this</span>$<span class="number">0</span>.refresh();</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">thread.start();</span><br></pre></td></tr></table></figure>
<p>果然都是直接创建的线程，估计插件开发者没有考虑到会发生阻塞的情况，当代码变化量比较大的时候，比如git分支切换，workspace又在干其它事儿的时候，这个地方就会创建大量线程，导致内存溢出。</p>
<h1 id="解决方案"><a class="header-anchor" href="#解决方案">¶</a>解决方案</h1>
<h2 id="修改插件源码"><a class="header-anchor" href="#修改插件源码">¶</a>修改插件源码</h2>
<p>于是呼就想着先把这个问题处理了，修改源码，使用线程池。</p>
<p>那么问题来了，怎么修改这个插件的源码呢。</p>
<p>首先打开 window-&gt;show view，选择 plugin-ins。</p>
<p>如果没有就先要安装，打开 Help -&gt; Install New Software，</p>
<p>work with 选择 --All Available Sites–，在下面找到 Plugin-in Development（好像是这个，不太记得了，看到就清楚）。</p>
<p>下面的操作就是next，finish之类的。</p>
<p>打开plugin-ins后，找到 org.eclipse.sapphire.java.jdt，右键单击，选择import as -&gt; source project，导入之后在你的 workspace</p>
<p>就可以看到这个project，如果没有src文件，你还得去下载源码。</p>
<p>源码链接：<span class="exturl" data-url="aHR0cDovL2Z0cC5nbm9tZS5vcmcvbWlycm9yL2VjbGlwc2Uub3JnL3NhcHBoaXJlLzkuMS4xL3JlcG9zaXRvcnkvcGx1Z2lucy8=" title="http://ftp.gnome.org/mirror/eclipse.org/sapphire/9.1.1/repository/plugins/">http://ftp.gnome.org/mirror/eclipse.org/sapphire/9.1.1/repository/plugins/<i class="fa fa-external-link"></i></span></p>
<p>下载后复制到Eclipse安装目录下的.\eclipse\plugins文件夹下，重启Eclipse，</p>
<p>重新import as就看到src文件夹了。</p>
<p><img src="https://s1.ax1x.com/2020/08/11/aLRpeU.png" alt="plugin.png"></p>
<p>然后找到<code>JdtJavaTypeReferenceService</code>类，修改对应代码，使用线程池：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">threadPool.execute(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>( ! value.disposed() &amp;&amp; ! value.root().disposed() )</span><br><span class="line">        &#123;</span><br><span class="line">            refresh();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<h2 id="打包插件"><a class="header-anchor" href="#打包插件">¶</a>打包插件</h2>
<p>接下只剩下打包，然后替换测试了，项目右键-》Plug-in Tools-》Open Manifest，然后开始打包，如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/08/11/aLRhtJ.png" alt="plugin-deploy.png"></p>
<p>最后替换到eclipse插件目录测试，当然记得备份。</p>
<p>到这里eclipse插件修改圆满结束。</p>
]]></content>
      <categories>
        <category>开发工具</category>
      </categories>
      <tags>
        <tag>软件开发</tag>
        <tag>Eclipse</tag>
        <tag>插件修改</tag>
      </tags>
  </entry>
  <entry>
    <title>UML基础</title>
    <url>/engineering/uml.html</url>
    <content><![CDATA[<p>近几年来，面向对象几乎成为软件技术的代名词。而UML是面向对象方法的一面旗帜，谈到面向对象的分析和设计就不能不谈到UML。如今UML也成为面向对象分析和设计事实上的行业标准。然而什么是UML，如何利用UML将一份原始需求经过层层分析和推导，最终形成可执行的代码？</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GVBGHf.png" alt></p>
<a id="more"></a>
<h1 id="为什么需要uml"><a class="header-anchor" href="#为什么需要uml">¶</a>为什么需要UML</h1>
<p>面向过程还是面向对象？</p>
<h2 id="面向过程方法"><a class="header-anchor" href="#面向过程方法">¶</a>面向过程方法</h2>
<p>面向过程方法认为我们的世界是由一个个相互关联的小系统组成的，每个小系统都有着明确的开始和明确的结束，开始和结束之间有着严谨的因果关系。只要我们将这个小系统中的每一个步骤和影响这个小系统走向的所有因素都分析出来，我们就能完全定义这个系统的行为。</p>
<h2 id="面向对象方法"><a class="header-anchor" href="#面向对象方法">¶</a>面向对象方法</h2>
<p>​       面向对象（Object Oriented，简称OO）方法将世界看作一个个相互独立的对象，相互之间并无因果关系，它们平时是“鸡犬之声相闻，老死不相往来”的。只有在某个外部力量的驱动下，对象之间才会依据某种规律相互传递信息。这些交互构成了这个生动世界的一个“过程”。在没有外力的情况下，对象则保持着“静止”的状态。</p>
<ul>
<li>对象是怎么被抽象出来的？现实世界和对象世界看上去差别是那么大，为什么要这么抽象而不是那么抽象呢？（Why）
<ul>
<li><strong>一种把现实世界映射到对象世界的方法</strong></li>
</ul>
</li>
<li>对象世界由于其灵活性，可以任意组合，可是我们怎么知道某个组合就正好满足了现实世界的需求呢？什么样的组合是好的，什么样的组合是差的呢？（How）</li>
<li><strong>一种从对象世界描述现实世界的方法</strong></li>
<li>抛开现实世界，对象世界是如此的难以理解。如果只给我一个对象组合，我怎么才能理解它表达了怎样的含义呢？（What）</li>
<li><strong>一种验证对象世界行为是否正确反映了现实世界的方法</strong></li>
</ul>
<h1 id="什么是uml"><a class="header-anchor" href="#什么是uml">¶</a>什么是UML</h1>
<p>统一建模语言（Unified Modeling Language）是一种建模用的语言，而所有的语言都是由基本词汇和语法两个部分构成的，UML也不例外。UML定义了一些建立模型所需要的、表达某种特定含义的基本元素，这些元素称为元模型，相当于语言中的基本词汇，例如用例、类等。另外，UML还定义了这些元模型互相之间关系的规则，以及如何用这些元素和规则绘制图形以建立模型来映射现实世界；这些规则和图形称为表示法或视图（View），相当于语言中的语法。</p>
<ul>
<li>
<p>统一</p>
<ul>
<li>秦始皇历史上的一大功绩便是统一语言和度量衡</li>
<li>统一的目标就是形成标准，任何一种组件化开发模式背后都有一个标准在规范和指导，可以说没有标准就没有工业现代化，没有标准就没有编程组件化，这就是标准的意义。</li>
</ul>
</li>
</ul>
<h2 id="从现实世界到业务模型"><a class="header-anchor" href="#从现实世界到业务模型">¶</a>从现实世界到业务模型</h2>
<p>这是把现实世界映射到对象世界的第一步。UML采用用例这一关键元素捕获了现实世界的人要做的事，再通过用例场景、领域模型等视图将现实世界的人、事、物、规则这些构成现实世界的元素用UML这种语言描述出来。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GV35uQ.png" alt="从现实世界到业务模型"></p>
<h2 id="从业务模型到概念模型"><a class="header-anchor" href="#从业务模型到概念模型">¶</a>从业务模型到概念模型</h2>
<p>这是从对象世界来描述现实世界的方法。用例所代表的现实的业务过程，被“边界”、“控制”、“实体”以及“包”、“组件”等概念替代。而这些概念是可以被计算机理解的，是抽象化了的对象。现实世界千差万别的业务，都用“边界”、“控制”、“实体”这几个固定的元素来描述，也就是说，现实具体的业务被“抽象”成几个固定的概念。同时，这些概念还可以用“包”、“组件”等这些与现实世界毫不相关的纯计算机逻辑术语包装。这说明概念模型是计算机视角，或者说是对象视角，而且这些对象视角的分析类所描述的信息是从映射了现实世界的业务模型转化而来的。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GV80P0.png" alt="从业务模型到概念模型"></p>
<h2 id="从概念模型到设计模型"><a class="header-anchor" href="#从概念模型到设计模型">¶</a>从概念模型到设计模型</h2>
<p>这是验证对象世界是否正确反映了现实世界的方法。“边界”、“控制”、“实体”这些对象化的概念，虽然是计算机可以理解的，但它并不是真正的对象实例，即它们并不是可执行代码，概念模型只是纸上谈兵。真正的对象世界行为是由Java类、C++类、EJB、COM+等这些可执行代码构成的。设计模型是概念模型在特定环境和条件下的“实例”化，实例化后的对象行为“执行”了概念模型描述的那些信息，因此设计模型得以通过概念模型追溯到原始需求来验证对象世界是否正确反映了现实世界。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GV8RaR.png" alt="从概念模型到设计模型"></p>
<p>如果把三个模型的建立过程综合起来，如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GVYhin.png" alt></p>
<p>从中我们可以更清楚地看到面向对象的困难是如何在模型的转化过程中解决的。这一过程看来是有规律、可推导、可追溯的过程。</p>
<h1 id="uml核心视图"><a class="header-anchor" href="#uml核心视图">¶</a>UML核心视图</h1>
<h2 id="静态视图"><a class="header-anchor" href="#静态视图">¶</a>静态视图</h2>
<p>故名思义，静态视图就是表达静态事物的。它只描述事物的静态结构，而不描述其动态行为。静态视图包括用例图、类图和包图。</p>
<h3 id="用例图"><a class="header-anchor" href="#用例图">¶</a>用例图</h3>
<p>用例视图采用参与者和用例作为基本元素，以不同的视角展现系统的功能性需求。</p>
<h4 id="业务用例视图"><a class="header-anchor" href="#业务用例视图">¶</a>业务用例视图</h4>
<p>业务用例视图使用业务主角和业务用例展现业务建模的结果。</p>
<ul>
<li>业务主角视图</li>
</ul>
<p>从业务主角视角来展示业务主角在业务中使用哪些业务用例来达成业务目标。如果业务主角认为其所有目标都已经齐全，则认为针对此主角的业务用例定义完成，以此来检查是否有遗漏的业务用例没有发现。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZb1df.png" alt="业务主角视图"></p>
<ul>
<li>业务模块视图</li>
</ul>
<p>从业务模块视角来展示业务领域的业务目标，将参与了达成这一业务目标的业务主角与业务用例展现在这个视图中。如果这项业务能够被这些业务主角和业务用例完整地说明，则认为针对此业务模块的业务用例定义完成，以此来检查是否有遗漏的业务用例没有发现。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZWYuD.png" alt="业务模块视图"></p>
<h4 id="业务用例实现视图"><a class="header-anchor" href="#业务用例实现视图">¶</a>业务用例实现视图</h4>
<p>​		业务用例实现视图展现业务用例有哪些实现途径。业务用例是业务需求，而业务用例实现则是业务的实现途径，从软件工程的角度说，这个视图展示了需求的可追溯特点。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZq9fg.png" alt="业务用例视图"></p>
<h4 id="概念用例视图"><a class="header-anchor" href="#概念用例视图">¶</a>概念用例视图</h4>
<p>​		概念用例视图用于展现从业务用例中经过分析分解出来的关键概念用例，并表示概念用例和业务用例之间的关系。一般来说这些关系有扩展、包含和精化。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZLCE6.png" alt="概念用例视图"></p>
<h4 id="系统用例视图"><a class="header-anchor" href="#系统用例视图">¶</a>系统用例视图</h4>
<p>​		系统用例视图展现系统范围，将对业务用例进行分析以后得到的系统用例展现出来。系统用例视图就是系统的开发范围。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZLnbt.png" alt="系统用例视图"></p>
<h4 id="系统用例实现视图"><a class="header-anchor" href="#系统用例实现视图">¶</a>系统用例实现视图</h4>
<p>​		如果一个系统用例有多种实现方式，也应当为其绘制实现视图。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZL6M9.png" alt="系统用例实现视图"></p>
<blockquote>
<p>​		在实际项目中，不是所有的用例视图都一定要采用。根据情况可进行适当裁减，例如只保留业务用例视图和系统用例视图。在许多项目中实际上只有系统用例视图，不论如何，我们应当知道用例图在不同的生命周期阶段还有不同的表达，从而选择适合自己项目的视图。</p>
</blockquote>
<h4 id="常用元素参考"><a class="header-anchor" href="#常用元素参考">¶</a>常用元素参考</h4>
<p><img src="https://s1.ax1x.com/2020/03/30/GeCCy8.png" alt="用例图常用元素"></p>
<h3 id="类图"><a class="header-anchor" href="#类图">¶</a>类图</h3>
<p>类图用于展示系统中的类及其相互之间的关系。本质上说，类图是现实世界问题领域的抽象对象的结构化、概念化、逻辑化描述。</p>
<h4 id="概念层类图"><a class="header-anchor" href="#概念层类图">¶</a>概念层类图</h4>
<p>概念层的观点认为，在这个层次的类图描述的是现实世界中问题领域的概念理解，类图中表达的类与现实世界的问题领域有着明显的对应关系，类之间的关系也与问题领域中实际事物的关系有着明显的对应关系。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZLXIf.png" alt="概念层类图"></p>
<h4 id="说明层类图"><a class="header-anchor" href="#说明层类图">¶</a>说明层类图</h4>
<p>​		说明层的观点认为，在这个层次的类图考察的是类的接口而不是实现，类图中表达的类和类关系应当是对问题领域在接口层次抽象的描述。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZOAoV.png" alt="说明层类图"></p>
<h4 id="实现层类图"><a class="header-anchor" href="#实现层类图">¶</a>实现层类图</h4>
<p>​		实现层观点认为，类是实现代码的描述，类图中的类直接映射到可执行代码。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZO3o6.png" alt="实现层类图"></p>
<h4 id="常用元素参考-v2"><a class="header-anchor" href="#常用元素参考-v2">¶</a>常用元素参考</h4>
<p><img src="https://s1.ax1x.com/2020/03/30/Ge9Xod.png" alt="类图常用元素"></p>
<h3 id="包图"><a class="header-anchor" href="#包图">¶</a>包图</h3>
<p>​		在实际项目中，建模过程中获得的元素是非常多的，如果要将这些元素的关系都绘制出来，将如同蜘蛛网一样难以辨别。通过包这个容器来从大到小、从粗到细地建立关系是一种很好的办法。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZO0eI.png" alt="网上购物-领域包图"></p>
<p>​		在UML所有视图中，包图或许是最自由，约束最小的一种。除了特定的版型之外，包几乎可以用在任何阶段。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZO7fU.png" alt="层次包图"></p>
<h2 id="动态视图"><a class="header-anchor" href="#动态视图">¶</a>动态视图</h2>
<p>故名思义，动态视图是描述事物动态行为的。需要注意的是，动态视图不能够独立存在，它必须特指一个静态视图或UML元素，说明在静态视图规定的事物结构下它们的动态行为。动态视图包括活动图、状态图、时序图和协作图。</p>
<h3 id="活动图"><a class="header-anchor" href="#活动图">¶</a>活动图</h3>
<p>活动图描述了为了完成某一个目标需要做的活动以及这些活动的执行顺序。UML中有两个层面的活动图，一种用于描述用例场景，另一种用于描述对象交互。</p>
<h4 id="用例活动图"><a class="header-anchor" href="#用例活动图">¶</a>用例活动图</h4>
<p>用例活动图是最经常使用的。用例表达了参与者的一个目标，用例场景则描述了如何来达到这个目标。活动图用来描述用例场景，也就是通常所说的业务流程。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZXi1e.png" alt="登机手续用例场景活动图示例"></p>
<h4 id="对象活动图"><a class="header-anchor" href="#对象活动图">¶</a>对象活动图</h4>
<p>​		对象活动图用于展示对象的交互。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZXw3F.png" alt="对象活动图"></p>
<h4 id="泳道"><a class="header-anchor" href="#泳道">¶</a>泳道</h4>
<p>​		上面的活动图描述了业务流程中活动的执行顺序，却没有描述出谁来执行这些活动，即执行业务流程的职责被遗漏了。泳道技术的引入多多少少解决了活动图不能描述对象职责的遗憾。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZXOC8.png" alt="对象交互泳道图"></p>
<blockquote>
<p>​		状态图是很有用的技术，尤其在描述单个复杂对象的行为时非常有助于我们理解一个对象的行为。但是在建模时并不需要对每个对象都绘制状态图。建议，仅对领域模型中最为关键的业务对象，尤其是当其在一个或多个用例场景中参与了多个活动时，才对其建模。</p>
</blockquote>
<h4 id="常用元素参考-v3"><a class="header-anchor" href="#常用元素参考-v3">¶</a>常用元素参考</h4>
<p><span class="exturl" data-url="aHR0cHM6Ly9pbWdjaHIuY29tL2kvR2U5Z0xG" title="https://imgchr.com/i/Ge9gLF"><img src="https://s1.ax1x.com/2020/03/30/Ge9gLF.png" alt="活动图常用元素"><i class="fa fa-external-link"></i></span></p>
<h3 id="状态图"><a class="header-anchor" href="#状态图">¶</a>状态图</h3>
<p>​		状态图显示一个状态机，通常使用状态图来说明业务角色或业务实体可能的状态——导致状态转换的事件和状态转换引起的操作。状态机主要用于描述对象的状态变化以确定何种行为改变了对象状态，以及对象状态变化对系统的影响。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZjkCT.png" alt="图书生命周期状态图"></p>
<h4 id="常用元素参考-v4"><a class="header-anchor" href="#常用元素参考-v4">¶</a>常用元素参考</h4>
<p><img src="https://s1.ax1x.com/2020/03/30/Ge9wIs.png" alt="状态图常用元素"></p>
<h3 id="时序图"><a class="header-anchor" href="#时序图">¶</a>时序图</h3>
<p>​		时序图用于描述按时间顺序排列的对象之间的交互模式；它按照参与交互的对象所具有的“生命线”和它们相互发送的消息来显示这些对象。在时序图中包含对象和主角实例，以及说明它们如何交互的消息。</p>
<h4 id="业务模型时序图"><a class="header-anchor" href="#业务模型时序图">¶</a>业务模型时序图</h4>
<p>​		业务模型时序图用于为领域模型中的业务实体交互建模，其目标是实现业务用例。在绘制业务实体时序图之前，你应当已经绘制了业务用例实现过程的活动图。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZjYKH.png" alt="网上购买商品业务模型时序图"></p>
<h4 id="概念模型时序图"><a class="header-anchor" href="#概念模型时序图">¶</a>概念模型时序图</h4>
<p>​		概念用例时序图通常是依据业务模型场景图来绘制的，它将业务模型场景用分析类重新绘制一遍，这样，既保留了实际业务需求，又得到了计算机实现的基本理念。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZjdat.png" alt="购买商品概念模型时序图片断"></p>
<h4 id="设计模型时序图"><a class="header-anchor" href="#设计模型时序图">¶</a>设计模型时序图</h4>
<p>​		设计模型时序图使用设计类作为对象绘制。目标是实现概念模型中的某个事件流，一般以一个完整交互为单位，消息细致到方法级别。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZjXIx.png" alt="登录和查询事件流设计模型时序图片断"></p>
<blockquote>
<p>​		时序图的三种应用场合是在建模过程中经常使用的动态视图。除了这些场合，在任何时候需要表达对象间的交互时，或者想分析对象的职责和接口时都可以使用时序图。</p>
</blockquote>
<h4 id="常用元素参考-v5"><a class="header-anchor" href="#常用元素参考-v5">¶</a>常用元素参考</h4>
<p><img src="https://s1.ax1x.com/2020/03/30/GepX80.png" alt="时序图常用元素"></p>
<h3 id="协作图"><a class="header-anchor" href="#协作图">¶</a>协作图</h3>
<h4 id="业务模型协作图"><a class="header-anchor" href="#业务模型协作图">¶</a>业务模型协作图</h4>
<p>​		业务模型协作图同样采用业务实体来绘制，目标也是实现用例场景。不过有时候协作图并不要求实现完整的场景，只需要将影响对象的关键消息绘制出来即可。因为协作图更在意的是对象的结构及其相互的影响。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZvFeA.png" alt="业务模型协作图"></p>
<blockquote>
<p>某些建模工具（rose powerdesigner）可以直接把时序图转化为协作图</p>
</blockquote>
<h4 id="概念模型协作图"><a class="header-anchor" href="#概念模型协作图">¶</a>概念模型协作图</h4>
<p>​		与时序图相同，概念阶段的协作图采用分析类来绘制，目标是实现业务用例。同样这个阶段的协作图已经带有计算机理解。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZvnSS.png" alt="购买商品概念模型协作图片断"></p>
<h4 id="设计模型协作图"><a class="header-anchor" href="#设计模型协作图">¶</a>设计模型协作图</h4>
<p>​		与时序图相同，设计模型协作图使用设计类为对象来绘制。目标是实现概念模型中的某个事件流，一般以一个完整交互为单位，消息细致到方法级别。</p>
<p><img src="https://s1.ax1x.com/2020/03/29/GZvKyQ.png" alt="登录和查询事件流设计模型协作图片断"></p>
<h4 id="常用元素参考-v6"><a class="header-anchor" href="#常用元素参考-v6">¶</a>常用元素参考</h4>
<p><img src="https://s1.ax1x.com/2020/03/30/Gep9ht.png" alt="协作图常用元素"></p>
<h2 id="参考文献"><a class="header-anchor" href="#参考文献">¶</a>参考文献</h2>
<ul>
<li>《大象：Thinking in UML（第二版）》</li>
</ul>
]]></content>
      <categories>
        <category>软件工程</category>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>软件开发</tag>
        <tag>UML</tag>
        <tag>统一建模语言</tag>
      </tags>
  </entry>
  <entry>
    <title>软件开发模型</title>
    <url>/engineering/software-develop-model.html</url>
    <content><![CDATA[<p>第一次接触软件工程还是在学校的课本上，初识软件开发模型（或软件生命周期模型），工作几年之后对其终于有了一些了解。<br>
趁着这次读《系统架构设计师教程》，做个读书笔记。</p>
<p><img src="https://timgsa.baidu.com/timg?image&amp;quality=80&amp;size=b9999_10000&amp;sec=1578237412825&amp;di=0c0043ac5c95f6b861a856d0658b3f5c&amp;imgtype=0&amp;src=http%3A%2F%2Fwww.3deepspace.com%2Fwp-content%2Fuploads%2F2017%2F01%2Fsoftware-1024x410.png" alt="ZSUqoT.png"></p>
<a id="more"></a>
<h1 id="软件开发生命周期"><a class="header-anchor" href="#软件开发生命周期">¶</a>软件开发生命周期</h1>
<p>​		传统的软件生命周期是指<strong>软件产品从形成概念开始，经过定义、开发、使用和维护，直到最后被废弃（不能再使用）为止的全过程。</strong></p>
<h1 id="软件开发模型"><a class="header-anchor" href="#软件开发模型">¶</a>软件开发模型</h1>
<p>​		软件生存周期模型又称软件开发模型（software develop model）或软件过程模型（software process model），它是<strong>从某一个特定角度提出的软件过程的简化描述</strong>。</p>
<p>​		软件过程模型的基本概念：软件过程是制作软件产品的一组活动以及结果，这些活动主要由软件人员来完成，软件活动主要如下：</p>
<ul>
<li>
<p>软件描述</p>
</li>
<li>
<p>软件开发</p>
</li>
<li>
<p>软件有效性验证</p>
</li>
<li>
<p>软件进化</p>
</li>
</ul>
<p>软件过程模型是软件工程的重要内容，它为软件工程管理提供<strong>里程碑</strong>，为软件开发过程提供<strong>原则和方法</strong>。</p>
<h2 id="瀑布模型"><a class="header-anchor" href="#瀑布模型">¶</a>瀑布模型</h2>
<p>瀑布模型（waterfall model）可以说是最早使用的软件生命周期模型之一。该模型描述的活动从一个阶段到另一个阶段逐次下降，它的工作流程形式上又很想瀑布，该模型如下图所示：</p>
<p><img src="https://ss2.bdstatic.com/70cFvnSh_Q1YnxGkpoWK1HF6hhy/it/u=243514752,3051001484&amp;fm=26&amp;gp=0.jpg" alt="瀑布模型.png"></p>
<p><strong>特点:</strong></p>
<ul>
<li>阶段间具有顺序性和依赖性:
<ul>
<li>因果关系紧密相连：前一阶段完成后，才能开始后一阶段。</li>
<li>前一个阶段工作的结果是后一个阶段工作的输入</li>
</ul>
</li>
<li>质量保证:
<ul>
<li>每个阶段必须交付出合格的文档</li>
<li>对文档进行审核</li>
</ul>
</li>
</ul>
<p><strong>缺点：</strong></p>
<ul>
<li>软件需求分析的准确性很难确定</li>
<li>初始版本周期长</li>
<li>如果改变需求，代价巨大</li>
</ul>
<h2 id="原型模型"><a class="header-anchor" href="#原型模型">¶</a>原型模型</h2>
<p>​		原型模型（prototype model）又称快速原型。由于瀑布模型的缺点，人们借助建筑师、工程师建造原型的经验，提出了原型模型。原型模型主要有两个阶段，如下图所示：</p>
<p><img src="http://31.toocle.com/Upload/zk/glbk/20089208594231277801.jpg" alt="原型模型.png"></p>
<ul>
<li><strong>原型开发阶段</strong>：根据用户提出的软件系统定义，快速地开发一个原型。
<ul>
<li>利用模拟软件系统的人机界面和人机交互方式。</li>
<li>真正开发一个原型。</li>
<li>找一个或多个正在运行的类似软件进行比较。</li>
</ul>
</li>
<li>目标软件开发阶段。</li>
</ul>
<p>使用原型模型应该注意：</p>
<ul>
<li>
<p>要有一定的开发环境和工具支持。</p>
</li>
<li>
<p>经过对原型的若干次修改，应<strong>收敛到目标范围内</strong>，否则可能会失败。</p>
</li>
<li>
<p>用户对系统模糊不清，无法准确回答目标系统的需求。</p>
</li>
<li>
<p>对于大型软件来说，原型可能非常复杂而难以快速形成，如果没有现成的，就不应该考虑原型法。</p>
</li>
</ul>
<h2 id="螺旋模型"><a class="header-anchor" href="#螺旋模型">¶</a>螺旋模型</h2>
<p>​		螺旋模型（Spiral Model）是在快速原型的基础上扩展而成，实际上，它是<strong>软件生命周期模型与原型模型的一个结合</strong>，如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/08/16/dVCeEV.png" alt="螺旋模型.png"></p>
<p>该模型将整个软件开发流程分为多个阶段，每个阶段都由4部分组成：</p>
<ul>
<li>目标设定</li>
<li>风险分析</li>
<li>开发和有效性验证</li>
<li>评审</li>
</ul>
<p>螺旋模型的软件开发过程实际上是上述4个部分的迭代过程，每迭代一次，螺旋线就增加一周，代表软件的一个新版本。<strong>经过若干次迭代后，系统应尽快收敛到用户允许或可以接受的目标范围。</strong></p>
<p><strong>优点:</strong></p>
<ul>
<li>设计上的灵活性,可以在项目的各个阶段进行变更。</li>
<li>以小的分段来构建大型系统，使成本计算变得简单容易。</li>
<li>客户始终参为保证了项目不偏离正确方向以及项目的可控性。</li>
<li>客户始终掌握项目的最新信息，从而他或她能够和管理层有效地交互。</li>
<li>客户认可这种公司内部的开发方式带来的良好的沟通和高质量的产品。</li>
</ul>
<p><strong>缺点:</strong></p>
<p>​		很难让用户确信这种演化方法的结果是可以控制的。建设周期长，而软件技术发展比较快，所以经常出现软件开发完毕后，和当前的技术水平有了较大的差距，无法满足当前用户需求。</p>
<h2 id="增量模型"><a class="header-anchor" href="#增量模型">¶</a>增量模型</h2>
<p>增量模型（Incremental Model）把待开发的软件系统模块化，将每个模块作为一个增量组件，从而分批次地分析、设计、编码和测试这些增量组件，如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/08/16/dVC8D1.png" alt="增量模型.png"></p>
<p>每个新的构件集成到现有的软件结构中必须破坏原来以开发的产品，所以必须定义很好的接口。</p>
<p><strong>优点</strong>:</p>
<ul>
<li>短时间内向用户提供可完成部分工作的产品。</li>
<li>逐步增加产品功能可以使用户有时间了解和适应新产品。</li>
<li>开放结构的软件拥有的维护性明显好于封闭结构的软件。</li>
</ul>
<p><strong>缺点</strong>：</p>
<ul>
<li>容易退化为边做边改模型，从而使软件过程的控制失去整体性。</li>
<li>如果增量包之间存在相交的情况且未很好处理，则必须做全盘系统分析。</li>
</ul>
<h2 id="喷泉模型"><a class="header-anchor" href="#喷泉模型">¶</a>喷泉模型</h2>
<p>​		喷泉模型（fountain model）是一种<strong>以用户需求为动力，以对象为驱动</strong>的模型，主要用于描述面向对象的软件开发过程。该模型认为软件开发过程自下而上周期的各阶段是相互迭代和无间隙的特性，该模型如下图所示：</p>
<p><img src="https://timgsa.baidu.com/timg?image&amp;quality=80&amp;size=b9999_10000&amp;sec=1578285715275&amp;di=fd8ff6300c55f97f912b110aef1d5ff9&amp;imgtype=0&amp;src=http%3A%2F%2Fwww.cnitpm.com%2Fupload%2F2013-04%2F201304091646335151.jpg" alt="喷泉模型.png"></p>
<p><strong>优点</strong>:</p>
<ul>
<li>喷泉模型不像瀑布模型那样，需要分析活动结束后才开始设计活动，设计活动结束后才开始编码活动。</li>
<li>该模型的各个阶段没有明显的界限，开发人员可以同步进行开发。</li>
<li>可以提高软件项目开发效率，节省开发时间，适应于面向对象的软件开发过程。</li>
</ul>
<p><strong>缺点</strong>:</p>
<ul>
<li>由于喷泉模型在各个开发阶段是重叠的，因此在开发过程中需要大量的开发人员，因此不利于项目的管理。</li>
<li>这种模型要求严格管理文档，使得审核的难度加大，尤其是面对可能随时加入各种信息、需求与资料的情况。</li>
</ul>
<h1 id="敏捷开发"><a class="header-anchor" href="#敏捷开发">¶</a>敏捷开发</h1>
<p>敏捷开发以用户的需求进化为核心，采用迭代、循序渐进的方法进行软件开发，其核心思想主要有以下三点：</p>
<ul>
<li><strong>适应型</strong>，而非可预测型。</li>
<li><strong>以人为本</strong>，而非已过程为本。</li>
<li>迭代增量式的开发过程。</li>
</ul>
<p>敏捷开发知识体系整体框架</p>
<p><img src="http://www.uml.org.cn/SoftWareProcess/images/2018081544.png" alt="img"></p>
<p><img src="http://www.uml.org.cn/SoftWareProcess/images/2018081545.png" alt></p>
<h2 id="敏捷软件开发宣言"><a class="header-anchor" href="#敏捷软件开发宣言">¶</a>敏捷软件开发宣言</h2>
<ul>
<li>
<p>个体和交互胜过过程和工具。</p>
<p>以人为本，强调个体及个体间的沟通与协作在软件开发过程中的重要性。</p>
</li>
<li>
<p>可以工作的软件胜过面面俱到的文档。</p>
<p>目标导向，自解释的友好的代码和架构。</p>
</li>
<li>
<p>客户合作胜过合同谈判。</p>
<p>客户为先，理解客户的需求，懂得如何与客户合作。</p>
</li>
<li>
<p>响应变化胜过遵循计划。</p>
<p>拥抱变化，在客户断变化的需求中了解其真正的需要。</p>
</li>
</ul>
<h2 id="敏捷宣言遵循的原则"><a class="header-anchor" href="#敏捷宣言遵循的原则">¶</a>敏捷宣言遵循的原则</h2>
<ul>
<li>
<p>我们最优先要做的是通过尽早的、持续的交付有价值的软件来使客户满意。</p>
</li>
<li>
<p>即使到了开发的后期，也欢迎改变需求。敏捷过程利用变化来为客户创造竞争优势。</p>
</li>
<li>
<p>经常性地交付可以工作的软件，交付的间隔可以从几个星期到几个月，交付的时间间隔越短越好。</p>
</li>
<li>
<p>在整个项目开发期间，业务人员和开发人员必须天天都在一起工作。</p>
</li>
<li>
<p>围绕被激励起来的个体来构建项目。给他们提供所需的环境和支持，并且信任他们能够完成工作。</p>
</li>
<li>
<p>在团队内部，最具有效果并且富有效率的传递信息的方法，就是面对面的交谈。</p>
</li>
<li>
<p>工作的软件是首要的进度度量标准。</p>
</li>
<li>
<p>敏捷过程提倡可持续的开发速度。责任人、开发者和用户应该能够保持一个长期的、恒定的开发速度。</p>
</li>
<li>
<p>不断地关注优秀的技能和好的设计会增强敏捷能力。</p>
</li>
<li>
<p>简单——使未完成的工作最大化的艺术——是根本的。</p>
</li>
<li>
<p>最好的构架、需求和设计是出自于自组织的团队。</p>
</li>
<li>
<p>每隔一定时间，团队会在如何才能更有效地工作方面进行反省，然后相应地对自己的行为进行调整。</p>
</li>
</ul>
<h2 id="敏捷设计"><a class="header-anchor" href="#敏捷设计">¶</a>敏捷设计</h2>
<h3 id="拙劣设计的症状"><a class="header-anchor" href="#拙劣设计的症状">¶</a>拙劣设计的症状</h3>
<p>僵化性：设计难以改变。</p>
<p>脆弱性：设计易于遭到破坏。</p>
<p>牢固性：设计难以重用。</p>
<p>粘滞性：难以做正确的事情。</p>
<p>不必要的复杂性：过分设计。</p>
<p>不必要的重复：复制黏贴。</p>
<p>晦涩性：混乱的表达。</p>
<p>这些症状在本质上和代码的臭味相似，但是它们所处的层次稍高一些。它们是遍及整个软件结构的臭味，而不仅仅是一小段代码。</p>
<h3 id="面向对象的设计原则"><a class="header-anchor" href="#面向对象的设计原则">¶</a>面向对象的设计原则</h3>
<ul>
<li>
<p>SRP 单一职责原则：就一个类而言，应该仅有一个引起它变化的原因。</p>
</li>
<li>
<p>OCP 开放-封闭原则：软件实体（类、模块、函数等）应该是可以扩展的，但是不可修改。</p>
</li>
<li>
<p>LSP Liskov替换原则：子类型必须能够替换掉它们的及类型。</p>
</li>
<li>
<p>DIP 依赖倒置原则：抽象不应该依赖于细节，细节应该依赖于抽象。</p>
</li>
<li>
<p>ISP 接口隔离原则：不应该强迫客户依赖于它们不用的方法。接口属于客户，不属于它所在的类层次结构。</p>
</li>
</ul>
<p>23种设计模式：<span class="exturl" data-url="aHR0cDovL3d3dy5ydW5vb2IuY29tL2Rlc2lnbi1wYXR0ZXJuL2Rlc2lnbi1wYXR0ZXJuLWludHJvLmh0bWw=" title="http://www.runoob.com/design-pattern/design-pattern-intro.html">http://www.runoob.com/design-pattern/design-pattern-intro.html<i class="fa fa-external-link"></i></span></p>
<p>我们最常用的spring框架就使用了很多种设计模式，例如BeanFactory中的工厂模式，AOP中的代理模式。</p>
<p>参考文献：</p>
<ul>
<li>《系统架构设计师教程》</li>
<li>《敏捷软件开发(原则模式与实践)》</li>
</ul>
]]></content>
      <categories>
        <category>软件工程</category>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>软件开发模型</tag>
        <tag>software develop model</tag>
        <tag>软件生命周期模型 - 敏捷开发</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo搭建个人博客</title>
    <url>/hexo/init.html</url>
    <content><![CDATA[<p>之前基于Hexo搭建了个人博客网站，最近换了个Linux系统，差不多又重新搭建了一遍hexo，而且还遇到了许多问题，所以在这里记录一下hexo的搭建方法。</p>
<p><img src="https://s2.ax1x.com/2019/06/22/ZpxeNd.png" alt="ZpxeNd.png"></p>
<a id="more"></a>
<h1 id="什么是hexo"><a class="header-anchor" href="#什么是hexo">¶</a>什么是Hexo</h1>
<p>Hexo 是一个快速、简洁且高效的博客框架。Hexo 使用 Markdown（或其他渲染引擎）解析文章，在几秒内，即可利用靓丽的主题生成静态网页。</p>
<h1 id="安装前提"><a class="header-anchor" href="#安装前提">¶</a>安装前提</h1>
<h2 id="node-js-should-be-at-least-nodejs-6-9"><a class="header-anchor" href="#node-js-should-be-at-least-nodejs-6-9">¶</a>Node.js (Should be at least nodejs 6.9)</h2>
<p>直接使用已编译好的包<br>
Node 官网已经把 linux 下载版本更改为已编译好的版本了，我们可以直接下载解压后使用：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">wget https://nodejs.org/dist/v10.16.0/node-v10.16.0-linux-x64.tar.xz    // 下载</span><br><span class="line">tar xf  node-v10.16.0-linux-x64.tar.xz       // 解压</span><br><span class="line">cd node-v10.16.0-linux-x64/                  // 进入解压目录</span><br><span class="line">./bin/node -v                               // 执行node命令 查看版本</span><br><span class="line">v10.16.0</span><br></pre></td></tr></table></figure>
<p>也可以进入Node官网下载最新版本：<span class="exturl" data-url="aHR0cHM6Ly9ub2RlanMub3JnLw==" title="https://nodejs.org/">https://nodejs.org/<i class="fa fa-external-link"></i></span></p>
<p>解压文件的 bin 目录底下包含了 node、npm 等命令，我们可以使用 ln 命令来设置软连接：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ln -s /usr/software/node-v10.16.0-linux-x64/bin/npm   /usr/local/bin/ </span><br><span class="line">ln -s /usr/software/node-v10.16.0-linux-x64/bin/node   /usr/local/bin/</span><br></pre></td></tr></table></figure>
<h2 id="git"><a class="header-anchor" href="#git">¶</a>Git</h2>
<p>这个就不用多介绍了：sudo apt-get install git</p>
<h2 id="安装hexo"><a class="header-anchor" href="#安装hexo">¶</a>安装Hexo</h2>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ npm install -g hexo-cli</span><br></pre></td></tr></table></figure>
<p>安装后会执行 hexo，会发现找不到命令，把hexo加入到软连接即可：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ln -s /usr/software/node-v10.16.0-linux-x64/lib/node_modules/hexo-cli/bin/hexo   /usr/local/bin/</span><br></pre></td></tr></table></figure>
<h1 id="建站"><a class="header-anchor" href="#建站">¶</a>建站</h1>
<p>参考官方文档即可：<span class="exturl" data-url="aHR0cHM6Ly9oZXhvLmlvL3poLWNuL2RvY3Mvc2V0dXA=" title="https://hexo.io/zh-cn/docs/setup">https://hexo.io/zh-cn/docs/setup<i class="fa fa-external-link"></i></span></p>
<h1 id="主题配置"><a class="header-anchor" href="#主题配置">¶</a>主题配置</h1>
<p>参考官方文档即可：<span class="exturl" data-url="aHR0cHM6Ly90aGVtZS1uZXh0Lm9yZy8=" title="https://theme-next.org/">https://theme-next.org/<i class="fa fa-external-link"></i></span></p>
<h1 id="启动"><a class="header-anchor" href="#启动">¶</a>启动</h1>
<p>hexo server</p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>个人博客</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo 下拉刷新</title>
    <url>/hexo/pull-refresh.html</url>
    <content><![CDATA[<p>现在手机上大部分的app、网页都具备下拉刷新的功能，用着还挺爽的。</p>
<p>最近基于Hexo搭建的个人博客网站，默认居然不支持下拉刷新，索性就自己手动弄了一个下拉刷新。</p>
<p><img src="https://s2.ax1x.com/2019/06/21/ZSUqoT.png" alt="ZSUqoT.png"></p>
<a id="more"></a>
<h1 id="为什么要下拉刷新"><a class="header-anchor" href="#为什么要下拉刷新">¶</a>为什么要下拉刷新</h1>
<p>现在浏览器不都有自带的刷新功能么？</p>
<p>原因如下：</p>
<ul>
<li>相较于点击右上角刷新按钮（还有可能要点两次，第一次先展开 menu bar，然后才能看到 refresh 按钮），直接了当地下拉刷新无疑提供了更好的用户体验</li>
<li>点击刷新按钮同步重载页面必然存在一定白屏时间，而通过下拉刷新的逻辑完全可以对于页面内容进行异步更新，其体验毫无疑问更加优秀</li>
<li>移动端特有 touch 相关事件，用户在移动设备上的触摸、滑动操作频繁，习惯已经养成，下拉刷新在提供更好的体验的同时，丝毫没有增加用户的学习成本</li>
<li>很多内容 + 社交的业务场景里面，主页面的存留时长极高且内容实时性强（如微博、知乎、头条等）, 这些 Native App 已经普遍向用户提供了这种(下拉刷新)更新页面内容的交互方式。作为一个 Web 开发者，如有志于在移动领域让 Web App 和 Native App 在体验方面一较高下，那 H5 页（异步）下拉刷新功能也算是不可或缺的一环</li>
<li>当然，还可以应对一些特殊场景 … （如 Webview 不提供刷新按钮 =,=）</li>
</ul>
<h1 id="如何添加"><a class="header-anchor" href="#如何添加">¶</a>如何添加</h1>
<p>GitHub上找了很多类似的轮子，最终决定采用mescroll，下面就基于mescroll来实现hexo博客的下拉刷新功能。</p>
<h2 id="下载mescroll"><a class="header-anchor" href="#下载mescroll">¶</a>下载mescroll</h2>
<p>到mescroll官网下载，<span class="exturl" data-url="aHR0cDovL3d3dy5tZXNjcm9sbC5jb20vbG9hZC5odG1s" title="http://www.mescroll.com/load.html">mescroll.min.css,mescroll.min.js<i class="fa fa-external-link"></i></span>文件，放到themes/next/source/lib/mescroll文件夹下。</p>
<p>api文档请参考：<span class="exturl" data-url="aHR0cDovL3d3dy5tZXNjcm9sbC5jb20vYXBpLmh0bWw/dj0xOTA0MjY=" title="http://www.mescroll.com/api.html?v=190426">http://www.mescroll.com/api.html?v=190426<i class="fa fa-external-link"></i></span></p>
<h2 id="引入js文件和css文件"><a class="header-anchor" href="#引入js文件和css文件">¶</a>引入js文件和css文件</h2>
<p>修改themes/next/layout/_custom/head.swig文件，添加如下内容：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;#</span><br><span class="line">Custom head.</span><br><span class="line">#&#125;</span><br><span class="line"></span><br><span class="line">&lt;link rel=&quot;stylesheet&quot; href=&quot;&#123;&#123; url_for(theme.vendors._internal + &apos;/mescroll/mescroll.min.css?v=1.4.1&apos;) &#125;&#125;&quot;/&gt;</span><br><span class="line">&lt;script src=&quot;&#123;&#123; url_for(theme.vendors._internal + &apos;/mescroll/mescroll.min.js?v=1.4.1&apos;) &#125;&#125;&quot; charset=&quot;utf-8&quot;&gt;&lt;/script&gt;</span><br></pre></td></tr></table></figure>
<p>这样就能将js和css文件引入到HTML页面中。</p>
<h2 id="使用"><a class="header-anchor" href="#使用">¶</a>使用</h2>
<p>根据mescroll的api文档添加pull-refresh.js文件：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">new</span> MeScroll(<span class="string">'body'</span>,&#123;</span><br><span class="line">      down: &#123;</span><br><span class="line">        callback: <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">          <span class="built_in">window</span>.location.reload();</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>存放在：themes/next/source/js/pull-refresh.js路径下，然后将其引入到HTML页面中，修改themes/next/layout/_scripts/commons.swig文件，将pull-refresh.js文件引入进去。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;%</span><br><span class="line">  set js_commons = [</span><br><span class="line">    &apos;utils.js&apos;,</span><br><span class="line">    &apos;motion.js&apos;,</span><br><span class="line">    &apos;pull-refresh.js&apos;</span><br><span class="line">  ]</span><br><span class="line">%&#125;</span><br><span class="line"></span><br><span class="line">&#123;% for common in js_commons %&#125;</span><br><span class="line">  &lt;script src=&quot;&#123;&#123; url_for(theme.js) &#125;&#125;/&#123;&#123; common &#125;&#125;?v=&#123;&#123; version &#125;&#125;&quot;&gt;&lt;/script&gt;</span><br><span class="line">&#123;% endfor %&#125;</span><br></pre></td></tr></table></figure>
<p>这里只所以不将pull-refresh.js一并放到head.swig文件中，是因为其中引用的jQuery引入顺序在head.swig之后。</p>
<p>到这里本来以为搞定了，结果打开页面发现页面会不停地刷新，然后调试源代码，发现mescroll初始的时候会根据配置自动刷新一次：</p>
<p>mescroll.min.js</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (h.optDown.use &amp;&amp; h.optDown.auto &amp;&amp; f) &#123;</span><br><span class="line">        <span class="keyword">if</span> (h.optDown.autoShowLoading) &#123;</span><br><span class="line">            h.triggerDownScroll()</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            h.optDown.callback &amp;&amp; h.optDown.callback(h)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    h.optUp.use &amp;&amp; h.optUp.auto &amp;&amp; !h.isUpAutoLoad &amp;&amp; h.triggerUpScroll()</span><br><span class="line">&#125;, <span class="number">30</span>)</span><br></pre></td></tr></table></figure>
<p>于是乎修改mescroll的初始化代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">new</span> MeScroll(<span class="string">'body'</span>,&#123;</span><br><span class="line">      down: &#123;</span><br><span class="line">        callback: <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">          <span class="built_in">window</span>.location.reload();</span><br><span class="line">        &#125;,</span><br><span class="line">        auto: <span class="literal">false</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>至此，下拉刷新终于搞定了。</p>
<p>后来查看mescroll的<span class="exturl" data-url="aHR0cDovL3d3dy5tZXNjcm9sbC5jb20vYXBpLmh0bWw/dj0xOTA0MjYjb3B0aW9ucw==" title="http://www.mescroll.com/api.html?v=190426#options">参数说明<i class="fa fa-external-link"></i></span>，发现里面有提到auto的含义，这个故事告诉我们，使用前尽量多看看文档，也许能节省不少的时间。</p>
<p>参考链接</p>
<p><span class="exturl" data-url="aHR0cDovL3d3dy5tZXNjcm9sbC5jb20vaW5kZXguaHRtbA==" title="http://www.mescroll.com/index.html">http://www.mescroll.com/index.html<i class="fa fa-external-link"></i></span></p>
<p><span class="exturl" data-url="aHR0cHM6Ly9oZXhvLmlvL3poLWNuL2RvY3Mv" title="https://hexo.io/zh-cn/docs/">https://hexo.io/zh-cn/docs/<i class="fa fa-external-link"></i></span></p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>pull refresh</tag>
        <tag>下拉刷新</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows换Linux之旅</title>
    <url>/linux/Windows_to_Linux.html</url>
    <content><![CDATA[<p>随着时代的发展，各种硬件软件急速发展，Windows都出到10了，越来越觉得七八年前的笔记本安装Windows10实在是太卡了。最近闲来无事，打算将其换成Linux，换完之后确实觉得比Windows流畅多了**<sup>_</sup>**</p>
<p><img src="https://www.deepin.org/wp-content/uploads/2016/12/desktop-zh.png" alt="Ubuntu"></p>
<a id="more"></a>
<h1 id="前言"><a class="header-anchor" href="#前言">¶</a>前言</h1>
<p>本人对的Linux的了解可能比小白强一点点儿（自我感觉的，本次换系统来看其实就是小白）。</p>
<p>最开始安装的Ubuntu，感觉使用起来各种不爽。</p>
<ul>
<li>默认安装的Firefox，对于一个程序猿来说，咋能少了Chrome呢</li>
<li>自带的vim编辑功能不全，还得删除了，重新安装一个</li>
<li>软件商店的应用太少，只能自己装</li>
<li>界面实在不太好看</li>
</ul>
<p>于是乎，就思索着找其它的系统来代替，然后找到了Deepin，感觉确实比Ubuntu强太多了。</p>
<ul>
<li>社区网站自带U盘启动制作工具，省心</li>
<li>安装Chrome作为默认浏览器</li>
<li>操作习惯更接近于Windows，对于从Windows转过来的用户比较友好</li>
<li>常用的软件都基本能在应用商店里面找到</li>
<li>界面是最大的亮点吧</li>
<li>国产，点个赞</li>
</ul>
<p>下面就记录一下安装和配置Deepin的过程，为本次系统切换画上一个句号。</p>
<h1 id="安装"><a class="header-anchor" href="#安装">¶</a>安装</h1>
<p>下载系统镜像：<span class="exturl" data-url="aHR0cHM6Ly93d3cuZGVlcGluLm9yZy9kb3dubG9hZC8=" title="https://www.deepin.org/download/">https://www.deepin.org/download/<i class="fa fa-external-link"></i></span></p>
<p>下载深度启动盘制作工具：<span class="exturl" data-url="aHR0cHM6Ly93d3cuZGVlcGluLm9yZy9vcmlnaW5hbC9kZWVwaW4tYm9vdC1tYWtlci8=" title="https://www.deepin.org/original/deepin-boot-maker/">https://www.deepin.org/original/deepin-boot-maker/<i class="fa fa-external-link"></i></span></p>
<p>使用下载深度启动盘制作工具，选择Deepin镜像，然后下一步选择U盘，OK，等待制作完成。</p>
<p><img src="https://s2.ax1x.com/2019/06/19/VXaGE4.png" alt="VXaGE4.png"></p>
<p>现在开始安装Deepin系统，开机前按F12进入引导选择页面（不同的主板可能不太一样，自行查询一下吧，有些比较老的得通过改BIOS设置才行）<br>
由于我的机子是支持UEFI启动的，所以识别出的UEFI的U盘引导项。</p>
<p>接下来进入安装过程：</p>
<ul>
<li>直接回车选择 Install Deepin。</li>
<li>语言选择-简体中文</li>
<li>设置用户名，主机名，密码</li>
<li>磁盘分区
<ul>
<li>普通模式，省心，直接安装就行了</li>
<li>专家模式，必须设置一个根目录分区“/”，其它磁盘可以挂载到其它目录</li>
</ul>
</li>
<li>确认，开始安装，一杯咖啡的时间就能装好</li>
<li>安装完成，开始使用</li>
</ul>
<h1 id="配置"><a class="header-anchor" href="#配置">¶</a>配置</h1>
<h2 id="修改软件源"><a class="header-anchor" href="#修改软件源">¶</a>修改软件源</h2>
<p>Deepin官方的软件源慢得跟蜗牛一样，在设置中指定镜像源又不能生效。</p>
<p>so，手工修改/etc/apt/sources.list文件。</p>
<p><strong>修改前先备份一下</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak</span><br><span class="line"></span><br><span class="line">sudo vi /etc/apt/sources.list </span><br><span class="line"></span><br><span class="line"></span><br><span class="line">## Generated by deepin-installer</span><br><span class="line">## 将官方源禁用</span><br><span class="line">## deb [by-hash=force] http://packages.deepin.com/deepin lion main contrib non-free</span><br><span class="line"></span><br><span class="line">## 设置阿里云，则将地址设置为</span><br><span class="line">deb [by-hash=force] https://mirrors.aliyun.com/deepin/ lion main contrib non-free</span><br><span class="line">#deb-src http://packages.deepin.com/deepin lion main contrib non-free</span><br></pre></td></tr></table></figure>
<p>修改完成后<code>:wq</code>保存，然后刷新软件源列表，在终端执行：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get update</span><br><span class="line"></span><br><span class="line"># 软件更新，可选</span><br><span class="line">sudo apt-get upgrade</span><br></pre></td></tr></table></figure>
<p>然后其它的工具就可以慢慢装啦</p>
<h2 id="修改文件夹权限"><a class="header-anchor" href="#修改文件夹权限">¶</a>修改文件夹权限</h2>
<p>我在安装的时候将一块磁盘挂载到/home目录下，将代码下载到该目录下，<br>
用idea开发的时候发现没有权限创建文件、文件夹和文件修改等权限，于是乎使出绝招：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo chomd -R 777 /home</span><br></pre></td></tr></table></figure>
<h2 id="设置ll命令"><a class="header-anchor" href="#设置ll命令">¶</a>设置ll命令</h2>
<p>在上面查看目录的过程中发现没有<code>ll</code>命令，然后网上查询了一下，发现是ll命令其实是 ls -l命令的别名,在deepin中这个配置被注释掉了。</p>
<p>使用下面的命令编辑配置文件打开即可：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo vim ~/.bashrc</span><br></pre></td></tr></table></figure>
<p>将alias ll=’ls -l’前的注释打开,然后<code>source ~/.bashrc</code>刷新一下即可</p>
<h2 id="设置su密码"><a class="header-anchor" href="#设置su密码">¶</a>设置su密码</h2>
<p>deepin的用户默认就在sudo组里面，但是每次使用该命令都要输入密码，于是进行如下设置：</p>
<ol>
<li>打开一个深度终端，然后输入下面的命令</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo passwd </span><br><span class="line">[sudo] password for you ：---&gt; 输入你的密码（你现在这个用户的密码），不回显</span><br><span class="line">Enter new UNIX password: --- &gt; 设置root 密码</span><br><span class="line">Retype new UNIX password: --&gt; 重复密码</span><br></pre></td></tr></table></figure>
<p>注：su和sudo的区别是：</p>
<ul>
<li>
<p>su的密码是root的密码，而sudo的密码是用户的密码；</p>
</li>
<li>
<p>su直接将身份变成root，而sudo是以用户登录后以root的身份运行命令，不需要知道root密码；</p>
</li>
</ul>
<h2 id="设置sudo不需要每次都输入密码"><a class="header-anchor" href="#设置sudo不需要每次都输入密码">¶</a>设置sudo不需要每次都输入密码</h2>
<p>如果想要sudo不每次都输入密码，则修改/etc/sudoers文件：</p>
<p>sudo vi /etc/sudoers</p>
<p>然后在<code>root ALL=(ALL:ALL) ALL</code>下一行添加<code>username ALL=(ALL:ALL) ALL</code></p>
<p>修改<code>%sudo ALL=(ALL:ALL) ALL</code>为<code>%sudo ALL=(ALL:ALL) NOPASSWD:ALL</code><br>
然后保存</p>
<p>搞定，本次安装过程记录到此为止，现在开始拥抱Linux吧。</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Deepin</tag>
        <tag>Windows换Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 科学上网</title>
    <url>/linux/shadowsocks.html</url>
    <content><![CDATA[<p>最近因为各种原因，从Windows转Linux，之前在Windows上用得非常爽的SSR客户端，但是在应用商店找了很久，都不好使。<br>
最终找到electron-ssr，和Windows上的差不多。</p>
<p><img src="https://s2.ax1x.com/2019/06/20/VvOOl4.png" alt="VvOOl4.png"></p>
<a id="more"></a>
<h1 id="shadowsocksr跨平台客户端"><a class="header-anchor" href="#shadowsocksr跨平台客户端">¶</a>ShadowsocksR跨平台客户端</h1>
<p>这是一个跨平台（支持Windows MacOS Linux系统）的ShadowsocksR客户端桌面应用，它功能丰富，支持windows版大部分功能，更有更多人性化功能。它是开源的，它来源于开源，回馈以开源。</p>
<p>功能特色</p>
<ul>
<li>支持手动添加配置</li>
<li>支持服务器订阅更新，复制该地址测试</li>
<li>支持二维码扫描(请确保屏幕中只有一个有效的二维码)，扫描该二维码测试</li>
<li>支持从剪贴板复制、从配置文件导入等方式添加配置</li>
<li>支持复制二维码图片、复制SSR链接(右键应用内二维码，点击右键菜单中的复制)</li>
<li>支持通过点击ss/ssr链接添加配置并打开应用(仅Mac和Windows)</li>
<li>支持切换系统代理模式:PAC、全局、不代理</li>
<li>内置http_proxy服务，可在选项中开启或关闭</li>
<li>支持配置项变更</li>
<li>更多功能尽在任务栏菜单中</li>
</ul>
<h1 id="下载"><a class="header-anchor" href="#下载">¶</a>下载</h1>
<p>该软件的作者已经将其从GitHub上删除了，不再维护了，不过还好找到了备份，传送门：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3FpbmdzaHVpc2l5dWFuL2VsZWN0cm9uLXNzci1iYWNrdXAvcmVsZWFzZXM=" title="https://github.com/qingshuisiyuan/electron-ssr-backup/releases">https://github.com/qingshuisiyuan/electron-ssr-backup/releases<i class="fa fa-external-link"></i></span></p>
<p>Deepin、Ubuntu系列下载<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3FpbmdzaHVpc2l5dWFuL2VsZWN0cm9uLXNzci1iYWNrdXAvcmVsZWFzZXMvZG93bmxvYWQvdjAuMi42L2VsZWN0cm9uLXNzci0wLjIuNi5kZWI=" title="https://github.com/qingshuisiyuan/electron-ssr-backup/releases/download/v0.2.6/electron-ssr-0.2.6.deb">electron-ssr-0.2.6.deb<i class="fa fa-external-link"></i></span></p>
<h1 id="安装和配置"><a class="header-anchor" href="#安装和配置">¶</a>安装和配置</h1>
<p>sudo dpkg -i electron-ssr-0.2.6.deb</p>
<p>配置很简单，和Windows上的SSR客户端差不多，拷贝SSR的订阅连接，更新，然后选择喜欢的节点即可。</p>
<p><img src="https://s2.ax1x.com/2019/06/20/Vvjhaq.png" alt="Vvjhaq.png"></p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Deepin</tag>
        <tag>SSR</tag>
        <tag>ShadowsocksR</tag>
      </tags>
  </entry>
  <entry>
    <title>Deepin install Typora</title>
    <url>/linux/typora.html</url>
    <content><![CDATA[<p>在Deepin系统商店中就有携带Typora，提是使用深度源，但是大家一般都换成阿里云等速度比较快的源。按照官方文档安装也是一大堆问题，后来采用Linux Mint的安装方式完美解决</p>
<p><img src="https://www.typora.io/img/theme-prev/Snip20141101_3.png" alt="Typora.png"></p>
<a id="more"></a>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span> or use</span><br><span class="line"><span class="meta">#</span> sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys BA300B7755AFCFAE</span><br><span class="line">wget -qO - https://typora.io/linux/public-key.asc | sudo apt-key add -</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> add Typora's repository</span><br><span class="line">echo -e "\ndeb https://typora.io/linux ./" | sudo tee -a /etc/apt/sources.list</span><br><span class="line">sudo apt-get update</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> install typora</span><br><span class="line">sudo apt-get install typora</span><br></pre></td></tr></table></figure>
<p>注：不要使用官方推荐的Deb和Ubuntu的那个安装方法，采用Linux Mint的。</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Deepin</tag>
        <tag>Markdown</tag>
        <tag>Typora</tag>
      </tags>
  </entry>
  <entry>
    <title>支持生产阻塞的线程池</title>
    <url>/mutlithreading/blocking-productor-threadpool.html</url>
    <content><![CDATA[<p>在各种并发编程模型中，生产者-消费者模式大概是最常用的了。在实际工作中，对于生产消费的速度，通常需要做一下权衡。通常来说，生产任务的速度要大于消费的速度。一个细节问题是，队列长度，以及如何匹配生产和消费的速度。</p>
<p>一个典型的生产者-消费者模型如下：</p>
<p><img src="http://ifeve.com/wp-content/uploads/2013/04/producer-consumer.png" alt="image"></p>
<a id="more"></a>
<p>在并发环境下利用J.U.C提供的Queue实现可以很方便地保证生产和消费过程中的线程安全。这里需要注意的是，Queue必须设置初始容量，防止生产者生产过快导致队列长度暴涨，最终触发OutOfMemory。</p>
<p>对于一般的生产快于消费的情况。当队列已满时，我们并不希望有任何任务被忽略或得不到执行，此时生产者可以等待片刻再提交任务，更好的做法是，把生产者阻塞在提交任务的方法上，待队列未满时继续提交任务，这样就没有浪费的空转时间了。阻塞这一点也很容易，BlockingQueue就是为此打造的，ArrayBlockingQueue和LinkedBlockingQueue在构造时都可以提供容量做限制，其中LinkedBlockingQueue是在实际操作队列时在每次拿到锁以后判断容量。</p>
<p>更进一步，当队列为空时，消费者拿不到任务，可以等一会儿再拿，更好的做法是，用BlockingQueue的take方法，阻塞等待，当有任务时便可以立即获得执行，建议调用take的带超时参数的重载方法，超时后线程退出。这样当生产者事实上已经停止生产时，不至于让消费者无限等待。</p>
<p>于是一个高效的支持阻塞的生产消费模型就实现了。</p>
<p>等一下，既然J.U.C已经帮我们实现了线程池，为什么还要采用这一套东西？直接用ExecutorService不是更方便？</p>
<p>我们来看一下ThreadPoolExecutor的基本结构：</p>
<p><img src="http://ifeve.com/wp-content/uploads/2013/04/threadpoolexecutor.png" alt="image"></p>
<p>可以看到，在ThreadPoolExecutor中，BlockingQueue和Consumer部分已经帮我们实现好了，并且直接采用线程池的实现还有很多优势，例如线程数的动态调整等。</p>
<p>但问题在于，即便你在构造ThreadPoolExecutor时手动指定了一个BlockingQueue作为队列实现，事实上当队列满时，execute方法并不会阻塞，原因在于ThreadPoolExecutor调用的是BlockingQueue非阻塞的offer方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable command)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (command == <span class="keyword">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="keyword">if</span> (poolSize &gt;= corePoolSize || !addIfUnderCorePoolSize(command)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (runState == RUNNING &amp;&amp; workQueue.offer(command)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (runState != RUNNING || poolSize == <span class="number">0</span>)</span><br><span class="line">                ensureQueuedTaskHandled(command);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (!addIfUnderMaximumPoolSize(command))</span><br><span class="line">            reject(command); <span class="comment">// is shutdown or saturated</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这时候就需要做一些事情来达成一个结果：当生产者提交任务，而队列已满时，能够让生产者阻塞住，等待任务被消费。</p>
<p>关键在于，在并发环境下，队列满不能由生产者去判断，不能调用ThreadPoolExecutor.getQueue().size()来判断队列是否满。</p>
<p>线程池的实现中，当队列满时会调用构造时传入的RejectedExecutionHandler去拒绝任务的处理。默认的实现是AbortPolicy，直接抛出一个RejectedExecutionException。</p>
<p>几种拒绝策略在这里就不赘述了，这里和我们的需求比较接近的是CallerRunsPolicy，这种策略会在队列满时，让提交任务的线程去执行任务，相当于让生产者临时去干了消费者干的活儿，这样生产者虽然没有被阻塞，但提交任务也会被暂停。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">CallerRunsPolicy</span> <span class="keyword">implements</span> <span class="title">RejectedExecutionHandler</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">   * Creates an &lt;tt&gt;AbortPolicy&lt;/tt&gt;.</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">CallerRunsPolicy</span><span class="params">()</span> </span>&#123; &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">   * Executes task r in the caller's thread, unless the executor</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">   * has been shut down, in which case the task is discarded.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">   * <span class="doctag">@param</span> r the runnable task requested to be executed</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">   * <span class="doctag">@param</span> e the executor attempting to execute this task</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">rejectedExecution</span><span class="params">(Runnable r, ThreadPoolExecutor e)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (!e.isShutdown()) &#123;</span><br><span class="line"></span><br><span class="line">          r.run();</span><br><span class="line"></span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>但这种策略也有隐患，当生产者较少时，生产者消费任务的时间里，消费者可能已经把任务都消费完了，队列处于空状态，当生产者执行完任务后才能再继续生产任务，这个过程中可能导致消费者线程的饥饿。</p>
<p>参考类似的思路，最简单的做法，我们可以直接定义一个RejectedExecutionHandler，当队列满时改为调用BlockingQueue.put来实现生产者的阻塞：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> class <span class="title">RejectedExecutionHandler</span><span class="params">()</span> implements RejectedExecutionHandler </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">rejectedExecution</span><span class="params">(Runnable r, ThreadPoolExecutor executor)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (!executor.isShutdown()) &#123;</span><br><span class="line">          <span class="keyword">try</span> &#123;</span><br><span class="line">              executor.getQueue().put(r);</span><br><span class="line">          &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line"></span><br><span class="line">              <span class="comment">// should not be interrupted</span></span><br><span class="line">          &#125;</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>这样，我们就无需再关心Queue和Consumer的逻辑，只要把精力集中在生产者和消费者线程的实现逻辑上，只管往线程池提交任务就行了。</p>
<p>相比最初的设计，这种方式的代码量能减少不少，而且能避免并发环境的很多问题。当然，你也可以采用另外的手段，例如在提交时采用信号量做入口限制等，但是如果仅仅是要让生产者阻塞，那就显得复杂了。</p>
]]></content>
      <categories>
        <category>java多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java多线程</tag>
        <tag>ThreadPool</tag>
      </tags>
  </entry>
  <entry>
    <title>多线程编程--并发包</title>
    <url>/mutlithreading/multithreading-juc.html</url>
    <content><![CDATA[<p>之前我们了解了线程的基础知识，那么多个线程一起运行的时候，难免会操作同一个资源。从上一讲里面我们了解到每个线程会有自己的缓存，那么多个线程操作同一个资源的时候，怎么保证资源操作结果的正确性。这些问题，其实从JDK1.5起已经提供了解决方案，接下来就让我们一起来看看<code>java.util.concurrent</code>。</p>
<p><img src="https://10.url.cn/qqcourse_logo_ng/ajNVdqHZLLDpfiageJ021AspmrAdS6FlCPC7JgO4Zgjp5ozCFiaZRJguDIEWicUBnfG9f5bxFdbQcY/" alt="java并发包"></p>
<a id="more"></a>
<p>首先我们来看一个经典问题：i++是否线程安全？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">   <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">AddTest</span></span>&#123;</span><br><span class="line">       <span class="keyword">volatile</span> <span class="keyword">static</span> <span class="keyword">int</span> i;</span><br><span class="line">       <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">unsafeAddCount</span><span class="params">()</span></span>&#123;</span><br><span class="line">           i++;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unsafeAddCountTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">       <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">           Thread thread = <span class="keyword">new</span> Thread(() -&gt; AddTest.unsafeAddCount());</span><br><span class="line">           thread.start();</span><br><span class="line">       &#125;</span><br><span class="line">       Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">       System.out.println(<span class="string">"i = "</span> + AddTest.i);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>运行结果i会是多少呢，有可能：<code>i = 98</code>、<code>i = 99</code>、<code>i = 100</code>，为什么会出现这样的结果呢？</p>
<p>volatile只能保证可见性，并不能保证原子性，表达式i++操作步骤分解如下：</p>
<blockquote>
<p>1、从主存取出i的值放到线程栈<br><br>
2、在线程栈中计算i+1的值<br><br>
3、将i+1的值写到主存中的变量i<br></p>
</blockquote>
<p>很不幸的是，这几个操作并不是原子性的，如果多个同时进行i++操作，就会出现线程安全问题。<br></p>
<blockquote>
<p>1、获取–&gt; 线程A：i=1，线程B：i=1<br><br>
2、计算–&gt; 线程A：i+1=2，线程B：i+1=2<br><br>
3、回写–&gt; 线程A：i=2，线程B：i=2<br></p>
</blockquote>
<h1 id="原子类-atomic"><a class="header-anchor" href="#原子类-atomic">¶</a>原子类（Atomic）</h1>
<p>要保证i++的线程安全，加上<code>synchronized</code>关键字即可：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">AddTest</span></span>&#123;</span><br><span class="line">    <span class="keyword">volatile</span> <span class="keyword">static</span> <span class="keyword">int</span> i;</span><br><span class="line">    <span class="function"><span class="keyword">synchronized</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">safeAddCount</span><span class="params">()</span></span>&#123;</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">safeAddCountTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">        Thread thread = <span class="keyword">new</span> Thread(() -&gt; AddTest.safeAddCount());</span><br><span class="line">        thread.start();</span><br><span class="line">    &#125;</span><br><span class="line">    Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">    System.out.println(<span class="string">"i = "</span> + AddTest.i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这下不管运行多少次i的结果都是100，不信的可以尝试一下。</p>
<p>在<code>java.util.concurrent.atomic</code>提供了原子类，解决i++也可以使用<code>AtomicInteger</code>，如下所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">atomicIntegerSafeTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    AtomicInteger i = <span class="keyword">new</span> AtomicInteger();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; <span class="number">100</span>; j++) &#123;</span><br><span class="line">        Thread thread = <span class="keyword">new</span> Thread(() -&gt; i.getAndIncrement());</span><br><span class="line">        thread.start();</span><br><span class="line">    &#125;</span><br><span class="line">    Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">    System.out.println(<span class="string">"i = "</span> + i.get());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么<code>AtomicInteger</code>是怎么保证线程安全的呢，让我们看看<code>getAndIncrement</code>方法，：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">getAndIncrement</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> unsafe.getAndAddInt(<span class="keyword">this</span>, valueOffset, <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">getAndAddInt</span><span class="params">(Object var1, <span class="keyword">long</span> var2, <span class="keyword">int</span> var4)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> var5;</span><br><span class="line">        <span class="keyword">do</span> &#123;</span><br><span class="line">            <span class="comment">//获取对象内存地址偏移量上的数值v</span></span><br><span class="line">            var5 = <span class="keyword">this</span>.getIntVolatile(var1, var2);</span><br><span class="line">        &#125; <span class="keyword">while</span>(!<span class="keyword">this</span>.compareAndSwapInt(var1, var2, var5, var5 + var4));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> var5;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* 比较obj的offset处内存位置中的值和期望的值，如果相同则更新。此更新是不可中断的。</span></span><br><span class="line"><span class="comment">* </span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> obj 需要更新的对象</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> offset obj中整型field的偏移量</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> expect 希望field中存在的值</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> update 如果期望值expect与field的当前值相同，设置filed的值为这个新值</span></span><br><span class="line"><span class="comment">* <span class="doctag">@return</span> 如果field的值被更改返回true</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">native</span> <span class="keyword">boolean</span> <span class="title">compareAndSwapInt</span><span class="params">(Object obj, <span class="keyword">long</span> offset, <span class="keyword">int</span> expect, <span class="keyword">int</span> update)</span></span>;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>Unsafe类只能由jdk源码使用，否则会抛出异常：java.lang.SecurityException: Unsafe</p>
</blockquote>
<p>jdk中提供的原子类如下：</p>
<ul>
<li>
<p><code>AtomicBoolean</code> 保证布尔值的原子性</p>
</li>
<li>
<p><code>AtomicInteger</code> 保证整型的原子性</p>
</li>
<li>
<p><code>AtomicLong</code> 保证长整型的原子性</p>
</li>
<li>
<p><code>AtomicIntegerArray</code> 保证整型数组的原子性</p>
</li>
<li>
<p><code>AtomicLongArray</code> 保证长整型数组原子性</p>
</li>
<li>
<p><code>AtomicIntegerFieldUpdater</code> 保证整型的字段更新</p>
</li>
<li>
<p><code>AtomicLongFieldUpdater</code> 保证长整型的字段更新</p>
</li>
<li>
<p><code>AtomicReferenceArray</code> 保证引用数组的原子性</p>
</li>
<li>
<p><code>AtomicReferenceFieldUpdater</code> 保证引用类型的字段更新</p>
</li>
<li>
<p><code>AtomicStampedReference</code> 可以解决<code>CAS</code>的<code>ABA</code>问题，类似提供版本号</p>
</li>
<li>
<p><code>AtomicMarkableReference</code> 可以解决<code>CAS</code>的<code>ABA</code>问题，提供是或否进行判断</p>
</li>
</ul>
<h1 id="锁-lock"><a class="header-anchor" href="#锁-lock">¶</a>锁（Lock）</h1>
<h2 id="简单锁实现"><a class="header-anchor" href="#简单锁实现">¶</a>简单锁实现</h2>
<p>上一讲我们讲到<span class="exturl" data-url="aHR0cHM6Ly9mYW56aG9uZ3dlaS5jb20vbXV0bGl0aHJlYWRpbmcvbXVsdGl0aHJlYWRpbmctYmFzZS5odG1sIyVFNyVCQSVCRiVFNyVBOCU4QiVFOSU5NyVCNCVFOSU4MCU5QSVFNCVCRiVBMQ==" title="https://fanzhongwei.com/mutlithreading/multithreading-base.html#%E7%BA%BF%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1">线程间通信<i class="fa fa-external-link"></i></span>，在这里我们实现一个简单的锁。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleLock</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">boolean</span> isLocked = <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span></span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (isLocked) &#123;</span><br><span class="line">            wait();</span><br><span class="line">        &#125;</span><br><span class="line">        isLocked = <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        isLocked = <span class="keyword">false</span>;</span><br><span class="line">        notify();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> SimpleLock simpleLock = <span class="keyword">new</span> SimpleLock();</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">int</span> instanceNum;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">simpleLockTest</span><span class="params">(String username)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        simpleLock.lock();</span><br><span class="line">        <span class="keyword">if</span> (<span class="string">"b"</span>.equals(username)) &#123;</span><br><span class="line">            instanceNum = <span class="number">200</span>;</span><br><span class="line">            System.out.println(<span class="string">"b set over!"</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            instanceNum = <span class="number">100</span>;</span><br><span class="line">            System.out.println(<span class="string">"a set over!"</span>);</span><br><span class="line">            Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(username + <span class="string">" num = "</span> + instanceNum);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        simpleLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">threadSetInstanceNumTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    Thread threadA = <span class="keyword">new</span> Thread(() -&gt; simpleLockTest(<span class="string">"a"</span>));</span><br><span class="line">    Thread threadB = <span class="keyword">new</span> Thread(() -&gt; simpleLockTest(<span class="string">"b"</span>));</span><br><span class="line">    threadA.start();</span><br><span class="line">    threadB.start();</span><br><span class="line">    Thread.sleep(<span class="number">3000</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>这个锁存在什么问题呢，可重入？公平性？</p>
</blockquote>
<h2 id="reentrantlock"><a class="header-anchor" href="#reentrantlock">¶</a>ReentrantLock</h2>
<p>除了使用<code>synchronized</code>关键字保证线程安全之外，还能够使用<code>java.util.concurrent.locks</code>包中所提供的Lock，先来看一个简单的例子吧。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">int</span> instanceNum;</span><br><span class="line"><span class="keyword">private</span> Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">addInstanceNum</span><span class="params">(String username)</span> </span>&#123;</span><br><span class="line">    lock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="string">"b"</span>.equals(username)) &#123;</span><br><span class="line">            instanceNum = <span class="number">200</span>;</span><br><span class="line">            System.out.println(<span class="string">"b set over!"</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            instanceNum = <span class="number">100</span>;</span><br><span class="line">            System.out.println(<span class="string">"a set over!"</span>);</span><br><span class="line">            Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(username + <span class="string">" num = "</span> + instanceNum);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">threadSetInstanceNumTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    Thread threadA = <span class="keyword">new</span> Thread(() -&gt; addInstanceNum(<span class="string">"a"</span>));</span><br><span class="line">    Thread threadB = <span class="keyword">new</span> Thread(() -&gt; addInstanceNum(<span class="string">"b"</span>));</span><br><span class="line">    threadA.start();</span><br><span class="line">    threadB.start();</span><br><span class="line">    Thread.sleep(<span class="number">3000</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>不加lock的时候输出：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">a set num is 100!</span><br><span class="line">b set num is 200!</span><br><span class="line">b num = 200</span><br><span class="line">a num = 200	// 这里出现了线程安全问题，a线程设置的值被b线程设置的值覆盖了。</span><br></pre></td></tr></table></figure>
<p>加上lock之后，只有等一个线程执行完之后，另一个线程才能进入：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">a set num is 100!</span><br><span class="line">a num = 100</span><br><span class="line">b set num is 200!</span><br><span class="line">b num = 200</span><br></pre></td></tr></table></figure>
<h2 id="reentrantreadwritelock"><a class="header-anchor" href="#reentrantreadwritelock">¶</a>ReentrantReadWriteLock</h2>
<p>如果我们对资源的读取比较频繁，而修改相对较少，使用前面提到的锁有什么弊端呢，两个线程同时读取资源需要加锁吗？</p>
<p>ReadWriteLock就是读写锁，它是一个接口，ReentrantReadWriteLock实现了这个接口。可以通过readLock()获取读锁，只要没有线程拥有写锁（writers==0），且没有线程在请求写锁（writeRequests ==0），所有想获得读锁的线程都能成功获取。</p>
<p>通过writeLock()获取写锁，当一个线程想获得写锁的时候，首先会把写锁请求数加1（writeRequests++），然后再去判断是否能够真能获得写锁，当没有线程持有读锁（readers==0 ）,且没有线程持有写锁（writers==0）时就能获得写锁。有多少线程在请求写锁并无关系。</p>
<p>简单说就是：</p>
<ul>
<li>读读共享</li>
<li>写写互斥</li>
<li>读写互斥</li>
<li>写读互斥</li>
</ul>
<h2 id="aqs-abstractqueuedsynchronizer"><a class="header-anchor" href="#aqs-abstractqueuedsynchronizer">¶</a>AQS（AbstractQueuedSynchronizer）</h2>
<p>锁分为“公平锁”和“非公平锁”，顾名思义：</p>
<ul>
<li>公平锁：线程获取锁的顺序是按照线程加锁的顺序来分配的，FIFO。</li>
<li>非公平锁：是一种获取锁的抢占机制，是随机获得锁的，先来的不一定先得到锁，可能导致某些线程一直拿不到锁。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">   <span class="function"><span class="keyword">public</span> <span class="title">ReentrantLock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">       sync = <span class="keyword">new</span> NonfairSync();</span><br><span class="line">   &#125;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ReentrantLock</span><span class="params">(<span class="keyword">boolean</span> fair)</span> </span>&#123;</span><br><span class="line">       sync = fair ? <span class="keyword">new</span> FairSync() : <span class="keyword">new</span> NonfairSync();</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ReentrantReadWriteLock</span><span class="params">(<span class="keyword">boolean</span> fair)</span> </span>&#123;</span><br><span class="line">       sync = fair ? <span class="keyword">new</span> FairSync() : <span class="keyword">new</span> NonfairSync();</span><br><span class="line">       readerLock = <span class="keyword">new</span> ReadLock(<span class="keyword">this</span>);</span><br><span class="line">       writerLock = <span class="keyword">new</span> WriteLock(<span class="keyword">this</span>);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>其中<code>FairSyncAQS</code>和<code>NonfairSync</code>继承<code>ReentrantLock.Sync</code>和<code>ReentrantReadWriteLock.Sync</code>，而他们都是AbstractQueuedSynchronizer（简称AQS）的子类。AQS是基于FIFO队列实现的，AQS整个类中没有任何一个abstract的抽象方法，取而代之的是，需要子类去实现的那些方法通过一个方法体抛出UnsupportedOperationException异常来让子类知道，告知如果没有实现这些方法，则直接抛出异常。</p>
<table>
<thead>
<tr>
<th>方法名</th>
<th>方法描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>tryAcquire</td>
<td>以独占模式尝试获取锁，独占模式下调用acquire，尝试去设置state的值，如果设置成功则返回，如果设置失败则将当前线程加入到等待队列，直到其他线程唤醒</td>
</tr>
<tr>
<td>tryRelease</td>
<td>尝试独占模式下释放状态</td>
</tr>
<tr>
<td>tryAcquireShared</td>
<td>尝试在共享模式获得锁，共享模式下调用acquire，尝试去设置state的值，如果设置成功则返回，如果设置失败则将当前线程加入到等待队列，直到其他线程唤醒</td>
</tr>
<tr>
<td>tryReleaseShared</td>
<td>尝试共享模式下释放状态</td>
</tr>
<tr>
<td>isHeldExclusively</td>
<td>是否是独占模式，表示是否被当前线程占用</td>
</tr>
</tbody>
</table>
<p>这里以ReentrantLock的公平锁来举例，看一下AQS内部是如何实现锁的获取和释放。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">   <span class="comment">// ReentrantLock.FairSync</span></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">       acquire(<span class="number">1</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// AbstractQueuedSynchronizer</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">acquire</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">       <span class="comment">// 第一步尝试获取锁，成功则返回</span></span><br><span class="line">       <span class="comment">// 获取锁失败后通过addWaiter添加到CLH队列的末尾</span></span><br><span class="line">       <span class="comment">// 通过acquireQueued判断是否轮到自己唤醒了</span></span><br><span class="line">       <span class="keyword">if</span> (!tryAcquire(arg) &amp;&amp;</span><br><span class="line">           acquireQueued(addWaiter(Node.EXCLUSIVE), arg))</span><br><span class="line">           selfInterrupt();</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * ReentrantLock.FairSync</span></span><br><span class="line"><span class="comment">    *</span></span><br><span class="line"><span class="comment">    * Fair version of tryAcquire.  Don't grant access unless</span></span><br><span class="line"><span class="comment">    * recursive call or no waiters or is first.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">   <span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">       <span class="keyword">int</span> c = getState();</span><br><span class="line">       <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">           <span class="comment">// hasQueuedPredecessors判断是否是队列的第一个元素，如果是则尝试获取锁（CAS更新state）</span></span><br><span class="line">           <span class="keyword">if</span> (!hasQueuedPredecessors() &amp;&amp;</span><br><span class="line">               compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">               setExclusiveOwnerThread(current);</span><br><span class="line">               <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="comment">// 判断获取到当前锁的线程是否是当前线程，可重入性体现在这里</span></span><br><span class="line">       <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">           <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">           <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>)</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">           setState(nextc);</span><br><span class="line">           <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">// AbstractQueuedSynchronizer</span></span><br><span class="line">   <span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">acquireQueued</span><span class="params">(<span class="keyword">final</span> Node node, <span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">boolean</span> failed = <span class="keyword">true</span>;</span><br><span class="line">       <span class="keyword">try</span> &#123;</span><br><span class="line">           <span class="keyword">boolean</span> interrupted = <span class="keyword">false</span>;</span><br><span class="line">           <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">               <span class="keyword">final</span> Node p = node.predecessor();</span><br><span class="line">               <span class="comment">// 如果前一个节点是head，当前节点排在第二，那么便有资格去尝试获取资源</span></span><br><span class="line">               <span class="keyword">if</span> (p == head &amp;&amp; tryAcquire(arg)) &#123;</span><br><span class="line">                   setHead(node);</span><br><span class="line">                   p.next = <span class="keyword">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                   failed = <span class="keyword">false</span>;</span><br><span class="line">                   <span class="keyword">return</span> interrupted;</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</span><br><span class="line">                   parkAndCheckInterrupt())</span><br><span class="line">                   interrupted = <span class="keyword">true</span>;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">           <span class="keyword">if</span> (failed)</span><br><span class="line">               cancelAcquire(node);</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ReentrantLock</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">       sync.release(<span class="number">1</span>);</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">// AbstractQueuedSynchronizer</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">release</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">       <span class="comment">// 释放锁</span></span><br><span class="line">       <span class="keyword">if</span> (tryRelease(arg)) &#123;</span><br><span class="line">           Node h = head;</span><br><span class="line">           <span class="keyword">if</span> (h != <span class="keyword">null</span> &amp;&amp; h.waitStatus != <span class="number">0</span>)</span><br><span class="line">               <span class="comment">// 释放head.next</span></span><br><span class="line">               unparkSuccessor(h);</span><br><span class="line">           <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>AQS只提供固定的方法给子类实现，就可以实现不同的功能，这个满足设么类设计原则，那么又使用了什么设计模式？</p>
</blockquote>
<h1 id="同步工具"><a class="header-anchor" href="#同步工具">¶</a>同步工具</h1>
<h2 id="countdownlatch：减法计数器"><a class="header-anchor" href="#countdownlatch：减法计数器">¶</a>CountDownLatch：减法计数器</h2>
<p>countDown() 执行一次计数器减一，await() 等待计数器停止，唤醒其他线程，示例如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CountDownLatchTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//优先执行，执行完毕之后，才能执行 main</span></span><br><span class="line">        <span class="comment">//1、实例化计数器，10</span></span><br><span class="line">        CountDownLatch countDownLatch = <span class="keyword">new</span> CountDownLatch(<span class="number">10</span>);</span><br><span class="line">        <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">                System.out.println(<span class="string">"++++++++++Thread"</span>);</span><br><span class="line">                countDownLatch.countDown();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line">        <span class="comment">//2、调用 await 方法 让主线程等待countdonwn运行完毕</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            countDownLatch.await();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"main--------------"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="cyclicbarrier：加法计数器"><a class="header-anchor" href="#cyclicbarrier：加法计数器">¶</a>CyclicBarrier：加法计数器</h2>
<p>await()执行一次计数器加一，执行次数完成后，再执行CyclicBarrier的Runnable，示例如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestCyclicBarrier</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        CyclicBarrier barrier = <span class="keyword">new</span> CyclicBarrier(<span class="number">20</span>, () -&gt; &#123;</span><br><span class="line">            System.out.println(Thread.currentThread().getName() + <span class="string">":完成最后任务"</span>);</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">20</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">"到达"</span>);</span><br><span class="line">                    Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                    barrier.await();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException | BrokenBarrierException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>countDownLatch是一个计数器，线程完成一个记录一个，计数器递减，只能只用一次。</p>
<p>CyclicBarrier的计数器更像一个阀门，需要所有线程都到达，然后继续执行，计数器递增，提供reset功能，可以多次使用。</p>
</blockquote>
<h2 id="semaphore：计数信号量"><a class="header-anchor" href="#semaphore：计数信号量">¶</a>Semaphore：计数信号量</h2>
<p>实际开发中主要用来做限流操作，即限制可以访问某些资源的线程数量。</p>
<ul>
<li>初始化</li>
<li>获取许可</li>
<li>释放许可</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SemaphoreTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//同时只能进5个人</span></span><br><span class="line">        Semaphore semaphore = <span class="keyword">new</span> Semaphore(<span class="number">5</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">15</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(()-&gt;&#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">//获得许可</span></span><br><span class="line">                    semaphore.acquire();</span><br><span class="line">                    System.out.println(Thread.currentThread().getName()+<span class="string">"进店购物"</span>);</span><br><span class="line">                    TimeUnit.SECONDS.sleep(<span class="number">5</span>);</span><br><span class="line">                    System.out.println(Thread.currentThread().getName()+<span class="string">"出店"</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">                    <span class="comment">//释放许可</span></span><br><span class="line">                    semaphore.release();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;,String.valueOf(i)).start();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="exchanger：数据交换"><a class="header-anchor" href="#exchanger：数据交换">¶</a>Exchanger：数据交换</h2>
<p>Exchanger 是 JDK 1.5 开始提供的一个用于两个工作线程之间交换数据的封装工具类，简单说就是一个线程在完成一定的事务后想与另一个线程交换数据，则第一个先拿出数据的线程会一直等待第二个线程，直到第二个线程拿着数据到来时才能彼此交换对应数据。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ExchangerTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> Exchanger&lt;String&gt; exchanger = <span class="keyword">new</span> Exchanger&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Thread t1 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            String t = <span class="string">"t1"</span>;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t = exchanger.exchange(t);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(Thread.currentThread().getName() + <span class="string">"-"</span> + t);</span><br><span class="line">        &#125;);</span><br><span class="line">        Thread t2 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            String t = <span class="string">"t2"</span>;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t = exchanger.exchange(t);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(Thread.currentThread().getName() + <span class="string">"-"</span> + t);</span><br><span class="line">        &#125;);</span><br><span class="line">        t1.start();</span><br><span class="line">        t2.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="并发容器"><a class="header-anchor" href="#并发容器">¶</a>并发容器</h1>
<p>一起来回想下，java中的容器有哪几种，List、Set、Queue、Map？</p>
<p>大家熟知的这些集合类ArrayList、HashSet、HashMap这些容器都是非线程安全的。</p>
<p>比如Vector、Stack、Hashtable以及Collections.synchronized等方法生成的容器都是线程安全的，通过查看源码可以知道<strong>这些容器都在需要同步的方法上加上了<code>synchronized</code>关键字</strong>，在高并发的情况下容器的吞吐量就会降低，为了解决性能问题就有了<strong>并发容器</strong>。</p>
<h2 id="concurrenthashmap"><a class="header-anchor" href="#concurrenthashmap">¶</a>ConcurrentHashMap</h2>
<p>对应的非并发容器：HashMap</p>
<p>目标：代替Hashtable、synchronizedMap，支持复合操作</p>
<p>原理：JDK6中采用一种更加细粒度的加锁机制Segment“分段锁”，JDK8中采用CAS无锁算法。</p>
<p><img src="https://s1.ax1x.com/2020/06/15/NPKvTI.png" alt="ConcurrentHashMap.png"></p>
<h2 id="copyonwritearraylist"><a class="header-anchor" href="#copyonwritearraylist">¶</a>CopyOnWriteArrayList</h2>
<p>对应的非并发容器：ArrayList</p>
<p>目标：代替Vector、synchronizedList</p>
<p>原理：利用高并发往往是读多写少的特性，对读操作不加锁，对写操作，先复制一份新的集合，在新的集合上面修改，然后将新集合赋值给旧的引用，并通过volatile 保证其可见性，当然写操作的锁是必不可少的了。</p>
<h2 id="copyonwritearrayset"><a class="header-anchor" href="#copyonwritearrayset">¶</a>CopyOnWriteArraySet</h2>
<p>对应的非并发容器：HashSet</p>
<p>目标：代替synchronizedSet</p>
<p>原理：基于CopyOnWriteArrayList实现，其唯一的不同是在add时调用的是CopyOnWriteArrayList的addIfAbsent方法，其遍历当前Object数组，如Object数组中已有了当前元素，则直接返回，如果没有则放入Object数组的尾部，并返回。</p>
<h2 id="concurrentskiplistmap"><a class="header-anchor" href="#concurrentskiplistmap">¶</a>ConcurrentSkipListMap</h2>
<p>对应的非并发容器：TreeMap</p>
<p>目标：代替synchronizedSortedMap(TreeMap)</p>
<p>原理：Skip list（跳表）是一种可以代替平衡树的数据结构，默认是按照Key值升序的。</p>
<p><img src="https://s1.ax1x.com/2020/06/15/NPM7En.png" alt="SkipList.png"></p>
<blockquote>
<p>跳表分为许多层(level)，每一层都可以看作是数据的索引，这些索引的意义就是加快跳表查找数据速度。每一层的数据都是有序的，上一层数据是下一层数据的子集，并且第一层(level 1)包含了全部的数据；层次越高，跳跃性越大，包含的数据越少。<br>
跳表包含一个表头，它查找数据时，是从上往下，从左往右进行查找。</p>
</blockquote>
<h2 id="concurrentskiplistset"><a class="header-anchor" href="#concurrentskiplistset">¶</a>ConcurrentSkipListSet</h2>
<p>对应的非并发容器：TreeSet</p>
<p>目标：代替synchronizedSortedSet</p>
<p>原理：内部基于ConcurrentSkipListMap实现。</p>
<h2 id="concurrentlinkedqueue"><a class="header-anchor" href="#concurrentlinkedqueue">¶</a>ConcurrentLinkedQueue</h2>
<p>不会阻塞的队列</p>
<p>对应的非并发容器：Queue</p>
<p>原理：基于链表实现的FIFO队列，CAS实现线程安全</p>
<h2 id="blockingqueue"><a class="header-anchor" href="#blockingqueue">¶</a>BlockingQueue</h2>
<p>特点：拓展了Queue，增加了可阻塞的插入和获取等操作</p>
<p>原理：通过ReentrantLock实现线程安全，通过Condition实现阻塞和唤醒</p>
<p><strong>实现类：</strong></p>
<ul>
<li>LinkedBlockingQueue：基于链表实现的可阻塞的FIFO队列</li>
<li>LinkedBlockingDeque：基于链表实现的可阻塞的双端队列</li>
<li>ArrayBlockingQueue：基于数组实现的可阻塞的FIFO队列</li>
<li>PriorityBlockingQueue：按优先级排序的队列</li>
<li>SynchronousQueue：只有一个元素的队列</li>
</ul>
<h1 id="线程池-threadpool"><a class="header-anchor" href="#线程池-threadpool">¶</a>线程池（ThreadPool）</h1>
<p>在面向对象编程中，创建和销毁对象是很费时间的，因为创建一个对象要获取内存资源或者其它更多资源。<br>
在Java中虚拟机将试图跟踪每一个对象，以便能够在对象销毁后进行垃圾回收。所以提高服务程序效率的一个<br>
手段就是尽可能减少创建和销毁对象的次数，特别是一些很耗资源的对象创建和销毁。<br>
如何利用已有对象来服务就是一个解决的关键问题，这也就是&quot;池化资源&quot;技术产生的原因。</p>
<p>线程池是一种多线程处理形式，处理过程中将任务添加到队列，然后在创建线程后自动启动这些任务。</p>
<blockquote>
<p>线程是稀缺资源，使用线程池可以减少创建和销毁线程的次数，每个工作线程都可以重复使用。</p>
<p>可以根据系统的承受能力，调整线程池中工作线程的数量，防止因为消耗过多内存导致服务器崩溃。</p>
</blockquote>
<p>一个线程池包括以下四个基本组成部分：</p>
<ul>
<li>线程池管理器（ThreadPool）：用于创建并管理线程池，包括创建线程池、销毁线程池，添加新任务；</li>
<li>工作线程（PoolWorker）：线程池中线程，在没有任务时处于等待状态，可以循环的执行任务；</li>
<li>任务接口（Task）：每个任务必须实现的接口，以供工作线程调度任务的执行，它主要规定了任务的入口，任务执行完后的收尾工作，任务的执行状态等；</li>
<li>任务队列（taskQueue）：用于存放没有处理的任务。提供一种缓冲机制。</li>
</ul>
<p>详细内容请点：<span class="exturl" data-url="aHR0cHM6Ly9mYW56aG9uZ3dlaS5jb20vbXV0bGl0aHJlYWRpbmcvdGhyZWFkLXBvb2wuaHRtbA==" title="https://fanzhongwei.com/mutlithreading/thread-pool.html">线程池详解<i class="fa fa-external-link"></i></span></p>
<h1 id="产考文献"><a class="header-anchor" href="#产考文献">¶</a>产考文献</h1>
<ul>
<li>
<p><span class="exturl" data-url="aHR0cHM6Ly9mYW56aG9uZ3dlaS5jb20vbXV0bGl0aHJlYWRpbmcvdGhyZWFkLXBvb2wuaHRtbA==" title="https://fanzhongwei.com/mutlithreading/thread-pool.html">https://fanzhongwei.com/mutlithreading/thread-pool.html<i class="fa fa-external-link"></i></span></p>
</li>
<li>
<p>《深入Java虚拟机：JVM高级特性与最佳实践（第2版）》</p>
</li>
</ul>
]]></content>
      <categories>
        <category>java多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java多线程</tag>
        <tag>多线程编程</tag>
        <tag>线程安全</tag>
        <tag>JUC 常用类</tag>
      </tags>
  </entry>
  <entry>
    <title>多线程编程--并发模型</title>
    <url>/mutlithreading/multithreading-models.html</url>
    <content><![CDATA[<p>我们在做普通功能开发的时候有设计模式作为指导，提供了开发过程中面临的一般问题的解决方案；那么我们在致力于提高系统吞吐量的时候，有没有一些通用解决方案呢？是的，那就是并发模型，由于并发模型类似于分布式系统架构，因此它们通常可以互相借鉴思想，接下来就让我们来一起学习并发模型，并思考如何扩展到分布式系统架构。</p>
<p><img src="https://pic4.zhimg.com/v2-6886a471843d6982483ee682b724f31c_b.jpg" alt="多线程编程"></p>
<a id="more"></a>
<p>并发模型指定了系统中的线程如何通过协作来完成分配给它们的作业，不同的并发模型采用不同的方式拆分作业，同时线程间的协作和交互方式也不相同，接下来本文就简单介绍几种并发模型。</p>
<h1 id="并行工作者"><a class="header-anchor" href="#并行工作者">¶</a>并行工作者</h1>
<p>在并行工作者模型中，委派者（Delegator）将传入的作业分配给不同的工作者。每个工作者完成<strong>整个任务</strong>，工作者们并行运作在不同的线程上，甚至可能在不同的CPU上。并行工作者模型如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/06/21/N1zjmT.png" alt="并行工作者.png"></p>
<blockquote>
<p>这就好比电话接线员一样， 收到的每一个电话投诉或者业务处理请求，都会提交对应的工单， 然后交由对应的工作人员来处理。</p>
</blockquote>
<p>在Java应用系统中，并行工作者模型是最常见的并发模型。<span class="exturl" data-url="aHR0cDovL3R1dG9yaWFscy5qZW5rb3YuY29tL2phdmEtdXRpbC1jb25jdXJyZW50L2luZGV4Lmh0bWw=" title="http://tutorials.jenkov.com/java-util-concurrent/index.html">java.util.concurrent包<i class="fa fa-external-link"></i></span>中的许多并发实用工具都是设计用于这个模型的。这里以线程池为例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">   <span class="comment">// ThreadPoolExecutor 就是作为Delegator</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable command)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">if</span> (command == <span class="keyword">null</span>)</span><br><span class="line">           <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">       <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">       <span class="keyword">if</span> (workerCountOf(c) &lt; corePoolSize) &#123;</span><br><span class="line">           <span class="comment">// 每个工作线程都是worker</span></span><br><span class="line">           <span class="keyword">if</span> (addWorker(command, <span class="keyword">true</span>))</span><br><span class="line">               <span class="keyword">return</span>;</span><br><span class="line">           c = ctl.get();</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">if</span> (isRunning(c) &amp;&amp; workQueue.offer(command)) &#123;</span><br><span class="line">           <span class="keyword">int</span> recheck = ctl.get();</span><br><span class="line">           <span class="keyword">if</span> (! isRunning(recheck) &amp;&amp; remove(command))</span><br><span class="line">               reject(command);</span><br><span class="line">           <span class="keyword">else</span> <span class="keyword">if</span> (workerCountOf(recheck) == <span class="number">0</span>)</span><br><span class="line">               addWorker(<span class="keyword">null</span>, <span class="keyword">false</span>);</span><br><span class="line">       &#125;</span><br><span class="line">      	<span class="comment">// 现有worker工作压力比较大，再增加些worker（根据线程池配置）</span></span><br><span class="line">       <span class="keyword">else</span> <span class="keyword">if</span> (!addWorker(command, <span class="keyword">false</span>))</span><br><span class="line">           reject(command);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<h2 id="并行工作者模型的优点"><a class="header-anchor" href="#并行工作者模型的优点">¶</a>并行工作者模型的优点</h2>
<p>并行工作者模式的优点是，<strong>它很容易理解</strong>，你只需添加更多的工作者来提高系统的并行度。在我们常使用的J2EE应用服务器的设计中能看到这个模型的踪迹，我们以jetty为例（不知道jetty内部如何实现？没关系），我们先dump下线程日志，找到http线程：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="string">"qtp1428842218-222"</span> prio=<span class="number">5</span> tid=<span class="number">0x00000000000000de</span> nid=<span class="number">0x74667b27</span> waiting on condition &lt;<span class="number">0x000000002da5bbe7</span>&gt; [<span class="number">0x0000000074667b27</span>] cpu=[total=<span class="number">0</span>ms,user=<span class="number">0</span>ms] </span><br><span class="line">   java.lang.Thread.State: TIMED_WAITING</span><br><span class="line"> ...</span><br><span class="line"> at org.eclipse.jetty.util.BlockingArrayQueue.poll(BlockingArrayQueue.java:<span class="number">342</span>)</span><br><span class="line"> at org.eclipse.jetty.util.thread.QueuedThreadPool.idleJobPoll(QueuedThreadPool.java:<span class="number">526</span>)</span><br><span class="line"> at org.eclipse.jetty.util.thread.QueuedThreadPool.access$<span class="number">600</span>(QueuedThreadPool.java:<span class="number">44</span>)</span><br><span class="line"> at org.eclipse.jetty.util.thread.QueuedThreadPool$<span class="number">3</span>.run(QueuedThreadPool.java:<span class="number">572</span>)</span><br><span class="line"> at java.lang.Thread.run(Thread.java:<span class="number">662</span>)</span><br><span class="line"> </span><br><span class="line">    <span class="string">"qtp1428842218-207"</span> prio=<span class="number">5</span> tid=<span class="number">0x00000000000000cf</span> nid=<span class="number">0x7bdfc87e</span> runnable [<span class="number">0x000000007bdfc87e</span>] cpu=[total=<span class="number">130000000</span>ms,user=<span class="number">110000000</span>ms] </span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"> ...</span><br><span class="line"> at org.eclipse.jetty.server.AsyncHttpConnection.handle(AsyncHttpConnection.java:<span class="number">82</span>)</span><br><span class="line"> at org.eclipse.jetty.io.nio.SelectChannelEndPoint.handle(SelectChannelEndPoint.java:<span class="number">667</span>)</span><br><span class="line"> at org.eclipse.jetty.io.nio.SelectChannelEndPoint$<span class="number">1</span>.run(SelectChannelEndPoint.java:<span class="number">52</span>)</span><br><span class="line"> at org.eclipse.jetty.util.thread.QueuedThreadPool.runJob(QueuedThreadPool.java:<span class="number">608</span>)</span><br><span class="line"> at org.eclipse.jetty.util.thread.QueuedThreadPool$<span class="number">3</span>.run(QueuedThreadPool.java:<span class="number">543</span>)</span><br><span class="line"> at java.lang.Thread.run(Thread.java:<span class="number">662</span>)</span><br></pre></td></tr></table></figure>
<p><code>org.eclipse.jetty.util.thread.QueuedThreadPool</code>源码在jetty-util包中，核心代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Runnable _runnable = <span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Runnable job=_jobs.poll();</span><br><span class="line">                <span class="keyword">while</span> (isRunning()) &#123;</span><br><span class="line">                    <span class="comment">// Job loop</span></span><br><span class="line">                    <span class="keyword">while</span> (job!=<span class="keyword">null</span> &amp;&amp; isRunning()) &#123;</span><br><span class="line">                        runJob(job);</span><br><span class="line">                        job=_jobs.poll();</span><br><span class="line">                    &#125;</span><br><span class="line">                    ......</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其中SelectChannelEndPoint就是每个job，想要增加服务器的吞吐量，最简单的方法只要增加worker就好了。</p>
<h2 id="并行工作者模型的缺点"><a class="header-anchor" href="#并行工作者模型的缺点">¶</a>并行工作者模型的缺点</h2>
<h3 id="共享状态可能会很复杂"><a class="header-anchor" href="#共享状态可能会很复杂">¶</a>共享状态可能会很复杂</h3>
<p>在实际应用中，并行工作者模型可能比前面所描述的情况要复杂得多。共享的工作者经常需要访问一些共享数据，无论是内存中的或者共享的数据库中的。下图展示了并行工作者模型是如何变得复杂的：<br>
<img src="https://s1.ax1x.com/2020/06/21/N3DMwj.png" alt="并行工作者模型的复杂情况"><br>
有些共享状态是在像作业队列这样的通信机制下。但也有一些共享状态是业务数据，数据缓存，数据库连接池等。</p>
<p>一旦共享状态潜入到并行工作者模型中，将会使情况变得复杂起来。线程需要以某种方式存取共享数据，以确保某个线程的修改能够对其他线程可见（数据修改需要同步到主存中，不仅仅将数据保存在执行这个线程的CPU的缓存中）</p>
<h3 id="无状态的工作者"><a class="header-anchor" href="#无状态的工作者">¶</a>无状态的工作者</h3>
<p>共享状态能够被系统中得其他线程修改。所以工作者在每次需要的时候必须重读状态，以确保每次都能访问到最新的副本，不管共享状态是保存在内存中的还是在外部数据库中。工作者无法在内部保存这个状态（但是每次需要的时候可以重读）称为无状态的。</p>
<p>每次都重读需要的数据，将会导致速度变慢，特别是状态保存在外部数据库中的时候。</p>
<h3 id="任务顺序是不确定的"><a class="header-anchor" href="#任务顺序是不确定的">¶</a>任务顺序是不确定的</h3>
<p>并行工作者模式的另一个缺点是，作业执行顺序是不确定的。无法保证哪个作业最先或者最后被执行。作业A可能在作业B之前就被分配工作者了，但是作业B反而有可能在作业A之前执行。</p>
<p>并行工作者模式的这种非确定性的特性，使得很难在任何特定的时间点推断系统的状态。这也使得它也更难（如果不是不可能的话）保证一个作业在其他作业之前被执行。</p>
<h1 id="流水线-worker-thread"><a class="header-anchor" href="#流水线-worker-thread">¶</a>流水线（Worker-Thread）</h1>
<p>Worker-Thread 模式也称为流水线设计模式， 这种设计模式类似于工厂流水线， 上游工作人员完成了某个产品的组装之后， 将半成品放到流水线传送带上， 接下来的加工工作则会交给下游的工人， 如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/06/21/N3sPPI.png" alt="工厂流水线.png"></p>
<p>每个工作者只负责作业中的部分工作，当完成了自己的这部分工作时工作者会将作业转发给下一个工作者。每个工作者在自己的线程中运行，并且不会和其他工作者共享状态，流水线模型如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/06/21/N3gDtH.png" alt="流水线并发模型.png"></p>
<p>在实际应用中，作业有可能不会沿着单一流水线进行。由于大多数系统可以执行多个作业，作业从一个工作者流向另一个工作者取决于作业需要做的工作。在实际中可能会有多个不同的虚拟流水线同时运行。这是现实当中作业在流水线系统中可能的移动情况：</p>
<p><img src="https://s1.ax1x.com/2020/06/21/N32sVU.png" alt="多条流水线.png"></p>
<h2 id="响应式"><a class="header-anchor" href="#响应式">¶</a>响应式</h2>
<p>采用流水线并发模型的系统有时候也称为反应器系统或事件驱动系统，系统内的工作者对系统内出现的事件做出响应，这些事件也有可能来自于外部世界或者发自其他工作者。事件可以是传入的HTTP请求，也可以是某个文件成功加载到内存中等。</p>
<p>以上面的流水线为例，流水线上的每个工作者的工作效率可能不一样，不同阶段作业的耗时可能也不一样，我们就可以对作业进行分类，根据实际情况为每类作业分配不同数量的工作者。对上面的流水线做一下调整，工作者之间不直接进行通信，相反，它们在不同的通道中发布自己的消息（事件），其他工作者们可以在这些通道上监听消息，发送者无需知道谁在监听。</p>
<p><img src="https://s1.ax1x.com/2020/06/21/N3Te56.png" alt="响应式流水线.png"></p>
<p>比如我们到饭店吃饭，从点菜到上菜，将中间整个过程当做流水线，worker的实现如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Worker</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> BlockingQueue&lt;String&gt; prevChannel;</span><br><span class="line">    <span class="keyword">private</span> BlockingQueue&lt;String&gt; nextChannel;</span><br><span class="line">    <span class="keyword">private</span> String work;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> useTime;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Worker</span><span class="params">(String work, <span class="keyword">int</span> useTime, BlockingQueue&lt;String&gt; prevChannel, BlockingQueue&lt;String&gt; nextChannel)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.work = work;</span><br><span class="line">        <span class="keyword">this</span>.prevChannel = prevChannel;</span><br><span class="line">        <span class="keyword">this</span>.useTime = useTime;</span><br><span class="line">        <span class="keyword">this</span>.nextChannel = nextChannel;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">            <span class="comment">// 给个随机数，表示工作者的耗时</span></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                String order = prevChannel.take();</span><br><span class="line">                TimeUnit.SECONDS.sleep(useTime);</span><br><span class="line">                <span class="comment">// 添加到channel</span></span><br><span class="line">                order = String.format(<span class="string">"%s%n order step:%s by worker[%s] use %ds"</span>, order, work, Thread.currentThread().getName(), useTime);</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (<span class="keyword">null</span> != nextChannel) &#123;</span><br><span class="line">                    nextChannel.put(order);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    System.out.println(order);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>每个Worker不需要知道下一个步骤具体是什么，只需要往指定的channel中发布自己的消息，Worker的使用如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">       BlockingQueue&lt;String&gt; order = <span class="keyword">new</span> ArrayBlockingQueue&lt;&gt;(<span class="number">100</span>);</span><br><span class="line">       BlockingQueue&lt;String&gt; channel1 = <span class="keyword">new</span> ArrayBlockingQueue&lt;&gt;(<span class="number">100</span>);</span><br><span class="line">       BlockingQueue&lt;String&gt; channel2 = <span class="keyword">new</span> ArrayBlockingQueue&lt;&gt;(<span class="number">100</span>);</span><br><span class="line">       BlockingQueue&lt;String&gt; channel3 = <span class="keyword">new</span> ArrayBlockingQueue&lt;&gt;(<span class="number">100</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">       <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++) &#123;</span><br><span class="line">           Worker worker = <span class="keyword">new</span> Worker(<span class="string">"洗菜"</span>, <span class="number">2</span>, order, channel1);</span><br><span class="line">           worker.setName(<span class="string">"洗菜员"</span> + i);</span><br><span class="line">           worker.start();</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">4</span>; i++) &#123;</span><br><span class="line">           Worker worker = <span class="keyword">new</span> Worker(<span class="string">"切菜"</span>, <span class="number">4</span>, channel1, channel2);</span><br><span class="line">           worker.setName(<span class="string">"切菜员"</span> + i);</span><br><span class="line">           worker.start();</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">8</span>; i++) &#123;</span><br><span class="line">           Worker worker = <span class="keyword">new</span> Worker(<span class="string">"炒菜"</span>, <span class="number">6</span>, channel2, channel3);</span><br><span class="line">           worker.setName(<span class="string">"炒菜员"</span> + i);</span><br><span class="line">           worker.start();</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++) &#123;</span><br><span class="line">           Worker worker = <span class="keyword">new</span> Worker(<span class="string">"上菜"</span>, <span class="number">1</span>, channel3, <span class="keyword">null</span>);</span><br><span class="line">           worker.setName(<span class="string">"上菜员"</span> + i);</span><br><span class="line">           worker.start();</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">int</span> i = <span class="number">1</span>;</span><br><span class="line">       <span class="keyword">while</span>(<span class="keyword">true</span>) &#123;</span><br><span class="line">           order.put(<span class="string">"点菜，菜品ID="</span> + i);</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<h2 id="流水线模型的优点"><a class="header-anchor" href="#流水线模型的优点">¶</a>流水线模型的优点</h2>
<h3 id="无需共享状态"><a class="header-anchor" href="#无需共享状态">¶</a>无需共享状态</h3>
<p>工作者之间无需共享状态，意味着实现的时候无需考虑所有因并发访问共享对象而产生的并发性问题。这使得在实现工作者的时候变得非常容易。在实现工作者的时候就好像是单个线程在处理工作-基本上是一个单线程的实现。</p>
<h3 id="有状态的工作者"><a class="header-anchor" href="#有状态的工作者">¶</a>有状态的工作者</h3>
<p>当工作者知道了没有其他线程可以修改它们的数据，工作者可以变成有状态的。对于有状态，我是指，它们可以在内存中保存它们需要操作的数据，只需在最后将更改写回到外部存储系统。因此，有状态的工作者通常比无状态的工作者具有更高的性能。</p>
<h3 id="较好的硬件整合-hardware-conformity"><a class="header-anchor" href="#较好的硬件整合-hardware-conformity">¶</a>较好的硬件整合（Hardware Conformity）</h3>
<p>单线程代码在整合底层硬件的时候往往具有更好的优势。首先，当能确定代码只在单线程模式下执行的时候，通常能够创建更优化的数据结构和算法。</p>
<p>其次，像前文描述的那样，单线程有状态的工作者能够在内存中缓存数据。在内存中缓存数据的同时，也意味着数据很有可能也缓存在执行这个线程的CPU的缓存中。这使得访问缓存的数据变得更快。</p>
<h3 id="合理的作业顺序"><a class="header-anchor" href="#合理的作业顺序">¶</a>合理的作业顺序</h3>
<p>基于流水线并发模型实现的并发系统，在某种程度上是有可能保证作业的顺序的。作业的有序性使得它更容易地推出系统在某个特定时间点的状态。更进一步，你可以将所有到达的作业写入到日志中去。一旦这个系统的某一部分挂掉了，该日志就可以用来重头开始重建系统当时的状态。按照特定的顺序将作业写入日志，并按这个顺序作为有保障的作业顺序。下图展示了一种可能的设计：</p>
<p><img src="https://s1.ax1x.com/2020/06/21/N3Te56.png" alt="job-ordering"></p>
<p>实现一个有保障的作业顺序是不容易的，但往往是可行的。如果可以，它将大大简化一些任务，例如备份、数据恢复、数据复制等，这些都可以通过日志文件来完成。</p>
<h2 id="流水线模式的缺点"><a class="header-anchor" href="#流水线模式的缺点">¶</a>流水线模式的缺点</h2>
<p>流水线并发模型最大的缺点是作业的执行往往分布到多个工作者上，甚至分不到多个不同机器上的工作者上。这样导致在追踪某个作业到底被什么代码执行时变得困难。</p>
<h1 id="producer-consumer"><a class="header-anchor" href="#producer-consumer">¶</a>Producer-Consumer</h1>
<p>Producer 是“ 生产者&quot; 的意思， 指的是生成数据的线程。Consumer 则是“ 消费者” 的意思， 指的是使用数据的线程。</p>
<p>生产者安全地将数据交给消费者。虽然仅是这样看似简单的操作， 但当生产者和消费者以不同的线程运行时， 两者之间的处理速度差异便会引起问题。例如， 消费者想要获取数据， 可数据还没生成， 或者生产者想要交付数据， 而消费者的状态还无法接收数据等。</p>
<p>Producer-Consumer 模式在生产者和消费者之间加入了一个“ 桥梁角色” ，该桥梁角色用于消除生产者和消费者线程之间处理速度的差异，该模型如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/06/21/N3H84P.png" alt="Producer-Consumer.png"></p>
<blockquote>
<p>在某种特殊情况，Producer和Consumer都只有一个的情况，是不是就变成流水线模型了。</p>
</blockquote>
<h1 id="fork-join"><a class="header-anchor" href="#fork-join">¶</a>Fork-Join</h1>
<p>在必要的情况下，将一个大任务，进行拆分（fork） 成若干个子任务（拆到不能再拆，这里就是指我们制定的拆分的临界值），再将一个个小任务的结果进行join汇总。，也就是分治思想，如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/06/21/N3OLWt.png" alt="ForkJoin.png"></p>
<blockquote>
<p><strong>MapReduce</strong></p>
<p>输入一个大，通过Split之后，将其分为多个片</p>
<p>每个文件分片由单独的机器去处理，这就是map方法</p>
<p>将各个机器计算的结果进行汇总并得到最终的结果，这就是reduce方法</p>
</blockquote>
<p>Fork/Join采用“工作窃取模式”，当执行新的任务时他可以将其拆分成更小的任务执行，并将小任务加到线程队列中，然后再从一个随即线程中偷一个并把它加入自己的队列中。</p>
<p>就比如两个CPU上有不同的任务，这时候A已经执行完，B还有任务等待执行，这时候A就会将B队尾的任务偷过来，加入自己的队列中，对于传统的线程池，ForkJoin更有效的利用的CPU资源！</p>
<p><img src="https://s1.ax1x.com/2020/06/21/N3vFMV.png" alt="工作窃取.png"></p>
<p>jdk1.7提供了Fork-Joink框架：ForkJoinPool，要使用ForkJoin框架，必须首先创建一个ForkJoin任务。它提供在任务中执行fork()和join()操作的机制，通常情况下我们不需要直接继承ForkJoinTask类，而只需要继承它的子类，Fork/Join框架提供了以下两个子类：</p>
<ul>
<li>RecursiveAction：用于没有返回结果的任务。</li>
<li>RecursiveTask ：用于有返回结果的任务。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ForkJoinWork</span> <span class="keyword">extends</span> <span class="title">RecursiveTask</span>&lt;<span class="title">Long</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Long start;</span><br><span class="line">    <span class="keyword">private</span> Long end;</span><br><span class="line">    <span class="comment">//临界值</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> LEAF_TARGET = ForkJoinPool.getCommonPoolParallelism() &lt;&lt; <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">long</span> CRITICAL;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ForkJoinWork</span><span class="params">(Long start, Long end)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.start = start;</span><br><span class="line">        <span class="keyword">this</span>.end = end;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">long</span> <span class="title">suggestTargetSize</span><span class="params">(<span class="keyword">long</span> sizeEstimate)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (CRITICAL != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> CRITICAL;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">long</span> est = sizeEstimate / LEAF_TARGET;</span><br><span class="line">        <span class="keyword">if</span> (est &gt; <span class="number">0L</span>) &#123;</span><br><span class="line">            CRITICAL = est;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            CRITICAL = <span class="number">1L</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> CRITICAL;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> Long <span class="title">compute</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">//判断是否是拆分完毕</span></span><br><span class="line">        Long length = end - start;</span><br><span class="line">        <span class="keyword">if</span> (length &lt;= suggestTargetSize(length)) &#123;</span><br><span class="line">            <span class="comment">//如果拆分完毕就相加</span></span><br><span class="line">            <span class="keyword">return</span> LongStream.rangeClosed(start, end).reduce(<span class="number">0</span>, Long::sum);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">//没有拆分完毕就开始拆分</span></span><br><span class="line">            Long middle = (end + start) / <span class="number">2</span>;</span><br><span class="line">            ForkJoinWork right = <span class="keyword">new</span> ForkJoinWork(start, middle);</span><br><span class="line">            <span class="comment">//拆分，并压入线程队列</span></span><br><span class="line">            right.fork();</span><br><span class="line"></span><br><span class="line">            ForkJoinWork left = <span class="keyword">new</span> ForkJoinWork(middle + <span class="number">1</span>, end);</span><br><span class="line">            <span class="comment">//拆分，并压入线程队列</span></span><br><span class="line">            left.fork();</span><br><span class="line"></span><br><span class="line">            <span class="comment">//合并</span></span><br><span class="line">            <span class="keyword">return</span> right.join() + left.join();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>接下来对比下：ForkJoin实现、单线程计算、Java 8 并行流的实现</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ForkJoinPoolTest</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fork_join_test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">//ForkJoin实现</span></span><br><span class="line">        <span class="keyword">long</span> startTime = System.currentTimeMillis();</span><br><span class="line">        ForkJoinPool forkJoinPool = <span class="keyword">new</span> ForkJoinPool(<span class="number">16</span>);</span><br><span class="line">        ForkJoinTask&lt;Long&gt; task = <span class="keyword">new</span> ForkJoinWork(<span class="number">0L</span>, <span class="number">20000000000L</span>);</span><br><span class="line">        Long invoke = forkJoinPool.invoke(task);</span><br><span class="line">        <span class="keyword">long</span> endTime = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"invoke = "</span> + invoke + <span class="string">"  time: "</span> + (endTime - startTime));</span><br><span class="line">        <span class="comment">//invoke = -2914184800805067776  time: 2873</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">single_thread_test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">//普通单线程实现</span></span><br><span class="line">        <span class="keyword">long</span> startTime = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">long</span> x = LongStream.rangeClosed(<span class="number">0</span>, <span class="number">20000000000L</span>).reduce(<span class="number">0</span>, Long::sum);</span><br><span class="line">        <span class="keyword">long</span> entTime = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"invoke = "</span> + x + <span class="string">"  time: "</span> + (entTime - startTime));</span><br><span class="line">        <span class="comment">// invoke = -2914184800805067776  time: 8046</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">parallel_test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">//Java 8 并行流的实现</span></span><br><span class="line">        <span class="keyword">long</span> startTime = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">long</span> reduce = LongStream.rangeClosed(<span class="number">0</span>, <span class="number">20000000000L</span>).parallel().reduce(<span class="number">0</span>, Long::sum);</span><br><span class="line">        <span class="keyword">long</span> endTime = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">"invoke = "</span> + reduce + <span class="string">"  time: "</span> + (endTime - startTime));</span><br><span class="line">        <span class="comment">//invoke = -2914184800805067776  time: 2188</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="产考文献"><a class="header-anchor" href="#产考文献">¶</a>产考文献</h1>
<ul>
<li>《图解Java多线程设计模式》</li>
<li>《Java高并发编程详解：多线程与架构设计》</li>
<li><span class="exturl" data-url="aHR0cDovL2lmZXZlLmNvbS8lRTUlQjklQjYlRTUlOEYlOTElRTclQkMlOTYlRTclQTglOEIlRTYlQTglQTElRTUlOUUlOEI=" title="http://ifeve.com/%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E6%A8%A1%E5%9E%8B">http://ifeve.com/并发编程模型<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>java多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java多线程</tag>
        <tag>多线程编程</tag>
        <tag>并发模型</tag>
      </tags>
  </entry>
  <entry>
    <title>多线程编程--基础知识</title>
    <url>/mutlithreading/multithreading-base.html</url>
    <content><![CDATA[<p><strong>线程</strong>（Thread）是操作系统能够进行运算调度的最小单位，java线程中创建的、引用的对象在jvm内存中是如何存放的，线程间是如何进行通信的呢，线程发生异常了jvm又是如何处理的呢，接下来让我们从线程的基础知识开始一步一步地了解。</p>
<p><img src="https://s2.ax1x.com/2019/04/13/AL3oVS.png" alt="多线程编程"></p>
<a id="more"></a>
<h1 id="线程基础知识"><a class="header-anchor" href="#线程基础知识">¶</a>线程基础知识</h1>
<h2 id="什么是线程"><a class="header-anchor" href="#什么是线程">¶</a>什么是线程</h2>
<p><strong>线程</strong>（Thread）是操作系统能够进行运算调度的最小单位。它被包含在进程之中，是进程中的实际运作单位。一条线程指的是进程中一个单一顺序的控制流，一个进程中可以并发多个线程，每条线程并行执行不同的任务。</p>
<h2 id="如何创建线程"><a class="header-anchor" href="#如何创建线程">¶</a>如何创建线程</h2>
<ul>
<li>继承Thread类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 继承Thread类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadTest</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">super</span>.run();</span><br><span class="line">        System.out.println(<span class="string">"Hello World! This is my first Thread."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">extendsThreadTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    ThreadTest threadTest = <span class="keyword">new</span> ThreadTest();</span><br><span class="line">    threadTest.start();</span><br><span class="line">    System.out.println(<span class="string">"运行结束。"</span>);</span><br><span class="line">    Thread.sleep(<span class="number">10</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>实现Runnable接口</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 实现Runnable接口</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">RunnableTest</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Hello World! This is my first Thread."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">implementsRunnableTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    RunnableTest runnableTest = <span class="keyword">new</span> RunnableTest();</span><br><span class="line">    Thread thread = <span class="keyword">new</span> Thread(runnableTest);</span><br><span class="line">    thread.start();</span><br><span class="line"></span><br><span class="line">    Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">    System.out.println(<span class="string">"运行结束。"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="线程的状态"><a class="header-anchor" href="#线程的状态">¶</a>线程的状态</h2>
<p>Java中的线程一共有六种状态：</p>
<ul>
<li>NEW（初始化状态）</li>
<li>RUNNABLE（可运行 / 运行状态）</li>
<li>BLOCKED（阻塞状态）</li>
<li>WAITING（无时限等待）</li>
<li>TIMED_WAITING（有时限等待）</li>
<li>TERMINATED（终止状态）</li>
</ul>
<p>线程可以在这六种状态之间相互转换，如图所示：</p>
<p><img src="https://s1.ax1x.com/2020/05/23/YvfvDg.jpg" alt="线程状态切换.jpg"></p>
<h1 id="内存模型"><a class="header-anchor" href="#内存模型">¶</a>内存模型</h1>
<h2 id="java内存模型"><a class="header-anchor" href="#java内存模型">¶</a>java内存模型</h2>
<ul>
<li>每一个运行在Java虚拟机里的线程都拥有自己的线程栈，存放当前线程运行的信息。</li>
<li>所有原始类型的本地变量都存放在线程栈上，因此对其它线程不可见。</li>
<li>所有引用类型的本地变量都存放在堆中，线程栈保存该对象的引用，因此其他线程只要有该对象的引用都可以访问。</li>
<li>Java程序中无论由哪个对象创建的对象，不管是原始类型对象，还是引用类型对象，都是存放在堆里面。</li>
</ul>
<p>接下来让我们先看看一段具体代码，这些变量都存放在JVM的什么位置。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MemoryModel</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MemorySharedObject</span> </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> MemorySharedObject sharedObject = <span class="keyword">new</span> MemorySharedObject();</span><br><span class="line">        <span class="keyword">public</span> Integer object2 = <span class="keyword">new</span> Integer(<span class="number">222</span>);</span><br><span class="line">        <span class="keyword">public</span> Integer object4 = <span class="keyword">new</span> Integer(<span class="number">444</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">long</span> member1 = <span class="number">12345L</span>;</span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">long</span> member2 = <span class="number">67890L</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> List&lt;Object&gt; list = Collections.synchronizedList(<span class="keyword">new</span> ArrayList&lt;&gt;());</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> Thread(<span class="keyword">this</span>::methodOne).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(<span class="keyword">this</span>::methodOne).start();</span><br><span class="line"></span><br><span class="line">        Thread.sleep(<span class="number">100000</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">methodOne</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> localVariable1 = <span class="number">999</span>;</span><br><span class="line">        MemorySharedObject localVariable2 = MemorySharedObject.sharedObject;</span><br><span class="line"></span><br><span class="line">        list.add(localVariable2);</span><br><span class="line">        methodTwo();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">methodTwo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Integer localVariable1 = <span class="keyword">new</span> Integer(<span class="number">4321</span>);</span><br><span class="line">        list.add(localVariable1);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当test用例执行的时候，各个变量在jvm内存中存放位置如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/05/23/YxN3LT.png" alt="YxN3LT.png"></p>
<p>上图中每个线程执行<code>methodOne()</code>都会在它们对应的线程栈上创建<code>localVariable1</code>和<code>localVariable2</code>的私有拷贝。<code>localVariable1</code>为基础类型对象只存在于线程栈上，<code>localVariable2</code>为堆内存中Object3的引用。methodTwo方法中的localVariable1都会各自在堆上创建一个对象<code>object1</code>和<code>object5</code>，线程栈中存放这两个对象的引用。</p>
<p>执行test用例的时候，我们执行如下两个步骤：</p>
<ul>
<li>jmap -dump:format=b,file=./heap_dump.txt 12792</li>
<li>dump出jvm内存后，使用mat进行分析，可以找到本例中对象的情况</li>
</ul>
<p>分析结果如下如所示：</p>
<p><img src="https://s1.ax1x.com/2020/05/24/tS30OS.png" alt="mat内存分析.png"></p>
<h2 id="硬件内存模型"><a class="header-anchor" href="#硬件内存模型">¶</a>硬件内存模型</h2>
<p><img src="https://s1.ax1x.com/2020/05/23/YxN0Qx.png" alt="YxN0Qx.png"></p>
<h2 id="java内存模型与硬件内存模型的关系"><a class="header-anchor" href="#java内存模型与硬件内存模型的关系">¶</a>java内存模型与硬件内存模型的关系</h2>
<p><img src="https://s1.ax1x.com/2020/05/23/YxNxXV.png" alt="YxNxXV.png"></p>
<h1 id="同步异步"><a class="header-anchor" href="#同步异步">¶</a>同步异步</h1>
<p><strong>同步和异步关注的是：消息通信机制(synchronous communication/ asynchronous communication)。</strong></p>
<h2 id="同步-synchronous"><a class="header-anchor" href="#同步-synchronous">¶</a>同步（Synchronous）</h2>
<p><strong>同步方法</strong>调用一旦开始，调用者必须等到方法调用返回后，才能继续后续的行为，如下图所示：</p>
<p><img src="https://img2018.cnblogs.com/blog/1680783/201905/1680783-20190521124754180-1985908967.png" alt="同步.png"></p>
<blockquote>
<ul>
<li>打电话</li>
<li>B/S模式</li>
</ul>
</blockquote>
<h2 id="异步-asynchronous"><a class="header-anchor" href="#异步-asynchronous">¶</a>异步（Asynchronous）</h2>
<p><strong>异步方法</strong>调用更像一个消息传递，一旦开始，方法调用就会立即返回，调用者就可以继续后续的操作。而，异步方法通常会在另外一个线程中，“真实”地执行着。整个过程，不会阻碍调用者的工作，如下图所示：</p>
<p><img src="https://img2018.cnblogs.com/blog/1680783/201905/1680783-20190521125411515-1327485285.png" alt="同步.png"></p>
<blockquote>
<ul>
<li>发短信</li>
<li>ajax</li>
<li>消息队列</li>
</ul>
</blockquote>
<h2 id="阻塞和非阻塞"><a class="header-anchor" href="#阻塞和非阻塞">¶</a>阻塞和非阻塞</h2>
<p><strong>阻塞和非阻塞</strong>：强调的是程序在等待调用结果（消息，返回值）时的状态。</p>
<p>阻塞调用是指调用结果返回之前，当前线程会被挂起，调用线程只有在得到结果之后才会返回。</p>
<p>非阻塞调用指在不能立刻得到结果之前，该调用不会阻塞当前线程。</p>
<h1 id="线程间通信"><a class="header-anchor" href="#线程间通信">¶</a>线程间通信</h1>
<h2 id="共享对象"><a class="header-anchor" href="#共享对象">¶</a>共享对象</h2>
<p>线程间发送信号的一个简单方式是在共享对象的变量里设置信号值，如下面代码所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MySignal</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">boolean</span> hasDataToProcess = <span class="keyword">false</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">boolean</span> <span class="title">hasDataToProcess</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.hasDataToProcess;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">setHasDataToProcess</span><span class="params">(<span class="keyword">boolean</span> process)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.hasDataToProcess = process;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>线程A在一个同步块里设置boolean型成员变量hasDataToProcess为true，线程B也在同步块里读取hasDataToProcess这个成员变量。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test_share_signal</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    MySignal signal = <span class="keyword">new</span> MySignal();</span><br><span class="line">    <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">        <span class="keyword">while</span>(!signal.hasDataToProcess()) &#123;</span><br><span class="line">            System.out.println(<span class="string">"线程A未接收到信号，sleep 1000ms"</span>);</span><br><span class="line">            sleep(<span class="number">1000</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"线程A接收到信号了，开始处理："</span> + data.remove(<span class="number">0</span>));</span><br><span class="line">    &#125;).start();</span><br><span class="line"></span><br><span class="line">    sleep(<span class="number">10000</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">        System.out.println(<span class="string">"线程B设置信号"</span>);</span><br><span class="line">        data.add(<span class="string">"线程B设置的数据"</span>);</span><br><span class="line">        signal.setHasDataToProcess(<span class="keyword">true</span>);</span><br><span class="line">    &#125;).start();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>注意：<strong>线程A和B必须获得指向一个MySignal共享实例的引用，否则线程A将收不到信号。</strong></p>
<h2 id="wait-notify-和notifyall"><a class="header-anchor" href="#wait-notify-和notifyall">¶</a>wait(),notify()和notifyAll()</h2>
<p>通过共享对象，循环检测信号是否被设置，如果没有被设置则进入等待，等待的间隔时间设置过短则对cpu消耗过大，等待的间隔时间设置过长则消息接收不及时。</p>
<p>Java有一个内建的等待机制来允许线程在等待信号的时候变为非运行状态。java.lang.Object 类定义了三个方法，wait()、notify()和notifyAll()来实现这个等待机制。</p>
<p>一个线程一旦调用了任意对象的wait()方法，就会变为非运行状态，直到另一个线程调用了同一个对象的notify()方法。为了调用wait()或者notify()，线程必须先获得那个对象的锁，也就是说，线程必须在同步块里调用wait()或者notify()，否则将抛出<code>java.lang.IllegalMonitorStateException</code>异常。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Test</span><br><span class="line">   public void test_wait_notify() &#123;</span><br><span class="line">       Object monitor = new Object();</span><br><span class="line">       List&lt;String&gt; data = new ArrayList&lt;&gt;();</span><br><span class="line">       Thread thread = new Thread(() -&gt; &#123;</span><br><span class="line">           try &#123;</span><br><span class="line">               synchronized (monitor) &#123;</span><br><span class="line">                   System.out.println(&quot;子线程开始等待notify信号&quot;);</span><br><span class="line">                   monitor.wait();</span><br><span class="line">               &#125;</span><br><span class="line">               System.out.println(&quot;子线程接收到notify信号了，开始处理：&quot; + data.remove(0));</span><br><span class="line">           &#125; catch (InterruptedException e) &#123;</span><br><span class="line">               e.printStackTrace();</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;);</span><br><span class="line">       thread.start();</span><br><span class="line"></span><br><span class="line">       sleep(3000);</span><br><span class="line">       data.add(&quot;主线程调用notify方法&quot;);</span><br><span class="line">       synchronized (monitor) &#123;</span><br><span class="line">           monitor.notify();</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>一旦线程调用了wait()方法，它就释放了所持有的监视器对象上的锁。这将允许其他线程也可以调用wait()或者notify()。一旦一个线程被唤醒，不能立刻就退出wait()的方法调用，直到调用notify()的线程退出了它自己的同步块。换句话说：**被唤醒的线程必须重新获得监视器对象的锁，才可以退出wait()的方法调用，因为wait方法调用运行在同步块里面。**如果多个线程被notifyAll()唤醒，那么在同一时刻将只有一个线程可以退出wait()方法，因为每个线程在退出wait()前必须获得监视器对象的锁。</p>
<h1 id="异常处理"><a class="header-anchor" href="#异常处理">¶</a>异常处理</h1>
<h2 id="thread默认的异常处理"><a class="header-anchor" href="#thread默认的异常处理">¶</a>Thread默认的异常处理</h2>
<p>线程都不允许抛出未捕获的checked exception（比如sleep时的<code>InterruptedException</code>）<strong>，也就是说各个线程需要自己把自己的checked exception处理掉</strong>。我们可以查看一下Thread类的run()方法声明，方法声明上没有对抛出异常进行约束。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Thread类中</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (target != <span class="keyword">null</span>) &#123;</span><br><span class="line">        target.run();<span class="comment">//实际上直接调用Runnable实例的run方法</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//Runnable接口中</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<p><strong>线程是独立执行的代码片断，线程的问题应该由线程自己来解决，而不要委托到外部。</strong></p>
<h2 id="未捕获的异常去哪儿了"><a class="header-anchor" href="#未捕获的异常去哪儿了">¶</a>未捕获的异常去哪儿了</h2>
<p>一个异常被抛出后，如果没有被捕获处理，则会一直向上抛。<font color="red"><strong>异常一旦被Thread.run() 抛出后，就不能在程序中对异常进行捕获，最终只能由JVM捕获。</strong></font></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test_thread_exception</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> Thread(() -&gt; &#123;<span class="keyword">int</span> a = <span class="number">1</span>/<span class="number">0</span>;&#125;).start();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 执行结果</span></span><br><span class="line">Exception in thread <span class="string">"Thread-0"</span> java.lang.ArithmeticException: / by zero</span><br><span class="line">	at com.teddy.thread.basic.ThreadExceptionTest.lambda$thread_exception$<span class="number">0</span>(ThreadExceptionTest.java:<span class="number">9</span>)</span><br><span class="line">	at java.lang.Thread.run(Thread.java:<span class="number">748</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test_catch_thread_exception</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(() -&gt; &#123;<span class="keyword">int</span> a = <span class="number">1</span>/<span class="number">0</span>;&#125;).start();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            System.out.println(<span class="string">"捕获到线程抛出的异常！"</span>);</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 执行结果</span></span><br><span class="line">Exception in thread <span class="string">"Thread-0"</span> java.lang.ArithmeticException: / by zero</span><br><span class="line">	at com.teddy.thread.basic.ThreadExceptionTest.lambda$test_catch_thread_exception$<span class="number">1</span>(ThreadExceptionTest.java:<span class="number">18</span>)</span><br><span class="line">	at java.lang.Thread.run(Thread.java:<span class="number">748</span>)</span><br></pre></td></tr></table></figure>
<h2 id="jvm如何处理线程中抛出的异常"><a class="header-anchor" href="#jvm如何处理线程中抛出的异常">¶</a>JVM如何处理线程中抛出的异常</h2>
<p>查看Thread类的源码，我们可以看到有个dispatchUncaughtException方法，此方法就是用来处理线程中抛出的异常的。JVM会调用dispatchUncaughtException方法来寻找异常处理器(UncaughtExceptionHandler)，处理异常。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 向handler分派未捕获的异常。该方法仅由JVM调用。</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">dispatchUncaughtException</span><span class="params">(Throwable e)</span> </span>&#123;</span><br><span class="line">    getUncaughtExceptionHandler().uncaughtException(<span class="keyword">this</span>, e);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取用来处理未捕获异常的handler，如果没有设置则返回当前线程所属的ThreadGroup</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> UncaughtExceptionHandler <span class="title">getUncaughtExceptionHandler</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> uncaughtExceptionHandler != <span class="keyword">null</span> ?</span><br><span class="line">        uncaughtExceptionHandler : group;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>UncaughtExceptionHandler必须显示的设置，否则默认为null。若为null，则使用线程默认的handler，即该线程所属的ThreadGroup。ThreadGroup自身就是一个handler，查看ThreadGroup的源码就可以发现，ThreadGroup实现了Thread.UncaughtExceptionHandler接口，并实现了默认的处理方法。默认的未捕获异常处理器处理时，会调用 System.err 进行输出，也就是直接打印到控制台了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">uncaughtException</span><span class="params">(Thread t, Throwable e)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (parent != <span class="keyword">null</span>) &#123; <span class="comment">// 父级优先处理</span></span><br><span class="line">        parent.uncaughtException(t, e);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        Thread.UncaughtExceptionHandler ueh = Thread.getDefaultUncaughtExceptionHandler();</span><br><span class="line">        <span class="keyword">if</span> (ueh != <span class="keyword">null</span>) &#123;</span><br><span class="line">            ueh.uncaughtException(t, e);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!(e <span class="keyword">instanceof</span> ThreadDeath)) &#123; </span><br><span class="line">            <span class="comment">// 没有配置handler时，默认直接打印到控制台</span></span><br><span class="line">            System.err.print(<span class="string">"Exception in thread \""</span></span><br><span class="line">                             + t.getName() + <span class="string">"\" "</span>);</span><br><span class="line">            e.printStackTrace(System.err);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="产考文献"><a class="header-anchor" href="#产考文献">¶</a>产考文献</h1>
<ul>
<li><span class="exturl" data-url="aHR0cDovL2lmZXZlLmNvbS9qYXZhLWNvbmN1cnJlbmN5LXRocmVhZC1kaXJlY3Rvcnkv" title="http://ifeve.com/java-concurrency-thread-directory/">http://ifeve.com/java-concurrency-thread-directory/<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cDovL3R1dG9yaWFscy5qZW5rb3YuY29tL2phdmEtY29uY3VycmVuY3kvaW5kZXguaHRtbA==" title="http://tutorials.jenkov.com/java-concurrency/index.html">http://tutorials.jenkov.com/java-concurrency/index.html<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9mYW56aG9uZ3dlaS5jb20vdGhyZWFkL2g1L3RocmVhZC5odG1s" title="https://fanzhongwei.com/thread/h5/thread.html">https://fanzhongwei.com/thread/h5/thread.html<i class="fa fa-external-link"></i></span></li>
<li>《深入Java虚拟机：JVM高级特性与最佳实践（第2版）》</li>
</ul>
]]></content>
      <categories>
        <category>java多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java多线程</tag>
        <tag>多线程编程</tag>
      </tags>
  </entry>
  <entry>
    <title>多线程与线程安全</title>
    <url>/mutlithreading/mutlithreading.html</url>
    <content><![CDATA[<p>《Java Concurrency In Practice》的作者Brian Goetz对“线程安全”有一个比较恰当的定义：“当多个线程访问一个对象时，如果不用考虑这些线程在运行时环境下的调度和交替执行，也不需要进行额外的同步，或者在调用方进行任何其他的协调操作，调用这个对象的行为都可以获得正确的结果，那这个对象是线程安全的”。</p>
<p><img src="https://s2.ax1x.com/2019/04/13/AL3oVS.png" alt="AL3oVS.png"></p>
<a id="more"></a>
<p>转载自姜函的：<span class="exturl" data-url="" title="http://172.25.16.82:3000/#/%E5%9B%A2%E9%98%9F%E7%9B%B8%E5%85%B3/%E5%9F%B9%E8%AE%AD%E5%88%86%E4%BA%AB/%E6%9D%90%E6%96%99/2019/%E5%A4%9A%E7%BA%BF%E7%A8%8B%E4%B8%8E%E7%BA%BF%E7%A8%8B%E5%AE%89%E5%85%A8">多线程与线程安全<i class="fa fa-external-link"></i></span></p>
<h1 id="多线程与线程安全"><a class="header-anchor" href="#多线程与线程安全">¶</a>多线程与线程安全</h1>
<h2 id="java内存模型"><a class="header-anchor" href="#java内存模型">¶</a>Java内存模型</h2>
<h3 id="线程不安全的源头"><a class="header-anchor" href="#线程不安全的源头">¶</a>线程不安全的源头</h3>
<h4 id="cpu-内存-i-o的恩怨情仇"><a class="header-anchor" href="#cpu-内存-i-o的恩怨情仇">¶</a>CPU、内存、I/O的恩怨情仇</h4>
<p>这些年，我们的 CPU、内存、I/O 设备都在不断迭代，不断朝着更快的方向努力。但是，在这个快速发展的过程中，有一个核心矛盾一直存在，就是这三者的速度差异。我们都知道就速度而言，CPU&gt;&gt;内存&gt;&gt;I/O设备，所以为了平衡这三者之间的速度差异，计算机体系机构、操作系统、编译程序都做出了贡献，主要体现为：</p>
<ul>
<li>CPU 增加了缓存，以均衡与内存的速度差异</li>
<li>操作系统增加了进程、线程，以分时复用 CPU，进而均衡 CPU 与 I/O 设备的速度差异</li>
<li>编译程序优化指令执行次序，使得缓存能够得到更加合理地利用</li>
</ul>
<p>但是，是的，凡事都有但是，这些优化让我们享受的同时，也带来了线程不安全的问题。</p>
<h4 id="源头之一：缓存导致的可见性"><a class="header-anchor" href="#源头之一：缓存导致的可见性">¶</a>源头之一：缓存导致的可见性</h4>
<p>多核时代，每颗 CPU 都有自己的缓存，当多个线程在不同的 CPU 上执行时，这些线程操作的是不同的 CPU 缓存。比如下图中，线程 A 操作的是 CPU-1 上的缓存，而线程 B 操作的是 CPU-2 上的缓存，很明显，这个时候线程 A 对变量 V 的操作对于线程 B 而言就不具备可见性了。这个就属于硬件程序员给软件程序员挖的“坑”。</p>
<p><img src="https://static001.geekbang.org/resource/image/e2/ea/e2aa76928b2bc135e08e7590ca36e0ea.png" alt="img"></p>
<h4 id="源头之二：线程切换带来的原子性问题"><a class="header-anchor" href="#源头之二：线程切换带来的原子性问题">¶</a>源头之二：线程切换带来的原子性问题</h4>
<p>操作系统允许某个进程执行一小段时间，例如 50 毫秒，过了 50 毫秒操作系统就会重新选择一个进程来执行（我们称为“任务切换”），这个 50 毫秒称为“时间片“。现代的操作系统都基于更轻量的线程来调度，现在我们提到的“任务切换”都是指“线程切换”。Java 并发程序都是基于多线程的，自然也会涉及到任务切换，也许你想不到，任务切换竟然也是并发编程里诡异 Bug 的源头之一。任务切换的时机大多数是在时间片结束的时候，我们现在基本都使用高级语言编程，高级语言里一条语句往往需要多条 CPU 指令完成，例如大家所熟悉的count++,这个语句需要3条指令才能执行：</p>
<ul>
<li>
<p>指令 1：首先，需要把变量 count 从内存加载到 CPU 的寄存器</p>
</li>
<li>
<p>指令 2：之后，在寄存器中执行 +1 操作</p>
</li>
<li>
<p>指令 3：最后，将结果写入内存（缓存机制导致可能写入的是 CPU 缓存而不是内存）</p>
</li>
</ul>
<p>操作系统做任务切换，可以发生在任何一条CPU指令。对于上面的三条指令来说，我们假设 count=0，如果线程 A 在指令 1 执行完后做线程切换，线程 A 和线程 B 按照下图的序列执行，那么我们会发现两个线程都执行了 count+=1 的操作，但是得到的结果不是我们期望的 2，而是 1。</p>
<p><img src="https://static001.geekbang.org/resource/image/33/63/33777c468872cb9a99b3cdc1ff597063.png" alt="img"></p>
<h4 id="源头之三：编译优化带来的有序性问题"><a class="header-anchor" href="#源头之三：编译优化带来的有序性问题">¶</a>源头之三：编译优化带来的有序性问题</h4>
<p>有序性，顾名思义，有序性指的是程序按照代码的先后顺序执行。编译器为了优化性能，有时候会改变程序中语句的先后顺序，例如程序中：“a=6；b=7；”编译器优化后可能变成“b=7；a=6；”，在这个例子中，编译器调整了语句的顺序，但是不影响程序的最终结果。不过有时候编译器及解释器的优化可能导致意想不到的 Bug。再次拿出这个臭名昭著的DCL单例模式来举栗子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</span><br><span class="line">       </span><br><span class="line">       <span class="keyword">static</span> Singleton instance;</span><br><span class="line">       </span><br><span class="line">       <span class="function"><span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">           <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;</span><br><span class="line">               <span class="keyword">synchronized</span>(Singleton<span class="class">.<span class="keyword">class</span>) </span>&#123;</span><br><span class="line">                   <span class="keyword">if</span> (instance == <span class="keyword">null</span>)</span><br><span class="line">                       instance = <span class="keyword">new</span> Singleton();</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="keyword">return</span> instance;</span><br><span class="line">       &#125;</span><br><span class="line">       </span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>这个创建单例的模式为什么在多线程的情况下会出问题呢？主要是在new Singleton()这一步上，我们凭直觉以为这个new操作是这样的：</p>
<ul>
<li>分配一块内存 M</li>
<li>在内存 M 上初始化 Singleton对象</li>
<li>然后 M 的地址赋值给 instance 变量</li>
</ul>
<p>实际上优化后的执行顺序长这样：</p>
<ul>
<li>分配一块内存 M</li>
<li>将 M 的地址赋值给 instance 变量</li>
<li>最后在内存 M 上初始化 Singleton 对象</li>
</ul>
<p>所以这种情况下当A线程执行这个new操作的时候，这个时候M的地址已经赋给instance 变量，但是还未初始化，这时另外一个线程B同时进入getInstance()方法，检测到instance不为null，直接返回，而返回的是一个无效值，会触发空指针异常。</p>
<h3 id="java内存模型的概念"><a class="header-anchor" href="#java内存模型的概念">¶</a>Java内存模型的概念</h3>
<p>Java虚拟机规范中试图定义一种Java内存模型来屏蔽掉各种硬件和操作系统的内存访问差异，以实现让Java程序在各种平台下都能达到一致的并发效果。Java内存模型的主要目标是定义程序中各个变量的访问规则，即在虚拟机中将变量存储到内存和从内存中取出变量这样的底层细节，此处的变量与Java编程中所说的变量略有区别，它包括了实例字段、静态字段和构成数组对象的元素，<br>
但是不包括局部变量与方法参数，因为后者是线程私有的，不会被共享，自然就不存在竟争问题。</p>
<p>Java内存模型规定了所有的变量都存储在主内存上，每条线程还有自己的工作内存，线程的工作内存中保存了被该线程使用到的变量的主内存副本拷贝，线程对变量的所有操作(读取、赋值等)都必须在工作内存中进行，而不能直接读写主内存中的变量。不同的线程之间也无法直接访问对方工作内存中的变量，线程间变量值的传递都需要通过主内存来传递，线程、主内存、工作内存的交互关系如下。</p>
<p><img src="http://120.79.172.214/mdpicture/1553328940111.png" alt="1553328940111"></p>
<p>JDK在1.5之后对Java内存模型做了很大的修正，我们接下来的讨论也是基于JDK1.5之后的Java内存模型。</p>
<h4 id="happens-before"><a class="header-anchor" href="#happens-before">¶</a>Happens-Before</h4>
<p>happens—before规则是Java内存模型中定义的两项操作之间的偏序关系，如果操作A先行发生于操作B，其意思就是说，在发生操作B之前，操作A产生的影响都能被操作B观察到，“影响”包括修改了内存中共享变量的值、发送了消息、调用了方法等，它与时间上的先后发生基本没有太大关系。这个原则特别重要，它是判断数据是否存在竞争、线程是否安全的主要依据。具体包括：</p>
<ul>
<li>程序次序规则：在一个单独的线程中，按照程序代码的执行流顺序，（时间上）先执行的操作happen—before（时间上）后执行的操作</li>
<li>管程锁定规则：一个unlock操作happen—before后面（时间上的先后顺序，下同）对同一个锁的lock操作</li>
<li>volatile变量规则：对一个volatile变量的写操作happen—before后面对该变量的读操作</li>
<li>线程启动规则：Thread对象的start（）方法happen—before此线程的每一个动作</li>
<li>线程终止规则：线程的所有操作都happen—before对此线程的终止检测，可以通过Thread.join（）方法结束或者Thread.isAlive（）的返回值等手段检测到线程已经终止执行</li>
<li>线程中断规则：对线程interrupt（）方法的调用happen—before发生于被中断线程的代码检测到中断时事件的发生</li>
<li>对象终结规则：一个对象的初始化完成（构造函数执行结束）happen—before它的finalize（）方法的开始</li>
<li>传递性：如果操作A happen—before操作B，操作B happen—before操作C，那么可以得出A happen—before操作C</li>
</ul>
<p>这是Java中仅有的无须任何同步手段就可以保证先行发生规则，我们可以通过一个简单的getter/setter示例感受一下“先行发生”与”时间上的先后发生“有何区别：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">int</span> value = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getValue</span><span class="params">()</span></span>&#123;</span><br><span class="line">       <span class="keyword">return</span> value;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setValue</span><span class="params">(<span class="keyword">int</span> value)</span></span>&#123;</span><br><span class="line">   	<span class="keyword">this</span>.value = value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>假设线程A先调用setValue(10)，然后线程B再去调用同一个对象的getValue，那么线程B收到的值是10吗？我们可以根据上述的规则进行分析，没有一个规则适用，也就是说无法保证这两个操作的先行发生关系，所以线程B获得的结果是不确定的，换句话说这是线程不安全的。而让他变得线程安全也至少有两种简单的方案：</p>
<ul>
<li>给get/set方法加锁，这样就可以套用管程锁定规则</li>
<li>让value定义为volatile变量，这样就能套用volatile变量规则</li>
</ul>
<h4 id="最轻量级的同步机制volatile"><a class="header-anchor" href="#最轻量级的同步机制volatile">¶</a>最轻量级的同步机制volatile</h4>
<p>当一个变量为定义为volatile之后，将具备两个特性：</p>
<ul>
<li>可见性：这里的可见是指当一个线程修改了变量的值，其他线程可以立即看见。但是请注意，volatile可以保证并发情况下变量在各个线程中保证一致性，但是并不能说在并发条件下是线程安全的，因为volatile并不能保证原子性，我们可以用一个示例的来说明：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">VolatileTest</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">volatile</span> <span class="keyword">int</span> race = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">increase</span><span class="params">()</span></span>&#123;</span><br><span class="line">        race++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> THREAD_COUNT = <span class="number">20</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>;i&lt;THREAD_COUNT;i++)&#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(</span><br><span class="line">                    () -&gt; &#123;</span><br><span class="line">                        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>;j&lt;<span class="number">10000</span>;j++)&#123;</span><br><span class="line">                            increase();</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">            ).start();</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//等待所有累加线程都结束</span></span><br><span class="line">        <span class="keyword">while</span> (Thread.activeCount()&gt;<span class="number">1</span>)&#123;</span><br><span class="line">            Thread.yield(); </span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(race);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代码发起了20个线程，每个线程对race变量进行10000次更新，如果正确并发结果应该是200000。但是实际上每次的输出结果都不一样，都是一个小于200000的数。这是由于incerese（）方法实际上由4条字节码组成，当getstatic指令将操作数取到栈顶时，race的值上正确的，但是执行icont_1,iadd指令的时候其他线程可能已经改变了race的值，在执行putstatic的时候就不再正确了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">increase</span><span class="params">()</span></span>&#123;</span><br><span class="line">     Code:</span><br><span class="line">      Stack = <span class="number">2</span>,Local = <span class="number">0</span>,Args_size = <span class="number">0</span>;</span><br><span class="line">      <span class="number">0</span>: getstatic</span><br><span class="line">      <span class="number">3</span>:iconst_1</span><br><span class="line">      <span class="number">4</span>:add</span><br><span class="line">      <span class="number">5</span>:putstatic</span><br><span class="line">      <span class="number">8</span>:<span class="keyword">return</span>    </span><br><span class="line">      </span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>volatile非常适合用作某个操作完成、发生中断或者状态的标志,如下图，调用shutdown方法后，所有线程会停止工作：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">volatile</span> <span class="keyword">boolean</span> requested;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">shutdown</span><span class="params">()</span></span>&#123;</span><br><span class="line">    requested = <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> viod <span class="title">doWork</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">while</span>(!requested)&#123;</span><br><span class="line">        <span class="comment">// do something </span></span><br><span class="line">        </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>volatile的第二个作用是特性是禁止指令重排序，volatile变量赋值后，会多执行一个“lock add1 $0X0”操作，这个操作相当于一个内存屏障，指令重排时不能把后面的指令重排序到内存屏障之前的位置。还是拿出我们上面那个DCL单例的模式来，这次我们使用volatile变量后，就可以正确的实现单例模式：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">volatile</span> Singleton instance;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">synchronized</span>(Singleton<span class="class">.<span class="keyword">class</span>) </span>&#123;</span><br><span class="line">                    <span class="keyword">if</span> (instance == <span class="keyword">null</span>)</span><br><span class="line">                        <span class="comment">// 由于是volatile变量，此处不会进行重排序</span></span><br><span class="line">                        instance = <span class="keyword">new</span> Singleton();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> instance;</span><br><span class="line">            </span><br><span class="line">    &#125;</span><br><span class="line">      </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通过上述的情况我们大概了解了volatile的基本特性，也知道了volatile是一个轻量级的同步机制，在某些情况下，volatile的性能会优于锁的性能，但是经过虚拟机对锁的优化和消除后，并不能量化的认为volatile就比synchronized快多少，所以我们选择volatile的唯一依据应该是它能否满足我们的应用需求，总结一下，当且仅当以下条件满足时，应该使用volatile变量：</p>
<ul>
<li>对变量的写入不依赖变量的当前值，或者能确保只有单个线程更新变量的值；</li>
<li>改变量不会与其他状态变量一起纳入不变性条件中；</li>
<li>访问变量时不需要加锁</li>
</ul>
<h3 id="java与线程"><a class="header-anchor" href="#java与线程">¶</a>Java与线程</h3>
<h4 id="线程的实现"><a class="header-anchor" href="#线程的实现">¶</a>线程的实现</h4>
<p>实现线程主要有三种方式:使用内核线程实现，使用用户线程实现，使用用户线程加轻量级进程混合实现。</p>
<h5 id="1-使用内核线程实现"><a class="header-anchor" href="#1-使用内核线程实现">¶</a>1.使用内核线程实现</h5>
<p>内核线程（Kernel Thread,  KLT）就是直接由操作系统内核（Kernel,下称内核）支持的线程，这种线程由内核来完成线程切换，内核通过操纵调度器（Scheduler）对线程进行调度，并负责将线程的任务映射到各个处理器上。每个内核线程都可以看做是内核的一个分身，这样操作系统就有能力同时处理多件事情，支持多线程的内核就叫多线程内核(Multi-Threads Kernel)。程序一般不会直接去使用内核线程，而是去使用内核线程的种高级接口–轻量级进程(Light Weight Process,  LWP),轻量级进程就是我们通常意义上所讲的线程，由于每个轻量级进程都由一个内核线程支持，因此只有先支持内核线程，才能有轻量级进程。这种轻量级进程与内核线程之间1:1的关系称为一对一的线程模型。如图所示:</p>
<p><img src="http://120.79.172.214/mdpicture/1553339177594.png" alt="1553339177594"></p>
<p>由于内核线程的支持，每个轻量级进程都成为一个独立的调度单元，即使有一个轻量级进程在系统调用中阻塞了，也不会影响整个进程继续工作，但是轻量级进程具有它的局限性:首先，由于是基于内核线程实现的，所以各种进程操作，如创建、析构及同步，都需要进行系统调用。而系统调用的代价相对较高，需要在用户态(User Mode )和内核态(Kernel Mode)中来回切换其次，每个轻量级进程都需要有一个内核线程的支持，因此轻量级进程要消耗一定的内核资源(如内核线程的栈空间)，因此一个系统支持轻量级进程的数量是有限的。</p>
<h5 id="2-使用用户线程实现"><a class="header-anchor" href="#2-使用用户线程实现">¶</a>2.使用用户线程实现</h5>
<p>广义上来讲，一个线程只要不是内核线程，那就可以认为是用户线程（UserThread,  UT ) ,因此从这个定义上来讲轻最级进程也属于用户线程，但轻量级进程的实现始终是建立在内核之上的，许多操作都要进行系统调用，因此效率会受到限制。 而狭义上的用户线程指的是完全建众在用户空间的线程库上，系统内核不能感知到线程存在的实现。用户线程的建立、同步、销毁和调度完全在用户态中完成，不需要内核的帮助。如果程序实现得当，这种线程不需要切换到内核态，因此操作可以是非常快速且低消耗的，也可以支持规模更大的线程数量，部分高性能数据库中的多线程就是由用户线程实现的。这种进程与用户线程之间1:N的关系称为一对多的线程模型，如图所示。</p>
<p><img src="http://120.79.172.214/mdpicture/1553343294793.png" alt="img"></p>
<p>使用用户线程的优势在于不需要系统内核支援，劣势也在于没有系统内核的支援，所有的线程操作都需要用户程序自己处理，而线程的创建、切换和调度都是需要考虑问题，而且由于操作系统只把处理器资源分配到进程，那诸如“阻塞如何处理”、“多处理器系统中如何将线程映射到其他处理器上”这类问题解决起来将会异常困难，甚至不可能完成。因而使用用户线程实现的程序一般都比较复杂，除了以前在不支持多线程的操作系统中(如Dos)的多线程程序与少数有特殊需求的程序外，现在使用用户线程的程序越来越少了，Java, Ruby等语言都曾经使用过用户线程，最终又都放弃了<br>
使用它。</p>
<h5 id="3-混合实现"><a class="header-anchor" href="#3-混合实现">¶</a>3.混合实现</h5>
<p>线程除了依赖内核线程实现和完全由用户程序自己实现之外，还有一种将内核线程与用户线程一起使用的实现方式。在这种混合实现下，既存在用户线程，也存在轻量级进程。用户线程还是完全建立在用户空间中，因此用户线程的创建、切换、析构等操作依然廉价，并且可以支持大规模的用户线程并发。而操作系统提供支持的轻量级进程则作为用户线程和内核线程之间的桥梁，这样可以使用内核提供的线程调度功能及处理器映射。井且用户线程的系统调用要通过轻量级线程来完成，大大降低了进程被阻塞的风险。在这种混合模式中，用户线程与轻量级进程的数量比是不的，是M:N的关系，如图所示，这种就是多对多的线程模型。</p>
<p><img src="http://120.79.172.214/mdpicture/mnmodel.png" alt="img"></p>
<h5 id="4-java线程的实现"><a class="header-anchor" href="#4-java线程的实现">¶</a>4.Java线程的实现</h5>
<p>Java线程在JDK1.2之前，是基于名为“绿色线程&quot; (Green Threads)的用户线程实现的，而在JDK l .2中，线程模型被替换为基于操作系统原生线程模型来实现。因此在目前的JDIC版本中，操作系统支持怎样的线程模型，在很大程度上就决定了Java虚拟机的线程是怎样映射的，这点在不同的平台上没有办法达成一致，虚拟机规范中也并未限定Java线程需要使用哪种线程模型来实现。线程模型只对线程的并发规模和操作成本产生影响，对Java程序的编码和运行过程来说，这些差异都是透明的。对于Sun JDK来说，它的Windows版与Linux版都是使用一对一的线程模型来实现的，一条Java线程就映射到一条轻量级进程之中，因为Windows和Linux系统提供的线程模型就是一对一的。</p>
<h4 id="线程的调度"><a class="header-anchor" href="#线程的调度">¶</a>线程的调度</h4>
<p>线程调度是指系统为线程分配处理器使用权的过程，主要调度方式有两种。分别是协程式(Cooperative Threads-Scheduling)线程调度和抢占式(Preemptive Threads-Scheduling)线程调度。</p>
<h5 id="协程式线程调度"><a class="header-anchor" href="#协程式线程调度">¶</a>协程式线程调度</h5>
<p>如果使用协同式调度的多线程系统，线程的执行时间由线程本身来控制，线程把自己的工作执行完了之后，要主动通知系统切换到另外一个线程上去。协同式多线程的最大好处是实现简单，而且由于线程要把白己的事情干完后才会进行线程切换，切换操作对线程自己是可知的，所以没有什么线程同步的问题。它的坏处也很明显:线程执行时间不可控制，甚至如果一个线程编写有问题，一直不告知系统进行线程切换，那么程序就会一直阻塞在那里。很久以前的Windows 3.x系统就是使用协同式来实现多进程多任务的，那是相当的不稳定，一个进程坚持不让出CPU执行时间就会导致整个系统的崩溃。</p>
<h5 id="抢占式线程调度"><a class="header-anchor" href="#抢占式线程调度">¶</a>抢占式线程调度</h5>
<p>如果使用抢占式调度的多线程系统，那么每个线程将由系统来分配执行时间，线程的切换不由线程本身来决定(在Java中，Thread.yield()可以让出执行时间，但是要获取执行时间的话，线程本身是没有什么办法的)。在这种实现线程调度的方式下，线程的执行时间是系统可控的，也不会有一个线程导致整个进程阻塞的问题，Java使用的线程调度方式就是抢占式调度。</p>
<p>虽然说Java线程调度是系统自动完成的，但是我们还是可以“建议”系统给某些线程多分配一点执行时间，另外的此线程则可以少分配一点。这项操作可以通过设置线程优先级来完成。Java语言一共设置了10个级别的线程优先级，在两个线程同时处于Ready状态时，优先级越高的线程越容易被系统选择执行。但是这种方法并不靠谱，因为Java的线程上被映射到系统的原生线程上实现的，所以最终线程调度还是由操作系统说了算，而不同操作系统的线程优先级不能保证与Java中的线程优先级一 一对应，同时有些操作系统如“Windows”存在一个“线程推进器”，它的大致作用是发现线程运行的特别“勤奋”，会越过线程优先级为线程分配时间。</p>
<h4 id="线程状态"><a class="header-anchor" href="#线程状态">¶</a>线程状态</h4>
<p>Java中的线程一共有六种状态：</p>
<ul>
<li>NEW（初始化状态）</li>
<li>RUNNABLE（可运行 / 运行状态）</li>
<li>BLOCKED（阻塞状态）</li>
<li>WAITING（无时限等待）</li>
<li>TIMED_WAITING（有时限等待）</li>
<li>TERMINATED（终止状态）</li>
</ul>
<p>线程可以在这六种状态之间相互转换，如图所示：</p>
<p><img src="http://120.79.172.214/mdpicture/1553349267664.png" alt="1553349267664"></p>
<h5 id="从-new-到-runnable-状态"><a class="header-anchor" href="#从-new-到-runnable-状态">¶</a>从 NEW 到 RUNNABLE 状态</h5>
<ul>
<li>每个线程刚被创建的时候都是出于new状态，而要转换到RUNNABEL状态很简单，调用线程的start方法即可。</li>
</ul>
<h5 id="runnable-与-blocked-的状态转换"><a class="header-anchor" href="#runnable-与-blocked-的状态转换">¶</a>RUNNABLE 与 BLOCKED 的状态转换</h5>
<p>只有一种场景会触发这种转换:</p>
<ul>
<li>就是线程等待 synchronized 的隐式锁。synchronized 修饰的方法、代码块同一时刻只允许一个线程执行，其他线程只能等待，这种情况下，等待的线程就会从 RUNNABLE 转换到 BLOCKED 状态。而当等待的线程获得 synchronized 隐式锁时，就又会从 BLOCKED 转换到 RUNNABLE 状态。</li>
</ul>
<h5 id="runnable-与-waiting-的状态转换"><a class="header-anchor" href="#runnable-与-waiting-的状态转换">¶</a>RUNNABLE 与 WAITING 的状态转换</h5>
<p>总的来说，有三种场景会触发这种改变</p>
<ul>
<li>第一种场景，获得 synchronized 隐式锁的线程，调用无参数的 Object.wait() 方法。</li>
<li>第二种场景，调用无参数的 Thread.join() 方法。其中的 join() 是一种线程同步方法，例如有一个线程对象 thread A，当调用 A.join() 的时候，执行这条语句的线程会等待 thread A 执行完，而等待中的这个线程，其状态会从 RUNNABLE 转换到 WAITING。当线程 thread A 执行完，原来等待它的线程又会从 WAITING 状态转换到 RUNNABLE。</li>
<li>第三种场景，调用 LockSupport.park() 方法。其中的 LockSupport 对象，也许大家有点陌生，其实 Java 并发包中的锁，都是基于它实现的。调用 LockSupport.park() 方法，当前线程会阻塞，线程的状态会从 RUNNABLE 转换到 WAITING。调用 LockSupport.unpark(Thread thread) 可唤醒目标线程，目标线程的状态又会从 WAITING 状态转换到 RUNNABLE。</li>
</ul>
<h5 id="runnable-与-timed-waiting-的状态转换"><a class="header-anchor" href="#runnable-与-timed-waiting-的状态转换">¶</a>RUNNABLE 与 TIMED_WAITING 的状态转换</h5>
<p>有五种场景会触发这种转换：</p>
<ul>
<li>调用带超时参数的 Thread.sleep(long millis) 方法；</li>
<li>获得 synchronized 隐式锁的线程，调用带超时参数的 Object.wait(long timeout) 方法；</li>
<li>调用带超时参数的 Thread.join(long millis) 方法；</li>
<li>调用带超时参数的 LockSupport.parkNanos(Object blocker, long deadline) 方法；</li>
<li>调用带超时参数的 LockSupport.parkUntil(long deadline) 方法。</li>
</ul>
<p>这里你会发现 TIMED_WAITING 和 WAITING 状态的区别，仅仅是触发条件多了带超时参数.</p>
<h5 id="从-runnable-到-terminated-状态"><a class="header-anchor" href="#从-runnable-到-terminated-状态">¶</a>从 RUNNABLE 到 TERMINATED 状态</h5>
<ul>
<li>线程执行完 run() 方法后，会自动转换到 TERMINATED 状态，当然如果执行 run() 方法的时候异常抛出，也会导致线程终止。有时候我们需要强制中断 run() 方法的执行，例如 run() 方法访问一个很慢的网络，我们等不下去了，想终止怎么办呢？Java 的 Thread 类里面倒是有个 stop() 方法，不过已经标记为 @Deprecated，所以不建议使用了。正确的姿势其实是调用 interrupt() 方法。</li>
</ul>
<h2 id="线程安全"><a class="header-anchor" href="#线程安全">¶</a>线程安全</h2>
<h3 id="java语言的线程安全"><a class="header-anchor" href="#java语言的线程安全">¶</a>Java语言的线程安全</h3>
<p>什么是线程安全？《Java Concurrency In Practice》的作者Brian Goetz对“线程安全”有一个比较恰当的定义:</p>
<blockquote>
<p>当多个线程访问一个对象时，如果不用考虑这些线程在运行时环境下的调度和交替执行，也不需要进行额外的同步，或者在调用方进行任何其他的协调操作，调用这个对象的行为都可以获得正确的结果，那这个对象就是线程安全的。</p>
</blockquote>
<p>已经有了线程安全的一个抽象定义，那接下来我们就讨论一下在Java语言中，线程安全具体是如何体现的以及有哪些操作是线程安全的。为了更深入理解线程安全，我们可以不把线程安全当做一个非真既假的二元排他概念，按照线程安全的“安全程度”由强到弱，我们把Java语言中各种操作共享的数据分为以下五类:不可变、绝对线程安全、相对线程安全、线程兼容和线程对立。</p>
<h4 id="1-不可变"><a class="header-anchor" href="#1-不可变">¶</a>1.不可变</h4>
<p>在Java语言里不可变( Immutalaie)的对象一定是线程安全的，无论是对象的方法实现还是方法的调用者，都不需要再进行任何的线程安全保障措施，只要一个不可变的对象被正确地构建出来，那其外部的可见状态永远也不会改变，永远也不会看到它在多个线程之中处于不一致的状态。“不可变”带来的安全性是最简单最纯粹的。比如String类型就是一个经典的不可变对象，我们调用substring(),replace()等方法时都会返回一个新的字符串对象，而不会影响原来的值。保证对象行为不影响自己状态的途径有很多种，其中最简单的就是把对象中带有状态的变量都声明为final。这样在构造函数结束之后，它就是不可变的。</p>
<h4 id="2-绝对线程安全"><a class="header-anchor" href="#2-绝对线程安全">¶</a>2.绝对线程安全</h4>
<p>绝对的线程安全完全满足Brian Goetz给出的线程安全的定义其实是很严格的，一个类要达到“不管运行时环境如何，调用者都不需要任何额外的同步措施”通常需要付出很大的，甚至是不切实际的代价。在Java API中标注自己是线程安全的类，大多数都不是绝对的线程安全。我们可以通过Java API中一个不是“绝对线程安全”的线程安全类来看看这里的“绝对”是什么意思。比如Vector是一个大家公认的线程安全类，但是这并不意味这调用他的时候不在需要任何同步手段了，比如下面这个栗子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AbsoluteThreadSafeTest</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Vector&lt;Integer&gt; vector = <span class="keyword">new</span> Vector&lt;&gt;();</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>)&#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>;i&lt;<span class="number">10</span>;i++)&#123;</span><br><span class="line">                vector.add(i);</span><br><span class="line">            &#125;</span><br><span class="line">    </span><br><span class="line">            <span class="keyword">new</span> Thread(()-&gt;&#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;vector.size();i++)&#123;</span><br><span class="line">                    vector.remove(i);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">    </span><br><span class="line">            <span class="keyword">new</span> Thread(()-&gt;&#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;vector.size();i++)&#123;</span><br><span class="line">                    vector.get(i);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">    </span><br><span class="line">            <span class="comment">//避免创建过多线程</span></span><br><span class="line">            <span class="keyword">while</span> (Thread.activeCount()&gt;<span class="number">20</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虽然vector的get(),size()以及get()方法都是同步的，但是上面的代码仍然会抛出ArrayIndexOutOfBoundsException异常，我们仍然需要通过加锁的方式来保证这段代码的线程安全性。</p>
<h4 id="3-相对线程安全"><a class="header-anchor" href="#3-相对线程安全">¶</a>3.相对线程安全</h4>
<p>相对的线程安全就是我们通常意义上所讲的线程安全，它需要保证对这个对象单独的操作是线程安全的，我们在调用的时候不需要做额外的保障措施，但是对于一些特定顺序的连续调用，就可能需要在调用端使用额外的同步手段来保证调用的正确性。上面的栗子就是相对线程安全的一个很明显的案例。在Java语言中，大部分的线程安全类都属于这种类型，例如Vector,  HashTable,<br>
Collections的synchronicedCollectiont()方法包装的集合等。</p>
<h4 id="4-线程兼容"><a class="header-anchor" href="#4-线程兼容">¶</a>4.线程兼容</h4>
<p>线程兼容是指对象本身并不是线程安全的，但是可以通过在调用端正确地使用同步手段来保证对象在并发环境中安全地使用，我们平常说一个类不是线程安全的，绝大多数指的都是这种情况。Java API中大部分的类都是线程兼容的，如与前面的Vector和HashTable相对应的集合类ArrayList和HashMap等。</p>
<h4 id="5-线程对立"><a class="header-anchor" href="#5-线程对立">¶</a>5.线程对立</h4>
<p>线程对立是指无论使用何种同步手段，代码都无法再多线程情况下并发使用的代码。由于Java语言的代码天生具有多线程性，所以这种情况极少出现。而且线程对立的代码是有害的，应该避免。</p>
<p>一个线程对立的例子是Thread类的suspend()和resume()方法，如果有两个线程同时持有一个线程对象，一个尝试去中断线程，一个尝试去恢复线程，如果并发进行的话，无论调用时是否进行了同步，目标线程都是存在死锁风险的，如果suspend()中断的线程就是即将要执行resume()的那个线程，那就肯定要产生死锁了。也正是由于这个原因，这两个方法已经被JDK声明废弃(@Deprecated )了。常见的线程对立的操作还有System.setIn(). Sytem.setout()和System. runFinalizersOnExit()等。</p>
<h3 id="实现线程安全的方法"><a class="header-anchor" href="#实现线程安全的方法">¶</a>实现线程安全的方法</h3>
<p>在了解了什么是线程安全后，我们接下来了解一些实现线程安全的方法。</p>
<h4 id="1-互斥同步"><a class="header-anchor" href="#1-互斥同步">¶</a>1.互斥同步</h4>
<p>在Java里面，最基本的互斥同步手段就是synchronized关键字，synchronized关键字经过编译之后，会在同步块的前后分别形成monitorenter和monitorexit这两个字节码指令，这两个字节码都需要一个reference类型的参数来指明要锁定和解锁的对象。如果Java程序中的synchronized明确指定了对象参数，那就是这个对象的reference，如果没有明确指定，那就根据synchronised修饰的是实例方法还是类方法，去取对应的对象实例或Class对象来作为锁对象。</p>
<p>根据Java虚拟机的规范，在执行monitorenter指令时，首先会去尝试获取对象的锁。如果这个对象没被锁定，或者当前程序已经获取了该对象的锁，则锁的计数器加1。相应的，在执行monitorexit指令时，如果计数器减1，当计数器为0，则代表锁被释放。</p>
<p>在虚拟机规范对monitorenter和monitnrexit的行为描述中，有两点是需要特别注意的：</p>
<ul>
<li>首先，synchronized同步块对同一条线程来说是可重入的，不会出现自己把自己锁死的问题。</li>
<li>其次，同步块在已进入的线程执行完之前。会阻塞后面其他线程的进入。我们讨论过，Java的线程是映射到操作系统的原生线程之上的，如果要阻塞或唤醒一条线程。都需要操作系统来帮忙完成，这就需要从用户态转换到核心态中，因此状态转换需要耗费很多的处理器时间。对于代码简单的同步块(如被synchronized修饰的gctter()或setter()方法)，状态转换消耗的时间可能比用户代码执行的时间还要长。所以synchronized语言中一个重量级(Heavyweight)的操作，有经验的程序员都会在确实必要的情况下才使用这种操作。而虚拟机本身也会进行一些优化，譬如在通知操作系统阻塞线程之前加人一段自旋等待过程，避免频繁地切人到核心态之中。</li>
</ul>
<h4 id="2-非阻塞同步"><a class="header-anchor" href="#2-非阻塞同步">¶</a>2.非阻塞同步</h4>
<p>互斥同步最主要的问题就是进行线程阻塞和唤醒所带来的性能问题，因此这种同步也被称为阻塞同步（Blocking Synchronization）,另外，它属于一种悲观的并发策略。总是认为只要不去做正确的同步措施（加锁)，那就肯定会出现问题，无论共享数据是否真的会出现竞争，它都要进行加锁(这里说的是概念模型。实际上虚拟机会优化掉很大一部分不必要的加锁)、用户态核心态转换、维护锁计数器和检查是否有被阻塞的线程需要被唤醒等操作。随着硬件指令集的发展，我们有了另外一个选择。基于冲突检测的乐观并发策略，通俗地说就是先进行操作，如果没有其他线程争用共享数据，那操作就成功了;如果共享数据有争用，产生了冲突，那就再进行共他的补偿措施(最常见的补偿措施就是不断地重试，直到试成功为止〕，这种乐观的并发策略的许多实现都不需要把线程挂起，因此这种同步操作被称为非阻塞同步(Non-Blocking Synchronization）。</p>
<p>能够帮助我们实现非阻塞同步的指令包括：</p>
<ul>
<li>测试并设置（Test-And-Set）</li>
<li>获取并增加（Fetch-and-Increament）</li>
<li>交换（Swap)</li>
<li>比较并交换（Compare-and-Swap,CAS）</li>
<li>加载链接/条件存储（Load-Linked/Store-Conditional,LL/SC）</li>
</ul>
<p>接下来我们重点讨论一下CAS指令。CAS指令需要有三个操作数，分别是内存位置(在Java中可以简单理解为变量的内存地址，用V表示)、旧的预期值(用A表示)和新值(用B表示)。CAS指令执<br>
行时，当且仅当V符合旧预期值A时，处理器用新值S更新V的值，否则它就不执行更新，但是不管是否更新了V的值，都会返回V的旧值，上述的处理过程是一个原子操作。</p>
<p>我们可以通过Java API来间接使用Unsafe类中提供的CAS操作，比如AtomicInteger类中的incrementAndGet()，getAndAdd()等方法，我们可以用volatile无法保证原子操作的示例来演示一下CAS用来避免阻塞同步：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AtomicTest</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> AtomicInteger race = <span class="keyword">new</span> AtomicInteger(<span class="number">0</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">increase</span><span class="params">()</span></span>&#123;</span><br><span class="line">        race.incrementAndGet();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> THREAD_COUNT = <span class="number">20</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>;i&lt;THREAD_COUNT;i++)&#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(</span><br><span class="line">                    () -&gt; &#123;</span><br><span class="line">                        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>;j&lt;<span class="number">10000</span>;j++)&#123;</span><br><span class="line">                            increase();</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">            ).start();</span><br><span class="line">    </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (Thread.activeCount()&gt;<span class="number">1</span>)&#123;</span><br><span class="line">            Thread.yield();</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">        System.out.println(race);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="3-无同步手段"><a class="header-anchor" href="#3-无同步手段">¶</a>3.无同步手段</h4>
<p>要保证线程安全不一定需要同步，在Java中有些代码天生就是线程安全的，我们简单的来讨论一下常见的两类。</p>
<h5 id="可重入性的代码"><a class="header-anchor" href="#可重入性的代码">¶</a>可重入性的代码</h5>
<p>这种代码也叫纯代码( Pure Code ），可以在代码执行的任何时刻中断它，转而去执行另外一段代码(包括递归调用它本身)，而在控制权返回后，原来的程序不会出现任何错误。相对线程安全来说，可重入性是更基本的特性，它可以保证线程安全，即所有的可重人的代码都是线程安全的，但是并非所有的线程安全的代码都是可重入的。</p>
<p>可重入代码有一些共同的特征:例如不依赖存储在堆上的数据和公用的系统资源、用到的状态量都由参数中传入、不调用非可重人的方法等。我们可以通过一个简单的原则来判断代码是否具备可重入性:如果一个方法，它的返回结果是可以预测的，只要输人了相同的数据，就都能返回相同的结果，那它就满足可重入性的要求，当然也就是线程安全的。</p>
<h5 id="线程封闭"><a class="header-anchor" href="#线程封闭">¶</a>线程封闭</h5>
<p>当访问共享的可变数据是，如果能限制在仅在单线程内访问数据，就不需要同步。这种技术被称为线程封闭。Java元语言没有强制规定某个变量必须由锁保护，同样也无法强制将对象封闭在某个线程中。Java语言及其核心库提供了一些机制帮助维持线程封闭性，比如局部变量和ThreadLocal类。</p>
<h6 id="栈封闭"><a class="header-anchor" href="#栈封闭">¶</a>栈封闭</h6>
<p>栈封闭上线程封闭的一种特例，在栈封闭中，只能通过局部变量才能访问对象。局部变量的固有属性之一就是封闭在执行线程中，它们位于执行线程的栈中，其他线程无法访问。</p>
<p>对于基本类型的局部变量，例如下面代码中loadTheArk方法中的numPairs，由于任何方法都不能获得对基本类型的引用，因此Java语言的这种语义保证了基本类型的局部变量始终封闭在线程内。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">loadTheArk</span><span class="params">(Collection&lt;Animal&gt; candidates)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">        SortedSet&lt;Animal&gt; animals;</span><br><span class="line">    </span><br><span class="line">        <span class="keyword">int</span> numPairs = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">        Animal candidate = <span class="keyword">null</span>;</span><br><span class="line">    </span><br><span class="line">        animals = <span class="keyword">new</span> TreeSet&lt;Animal&gt;();</span><br><span class="line">        animals.addAll(candidates);</span><br><span class="line">        <span class="keyword">for</span> (Animal a: animals)&#123;</span><br><span class="line">            <span class="comment">//do something</span></span><br><span class="line">            numPairs++;</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">        <span class="keyword">return</span>  numPairs;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>而在维持对象引用的栈封闭性时，我们需要使用一些手段保证被引用的对象不会逸出。比如我们在上面代码中实例化了一TreeSet对象，并将指向它的一个引用保存在animals中。此时，只有一个引用指向animals，这个引用被封闭在局部变量中，因此也封闭在执行线程中。然而，如果我们发布了animals的引用或者改对象中的任何数据的引用，这都会导致封闭性被破坏。</p>
<h6 id="threadlocal类"><a class="header-anchor" href="#threadlocal类">¶</a>ThreadLocal类</h6>
<p>Java语言中，如果一个变量要被多线程访问，可以使用volatile关键字声明它为“易变的”:如果一个变量要被某个线程独享，就可以通过ThreadLocal类来实现线程本地存储的功能。每一个线程的Thread对象中都有一个ThreadLocalMap对象，这个对象存储了一组以ThreadLocal.threadLocalHashCode为键，以本地线程变量为值的K-V值对，ThreadLocal对象就是当前线程ThreadLocalMap的访问入口，每个ThreadLocal对象都包含一个独一无二的threadLocalHashCode值，使用这个值就可以在线程K-V值对中找回对应的本地线程变量。需要注意的是，ThreadLocal变量类似于全局变量，会降低代码的可重入性，因此使用时要格外小心。</p>
<h2 id="参考资料"><a class="header-anchor" href="#参考资料">¶</a>参考资料</h2>
<ul>
<li>《深入理解Java虚拟机》第四版</li>
<li>《Java并发编程实战》</li>
<li>极客时间《Java并发编程实战》</li>
</ul>
]]></content>
      <categories>
        <category>java多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java多线程</tag>
        <tag>线程安全</tag>
      </tags>
  </entry>
  <entry>
    <title>性能优化-连接池排队问题</title>
    <url>/performance/connection-pool-queue.html</url>
    <content><![CDATA[<h1 id="性能优化-连接池排队问题"><a class="header-anchor" href="#性能优化-连接池排队问题">¶</a>性能优化-连接池排队问题</h1>
<p>数据库连接池中还有很多空闲连接，为什么应用的数据库操作都在排队等待<strong>获取</strong>和<strong>归还</strong>连接？</p>
<h2 id="背景"><a class="header-anchor" href="#背景">¶</a>背景</h2>
<p>生产环境系统上线前进行压测，应用共4个节点，每个节点4核8G，每个节点连接池大小1000，200业务并发（非单接口绝对并发）；从监控发现Druid连接池获取连接和释放连接都需要300ms左右的时间，这对于整个系统的吞吐量影响特别大。</p>
<blockquote>
<p>某次查询数据库操作中方法调用堆栈耗时监控：<br>
com.alibaba.druid.pool.DruidDataSource.getConnection()：耗时327ms<br>
com.oceanbase.jdbc.JDBC4PreparedStatement.execute()：耗时3ms<br>
com.alibaba.druid.pool.DruidPooledConnection.close()：耗时294ms</p>
</blockquote>
<h2 id="问题排查过程"><a class="header-anchor" href="#问题排查过程">¶</a>问题排查过程</h2>
<h3 id="线程堆栈分析"><a class="header-anchor" href="#线程堆栈分析">¶</a>线程堆栈分析</h3>
<p>dump应用的线程信息，发现有217个线程在等待获取数据库连接，有156个线程在等待释放数据库连接，关键线程信息如下所示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">217个线程在等待获取数据库连接</span><br><span class="line">&quot;HSFBizProcessor-DEFAULT-8-thread-446&quot; #1623 daemon prio=10 os_prio=0 tid=0x00007f436c8dc000 nid=0x341154 waiting on condition [0x00007f4310d17000]</span><br><span class="line">   java.lang.Thread.State: WAITING (parking)</span><br><span class="line">	at sun.misc.Unsafe.park(Native Method)</span><br><span class="line">	- parking to wait for  &lt;0x000000055d50cf68&gt; (a java.util.concurrent.locks.ReentrantLock$FairSync)</span><br><span class="line">	at java.util.concurrent.locks.LockSupport.park(LockSupport.java:175)</span><br><span class="line">	at java.util.concurrent.locks.AbstractQueuedSynchronizer.parkAndCheckInterrupt(AbstractQueuedSynchronizer.java:836)</span><br><span class="line">	at java.util.concurrent.locks.AbstractQueuedSynchronizer.doAcquireInterruptibly(AbstractQueuedSynchronizer.java:897)</span><br><span class="line">	at java.util.concurrent.locks.AbstractQueuedSynchronizer.acquireInterruptibly(AbstractQueuedSynchronizer.java:1222)</span><br><span class="line">	at java.util.concurrent.locks.ReentrantLock.lockInterruptibly(ReentrantLock.java:335)</span><br><span class="line">	at com.alibaba.druid.pool.DruidDataSource.getConnectionInternal(DruidDataSource.java:1632)</span><br><span class="line">	...</span><br><span class="line"></span><br><span class="line">	</span><br><span class="line">156个线程在等待释放数据库连接</span><br><span class="line">&quot;HSFBizProcessor-DEFAULT-8-thread-443&quot; #1620 daemon prio=10 os_prio=0 tid=0x00007f436404e000 nid=0x341151 waiting on condition [0x00007f4310e9b000]</span><br><span class="line">   java.lang.Thread.State: WAITING (parking)</span><br><span class="line">	at sun.misc.Unsafe.park(Native Method)</span><br><span class="line">	- parking to wait for  &lt;0x000000055d50cf68&gt; (a java.util.concurrent.locks.ReentrantLock$FairSync)</span><br><span class="line">	at java.util.concurrent.locks.LockSupport.park(LockSupport.java:175)</span><br><span class="line">	at java.util.concurrent.locks.AbstractQueuedSynchronizer.parkAndCheckInterrupt(AbstractQueuedSynchronizer.java:836)</span><br><span class="line">	at java.util.concurrent.locks.AbstractQueuedSynchronizer.acquireQueued(AbstractQueuedSynchronizer.java:870)</span><br><span class="line">	at java.util.concurrent.locks.AbstractQueuedSynchronizer.acquire(AbstractQueuedSynchronizer.java:1199)</span><br><span class="line">	at java.util.concurrent.locks.ReentrantLock$FairSync.lock(ReentrantLock.java:224)</span><br><span class="line">	at java.util.concurrent.locks.ReentrantLock.lock(ReentrantLock.java:285)</span><br><span class="line">	at com.alibaba.druid.pool.DruidDataSource.recycle(DruidDataSource.java:2016)</span><br><span class="line">	...</span><br></pre></td></tr></table></figure>
<h3 id="怀疑数据库连接泄露"><a class="header-anchor" href="#怀疑数据库连接泄露">¶</a>怀疑数据库连接泄露</h3>
<p>数据库连接泄露的问题主要有两类：</p>
<ol>
<li>手动获取数据库连接，未释放。</li>
<li>手动开始事务，未结束（提交或回滚）。</li>
</ol>
<p>全局搜索项目源码，未发现以上两种情况。根据压测结束后堆dump信息（没有开连接池信息打印），从堆信息中发现DruidDataSource对象的<strong>池中连接数poolingCount=1000</strong>，也就是说所有的连接已全部归还到连接池中。</p>
<p>因此数据库连接泄露的嫌疑被排除。</p>
<h3 id="怀疑有慢sql或者有大事务长时间占用连接"><a class="header-anchor" href="#怀疑有慢sql或者有大事务长时间占用连接">¶</a>怀疑有慢sql或者有大事务长时间占用连接</h3>
<p>根据线程信息分析，正在执行数据库操作<code>com.oceanbase.jdbc.JDBC4PreparedStatement.execute</code>的线程仅仅只有一个。因此有大量慢sql，导致数据库连接池耗尽的嫌疑被排除。</p>
<p>从线程信息中不能直接发现是否有大事务长时间占用连接，根据源码分析又仿佛大海捞针，这里再看看堆dump信息，发现DruidDataSource对象的<strong>活跃连接数峰值activePeak=303</strong>，也就是说连接池中始终都有空闲可用的连接。因此有大事务长时间占用连接，导致数据库连接池耗尽的嫌疑被排除。</p>
<h3 id="怀疑连接池本身的性能问题"><a class="header-anchor" href="#怀疑连接池本身的性能问题">¶</a>怀疑连接池本身的性能问题</h3>
<p>从线程信息中发现，所有的获取和释放连接的线程都在等待同一把锁（公平锁）：<code>- parking to wait for &lt;0x000000055d50cf68&gt; (a java.util.concurrent.locks.ReentrantLock$FairSync)</code>，对应DruidDataSource的关键源码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> DruidPooledConnection <span class="title">getConnectionInternal</span><span class="params">(<span class="keyword">long</span> maxWait)</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    </span><br><span class="line">    DruidConnectionHolder holder;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">boolean</span> createDirect = <span class="keyword">false</span>;;) &#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            lock.lockInterruptibly();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            connectErrorCountUpdater.incrementAndGet(<span class="keyword">this</span>);</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> SQLException(<span class="string">"interrupt"</span>, e);</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            ...</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">if</span> (maxWait &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// 配置了连接获取超时时间</span></span><br><span class="line">                holder = pollLast(nanos);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 未配置连接获取超时时间</span></span><br><span class="line">                holder = takeLast();</span><br><span class="line">            &#125;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line">        holder.incrementUseCount();</span><br><span class="line"></span><br><span class="line">        DruidPooledConnection poolalbeConnection = <span class="keyword">new</span> DruidPooledConnection(holder);</span><br><span class="line">        <span class="keyword">return</span> poolalbeConnection;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 回收连接</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">recycle</span><span class="params">(DruidPooledConnection pooledConnection)</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// 获取连接池的锁，归还holder，将其放入连接池中</span></span><br><span class="line">    lock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (holder.active) &#123;</span><br><span class="line">            activeCount--;</span><br><span class="line">            holder.active = <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        closeCount++;</span><br><span class="line"></span><br><span class="line">        result = putLast(holder, currentTimeMillis);</span><br><span class="line">        recycleCount++;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>查阅Druid的文档，发现有介绍Druid锁的公平模式问题：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2FsaWJhYmEvZHJ1aWQvd2lraS9EcnVpZCVFOSU5NCU4MSVFNyU5QSU4NCVFNSU4NSVBQyVFNSVCOSVCMyVFNiVBOCVBMSVFNSVCQyU4RiVFOSU5NyVBRSVFOSVBMiU5OA==" title="https://github.com/alibaba/druid/wiki/Druid%E9%94%81%E7%9A%84%E5%85%AC%E5%B9%B3%E6%A8%A1%E5%BC%8F%E9%97%AE%E9%A2%98">https://github.com/alibaba/druid/wiki/Druid锁的公平模式问题<i class="fa fa-external-link"></i></span></p>
<table>
<thead>
<tr>
<th>版本</th>
<th>处理方式</th>
<th>效果</th>
</tr>
</thead>
<tbody>
<tr>
<td>0.2.3之前</td>
<td>unfair</td>
<td>并发性能很好。<br><br>maxWait&gt;0的配置下，出现严重不公平现象</td>
</tr>
<tr>
<td>0.2.3 ~ 0.2.6</td>
<td>fair</td>
<td>公平，但是并发性能很差</td>
</tr>
<tr>
<td>0.2.7</td>
<td>通过构造函数传入参数指定fair或者unfair，缺省fair</td>
<td>按需要配置，但是比较麻烦</td>
</tr>
<tr>
<td>0.2.8</td>
<td>缺省unfair，通过构造函数传入参数指定fair或者unfair；<br><br>如果DruidDataSource还没有初始化，修改maxWait大于0，自动转换为fair模式</td>
<td>智能配置，能够兼顾性能和公平性</td>
</tr>
</tbody>
</table>
<p>应用确实配置了maxWait参数，从线程信息中看也确实是使用的公平锁<code>ReentrantLock$FairSync</code>，在高并发下性能表现很差。</p>
<p>到此为止，基本可以确定<font color="red"><strong>公平锁</strong>的并发性能差导致连接池排队等待<strong>获取</strong>和<strong>归还</strong>连接问题</font>，下面让我们来验证下公平锁和非公平锁对性能的影响到底有多大。</p>
<h2 id="锁的公平模式性能验证"><a class="header-anchor" href="#锁的公平模式性能验证">¶</a>锁的公平模式性能验证</h2>
<ul>
<li>测试接口每次请求中并发查询30次简单sql，具体代码如下：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">selectTest</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Promise&lt;?&gt;&gt; promises = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">30</span>; i++) &#123;</span><br><span class="line">        Promise&lt;String&gt; promise = CompletableHelper.promise(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">return</span> (String) SqlRunner.db()</span><br><span class="line">                                     .selectObj(<span class="string">"select '1' from dual"</span>);</span><br><span class="line">        &#125;);</span><br><span class="line">        promises.add(promise);</span><br><span class="line">    &#125;</span><br><span class="line">    List&lt;?&gt; results = CompletableHelper.waitAll(promises);</span><br><span class="line">    <span class="keyword">return</span> StringUtils.join(results, <span class="string">","</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>
<p>应用配置：</p>
<ul>
<li>单节点4核16G</li>
<li>JVM内存：-Xms5120m -Xmx5120m -Xmn1706m</li>
</ul>
</li>
<li>
<p>压测、监控工具</p>
<ul>
<li>JMeter</li>
<li>javaagent：<span class="exturl" data-url="aHR0cHM6Ly9tcC53ZWl4aW4ucXEuY29tL3MvVlFUc3Z0V29jUXg3dmVPM3NhTlVxQQ==" title="https://mp.weixin.qq.com/s/VQTsvtWocQx7veO3saNUqA">性能优化利器-JavaAgent<i class="fa fa-external-link"></i></span></li>
</ul>
</li>
<li>
<p>压测结果：</p>
</li>
</ul>
<table>
<thead>
<tr>
<th>锁模式</th>
<th>数据库连接池大小</th>
<th>并发数</th>
<th>获取连接：getConnectionInternal<br><br>平均耗时ms</th>
<th>归还连接：recycle<br><br>平均耗时ms</th>
<th>连接池最大活跃数：activePeak</th>
<th>接口请求样本数</th>
<th>接口请求响应平均ms</th>
<th>接口请求响应90%百分位ms</th>
<th>接口请求响应最小值ms</th>
<th>接口请求响应最大值ms</th>
<th>接口请求吞吐量</th>
</tr>
</thead>
<tbody>
<tr>
<td>非公平锁</td>
<td>200</td>
<td>100</td>
<td>5</td>
<td>1</td>
<td>200</td>
<td>631733</td>
<td>534</td>
<td>972</td>
<td>7</td>
<td>5689</td>
<td>187.2/sec</td>
</tr>
<tr>
<td>公平锁</td>
<td>200</td>
<td>100</td>
<td>107</td>
<td>62</td>
<td>200</td>
<td>292050</td>
<td>959</td>
<td>1324</td>
<td>354</td>
<td>3533</td>
<td>104.1/sec</td>
</tr>
<tr>
<td>非公平锁</td>
<td>500</td>
<td>100</td>
<td>1.8</td>
<td>2.2</td>
<td>500</td>
<td>220793</td>
<td>525</td>
<td>950</td>
<td>7</td>
<td>2976</td>
<td>189.0/sec</td>
</tr>
<tr>
<td>公平锁</td>
<td>500</td>
<td>100</td>
<td>71</td>
<td>70</td>
<td>500</td>
<td>152745</td>
<td>785</td>
<td>1207</td>
<td>347</td>
<td>5407</td>
<td>127.2/sec</td>
</tr>
<tr>
<td>非公平锁</td>
<td>1000</td>
<td>100</td>
<td>1</td>
<td>1.5</td>
<td>510</td>
<td>232726</td>
<td>520</td>
<td>947</td>
<td>8</td>
<td>2547</td>
<td>193.9/sec</td>
</tr>
<tr>
<td>公平锁</td>
<td>1000</td>
<td>100</td>
<td>83</td>
<td>82</td>
<td>557</td>
<td>134607</td>
<td>891</td>
<td>1367</td>
<td>68</td>
<td>5440</td>
<td>112.1/sec</td>
</tr>
</tbody>
</table>
<ul>
<li>压测结论：非公平锁模式下获取和归还连接的性能遥遥领先公平锁模式。</li>
</ul>
<h2 id="解决方案"><a class="header-anchor" href="#解决方案">¶</a>解决方案</h2>
<p>数据库连接池推荐配置（连接池大小需根据实际情况调整）</p>
<ul>
<li>initialSize：500</li>
<li>minIdle：500</li>
<li>maxActive：500</li>
<li>maxWait：6000</li>
<li>keepAlive：true</li>
<li>在连接池初始化之前，手动设置：dataSouce.setUseUnfairLock(true)</li>
</ul>
<blockquote>
<p>更多推荐配置见：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2FsaWJhYmEvZHJ1aWQvd2lraS9EcnVpZERhdGFTb3VyY2UlRTklODUlOEQlRTclQkQlQUU=" title="https://github.com/alibaba/druid/wiki/DruidDataSource%E9%85%8D%E7%BD%AE">https://github.com/alibaba/druid/wiki/DruidDataSource配置<i class="fa fa-external-link"></i></span></p>
</blockquote>
<p>修改Druid连接池配置后，更新应用到生产环境复测（200并发），javaagent监控druid相关方法耗时从几百ms降低到不足个位数，事务操作耗时从平均10s降低到平均2s，至此数据库操作都在排队等待获取和归还连接问题得以解决。</p>
<p>尽管极端情况下，在连接池中的连接不够用大量线程争用连接时，unfair模式的ReentrantLock.tryLock方法存在严重不公的现象，个别线程会等到超时了还获取不到连接。</p>
<p>个人观点：数据库连接池的锁调整为非公平锁<font color="red"><strong>整体来看利远大于弊</strong></font>，<strong>如果真的有这么大的并发量，更应该增加应用节点数量，缓解单节点的压力。</strong></p>
<h2 id="参考文档"><a class="header-anchor" href="#参考文档">¶</a>参考文档</h2>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9tcC53ZWl4aW4ucXEuY29tL3MvVlFUc3Z0V29jUXg3dmVPM3NhTlVxQQ==" title="https://mp.weixin.qq.com/s/VQTsvtWocQx7veO3saNUqA">性能优化利器-JavaAgent<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2FsaWJhYmEvZHJ1aWQvd2lraS9EcnVpZERhdGFTb3VyY2UlRTklODUlOEQlRTclQkQlQUU=" title="https://github.com/alibaba/druid/wiki/DruidDataSource%E9%85%8D%E7%BD%AE">https://github.com/alibaba/druid/wiki/DruidDataSource配置<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2FsaWJhYmEvZHJ1aWQvd2lraS9EcnVpZCVFOSU5NCU4MSVFNyU5QSU4NCVFNSU4NSVBQyVFNSVCOSVCMyVFNiVBOCVBMSVFNSVCQyU4RiVFOSU5NyVBRSVFOSVBMiU5OA==" title="https://github.com/alibaba/druid/wiki/Druid%E9%94%81%E7%9A%84%E5%85%AC%E5%B9%B3%E6%A8%A1%E5%BC%8F%E9%97%AE%E9%A2%98">https://github.com/alibaba/druid/wiki/Druid锁的公平模式问题<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9qbWV0ZXIuYXBhY2hlLm9yZy91c2VybWFudWFsL2NvbXBvbmVudF9yZWZlcmVuY2UuaHRtbCNBZ2dyZWdhdGVfUmVwb3J0" title="https://jmeter.apache.org/usermanual/component_reference.html#Aggregate_Report">https://jmeter.apache.org/usermanual/component_reference.html#Aggregate_Report<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>性能优化</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>连接池</tag>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>多线程编程与锁优化</title>
    <url>/mutlithreading/thread-lock.html</url>
    <content><![CDATA[<p>多线程是在同一个程序内部并行执行，因此会对相同的内存空间进行并发读写操作。如果一个线程在读一个内存时，另一个线程正向该内存进行写操作，那进行读操作的那个线程将获得什么结果呢？是写操作之前旧的值？还是写操作成功之后的新值？或是一半新一半旧的值？或者，如果是两个线程同时写同一个内存，在操作完成后将会是什么结果呢？是第一个线程写入的值？还是第二个线程写入的值？还是两个线程写入的一个混合值？从下面的图我们可以窥知一二。</p>
<p><img src="https://s2.ax1x.com/2019/04/14/AO9UHK.png" alt="Java memory model"></p>
<a id="more"></a>
<h1 id="多线程基础知识"><a class="header-anchor" href="#多线程基础知识">¶</a>多线程基础知识</h1>
<h2 id="竞态条件与临界区"><a class="header-anchor" href="#竞态条件与临界区">¶</a>竞态条件与临界区</h2>
<p>在同一程序中运行多个线程本身不会导致问题，问题在于多个线程访问了相同的资源。在一或多个线程向这些资源做了写操作时才有可能发生，只要资源没有发生变化,多个线程读取相同的资源就是安全的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Counter</span> </span>&#123;</span><br><span class="line">	<span class="keyword">protected</span> <span class="keyword">long</span> count = <span class="number">0</span>;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(<span class="keyword">long</span> value)</span></span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.count = <span class="keyword">this</span>.count + value;  </span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>JVM并不是将这段代码：<code>this.count = this.count + value;</code>，视为单条指令来执行的，而是按照下面的顺序：</p>
<blockquote>
<p>从内存获取 this.count 的值放到寄存器</p>
<p>将寄存器中的值增加value</p>
<p>将寄存器中的值写回内存</p>
</blockquote>
<p>当线程A和线程B同时执行add方法时，他们的实际执行顺序可能如下：</p>
<blockquote>
<p>this.count = 0;</p>
<p>A:读取 this.count 到一个寄存器 (0)</p>
<p>B:读取 this.count 到一个寄存器 (0)</p>
<p>B:将寄存器的值加2</p>
<p>B:回写寄存器值(2)到内存. this.count 现在等于 2</p>
<p>A:将寄存器的值加3</p>
<p>A:回写寄存器值(3)到内存. this.count 现在等于 3</p>
</blockquote>
<p>当两个线程竞争同一资源时，如果对资源的访问顺序敏感，就称存在竞态条件。导致竞态条件发生的代码区称作临界区。上例中add()方法就是一个临界区,它会产生竞态条件。在临界区中使用适当的同步就可以避免竞态条件。</p>
<h2 id="共享资源"><a class="header-anchor" href="#共享资源">¶</a>共享资源</h2>
<p>允许被多个线程同时执行的代码称作线程安全的代码，线程安全的代码不包含竞态条件，当多个线程同时更新共享资源时会引发竞态条件。</p>
<h3 id="局部变量"><a class="header-anchor" href="#局部变量">¶</a>局部变量</h3>
<p>局部变量存储在线程自己的栈中。也就是说，局部变量永远也不会被多个线程共享。所以，基础类型的局部变量是线程安全的。下面是基础类型的局部变量的一个例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 方法内的变量是线程安全的</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> username</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">addI</span><span class="params">(String username)</span></span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">int</span> num;</span><br><span class="line">        <span class="keyword">if</span>(<span class="string">"a"</span>.equals(username))&#123;</span><br><span class="line">            num = <span class="number">100</span>;</span><br><span class="line">            System.out.println(<span class="string">"a set over!"</span>);</span><br><span class="line">            Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            num = <span class="number">200</span>;</span><br><span class="line">            System.out.println(<span class="string">"b set over!"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(username + <span class="string">" num = "</span> + num);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">threadSetPrivateNumTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    Thread threadA = <span class="keyword">new</span> Thread(()-&gt;addI(<span class="string">"a"</span>));</span><br><span class="line">    Thread threadB = <span class="keyword">new</span> Thread(()-&gt;addI(<span class="string">"b"</span>));</span><br><span class="line">    threadA.start();</span><br><span class="line">    threadB.start();</span><br><span class="line">    Thread.sleep(<span class="number">3000</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="局部的对象引用"><a class="header-anchor" href="#局部的对象引用">¶</a>局部的对象引用</h3>
<p>对象的局部引用和基础类型的局部变量不太一样。尽管引用本身没有被共享，但引用所指的对象并没有存储在线程的栈内。所有的对象都存在共享堆中。如果在某个方法中创建的对象不会逃逸出（译者注：即该对象不会被其它方法获得，也不会被非局部变量引用到）该方法，那么它就是线程安全的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadSafeTest</span> </span>&#123;</span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">int</span> instanceNum;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">addInstanceNum</span><span class="params">(String username)</span></span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span>(<span class="string">"b"</span>.equals(username))&#123;</span><br><span class="line">                instanceNum = <span class="number">200</span>;</span><br><span class="line">                System.out.println(<span class="string">"b set over!"</span>);</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                instanceNum = <span class="number">100</span>;</span><br><span class="line">                System.out.println(<span class="string">"a set over!"</span>);</span><br><span class="line">                Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(username + <span class="string">" num = "</span> + instanceNum);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">	<span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">localObjectTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        addInstanceNum(<span class="string">"a"</span>);</span><br><span class="line">        addInstanceNum(<span class="string">"b"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面的示例中，localObject没有传递到其它线程中，那么它就是线程安全的，如果传到其它线程了呢？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">localObjectTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">       ThreadSafeTest test = <span class="keyword">new</span> ThreadSafeTest();</span><br><span class="line">       Thread threadA = <span class="keyword">new</span> Thread(()-&gt;addInstanceNum(<span class="string">"a"</span>));</span><br><span class="line">       Thread threadB = <span class="keyword">new</span> Thread(()-&gt;addInstanceNum(<span class="string">"b"</span>));</span><br><span class="line">       threadA.start();</span><br><span class="line">       threadB.start();</span><br><span class="line">       Thread.sleep(<span class="number">3000</span>);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>上面的示例中，localObject虽然是局部变量，但是在someMethod方法中将其引用传入到线程A和线程B中，他们的结果可能就不是预期的，那么localObject就不是线程安全的。</p>
<h3 id="对象成员"><a class="header-anchor" href="#对象成员">¶</a>对象成员</h3>
<p>对象成员存储在堆上，如果两个线程同时更新同一个对象的同一个成员，那这个代码就不是线程安全的，详情参考<a href="#%E7%AB%9E%E6%80%81%E6%9D%A1%E4%BB%B6%E4%B8%8E%E4%B8%B4%E7%95%8C%E5%8C%BA">竞态条件与临界区</a>。</p>
<h2 id="线程控制逃逸规则"><a class="header-anchor" href="#线程控制逃逸规则">¶</a>线程控制逃逸规则</h2>
<p>线程控制逃逸规则可以帮助你判断代码中对某些资源的访问是否是线程安全的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">如果一个资源的创建，使用，销毁都在同一个线程内完成，</span><br><span class="line">且永远不会脱离该线程的控制，则该资源的使用就是线程安全的。</span><br></pre></td></tr></table></figure>
<h1 id="多线程编程"><a class="header-anchor" href="#多线程编程">¶</a>多线程编程</h1>
<h2 id="并发编程三要素"><a class="header-anchor" href="#并发编程三要素">¶</a>并发编程三要素</h2>
<p>当多个线程要共享一个实例对象的值得时候，那么在考虑安全的多线程并发编程时就要保证下面3个要素：</p>
<ul>
<li>原子性（Synchronized, Lock）</li>
</ul>
<p>即一个操作或者多个操作 要么全部执行并且执行的过程不会被任何因素打断，要么就都不执行。</p>
<ul>
<li>有序性(Volatile, Synchronized, Lock)</li>
</ul>
<p>即程序执行的顺序按照代码的先后顺序执行。</p>
<ul>
<li>可见性(Volatile, Synchronized, Lock)</li>
</ul>
<p>可见性是指当多个线程访问同一个变量时，一个线程修改了这个变量的值，其他线程能够立即看得到修改的值。</p>
<h2 id="线程安全的实现方法"><a class="header-anchor" href="#线程安全的实现方法">¶</a>线程安全的实现方法</h2>
<h3 id="互斥同步-悲观锁"><a class="header-anchor" href="#互斥同步-悲观锁">¶</a>互斥同步–悲观锁</h3>
<p>互斥同步（Mutual Exclusion &amp; Synchronization）是常见的一种并发正确性保障手段。同步是指在多个线程并发访问共享数据时，保证共享数据在同一个时刻只被一个（或者是一些，使用信号量的时候）线程使用。而互斥是实现同步的一种手段，临界区（Critical Section）、互斥量（Mutex）和信号量（Semaphore）都是主要的互斥实现方式。因此，在这4个字里面，互斥是因，同步是果；互斥是方法，同步是目的。</p>
<p>这种方式实现的锁，总是假设最坏的情况，每次去拿数据的时候都认为别人会修改，所以每次在拿数据的时候都会上锁，这样别人想拿这个数据就会阻塞直到它拿到锁（<strong>共享资源每次只给一个线程使用（独占锁），其它线程阻塞，用完后再把资源转让给其它线程</strong>）。所以这也是悲观锁的思想的一种实现方式。</p>
<h4 id="synchronized"><a class="header-anchor" href="#synchronized">¶</a>synchronized</h4>
<p>在Java中，最基本的互斥同步手段就是synchronized关键字，synchronized关键字经过编译之后，会在同步块的前后分别形成monitorenter和monitorexit这两个字节码指令，这两个字节码都需要一个reference类型的参数来指明要锁定和解锁的对象。如果Java程序中的synchronized明确指定了对象参数，那就是这个对象的reference；如果没有明确指定，那就根据synchronized修饰的是实例方法还是类方法，去取对应的对象实例或Class对象来作为锁对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">methodB</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (<span class="keyword">this</span>)&#123;</span><br><span class="line">        methodA();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">这段代码编译之后的字节码如下：</span><br><span class="line"> <span class="number">0</span> aload_0 <span class="comment">// 入栈this</span></span><br><span class="line"> <span class="number">1</span> dup	<span class="comment">// 复制栈顶元素</span></span><br><span class="line"> <span class="number">2</span> astore_1	<span class="comment">// 将栈顶元素存储到局部变量表Slot 1中</span></span><br><span class="line"> <span class="number">3</span> monitorenter <span class="comment">// 以栈顶元素（即this）作为锁，开始同步</span></span><br><span class="line"> <span class="number">4</span> aload_0 <span class="comment">// 入栈this，用于调用methodA</span></span><br><span class="line"> 5 invokevirtual #19 &lt;com/teddy/thread/basic/ThreadSafeTest$SyncMethodLockTest.methodA&gt; // 调用methodA</span><br><span class="line"> <span class="number">8</span> aload_1 <span class="comment">// 入栈this，monitorexit的reference</span></span><br><span class="line"> <span class="number">9</span> monitorexit <span class="comment">// 退出同步</span></span><br><span class="line"><span class="number">10</span> goto <span class="number">18</span> (+<span class="number">8</span>) <span class="comment">// 方法正常结束，跳转到18返回</span></span><br><span class="line"><span class="number">13</span> astore_2 <span class="comment">// 异常路径</span></span><br><span class="line"><span class="number">14</span> aload_1 <span class="comment">// 入栈this，用于调用methodA</span></span><br><span class="line"><span class="number">15</span> monitorexit <span class="comment">// 退出同步</span></span><br><span class="line"><span class="number">16</span> aload_2 <span class="comment">// 异常对象入栈</span></span><br><span class="line"><span class="number">17</span> athrow <span class="comment">// 抛出异常给调用者</span></span><br><span class="line"><span class="number">18</span> <span class="keyword">return</span> <span class="comment">// 方法结束</span></span><br></pre></td></tr></table></figure>
<p>从上面了解到，synchronized修饰符在不同方法上以及同步构造器中传入的参数不同，监视的对象是不同的，大致分为以下几种情况：</p>
<ul>
<li>实例方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">methodB</span><span class="params">()</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"threadName = "</span> + Thread.currentThread().getName() + <span class="string">" enter sync methodB."</span>);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println(<span class="string">"threadName = "</span> + Thread.currentThread().getName() + <span class="string">" leave sync methodB."</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Java实例方法同步是同步在拥有该方法的对象上。这样，每个实例其方法同步都同步在不同的对象上，即该方法所属的实例对象。</p>
<ul>
<li>静态方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">synchronized</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">methodA</span><span class="params">()</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"threadName = "</span> + Thread.currentThread().getName() + <span class="string">" enter sync static methodA."</span>);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println(<span class="string">"threadName = "</span> + Thread.currentThread().getName() + <span class="string">" leave sync static methodA."</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>静态方法的同步是指同步在该方法所在的类对象上。因为在Java虚拟机中一个类只能对应一个类对象(即只能被一个ClassLoader加载)，所以同时只允许一个线程执行同一个类中的静态同步方法。</p>
<ul>
<li>实例方法中的同步块</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">methodA</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"methodA time = "</span> + System.currentTimeMillis());</span><br><span class="line">    <span class="keyword">synchronized</span> (<span class="keyword">this</span>)&#123;</span><br><span class="line">        System.out.println(<span class="string">"methodA begin time = "</span> + System.currentTimeMillis());</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"methodA end time = "</span> + System.currentTimeMillis());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在同步构造器中用括号括起来的对象叫做监视器对象。同时只有一个线程能够在同步于同一个监视器对象的Java方法内执行。</p>
<ul>
<li>静态方法中的同步块</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">InStaticMethodSync</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">methodA</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">synchronized</span>(InStaticMethodSync<span class="class">.<span class="keyword">class</span>)</span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"methodA begin time = "</span> + System.currentTimeMillis());</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"methodA begin time = "</span> + System.currentTimeMillis());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">methodB</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">synchronized</span>(InStaticMethodSync<span class="class">.<span class="keyword">class</span>)</span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"methodB begin time = "</span> + System.currentTimeMillis());</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"methodB begin time = "</span> + System.currentTimeMillis());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个地方同步构造器中是InStaticMethodSync.class，那么这里监视的就是class对象，因此InStaticMethodSync.class的其它被synchronized修饰的静态方法与该同步块同时只有一个线程能够执行。</p>
<blockquote>
<p>摘抄自《深入理解Java虚拟机：JVM高级特性与最佳实践（第二版）》</p>
<p>根据虚拟机规范的要求，在执行monitorenter指令时，首先要尝试获取对象的锁。如果这个对象没被锁定，或者当前线程已经拥有了那个对象的锁，把锁的计数器加1，相应的，在执行monitorexit指令时会将锁计数器减1，当计数器为0时，锁就被释放。如果获取对象锁失败，那当前线程就要阻塞等待，直到对象锁被另外一个线程释放为止。<br>
在虚拟机规范对monitorenter和monitorexit的行为描述中，有两点是需要特别注意的。首先，synchronized同步块对同一条线程来说是可重入的，不会出现自己把自己锁死的问题。其次，同步块在已进入的线程执行完之前，会阻塞后面其他线程的进入。Java的线程是映射到操作系统的原生线程之上的，如果要阻塞或唤醒一个线程，都需要操作系统来帮忙完成，这就需要从用户态转换到核心态中，因此状态转换需要耗费很多的处理器时间。对于代码简单的同步块（如被synchronized修饰的getter()或setter()方法），状态转换消耗的时间有可能比用户代码执行的时间还要长。所以synchronized是Java语言中一个重量级（Heavyweight）的操作，有经验的程序员都会在确实必要的情况下才使用这种操作。而虚拟机本身也会进行一些优化，譬如在通知操作系统阻塞线程之前加入一段自旋等待过程，避免频繁地切入到核心态之中。</p>
</blockquote>
<h4 id="lock"><a class="header-anchor" href="#lock">¶</a>Lock</h4>
<p>synchronized同步块一样，是一种线程同步机制，但比Java中的synchronized同步块更复杂。 自Java 5开始，java.util.concurrent.locks包中包含了一些锁的实现，因此我们不用去实现自己的锁了。但是我们仍然需要去了解怎样使用这些锁，且了解这些实现背后的理论也是很有用处的。</p>
<h5 id="锁的使用"><a class="header-anchor" href="#锁的使用">¶</a>锁的使用</h5>
<p>以之前的代码为例，使用Lock代替synchronized达到了同样的目的 ：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"></span><br><span class="line"><span class="comment">/** 实例变量非线程安全的 */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">int</span> instanceNum;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">addInstanceNum</span><span class="params">(String username)</span></span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">if</span>(<span class="string">"b"</span>.equals(username))&#123;</span><br><span class="line">            instanceNum = <span class="number">200</span>;</span><br><span class="line">            System.out.println(<span class="string">"b set over!"</span>);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            instanceNum = <span class="number">100</span>;</span><br><span class="line">            System.out.println(<span class="string">"a set over!"</span>);</span><br><span class="line">            Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(username + <span class="string">" num = "</span> + instanceNum);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">threadSetInstanceNumTest</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    Thread threadA = <span class="keyword">new</span> Thread(()-&gt;addInstanceNum(<span class="string">"a"</span>));</span><br><span class="line">    Thread threadB = <span class="keyword">new</span> Thread(()-&gt;addInstanceNum(<span class="string">"b"</span>));</span><br><span class="line">    threadA.start();</span><br><span class="line">    threadB.start();</span><br><span class="line">    Thread.sleep(<span class="number">3000</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>lock()方法会对Lock实例对象进行加锁，因此所有对该对象调用lock()方法的线程都会被阻塞，直到该Lock对象的unlock()方法被调用。</p>
<h5 id="锁的简单实现"><a class="header-anchor" href="#锁的简单实现">¶</a>锁的简单实现</h5>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleLock</span></span>&#123;</span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">boolean</span> isLocked = <span class="keyword">false</span>;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> InterruptedException</span>&#123;</span><br><span class="line">		<span class="keyword">while</span>(isLocked)&#123;</span><br><span class="line">			wait();</span><br><span class="line">		&#125;</span><br><span class="line">		isLocked = <span class="keyword">true</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span></span>&#123;</span><br><span class="line">		isLocked = <span class="keyword">false</span>;</span><br><span class="line">		notify();</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>注意其中的while(isLocked)循环，它又被叫做“自旋锁”。自旋锁以及wait()和notify()方法在<span class="exturl" data-url="aHR0cDovL2lmZXZlLmNvbS90aHJlYWQtc2lnbmFsaW5nLw==" title="http://ifeve.com/thread-signaling/">线程通信<i class="fa fa-external-link"></i></span>这篇文章中有更加详细的介绍。当isLocked为true时，调用lock()的线程在wait()调用上阻塞等待。为防止该线程没有收到notify()调用也从wait()中返回（也称作<span class="exturl" data-url="aHR0cDovL2lmZXZlLmNvbS90aHJlYWQtc2lnbmFsaW5nLyNzcHVyaW91c193YWtldXBz" title="http://ifeve.com/thread-signaling/#spurious_wakeups">虚假唤醒<i class="fa fa-external-link"></i></span>），这个线程会重新去检查isLocked条件以决定当前是否可以安全地继续执行还是需要重新保持等待，而不是认为线程被唤醒了就可以安全地继续执行了。如果isLocked为false，当前线程会退出while(isLocked)循环，并将isLocked设回true，让其它正在调用lock()方法的线程能够在Lock实例上加锁。</p>
<p>当线程完成了<span class="exturl" data-url="aHR0cDovL2lmZXZlLmNvbS9yYWNlLWNvbmRpdGlvbnMtYW5kLWNyaXRpY2FsLXNlY3Rpb25zLw==" title="http://ifeve.com/race-conditions-and-critical-sections/">临界区<i class="fa fa-external-link"></i></span>（位于lock()和unlock()之间）中的代码，就会调用unlock()。执行unlock()会重新将isLocked设置为false，并且通知（唤醒）其中一个（若有的话）在lock()方法中调用了wait()函数而处于等待状态的线程。</p>
<p>java中的锁ReentrantLock不是基于synchronized而是基于原子类实现的，其核心代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, <span class="number">1</span>))</span><br><span class="line">        setExclusiveOwnerThread(Thread.currentThread());</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        acquire(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 第一步compareAndSetState失败时，表示已经有线程获取到锁了，那么调用acquire请求锁</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">acquire</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 先尝试一下，能否请求到锁，不能请求到锁的话，开始排队请求锁(排队的请求者存放在链表中)</span></span><br><span class="line">    <span class="keyword">if</span> (!tryAcquire(arg) &amp;&amp;</span><br><span class="line">        acquireQueued(addWaiter(Node.EXCLUSIVE), arg))</span><br><span class="line">        selfInterrupt();</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">acquireQueued</span><span class="params">(<span class="keyword">final</span> Node node, <span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> failed = <span class="keyword">true</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">boolean</span> interrupted = <span class="keyword">false</span>;</span><br><span class="line">      	<span class="comment">// 这里有个死循环，知道请求到锁，其实也就是CAS</span></span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">final</span> Node p = node.predecessor();</span><br><span class="line">            <span class="keyword">if</span> (p == head &amp;&amp; tryAcquire(arg)) &#123;</span><br><span class="line">                setHead(node);</span><br><span class="line">                p.next = <span class="keyword">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                failed = <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">return</span> interrupted;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</span><br><span class="line">                parkAndCheckInterrupt())</span><br><span class="line">                interrupted = <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (failed)</span><br><span class="line">            cancelAcquire(node);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="可重入性"><a class="header-anchor" href="#可重入性">¶</a>可重入性</h4>
<p>Java中的synchronized同步块是可重入的，使用以下代码来进行分析：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">ReentrantSyncMethodTest</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">methodA</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"methodA"</span>);</span><br><span class="line">        methodB();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">methodB</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"methodB"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 可重入锁：自己可以再次获取自己的内部锁</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * 如果不可重入的话</span></span><br><span class="line"><span class="comment"> * 线程进入methodA获取了该对象的锁，然后执行methodB还是需要获取该对象的锁，</span></span><br><span class="line"><span class="comment"> * 但是methodA还没有执行完不会将锁释放，就会造成死锁。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">reentrantSyncMethodTest</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ReentrantSyncMethodTest reentrantSyncMethodTest = <span class="keyword">new</span> ReentrantSyncMethodTest();</span><br><span class="line">    Thread thread = <span class="keyword">new</span> Thread(()-&gt; reentrantSyncMethodTest.methodA());</span><br><span class="line">    thread.start();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>到这里想一想，我们之前的<code>SimpleLock</code>是否是可重入锁，很明显它不是，获取锁的条件是：只有当isLocked为false时lock操作才被允许，而没有考虑是哪个线程锁住了它。</p>
<p>为了让SimpleLock具有可重入性，我们只需要对其进行简单修改即可：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleReentrantLock</span></span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> isLocked = <span class="keyword">false</span>;</span><br><span class="line">    Thread  lockedBy = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">int</span> lockedCount = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span></span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> InterruptedException</span>&#123;</span><br><span class="line">        Thread callingThread =</span><br><span class="line">                Thread.currentThread();</span><br><span class="line">        <span class="keyword">while</span>(isLocked &amp;&amp; lockedBy != callingThread)&#123;</span><br><span class="line">            wait();</span><br><span class="line">        &#125;</span><br><span class="line">        isLocked = <span class="keyword">true</span>;</span><br><span class="line">        lockedCount++;</span><br><span class="line">        lockedBy = callingThread;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(Thread.currentThread() ==</span><br><span class="line">                <span class="keyword">this</span>.lockedBy)&#123;</span><br><span class="line">            lockedCount--;</span><br><span class="line">            <span class="keyword">if</span>(lockedCount == <span class="number">0</span>)&#123;</span><br><span class="line">                isLocked = <span class="keyword">false</span>;</span><br><span class="line">                notify();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>注意到现在的while循环（自旋锁）也考虑到了已锁住该Lock实例的线程。如果当前的锁对象没有被加锁(isLocked = false)，或者当前调用线程已经对该Lock实例加了锁，那么while循环就不会被执行，调用lock()的线程就可以退出该方法。</p>
<p>除此之外，我们需要记录同一个线程重复对一个锁对象加锁的次数。否则，一次unblock()调用就会解除整个锁，即使当前锁已经被加锁过多次。在unlock()调用没有达到对应lock()调用的次数之前，我们不希望锁被解除。</p>
<p>现在这个Lock类就是可重入的了。</p>
<p>java中的ReentrantLock实现方式与SimpleReentrantLock实现方式大同小异：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">nonfairTryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">    <span class="keyword">int</span> c = getState();</span><br><span class="line">    <span class="comment">// 当前状态为1的话，就表示已经有线程获取到锁了</span></span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">            setExclusiveOwnerThread(current);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果持有锁的线程是当前线程的话，直接返回true，表示已经请求到锁了，可重入行也就体现在这里</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">        <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">        <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>) <span class="comment">// overflow</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">        setState(nextc);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="公平性"><a class="header-anchor" href="#公平性">¶</a>公平性</h4>
<p>Java的synchronized块并不保证尝试进入它们的线程的顺序。因此，如果多个线程不断竞争访问相同的synchronized同步块，就存在一种风险，其中一个或多个线程永远也得不到访问权 —— 也就是说访问权总是分配给了其它线程。这种情况被称作线程饥饿。为了避免这种问题，锁需要实现公平性。本文所展现的锁在内部是用synchronized同步块实现的，因此它们也不保证公平性。</p>
<p><code>ReentrantLock、ReentrantReadWriteLock</code>可以通过构造函数指定是否为公平锁，其核心代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 默认实现</span></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">nonfairTryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">	···</span><br><span class="line">    <span class="comment">// 当有一个线程将锁释放了，这里的状态就为0了</span></span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 这里所有的等待线程都会进行这个CAS操作，谁能抢到就看运气了，所以是不公平的</span></span><br><span class="line">        <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">            setExclusiveOwnerThread(current);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;  </span><br><span class="line">    ···</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 公平锁的实现</span></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">	···</span><br><span class="line">    <span class="comment">// 当有一个线程将锁释放了，这里的状态就为0了</span></span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 这里进行CAS之前会判断一下，是否是排在链表最前端的线程，如果是则进行CAS操作，所以是公平的</span></span><br><span class="line">        <span class="keyword">if</span> (!hasQueuedPredecessors() &amp;&amp; compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">            setExclusiveOwnerThread(current);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;  </span><br><span class="line">    ···</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">hasQueuedPredecessors</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Node t = tail; </span><br><span class="line">    Node h = head;</span><br><span class="line">    Node s;</span><br><span class="line">  	<span class="comment">// 1、h == t 表示只有一个等待线程，直接获取锁即可</span></span><br><span class="line">    <span class="comment">// 2、h.next == null 表示只有一个等待线程，直接获取锁即可</span></span><br><span class="line">    <span class="comment">// 3、s.thread != Thread.currentThread() 排在链表最前端的不是当前线程，那么继续进入等待，知道当前线程排在最前端</span></span><br><span class="line">    <span class="keyword">return</span> h != t &amp;&amp;</span><br><span class="line">        ((s = h.next) == <span class="keyword">null</span> || s.thread != Thread.currentThread());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="读写锁"><a class="header-anchor" href="#读写锁">¶</a>读写锁</h4>
<p>读锁的获取条件：没有线程拥有写锁（writers==0），且没有线程在请求写锁（writeRequests ==0）</p>
<p>写锁的获取条件：当一个线程想获得写锁的时候，首先会把写锁请求数加1（writeRequests++），然后再去判断是否能够真能获得写锁，当没有线程持有读锁（readers==0 ）,且没有线程持有写锁（writers==0）时就能获得写锁。有多少线程在请求写锁并无关系。</p>
<h3 id="非阻塞同步-乐观锁"><a class="header-anchor" href="#非阻塞同步-乐观锁">¶</a>非阻塞同步–乐观锁</h3>
<blockquote>
<p>摘抄自《深入理解Java虚拟机：JVM高级特性与最佳实践（第二版）》</p>
<p>互斥同步最主要的问题就是进行线程阻塞和唤醒所带来的性能问题，因此这种同步也称为阻塞同步（Blocking Synchronization）。随着硬件指令集的发展，我们有了另外一个选择：基于冲突检测的乐观并发策略，通俗地说，就是先进行操作，如果没有其他线程争用共享数据，那操作就成功了；如果共享数据有争用，产生了冲突，那就再采取其他的补偿措施（最常见的补偿措施就是不断地重试，直到成功为止），这种乐观的并发策略的许多实现都不需要把线程挂起，因此这种同步操作称为非阻塞同步（Non-Blocking Synchronization）。</p>
<p>为什么笔者说使用乐观并发策略需要“硬件指令集的发展”才能进行呢？因为我们需要操作和冲突检测这两个步骤具备原子性，靠什么来保证呢？如果这里再使用互斥同步来保证就失去意义了，所以我们只能靠硬件来完成这件事情，硬件保证一个从语义上看起来需要多次操作的行为只通过一条处理器指令就能完成，这类指令常用的有：</p>
<ul>
<li>测试并设置（Test-and-Set）。</li>
<li>获取并增加（Fetch-and-Increment）。</li>
<li>交换（Swap）。</li>
<li>比较并交换（Compare-and-Swap，下文称CAS）。</li>
<li>加载链接/条件存储（Load-Linked/Store-Conditional，下文称LL/SC）。</li>
</ul>
<p>其中，前面的3条是20世纪就已经存在于大多数指令集之中的处理器指令，后面的两条是现代处理器新增的，而且这两条指令的目的和功能是类似的。在IA64、x86指令集中有cmpxchg指令完成CAS功能，在sparc-TSO也有casa指令实现，而在ARM和PowerPC架构下，则需要使用一对ldrex/strex指令来完成LL/SC的功能。<br>
CAS指令需要有3个操作数，分别是内存位置（在Java中可以简单理解为变量的内存地址，用V表示）、旧的预期值（用A表示）和新值（用B表示）。CAS指令执行时，当且仅当V符合旧预期值A时，处理器用新值B更新V的值，否则它就不执行更新，但是无论是否更新了V的值，都会返回V的旧值，上述的处理过程是一个原子操作。</p>
</blockquote>
<p>java中AtomicInteger的自增实现如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">incrementAndGet</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">int</span> current = get();</span><br><span class="line">        <span class="keyword">int</span> next = current + <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (compareAndSet(current, next))</span><br><span class="line">            <span class="keyword">return</span> next;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>JDK1.8起，Unsafe提供了compareAndSwapInt方法底层由C实现，直接实现了CAS操作：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">compareAndSet</span><span class="params">(<span class="keyword">int</span> expect, <span class="keyword">int</span> update)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> unsafe.compareAndSwapInt(<span class="keyword">this</span>, valueOffset, expect, update);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>CAS操作有个逻辑漏洞：如果一个变量V初次读取的时候是A值，并且在准备赋值的时候检查到它仍然为A值，那我们就能说它的值没有被其他线程改变过了吗？如果在这段期间它的值曾经被改成了B，后来又被改回为A，那CAS操作就会误认为它从来没有被改变过。这个漏洞称为CAS操作的“ABA”问题。J.U.C包为了解决这个问题，提供了一个带有标记的原子引用类“AtomicStampedReference”，它可以通过控制变量值的版本来保证CAS的正确性。不过目前来说这个类比较“鸡肋”，大部分情况下ABA问题不会影响程序并发的正确性，如果需要解决ABA问题，改用传统的互斥同步可能会比原子类更高效。</p>
<h3 id="无同步方案"><a class="header-anchor" href="#无同步方案">¶</a>无同步方案</h3>
<p>保证线程安全，并不是一定要进行同步的，两者没有因果关系。如果方法不涉及共享数据，那么它也不需要进行同步了。</p>
<ul>
<li>可重入代码
<ul>
<li>不依赖存储在堆上的数据和共用的资源</li>
<li>在入参相同的情况，执行结果都是相同的</li>
</ul>
</li>
<li>线程本地存储
<ul>
<li>共享数据的代码放到同一个线程执行</li>
<li>ThreadLocal</li>
</ul>
</li>
</ul>
<h2 id="锁优化"><a class="header-anchor" href="#锁优化">¶</a>锁优化</h2>
<h3 id="自旋锁与自适应自旋"><a class="header-anchor" href="#自旋锁与自适应自旋">¶</a>自旋锁与自适应自旋</h3>
<p>互斥同步对性能最大的影响是阻塞的实现，挂起线程和恢复线程的操作都需要转入内核态中完成，这些操作给系统的并发性能带来了很大的压力。同时，虚拟机的开发团队也注意到在许多应用上，共享数据的锁定状态只会持续很短的一段时间，为了这段时间去挂起和恢复线程并不值得。如果物理机器有一个以上的处理器，能让两个或以上的线程同时并行执行，我们就可以让后面请求锁的那个线程“稍等一下”，但不放弃处理器的执行时间，看看持有锁的线程是否很快就会释放锁。为了让线程等待，我们只需让线程执行一个忙循环（自旋），这项技术就是所谓的自旋锁。<br>
自旋等待本身虽然避免了线程切换的开销，但它是要占用处理器时间的，因此，<strong>如果锁被占用的时间很短，自旋等待的效果就会非常好</strong>，反之，<strong>如果锁被占用的时间很长，那么自旋的线程只会白白消耗处理器资源</strong>，而不会做任何有用的工作，反而会带来性能上的浪费。因此，<strong>自旋等待的时间必须要有一定的限度</strong>，如果自旋超过了限定的次数仍然没有成功获得锁，就应当使用传统的方式去挂起线程了。自旋次数的默认值是10次，用户可以使用参数-XX:PreBlockSpin来更改。<br>
在JDK 1.6中引入了自适应的自旋锁。自适应意味着自旋的时间不再固定了，而是由前一次在同一个锁上的自旋时间及锁的拥有者的状态来决定。如果在同一个锁对象上，自旋等待刚刚成功获得过锁，并且持有锁的线程正在运行中，那么虚拟机就会认为这次自旋也很有可能再次成功，进而它将允许自旋等待持续相对更长的时间，比如100个循环。另外，如果对于某个锁，自旋很少成功获得过，那在以后要获取这个锁时将可能省略掉自旋过程，以避免浪费处理器资源。有了自适应自旋，随着程序运行和性能监控信息的不断完善，虚拟机对程序锁的状况预测就会越来越准确，虚拟机就会变得越来越“聪明”了。</p>
<h3 id="锁消除"><a class="header-anchor" href="#锁消除">¶</a>锁消除</h3>
<p>锁消除是指虚拟机即时编译器在运行时，对一些代码上要求同步，但是被检测到不可能存在共享数据竞争的锁进行消除。锁消除的主要判定依据来源于逃逸分析的数据支持，如果判断在一段代码中，堆上的所有数据都不会逃逸出去从而被其他线程访问到，那就可以把它们当做栈上数据对待，认为它们是线程私有的，同步加锁自然就无须进行。</p>
<h3 id="锁粗化"><a class="header-anchor" href="#锁粗化">¶</a>锁粗化</h3>
<p>原则上，我们在编写代码的时候，总是推荐将同步块的作用范围限制得尽量小——只在共享数据的实际作用域中才进行同步，这样是为了使得需要同步的操作数量尽可能变小，如果存在锁竞争，那等待锁的线程也能尽快拿到锁。<br>
大部分情况下，上面的原则都是正确的，但是如果一系列的连续操作都对同一个对象反复加锁和解锁，甚至加锁操作是出现在循环体中的，那即使没有线程竞争，频繁地进行互斥同步操作也会导致不必要的性能损耗。</p>
<h3 id="轻量级锁"><a class="header-anchor" href="#轻量级锁">¶</a>轻量级锁</h3>
<p>“轻量级”是相对于使用操作系统互斥量来实现的传统锁而言的，因此传统的锁机制就称为“重量级”锁。首先需要强调一点的是，轻量级锁并不是用来代替重量级锁的，它的本意是在没有多线程竞争的前提下，减少传统的重量级锁使用操作系统互斥量产生的性能消耗。</p>
<p>使用轻量级锁时，不需要申请互斥量，仅仅<em>将Mark Word中的部分字节CAS更新指向线程栈中的Lock Record，如果更新成功，则轻量级锁获取成功</em>，记录锁状态为轻量级锁；<strong>否则，说明已经有线程获得了轻量级锁，目前发生了锁竞争（不适合继续使用轻量级锁），接下来膨胀为重量级锁</strong>。</p>
<blockquote>
<p>Mark Word是对象头的一部分；每个线程都拥有自己的线程栈（虚拟机栈），记录线程和函数调用的基本信息。</p>
</blockquote>
<h3 id="偏向锁"><a class="header-anchor" href="#偏向锁">¶</a>偏向锁</h3>
<p>“偏向”的意思是，<em>偏向锁假定将来只有第一个申请锁的线程会使用锁</em>（不会有任何线程再来申请锁），因此，<em>只需要在Mark Word中CAS记录owner（本质上也是更新，但初始值为空），如果记录成功，则偏向锁获取成功</em>，记录锁状态为偏向锁，<em>以后当前线程等于owner就可以零成本的直接获得锁；否则，说明有其他线程竞争，膨胀为轻量级锁</em>。</p>
<p>偏向锁无法使用自旋锁优化，因为一旦有其他线程申请锁，就破坏了偏向锁的假定。</p>
<p>参考文献</p>
<ul>
<li><span class="exturl" data-url="aHR0cDovL2lmZXZlLmNvbS9qYXZhLWNvbmN1cnJlbmN5LXRocmVhZC1kaXJlY3Rvcnkv" title="http://ifeve.com/java-concurrency-thread-directory/">并发编程网–Java并发性和多线程介绍 <i class="fa fa-external-link"></i></span></li>
<li>《深入理解Java虚拟机：JVM高级特性与最佳实践（第二版）》</li>
</ul>
]]></content>
      <categories>
        <category>java多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java多线程</tag>
        <tag>多线程编程</tag>
        <tag>lock</tag>
      </tags>
  </entry>
  <entry>
    <title>性能优化利器-javaagent</title>
    <url>/performance/javaagent-performance.html</url>
    <content><![CDATA[<h1 id="性能优化利器-javaagent"><a class="header-anchor" href="#性能优化利器-javaagent">¶</a>性能优化利器-javaagent</h1>
<p>你在做性能优化时有没有遇到过，知道某些接口慢但是<strong>不知道具体慢在哪里</strong>。当然你可能第一时间会想到使用阿里的arthas去trace一下就能看到调用栈上各个方法耗时了；但是在生产环境一般是单次访问不慢，高并发时才慢，再用arthas去trace就监控生产环境就不太现实了。</p>
<blockquote>
<p>如果你第一时间想到的是给各个方法调用加上耗时日志打印，然后根据日志再去分析哪里慢，那么你更应该把这篇文章看完！</p>
</blockquote>
<h2 id="javaagent"><a class="header-anchor" href="#javaagent">¶</a>javaagent</h2>
<p>接下来给大家介绍的工具叫<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2Rpbmdqcy9qYXZhYWdlbnQ=" title="https://github.com/dingjs/javaagent">javaagent<i class="fa fa-external-link"></i></span>，javaagent 是一个简单优雅的 java agent ,利用 java 自带的 instrument 特性+ javassist 字节码编辑技术，实现了无侵入的方法级性能监控。相比于NewRelic或者开源的<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL25hdmVyL3BpbnBvaW50" title="https://github.com/naver/pinpoint">pinpoint<i class="fa fa-external-link"></i></span>,以及阿里的<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2FsaWJhYmEvYXJ0aGFz" title="https://github.com/alibaba/arthas">arthas<i class="fa fa-external-link"></i></span>,本工具主打的是简单，我们只记录每个方法的执行次数和时间，并输出到json格式的日志文件中，然后可以使用配套的agent-analyer进行分析。</p>
<p>集成方式详见：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2Rpbmdqcy9qYXZhYWdlbnQ=" title="https://github.com/dingjs/javaagent">https://github.com/dingjs/javaagent<i class="fa fa-external-link"></i></span></p>
<p>监控出来的统计结果：</p>
<table>
<thead>
<tr>
<th>类名</th>
<th>方法名</th>
<th>总时间</th>
<th>总次数</th>
<th>平均数</th>
</tr>
</thead>
<tbody>
<tr>
<td>com.xxx.ClassA</td>
<td>methodA</td>
<td>778015748</td>
<td>2052812</td>
<td>379</td>
</tr>
<tr>
<td>com.xxx.ClassA</td>
<td>methodB</td>
<td>757438182</td>
<td>2052678</td>
<td>369</td>
</tr>
<tr>
<td>com.xxx.ClassA</td>
<td>methodC</td>
<td>162202981</td>
<td>2052678</td>
<td>79</td>
</tr>
</tbody>
</table>
<blockquote>
<p>从这个统计结果来看，方法运行了205万次，平均时间为379ms，看起来不慢。<br>
但是，性能测试的时候发现有的请求响应时间很长，JMeter聚合报告中<code>90%百分位</code>为2356ms，也就是说有10%的请求响应时间是大于等于2356ms。</p>
</blockquote>
<p>从统计结果来看平均响应时间都还很不错，但是对于性能优化来说不能直接的找出慢的方法，那么有没有办法能像JMeter聚合报告那样计算出方法执行耗时的百分比统计结果呢，我们继续往下看。</p>
<h2 id="t-digest在线计算"><a class="header-anchor" href="#t-digest在线计算">¶</a>T-Digest在线计算</h2>
<p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3RkdW5uaW5nL3QtZGlnZXN0" title="https://github.com/tdunning/t-digest">T-Digest<i class="fa fa-external-link"></i></span>是由Ted Dunning提出，旨在以极低的内存开销计算数据流的大致百分位数。不同于传统的精确计算方法要求大量内存来存储所有数据，tdigest通过聪明的数据结构和算法优化，能够在压缩数据到极小内存空间的同时，提供高质量的百分位数估计。</p>
<p>通过对比最终决定采用MergingDigest算法进行方法执行的统计，这里使用简单的demo进行验证：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testMethodPctCounter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> compression = <span class="number">150</span>;</span><br><span class="line">        <span class="keyword">int</span> factor = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">int</span> M = <span class="number">100</span>;</span><br><span class="line">        <span class="keyword">final</span> List&lt;MergingDigest&gt; mds = <span class="keyword">new</span> ArrayList&lt;MergingDigest&gt;(M);</span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">long</span>[] counts = <span class="keyword">new</span> <span class="keyword">long</span>[M];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; M; ++i) &#123;</span><br><span class="line">            mds.add(<span class="keyword">new</span> MergingDigest(compression, (factor + <span class="number">1</span>) * compression, compression));</span><br><span class="line">            counts[i] = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Fill all digests with random values (0~100).</span></span><br><span class="line">        <span class="keyword">final</span> Random random = <span class="keyword">new</span> Random();</span><br><span class="line"></span><br><span class="line">        ThreadPoolExecutor executorService = <span class="keyword">new</span> ThreadPoolExecutor(</span><br><span class="line">                <span class="number">100</span>,</span><br><span class="line">                <span class="number">100</span>,</span><br><span class="line">                <span class="number">0</span>,</span><br><span class="line">                TimeUnit.MINUTES,</span><br><span class="line">                <span class="keyword">new</span> ArrayBlockingQueue&lt;Runnable&gt;(<span class="number">1000</span>),</span><br><span class="line">                <span class="keyword">new</span> ThreadPoolExecutor.CallerRunsPolicy());</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5000000</span>; ++i) &#123;</span><br><span class="line">            executorService.execute((<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">                <span class="meta">@Override</span></span><br><span class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                    <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; M; ++j) &#123;</span><br><span class="line">                        MergingDigest md = mds.get(j);</span><br><span class="line">                        <span class="keyword">synchronized</span> (md) &#123;</span><br><span class="line">                            <span class="keyword">int</span> data = random.nextInt(<span class="number">101</span>);</span><br><span class="line">                            <span class="keyword">long</span> start = System.currentTimeMillis();</span><br><span class="line">                            md.add(data);</span><br><span class="line">                            counts[j] = counts[j] + (System.currentTimeMillis() - start);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (executorService.getActiveCount() != <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        executorService.shutdown();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Output</span></span><br><span class="line">        <span class="keyword">double</span>[] qArr = <span class="keyword">new</span> <span class="keyword">double</span>[]&#123;<span class="number">0.1</span>, <span class="number">0.2</span>, <span class="number">0.25</span>, <span class="number">0.5</span>, <span class="number">0.75</span>, <span class="number">0.90</span>, <span class="number">0.95</span>, <span class="number">0.99</span>&#125;;</span><br><span class="line">        System.out.printf(<span class="string">"%10s\t"</span>, <span class="string">"MIN"</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; qArr.length; ++i) &#123;</span><br><span class="line">            System.out.printf(<span class="string">"%4sth pct\t"</span>, (<span class="keyword">int</span>)(qArr[i] * <span class="number">100</span>));</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.printf(<span class="string">"%10s\t"</span>, <span class="string">"MAX"</span>);</span><br><span class="line">        System.out.printf(<span class="string">"%10s\t"</span>, <span class="string">"耗时"</span>);</span><br><span class="line">        System.out.println();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; <span class="number">11</span>; ++i) &#123;</span><br><span class="line">            System.out.print(<span class="string">"==========\t"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; mds.size(); ++i) &#123;</span><br><span class="line">            MergingDigest md = mds.get(i);</span><br><span class="line">            System.out.printf(<span class="string">"%10.0f\t"</span>, md.getMin());</span><br><span class="line">            <span class="keyword">long</span> start = System.currentTimeMillis();</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">double</span> q : qArr) &#123;</span><br><span class="line">                System.out.printf(<span class="string">"%10.0f\t"</span>, md.quantile(q));</span><br><span class="line">            &#125;</span><br><span class="line">            counts[i] = counts[i] + (System.currentTimeMillis() - start);</span><br><span class="line">            System.out.printf(<span class="string">"%10.0f\t"</span>, md.getMax());</span><br><span class="line">            System.out.printf(<span class="string">"%10d\t"</span>, counts[i]);</span><br><span class="line">            System.out.println();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>100个线程并发测试，每个线程往100个不同的Digest中添加5000000个随机数据[0, 100]，并计算耗时，jvm内存限制为：-Xms50m -Xmx50m。部分统计结果如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">       MIN	  10th pct	  20th pct	  25th pct	  50th pct	  75th pct	  90th pct	  95th pct	  99th pct	       MAX	        耗时</span><br><span class="line">==========	==========	==========	==========	==========	==========	==========	==========	==========	==========	==========</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       942</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       869</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       939</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       895</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       942</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       845</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       887</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       898</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       911</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       815</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       762</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       893</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       933</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       866</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       863</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       920</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       830</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	        99	       100	       870</span><br><span class="line">         0	        10	        20	        25	        50	        75	        90	        95	       100	       100	       913</span><br></pre></td></tr></table></figure>
<p><strong>测试结论</strong>：从统计值来看，统计结果基本符合预期，并且耗时基本在800ms~1000ms之间，未发生内存溢出，初步评估可以加入到javaagent方法耗时统计中并且对性能影响在可控范围内。</p>
<p>因此fork出<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2Rpbmdqcy9qYXZhYWdlbnQ=" title="https://github.com/dingjs/javaagent">https://github.com/dingjs/javaagent<i class="fa fa-external-link"></i></span>仓库添加方法执行的百分比统计功能，配置方式在agent.properties配置文件中添加如下配置：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 是否统计方法执行时间百分比，同JMeter性能测试百分比计算方式，如果开启默认会统计最大值、最小值</span><br><span class="line">agent.log.stat.execute.time=false</span><br><span class="line"># 方法执行时间统计百分比（agent.log.stat.execute.time=true时有效），多选范围[0, 1]，例如：0.5,0.9,0.95,0.99</span><br><span class="line">agent.log.stat.execute.time.pct=0.5,0.9,0.95,0.99</span><br></pre></td></tr></table></figure>
<p>最终监控出来的统计结果：</p>
<table>
<thead>
<tr>
<th>类名</th>
<th>方法名</th>
<th>总时间</th>
<th>总次数</th>
<th>平均数</th>
<th>最小值</th>
<th>最大值</th>
<th>中位数</th>
<th>90th pct</th>
<th>95th pct</th>
<th>99th pct</th>
</tr>
</thead>
<tbody>
<tr>
<td>com.xxx.ClassA</td>
<td>methodA</td>
<td>778015748</td>
<td>2052812</td>
<td>379</td>
<td>10</td>
<td>4399</td>
<td>1029</td>
<td>2356</td>
<td>3476</td>
<td>4399</td>
</tr>
<tr>
<td>com.xxx.ClassA</td>
<td>methodB</td>
<td>757438182</td>
<td>2052678</td>
<td>369</td>
<td>10</td>
<td>4399</td>
<td>1029</td>
<td>2356</td>
<td>3476</td>
<td>4399</td>
</tr>
<tr>
<td>com.xxx.ClassA</td>
<td>methodC</td>
<td>162202981</td>
<td>2052678</td>
<td>79</td>
<td>10</td>
<td>1499</td>
<td>125</td>
<td>468</td>
<td>1276</td>
<td>1399</td>
</tr>
</tbody>
</table>
<p>最终该修改顺利地合并回<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2Rpbmdqcy9qYXZhYWdlbnQ=" title="https://github.com/dingjs/javaagent">https://github.com/dingjs/javaagent<i class="fa fa-external-link"></i></span>仓库，合并请求：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2Rpbmdqcy9qYXZhYWdlbnQvcHVsbC8xMg==" title="https://github.com/dingjs/javaagent/pull/12">https://github.com/dingjs/javaagent/pull/12<i class="fa fa-external-link"></i></span></p>
<p>至此可以尽情地使用javaagent工具帮助我们快乐地进行性能优化了^_^</p>
<h2 id="参考文档"><a class="header-anchor" href="#参考文档">¶</a>参考文档</h2>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2Rpbmdqcy9qYXZhYWdlbnQ=" title="https://github.com/dingjs/javaagent">https://github.com/dingjs/javaagent<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9qbWV0ZXIuYXBhY2hlLm9yZy91c2VybWFudWFsL2NvbXBvbmVudF9yZWZlcmVuY2UuaHRtbCNBZ2dyZWdhdGVfUmVwb3J0" title="https://jmeter.apache.org/usermanual/component_reference.html#Aggregate_Report">https://jmeter.apache.org/usermanual/component_reference.html#Aggregate_Report<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3RkdW5uaW5nL3QtZGlnZXN0" title="https://github.com/tdunning/t-digest">https://github.com/tdunning/t-digest<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>性能优化</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>Java</tag>
        <tag>JavaAgent</tag>
      </tags>
  </entry>
  <entry>
    <title>性能优化-解决MyBatis高并发下OGNL反射调用方法的锁竞争</title>
    <url>/performance/mybatis-ognl-lock.html</url>
    <content><![CDATA[<h1 id="性能优化-解决mybatis高并发下ognl反射调用方法的锁竞争"><a class="header-anchor" href="#性能优化-解决mybatis高并发下ognl反射调用方法的锁竞争">¶</a>性能优化-解决MyBatis高并发下OGNL反射调用方法的锁竞争</h1>
<blockquote>
<p>摘要： 继解决OGNL安全属性检查的性能问题后，线上服务再次因MyBatis查询阻塞而超时。分析发现，近千个线程阻塞在OgnlRuntime.invokeMethod方法，竞争同一个Method对象的锁。根本原因在于，MyBatis-Plus动态SQL在解析时，会高并发地通过反射调用Wrapper对象的固定几个Getter方法（如getSqlSegment）。OGNL为缓存每个方法的访问权限，在首次检查时使用了synchronized(method)，导致严重锁竞争。解决方案是为其缓存机制引入“双重检查锁定”优化，避免后续调用仍需同步，从而根治此瓶颈。该修复已向OGNL社区提交并已于3.4.10版本发布。</p>
</blockquote>
<h2 id="背景"><a class="header-anchor" href="#背景">¶</a>背景</h2>
<p>经过上一篇文章，<span class="exturl" data-url="aHR0cHM6Ly9zaGFyZS5ub3RlLnlvdWRhby5jb20vcy9jVm5CM1dQTQ==" title="https://share.note.youdao.com/s/cVnB3WPM">性能优化-巧解MyBatis高并发下OGNL安全检查导致的全局锁瓶颈.md<i class="fa fa-external-link"></i></span>，我们成功解决了OGNL高并发调用<code>System.getProperty(&quot;ognl.security.manager&quot;)</code>导致的性能瓶颈。</p>
<p>但好景不长，没过几天线上环境又频繁报接口响应超时，很多节点同时报<code>[HSF-Provider] HSF thread pool is full.</code>，dump线程信息进行分析，发现大部分线程还是卡在mybatis查询数据库，这又是为什么呢？</p>
<h2 id="问题排查过程"><a class="header-anchor" href="#问题排查过程">¶</a>问题排查过程</h2>
<h3 id="程堆栈分析"><a class="header-anchor" href="#程堆栈分析">¶</a>程堆栈分析</h3>
<p>于是我们又dump线程信息进行分析，结果还是<code>OgnlRuntime</code>这个类导致，不过这次有981个线程阻塞在<code>org.apache.ibatis.ognl.OgnlRuntime.invokeMethod(OgnlRuntime.java:1151)</code>，具体线程堆栈信息如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 980个线程被阻塞</span><br><span class="line">&quot;default-17571&quot; #91915 prio=5 os_prio=0 tid=0x00007f7b7c05d800 nid=0x167f9 waiting for monitor entry [0x00007f757f772000]</span><br><span class="line">   java.lang.Thread.State: BLOCKED (on object monitor)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.invokeMethod(OgnlRuntime.java:1151)</span><br><span class="line">	- waiting to lock &lt;0x00000004a55aaf48&gt; (a java.lang.reflect.Method)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.getMethodValue(OgnlRuntime.java:2146)</span><br><span class="line">	at org.apache.ibatis.ognl.ObjectPropertyAccessor.getPossibleProperty(ObjectPropertyAccessor.java:66)</span><br><span class="line">	at org.apache.ibatis.ognl.ObjectPropertyAccessor.getProperty(ObjectPropertyAccessor.java:160)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.getProperty(OgnlRuntime.java:3356)</span><br><span class="line">	...</span><br><span class="line"></span><br><span class="line"># 获取到锁的线程</span><br><span class="line">&quot;HSFBizProcessor-DEFAULT-6-thread-5179&quot; #90650 daemon prio=10 os_prio=0 tid=0x00007f794c319000 nid=0x16214 waiting for monitor entry [0x00007f758366e000]</span><br><span class="line">   java.lang.Thread.State: BLOCKED (on object monitor)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.invokeMethod(OgnlRuntime.java:1151)</span><br><span class="line">	- locked &lt;0x00000004a55aaf48&gt; (a java.lang.reflect.Method)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.getMethodValue(OgnlRuntime.java:2146)</span><br><span class="line">	at org.apache.ibatis.ognl.ObjectPropertyAccessor.getPossibleProperty(ObjectPropertyAccessor.java:66)</span><br><span class="line">	at org.apache.ibatis.ognl.ObjectPropertyAccessor.getProperty(ObjectPropertyAccessor.java:160)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.getProperty(OgnlRuntime.java:3356)</span><br><span class="line">	...</span><br></pre></td></tr></table></figure>
<h3 id="源码分析"><a class="header-anchor" href="#源码分析">¶</a>源码分析</h3>
<p>从上面的线程堆栈信息可以看到所有的线程都阻塞在<code>org.apache.ibatis.ognl.OgnlRuntime.invokeMethod(OgnlRuntime.java:1151)</code>，看看源码这里在干啥呢：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">invokeMethod</span><span class="params">(Object target, Method method, Object[] argsArray)</span></span></span><br><span class="line"><span class="function">        <span class="keyword">throws</span> InvocationTargetException, IllegalAccessException</span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> syncInvoke;</span><br><span class="line">    <span class="keyword">boolean</span> checkPermission;</span><br><span class="line">    Boolean methodAccessCacheValue;</span><br><span class="line">    Boolean methodPermCacheValue;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment">// only synchronize method invocation if it actually requires it</span></span><br><span class="line">    <span class="comment">// 这里就是堆栈中的1151行，对method加锁，然后将method的方法可见性和方法执行权限缓存</span></span><br><span class="line">    <span class="keyword">synchronized</span>(method) &#123;</span><br><span class="line">        methodAccessCacheValue = _methodAccessCache.get(method);</span><br><span class="line">        <span class="keyword">if</span> (methodAccessCacheValue == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 检查方法是否是public的</span></span><br><span class="line">            <span class="keyword">if</span> (!Modifier.isPublic(method.getModifiers()) || !Modifier.isPublic(method.getDeclaringClass().getModifiers()))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 检查是否是可访问的</span></span><br><span class="line">                <span class="keyword">if</span> (!(((AccessibleObject) method).isAccessible()))</span><br><span class="line">                &#123;</span><br><span class="line">                    methodAccessCacheValue = Boolean.TRUE;</span><br><span class="line">                    _methodAccessCache.put(method, methodAccessCacheValue);</span><br><span class="line">                &#125; <span class="keyword">else</span></span><br><span class="line">                &#123;</span><br><span class="line">                    methodAccessCacheValue = Boolean.FALSE;</span><br><span class="line">                    _methodAccessCache.put(method, methodAccessCacheValue);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                methodAccessCacheValue = Boolean.FALSE;</span><br><span class="line">                _methodAccessCache.put(method, methodAccessCacheValue);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 如果不可访问标记为同步执行</span></span><br><span class="line">        syncInvoke = Boolean.TRUE.equals(methodAccessCacheValue);</span><br><span class="line"></span><br><span class="line">        methodPermCacheValue = _methodPermCache.get(method);</span><br><span class="line">        <span class="keyword">if</span> (methodPermCacheValue == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (_securityManager != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span></span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="comment">// 检查方法执行权限</span></span><br><span class="line">                    _securityManager.checkPermission(getPermission(method));</span><br><span class="line">                    methodPermCacheValue = Boolean.TRUE;</span><br><span class="line">                    _methodPermCache.put(method, methodPermCacheValue);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (SecurityException ex) &#123;</span><br><span class="line">                    methodPermCacheValue = Boolean.FALSE;</span><br><span class="line">                    _methodPermCache.put(method, methodPermCacheValue);</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> IllegalAccessException(<span class="string">"Method ["</span> + method + <span class="string">"] cannot be accessed."</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                methodPermCacheValue = Boolean.TRUE;</span><br><span class="line">                _methodPermCache.put(method, methodPermCacheValue);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        checkPermission = Boolean.FALSE.equals(methodPermCacheValue);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Object result;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (syncInvoke) <span class="comment">//if is not public and is not accessible</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 加锁同步反射调用method，因为需要先将方法设置为可访问，反射调用完，再将其设置为不可访问</span></span><br><span class="line">        <span class="comment">// 如果不加锁，并发时可能导致方法反射调用失败</span></span><br><span class="line">        <span class="comment">// 线程A -&gt; _accessibleObjectHandler.setAccessible(method, false);</span></span><br><span class="line">        <span class="comment">// 线程B -&gt; result = invokeMethodInsideSandbox(target, method, argsArray);</span></span><br><span class="line">        <span class="keyword">synchronized</span>(method)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (checkPermission)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">try</span></span><br><span class="line">                &#123;</span><br><span class="line">                    _securityManager.checkPermission(getPermission(method));</span><br><span class="line">                &#125; <span class="keyword">catch</span> (SecurityException ex) &#123;</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> IllegalAccessException(<span class="string">"Method ["</span> + method + <span class="string">"] cannot be accessed."</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            _accessibleObjectHandler.setAccessible(method, <span class="keyword">true</span>);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                result = invokeMethodInsideSandbox(target, method, argsArray);</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                _accessibleObjectHandler.setAccessible(method, <span class="keyword">false</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (checkPermission)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">try</span></span><br><span class="line">            &#123;</span><br><span class="line">                _securityManager.checkPermission(getPermission(method));</span><br><span class="line">            &#125; <span class="keyword">catch</span> (SecurityException ex) &#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> IllegalAccessException(<span class="string">"Method ["</span> + method + <span class="string">"] cannot be accessed."</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        result = invokeMethodInsideSandbox(target, method, argsArray);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>完整源码请查看：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL29ycGhhbi1vc3Mvb2dubC9ibG9iL09HTkxfM18zXzAvc3JjL21haW4vamF2YS9vZ25sL09nbmxSdW50aW1lLmphdmEjTDExNTE=" title="https://github.com/orphan-oss/ognl/blob/OGNL_3_3_0/src/main/java/ognl/OgnlRuntime.java#L1151">https://github.com/orphan-oss/ognl/blob/OGNL_3_3_0/src/main/java/ognl/OgnlRuntime.java#L1151<i class="fa fa-external-link"></i></span></p>
<p>我们项目mybatis的版本是3.5.9，对应的ognl版本是3.3.0，从<code>/mybatis-3.5.9.jar!/META-INF/maven/ognl/ognl/pom.xml</code>中可以看到对应的版本，mybatis是将ognl的class直接构建到了mybatis的jar包中了。</p>
</blockquote>
<p>从上面的源码分析结合堆栈分析，所有的线程都阻塞在1151行<code>synchronized(method) {</code>，等待获取<code>method</code>的访问权限和执行权限，证明有大量线程要去调用同一个<code>method</code>对象，这是为什么呢？</p>
<h3 id="问题剖析"><a class="header-anchor" href="#问题剖析">¶</a>问题剖析</h3>
<p>为什么有大量线程在mybatis的查询中都在反射调用相同的方法呢，这就要从MyBatis-Plus说起了，项目使用的是MyBatis-Plus做动态SQL拼装，例如下面的动态SQL：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LambdaQueryWrapper&lt;User&gt; wrapper = <span class="keyword">new</span> LambdaQueryWrapper&lt;&gt;();</span><br><span class="line">wrapper.eq(User::getName, <span class="string">"A"</span>)      </span><br><span class="line">       .eq(User::getAge, <span class="number">20</span>)        </span><br><span class="line">       .like(User::getEmail, <span class="string">"a"</span>)   </span><br><span class="line">       .gt(User::getScore, <span class="number">60</span>)      </span><br><span class="line">       .orderByAsc(User::getId);    </span><br><span class="line">       </span><br><span class="line">   service.list(wrapper);</span><br></pre></td></tr></table></figure>
<p>实际上调用的是<code>com.baomidou.mybatisplus.core.mapper.BaseMapper.selectList</code>方法，其动态SQL的XML如下:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" standalone="no"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"ew != null and ew.sqlFirst != null"</span>&gt;</span></span></span><br><span class="line">    $&#123;ew.sqlFirst&#125;</span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span></span><br><span class="line">     SELECT </span><br><span class="line"><span class="xml">    <span class="tag">&lt;<span class="name">choose</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">when</span> <span class="attr">test</span>=<span class="string">"ew != null and ew.sqlSelect != null"</span>&gt;</span></span></span><br><span class="line">        $&#123;ew.sqlSelect&#125;</span><br><span class="line"><span class="xml">        <span class="tag">&lt;/<span class="name">when</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">otherwise</span>&gt;</span>ID,NAME,AGE,EMAIL,SCORE<span class="tag">&lt;/<span class="name">otherwise</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">choose</span>&gt;</span></span></span><br><span class="line">     FROM user </span><br><span class="line"><span class="xml">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"ew != null"</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">where</span>&gt;</span></span></span><br><span class="line"><span class="xml">            <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"ew.entity != null"</span>&gt;</span></span></span><br><span class="line"><span class="xml">                <span class="comment">&lt;!--  --&gt;</span></span></span><br><span class="line"><span class="xml">                <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"ew.entity['id'] != null"</span>&gt;</span> AND ID=#&#123;ew.entity.idB&#125;<span class="tag">&lt;/<span class="name">if</span>&gt;</span></span></span><br><span class="line"><span class="xml">            <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span></span><br><span class="line"><span class="xml">            <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"ew.sqlSegment != null and ew.sqlSegment != '' and ew.nonEmptyOfWhere"</span>&gt;</span></span></span><br><span class="line"><span class="xml">                <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"ew.nonEmptyOfEntity and ew.nonEmptyOfNormal"</span>&gt;</span> AND<span class="tag">&lt;/<span class="name">if</span>&gt;</span></span></span><br><span class="line">                $&#123;ew.sqlSegment&#125;</span><br><span class="line"><span class="xml">            <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/<span class="name">where</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"ew.sqlSegment != null and ew.sqlSegment != '' and ew.emptyOfWhere"</span>&gt;</span></span></span><br><span class="line">         $&#123;ew.sqlSegment&#125;</span><br><span class="line"><span class="xml">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"ew != null and ew.sqlComment != null"</span>&gt;</span></span></span><br><span class="line">    $&#123;ew.sqlComment&#125;</span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>其中<code>BaseMapper.selectList</code>方法的SQL脚本是由<code>com.baomidou.mybatisplus.core.injector.methods.SelectList</code>注入的。<br>
SQL注入器的详细文档请参考：<span class="exturl" data-url="aHR0cHM6Ly9iYW9taWRvdS5jb20vZ3VpZGVzL3NxbC1pbmplY3Rvci8=" title="https://baomidou.com/guides/sql-injector/">https://baomidou.com/guides/sql-injector/<i class="fa fa-external-link"></i></span></p>
</blockquote>
<p>从上面的动态SQL分析可以发现，<strong>每一个动态SQL在mybatis解析OGNL表达式时都必然会通过反射获取<code>Wrapper</code>对象这些属性的值：<code>sqlFirst</code>、<code>sqlSelect</code>、<code>sqlSegment</code>、<code>nonEmptyOfEntity</code>、<code>sqlComment</code></strong>。再高并发的时候，都会通过反射调用这些属性对应的get方法获取这些属性的值，那么<code>synchronized(method)</code>锁的都是相同的方法也就不奇怪了。</p>
<h2 id="解决方案"><a class="header-anchor" href="#解决方案">¶</a>解决方案</h2>
<p>从上面的线程堆栈以及源码分析，我们知道了问题产生的原因，那么我们将<code>synchronized(method)</code>锁的的竞争降低就可以解决问题，于是我们可以这样修改<code>Ognl</code>的源码，给<code>method</code>加上<code>Double null ckeck</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">invokeMethod</span><span class="params">(Object target, Method method, Object[] argsArray)</span></span></span><br><span class="line"><span class="function">        <span class="keyword">throws</span> InvocationTargetException, IllegalAccessException </span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> syncInvoke;</span><br><span class="line">    Boolean methodAccessCacheValue;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment">// only synchronize method invocation if it actually requires it</span></span><br><span class="line">    methodAccessCacheValue = _methodAccessCache.get(method);</span><br><span class="line">    <span class="comment">// double null check to avoid synchronizing on the method</span></span><br><span class="line">    <span class="keyword">if</span> (methodAccessCacheValue == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">synchronized</span> (method) &#123;</span><br><span class="line">            methodAccessCacheValue = _methodAccessCache.get(method);</span><br><span class="line">            <span class="keyword">if</span> (methodAccessCacheValue == <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (!Modifier.isPublic(method.getModifiers()) || !Modifier.isPublic(method.getDeclaringClass().getModifiers())) &#123;</span><br><span class="line">                    <span class="keyword">var</span> obj = Modifier.isStatic(method.getModifiers()) ? <span class="keyword">null</span> : target;</span><br><span class="line">                    <span class="keyword">if</span> (method.canAccess(obj)) &#123;</span><br><span class="line">                        methodAccessCacheValue = Boolean.FALSE;</span><br><span class="line">                        _methodAccessCache.put(method, methodAccessCacheValue);</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        methodAccessCacheValue = Boolean.TRUE;</span><br><span class="line">                        _methodAccessCache.put(method, methodAccessCacheValue);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    methodAccessCacheValue = Boolean.FALSE;</span><br><span class="line">                    _methodAccessCache.put(method, methodAccessCacheValue);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            syncInvoke = Boolean.TRUE.equals(methodAccessCacheValue);</span><br><span class="line"></span><br><span class="line">            _methodPermCache.putIfAbsent(method, Boolean.TRUE);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        syncInvoke = Boolean.TRUE.equals(methodAccessCacheValue);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>上面是基于<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL29ycGhhbi1vc3Mvb2dubA==" title="https://github.com/orphan-oss/ognl">https://github.com/orphan-oss/ognl<i class="fa fa-external-link"></i></span>最新的源码修改，和本文项目中用到的<code>3.3.0</code>版本有细微差异，但问题也同样存在。</p>
</blockquote>
<p>经过如上修改<code>OgnlRuntime#invokeMethod</code>源码升级项目后，再也没出现过<code>OgnlRuntime.invokeMethod</code>阻塞的问题了。</p>
<p>具体的issue和Pull request请查看：</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL215YmF0aXMvbXliYXRpcy0zL2lzc3Vlcy8zNTg5" title="https://github.com/mybatis/mybatis-3/issues/3589">https://github.com/mybatis/mybatis-3/issues/3589<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL29ycGhhbi1vc3Mvb2dubC9wdWxsLzUyMQ==" title="https://github.com/orphan-oss/ognl/pull/521">https://github.com/orphan-oss/ognl/pull/521<i class="fa fa-external-link"></i></span></li>
</ul>
<p>OGNL社区已采纳，已合并到3.4.10版本发布。</p>
<p>参考资料：</p>
<ul>
<li>
<p>OGNL 源码：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL29ycGhhbi1vc3Mvb2dubC9ibG9iL09HTkxfM18zXzAvc3JjL21haW4vamF2YS9vZ25sL09nbmxSdW50aW1lLmphdmEjTDExNTE=" title="https://github.com/orphan-oss/ognl/blob/OGNL_3_3_0/src/main/java/ognl/OgnlRuntime.java#L1151">https://github.com/orphan-oss/ognl/blob/OGNL_3_3_0/src/main/java/ognl/OgnlRuntime.java#L1151<i class="fa fa-external-link"></i></span></p>
</li>
<li>
<p>MyBatis-Plus 官方文档（SQL注入器）：<span class="exturl" data-url="aHR0cHM6Ly9iYW9taWRvdS5jb20vZ3VpZGVzL3NxbC1pbmplY3Rvci8=" title="https://baomidou.com/guides/sql-injector/">https://baomidou.com/guides/sql-injector/<i class="fa fa-external-link"></i></span></p>
</li>
<li>
<p>向MyBatis社区提交的Issue：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL215YmF0aXMvbXliYXRpcy0zL2lzc3Vlcy8zNTg5" title="https://github.com/mybatis/mybatis-3/issues/3589">https://github.com/mybatis/mybatis-3/issues/3589<i class="fa fa-external-link"></i></span></p>
</li>
<li>
<p>向OGNL社区提交的Pull Request：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL29ycGhhbi1vc3Mvb2dubC9wdWxsLzUyMQ==" title="https://github.com/orphan-oss/ognl/pull/521">https://github.com/orphan-oss/ognl/pull/521<i class="fa fa-external-link"></i></span></p>
</li>
</ul>
]]></content>
      <categories>
        <category>性能优化</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>MyBatis</tag>
        <tag>OGNL</tag>
      </tags>
  </entry>
  <entry>
    <title>性能优化-巧解MyBatis高并发下OGNL安全检查导致的全局锁瓶颈</title>
    <url>/performance/mybatis-ognl-security.html</url>
    <content><![CDATA[<h1 id="性能优化-巧解mybatis高并发下ognl安全检查导致的全局锁瓶颈"><a class="header-anchor" href="#性能优化-巧解mybatis高并发下ognl安全检查导致的全局锁瓶颈">¶</a>性能优化-巧解MyBatis高并发下OGNL安全检查导致的全局锁瓶颈</h1>
<blockquote>
<p>摘要：线上接口频繁超时，HSF线程池爆满。经排查，发现大量线程并非阻塞在数据库，而是卡在System.getProperty方法上。其根本原因是MyBatis使用的OGNL表达式引擎在每次解析动态SQL时，都会同步查询ognl.security.manager系统属性。由于Properties使用synchronized全局锁，高并发下引发严重性能瓶颈。最终通过为JVM添加-Dognl.security.manager=forceDisableOnInit参数，在初始化时即禁用安全检查，从而彻底避免了锁竞争，系统性能得到显著恢复。</p>
</blockquote>
<h2 id="背景"><a class="header-anchor" href="#背景">¶</a>背景</h2>
<p>线上环境频繁报接口响应超时，很多节点同时报<code>[HSF-Provider] HSF thread pool is full.</code>，于是我们dump线程信息进行分析，发现居然大部分线程都卡在mybatis查询数据库了，我们难道遇到mybatis的性能问题了吗？</p>
<h2 id="问题排查过程"><a class="header-anchor" href="#问题排查过程">¶</a>问题排查过程</h2>
<h3 id="线程堆栈分析"><a class="header-anchor" href="#线程堆栈分析">¶</a>线程堆栈分析</h3>
<p>分析dump出来的线程堆栈信息，发现有752个线程都<code>BLOCKED</code>在<code>java.lang.System.getProperty</code>方法，该方法是获取JVM或操作系统中的系统属性值，具体堆栈信息如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 752个等待获取锁的线程</span><br><span class="line">&quot;HSFBizProcessor-DEFAULT-6-thread-4702&quot; #27229 daemon prio=10 os_prio=0 tid=0x00007f032c110800 nid=0x6a50 waiting for monitor entry [0x00007efeeaae8000]</span><br><span class="line">   java.lang.Thread.State: BLOCKED (on object monitor)</span><br><span class="line">	at java.util.Hashtable.get(Hashtable.java:363)</span><br><span class="line">	- waiting to lock &lt;0x00000004401b0198&gt; (a java.util.Properties)</span><br><span class="line">	at java.util.Properties.getProperty(Properties.java:969)</span><br><span class="line">	at java.lang.System.getProperty(System.java:741)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.invokeMethodInsideSandbox(OgnlRuntime.java:1244)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.invokeMethod(OgnlRuntime.java:1230)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.getMethodValue(OgnlRuntime.java:2146)</span><br><span class="line">	....</span><br><span class="line">	</span><br><span class="line"># 获取到锁的线程</span><br><span class="line">&quot;task-46&quot; #834 prio=5 os_prio=0 tid=0x00007f02a4075000 nid=0x336 waiting for monitor entry [0x00007eff47135000]</span><br><span class="line">   java.lang.Thread.State: BLOCKED (on object monitor)</span><br><span class="line">	at java.util.Hashtable.get(Hashtable.java:363)</span><br><span class="line">	- locked &lt;0x00000004401b0198&gt; (a java.util.Properties)</span><br><span class="line">	at java.util.Properties.getProperty(Properties.java:969)</span><br><span class="line">	at java.lang.System.getProperty(System.java:741)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.invokeMethodInsideSandbox(OgnlRuntime.java:1244)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.invokeMethod(OgnlRuntime.java:1230)</span><br><span class="line">	at org.apache.ibatis.ognl.OgnlRuntime.getMethodValue(OgnlRuntime.java:2146)</span><br><span class="line">	...</span><br></pre></td></tr></table></figure>
<h3 id="源码分析"><a class="header-anchor" href="#源码分析">¶</a>源码分析</h3>
<p>从上面的堆栈信息可以看到全部都阻塞在<code>java.util.Hashtable.get</code>方法，Properties是继承Hashtable的，而众所周知Hashtable是线程安全的，我们来一起看看JDK中这两个类涉及的部分源码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Properties</span> <span class="keyword">extends</span> <span class="title">Hashtable</span>&lt;<span class="title">Object</span>,<span class="title">Object</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getProperty</span><span class="params">(String key)</span> </span>&#123;</span><br><span class="line">        Object oval = <span class="keyword">super</span>.get(key);</span><br><span class="line">        String sval = (oval <span class="keyword">instanceof</span> String) ? (String)oval : <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">return</span> ((sval == <span class="keyword">null</span>) &amp;&amp; (defaults != <span class="keyword">null</span>)) ? defaults.getProperty(key) : sval;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Hashtable</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="keyword">extends</span> <span class="title">Dictionary</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="keyword">implements</span> <span class="title">Map</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt;, <span class="title">Cloneable</span>, <span class="title">java</span>.<span class="title">io</span>.<span class="title">Serializable</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> V <span class="title">get</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">        Entry&lt;?,?&gt; tab[] = table;</span><br><span class="line">        <span class="keyword">int</span> hash = key.hashCode();</span><br><span class="line">        <span class="keyword">int</span> index = (hash &amp; <span class="number">0x7FFFFFFF</span>) % tab.length;</span><br><span class="line">        <span class="keyword">for</span> (Entry&lt;?,?&gt; e = tab[index] ; e != <span class="keyword">null</span> ; e = e.next) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((e.hash == hash) &amp;&amp; e.key.equals(key)) &#123;</span><br><span class="line">                <span class="keyword">return</span> (V)e.value;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>synchronized</code>是悲观锁，在高并发情况下确实会有性能问题，那么为什么会有这么大的并发量同时获取系统属性呢，我们一起来看mybatis中<code>OgnlRuntime.invokeMethodInsideSandbox</code>方法中1244行在干什么呢：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> Object <span class="title">invokeMethodInsideSandbox</span><span class="params">(Object target, Method method, Object[] argsArray)</span></span></span><br><span class="line"><span class="function">        <span class="keyword">throws</span> InvocationTargetException, IllegalAccessException </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (_disableOgnlSecurityManagerOnInit) &#123;</span><br><span class="line">        <span class="keyword">return</span> method.invoke(target, argsArray);  <span class="comment">// Feature was disabled at OGNL initialization.</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 这里就是1244行，所有线程都阻塞到这里了</span></span><br><span class="line">        <span class="keyword">if</span> (System.getProperty(<span class="string">"ognl.security.manager"</span>) == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> method.invoke(target, argsArray);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (SecurityException ignored) &#123;</span><br><span class="line">        <span class="comment">// already enabled or user has applied a policy that doesn't allow read property so we have to honor user's sandbox</span></span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>ognl.security.manager</code>属性是为了增强OGNL表达式执行的安全性而引入的。通过启用它，可以防止恶意的OGNL表达式执行危险操作。</p>
<p>项目使用的是MyBatis-Plus进行动态SQL拼装，大量数据库查询都要解析OGNL表达式，每一次OGNL表达式解析都要获取一次<code>ognl.security.manager</code>属性，所以出现了大量线程阻塞在<code>java.util.Hashtable.get</code>这个方法。</p>
<blockquote>
<p>完整源码请查看：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL29ycGhhbi1vc3Mvb2dubC9ibG9iL09HTkxfM18zXzAvc3JjL21haW4vamF2YS9vZ25sL09nbmxSdW50aW1lLmphdmEjTDEyMzY=" title="https://github.com/orphan-oss/ognl/blob/OGNL_3_3_0/src/main/java/ognl/OgnlRuntime.java#L1236">https://github.com/orphan-oss/ognl/blob/OGNL_3_3_0/src/main/java/ognl/OgnlRuntime.java#L1236<i class="fa fa-external-link"></i></span></p>
<p>我们项目mybatis的版本是3.5.9，对应的ognl版本是3.3.0，从<code>/mybatis-3.5.9.jar!/META-INF/maven/ognl/ognl/pom.xml</code>中可以看到对应的版本，mybatis是将ognl的class直接构建到了mybatis的jar包中了。</p>
</blockquote>
<h2 id="解决方案"><a class="header-anchor" href="#解决方案">¶</a>解决方案</h2>
<p>上面我们通过源码分析到是由于大量的线程都在同时执行<code>System.getProperty(&quot;ognl.security.manager&quot;)</code>获取属性，而<code>java.util.Hashtable.get</code>这个方法是同步的，我们可以从减少<code>System.getProperty(&quot;ognl.security.manager&quot;)</code>的调用量的思路入手。</p>
<p>我们继续来看<code>OgnlRuntime.invokeMethodInsideSandbox</code>方法，当<code>_disableOgnlSecurityManagerOnInit</code>属性为true时直接反射调用方法返回结果。<code>_disableOgnlSecurityManagerOnInit</code>初始化逻辑如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Control usage of the OGNL Security Manager using the JVM option:</span></span><br><span class="line"><span class="comment"> *   -Dognl.security.manager=true  (or any non-null value other than 'disable')</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Omit '-Dognl.security.manager=' or nullify the property to disable the feature.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * To forcibly disable the feature (only possible at OGNL Library initialization, use the option:</span></span><br><span class="line"><span class="comment"> *   -Dognl.security.manager=forceDisableOnInit</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Users that have their own Security Manager implementations and no intention to use the OGNL SecurityManager</span></span><br><span class="line"><span class="comment"> *   sandbox may choose to use the 'forceDisableOnInit' flag option for performance reasons (avoiding overhead</span></span><br><span class="line"><span class="comment"> *   involving the system property security checks - when that feature will not be used).</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> String OGNL_SECURITY_MANAGER = <span class="string">"ognl.security.manager"</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> String OGNL_SM_FORCE_DISABLE_ON_INIT = <span class="string">"forceDisableOnInit"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Hold environment flag state associated with OGNL_SECURITY_MANAGER.  See</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@link</span> OgnlRuntime#OGNL_SECURITY_MANAGER&#125; for more details.</span></span><br><span class="line"><span class="comment"> *   Default: false (if not set).</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">boolean</span> _disableOgnlSecurityManagerOnInit;</span><br><span class="line"><span class="keyword">static</span> &#123;</span><br><span class="line">    <span class="keyword">boolean</span> initialFlagState = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> String propertyString = System.getProperty(OGNL_SECURITY_MANAGER);</span><br><span class="line">        <span class="keyword">if</span> (propertyString != <span class="keyword">null</span> &amp;&amp; propertyString.length() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            initialFlagState = OGNL_SM_FORCE_DISABLE_ON_INIT.equalsIgnoreCase(propertyString);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">        <span class="comment">// Unavailable (SecurityException, etc.)</span></span><br><span class="line">    &#125;</span><br><span class="line">    _disableOgnlSecurityManagerOnInit = initialFlagState;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>因此在jvm参数中添加<code>-Dognl.security.manager=forceDisableOnInit</code>即可在初始化时禁用<br>
OGNL SecurityManager。</p>
<p>由于目前项目并没有配置<code>ognl.security.manager</code>属性，<code>if (System.getProperty(&quot;ognl.security.manager&quot;) == null)</code>始终为true，大量无效调用<code>System.getProperty(&quot;ognl.security.manager&quot;)</code>。因此修改应用的jvm配置添加<code>-Dognl.security.manager=forceDisableOnInit</code>即可解决高并发下OGNL获取<code>ognl.security.manager</code>属性导致的性能问题。</p>
<blockquote>
<p><strong><font color="red">注意：OGNL SecurityManager处于关闭状态，为保证系统安全，在编写动态SQL时应该避免在 OGNL 表达式中使用用户可控的参数并结合其它手段防止恶意注入</font></strong></p>
</blockquote>
<p>参考资料：</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL29ycGhhbi1vc3Mvb2dubC9ibG9iL09HTkxfM18zXzAvc3JjL21haW4vamF2YS9vZ25sL09nbmxSdW50aW1lLmphdmEjTDEyMzY=" title="https://github.com/orphan-oss/ognl/blob/OGNL_3_3_0/src/main/java/ognl/OgnlRuntime.java#L1236">https://github.com/orphan-oss/ognl/blob/OGNL_3_3_0/src/main/java/ognl/OgnlRuntime.java#L1236<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>性能优化</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>MyBatis</tag>
        <tag>OGNL</tag>
      </tags>
  </entry>
  <entry>
    <title>性能优化-高效生成全局唯一自增序列</title>
    <url>/performance/select-for-update.html</url>
    <content><![CDATA[<h1 id="性能优化-高效生成全局唯一自增序列"><a class="header-anchor" href="#性能优化-高效生成全局唯一自增序列">¶</a>性能优化-高效生成全局唯一自增序列</h1>
<p>如何高效地生成全局唯一自增序列，一个设置让性能提升100倍。</p>
<h2 id="背景"><a class="header-anchor" href="#背景">¶</a>背景</h2>
<p>系统上线前性能测试时，对生成具有业务含义的全局唯一且自增的序列进行压测；200并发，4个8C16G的应用节点，压测结果如下：</p>
<table>
<thead>
<tr>
<th>类名名</th>
<th>方法名</th>
<th>调用次数</th>
<th>avg</th>
<th>min</th>
<th>max</th>
<th>90% pct</th>
<th>95% pct</th>
<th>99% pct</th>
</tr>
</thead>
<tbody>
<tr>
<td>LockUtils</td>
<td>forUpdateLockAndRun</td>
<td>5505</td>
<td>3821</td>
<td>11</td>
<td>35020</td>
<td>9110</td>
<td>11014</td>
<td>18045</td>
</tr>
<tr>
<td>LockExecutor</td>
<td>executeLock</td>
<td>5505</td>
<td>36</td>
<td>6</td>
<td>389</td>
<td>88</td>
<td>122</td>
<td>208</td>
</tr>
</tbody>
</table>
<p>这结果可以说是惨不忍睹～～～</p>
<blockquote>
<p>如何获取方法级别的性能测试报告，详情请查看：<span class="exturl" data-url="aHR0cHM6Ly9tcC53ZWl4aW4ucXEuY29tL3MvVlFUc3Z0V29jUXg3dmVPM3NhTlVxQQ==" title="https://mp.weixin.qq.com/s/VQTsvtWocQx7veO3saNUqA">性能优化利器-JavaAgent<i class="fa fa-external-link"></i></span></p>
</blockquote>
<h2 id="生成全局唯一自增序列"><a class="header-anchor" href="#生成全局唯一自增序列">¶</a>生成全局唯一自增序列</h2>
<p>生成具有业务含义的全局唯一且自增的序列，是采用SELECT FOR UPDATE方案实现，生成逻辑：</p>
<ul>
<li>1、<code>select for update</code>获取数据库序列配置的行锁</li>
<li>2、对序列配置进行自增操作</li>
<li>3、更新序列配置</li>
<li>如果任何一步失败则进行重试，最多重试三次</li>
</ul>
<p>核心源码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> TransactionDefinition TRANSACTION_DEFINITION = <span class="keyword">new</span> DefaultTransactionDefinition();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">forUpdateLockAndRun</span><span class="params">(Class&lt;IEntity&gt; lockEntityClass, QueryWrapper&lt;IEntity&gt; selectForUpdate, LockExecutor lockExecutor, <span class="keyword">int</span> retryTimes)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (retryTimes &gt;= <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ResourceUpdateFailedException(<span class="string">"tryForUpdateLockAndRun失败，重试次数已达到上限，请稍后再试"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    BaseMapper&lt;IEntity&gt; mapper = DB_OPERATE_SERVICE.getMapperByEntityClass(lockEntityClass);</span><br><span class="line">    TransactionStatus transactionStatus = TRANSACTION_MANAGER.getTransaction(TRANSACTION_DEFINITION);</span><br><span class="line">    IEntity lockData = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        selectForUpdate.last(<span class="string">" for update"</span>);</span><br><span class="line">        lockData = mapper.selectOne(selectForUpdate);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        log.error(<span class="string">"tryForUpdateLockAndRun异常，获取锁失败，进行重试"</span>, e);</span><br><span class="line">        TRANSACTION_MANAGER.rollback(transactionStatus);</span><br><span class="line">        forUpdateLockAndRun(lockEntityClass, selectForUpdate, lockExecutor, retryTimes + <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    Assert.notNull(lockData, <span class="string">"tryForUpdateLockAndRun异常，未获取到需要锁定的数据，无法进行重试"</span>);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 对lockData进行自增操作</span></span><br><span class="line">        IEntity newLockData = lockExecutor.executeLock(lockData);</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">null</span> != newLockData) &#123;</span><br><span class="line">            selectForUpdate.last(<span class="string">""</span>);</span><br><span class="line">            mapper.update(newLockData, selectForUpdate);</span><br><span class="line">        &#125;</span><br><span class="line">        TRANSACTION_MANAGER.commit(transactionStatus);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        log.error(<span class="string">"tryForUpdateLockAndRun获取到锁，但更新数据失败，进行重试"</span>, e);</span><br><span class="line">        TRANSACTION_MANAGER.rollback(transactionStatus);</span><br><span class="line">        forUpdateLockAndRun(lockEntityClass, selectForUpdate, lockExecutor, retryTimes + <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">interface</span> <span class="title">LockExecutor</span> </span>&#123;</span><br><span class="line">    <span class="function">IEntity <span class="title">executeLock</span><span class="params">(IEntity lockData)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="什么是select-for-update"><a class="header-anchor" href="#什么是select-for-update">¶</a>什么是SELECT FOR UPDATE</h3>
<p><code>SELECT FOR UPDATE</code>是由数据库（MySQL、PostgreSQL 和 Oracle等）提供的一种事务锁定机制，用于在事务中锁定所选的行，以防止其他事务对这些行进行修改。</p>
<p>当一个事务执行<code>SELECT FOR UPDATE</code>语句时，数据库会对查询结果集中的每一行进行加锁。这些锁会一直保持到事务提交或回滚时才会释放。在此期间，其他事务无法对这些被锁定的行进行修改或删除操作，从而确保了数据的一致性。</p>
<p>SELECT FOR UPDATE的实际应用有：</p>
<ul>
<li>生成自增全局唯一标识：生成具有业务含义的全局唯一且自增的序列，避免生成重复序列。</li>
<li>库存管理：确保在扣减库存时，只有一个事务能够成功更新库存数量，避免超卖问题。</li>
<li>账户余额更新：在金融系统中，账户余额的更新需要确保数据的一致性。</li>
<li>订单处理：锁定订单状态，确保订单状态的更新按照预期的顺序执行，避免并发问题</li>
</ul>
<h2 id="性能问题分析过程"><a class="header-anchor" href="#性能问题分析过程">¶</a>性能问题分析过程</h2>
<p>通过方法的性能测试报告中可以发现forUpdateLockAndRun方法执行时间特别长，但executeLock方法的执行时间又特别短，同时查看应用的jbdc日志发现<code>select for update</code>的sql执行时间居然达到35秒：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> t_wybs_scpz <span class="keyword">where</span> <span class="keyword">key</span>=<span class="string">'xxx_wybs'</span> <span class="keyword">for</span> <span class="keyword">update</span> <span class="comment">##^^## select * from t_wybs_scpz where key=? for update</span></span><br><span class="line"> &#123;executed <span class="keyword">in</span> <span class="number">35004</span> msec&#125;</span><br><span class="line">&#123;resultSet <span class="keyword">rows</span> <span class="number">1</span>, <span class="keyword">build</span> <span class="keyword">in</span> <span class="number">0</span> msec&#125;</span><br></pre></td></tr></table></figure>
<h3 id="怀疑缺少索引"><a class="header-anchor" href="#怀疑缺少索引">¶</a>怀疑缺少索引</h3>
<p>查看<code>t_wybs_scpz</code>表结构发现有索引，分析sql的执行计划也确实走了索引：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">explain</span> </span><br><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> t_wybs_scpz <span class="keyword">where</span> <span class="keyword">key</span>=<span class="string">'xxx_wybs'</span> <span class="keyword">for</span> <span class="keyword">update</span></span><br><span class="line"></span><br><span class="line">=================================================================================================</span><br><span class="line">|<span class="keyword">ID</span>|<span class="keyword">OPERATOR</span>                        |<span class="keyword">NAME</span>                                 |EST.ROWS|EST.TIME(us)|</span><br><span class="line"><span class="comment">-------------------------------------------------------------------------------------------------</span></span><br><span class="line">|<span class="number">0</span> |MATERIAL                        |                                     |<span class="number">1</span>       |<span class="number">32</span>          |</span><br><span class="line">|<span class="number">1</span> |└─<span class="keyword">DISTRIBUTED</span> <span class="keyword">FOR</span> <span class="keyword">UPDATE</span>        |                                     |<span class="number">1</span>       |<span class="number">32</span>          |</span><br><span class="line">|<span class="number">2</span> |  └─<span class="keyword">DISTRIBUTED</span> <span class="keyword">TABLE</span> <span class="keyword">RANGE</span> <span class="keyword">SCAN</span>|T_WYBS_SCPZ(I_WYBS_SCPZ_KEY)         |<span class="number">1</span>       |<span class="number">32</span>          |</span><br><span class="line">=================================================================================================</span><br></pre></td></tr></table></figure>
<p>因此，可以排除缺少索引的嫌疑。</p>
<h3 id="怀疑for-update锁表"><a class="header-anchor" href="#怀疑for-update锁表">¶</a>怀疑FOR UPDATE锁表</h3>
<p>虽然<code>SELECT FOR UPDATE</code>是只索引查询返回的行，但是在某些情况下还是会锁表：</p>
<h4 id="没有合适的索引或索引未使用"><a class="header-anchor" href="#没有合适的索引或索引未使用">¶</a>没有合适的索引或索引未使用</h4>
<p>如果查询条件没有使用索引，或者查询的列没有合适的索引，数据库将进行<strong>全表扫描</strong>。数据库进行全表扫描时，需要先将数据加载到内存然后进行匹配（如果某行数据被锁住，这一步就会阻塞），因此即使只锁住一行数据也会表现出锁表的现象。</p>
<p>通过上面的sql执行计划可以确定索引已生效，排除这种可能。</p>
<h4 id="锁升级"><a class="header-anchor" href="#锁升级">¶</a>锁升级</h4>
<p>在某些数据库（如 Oracle）中，锁机制是行级别的。但如果事务中的行锁数量过多，数据库可能会触发锁升级，将行锁升级为表锁。锁升级的发生是为了减少系统开销，但可能会导致表级别的锁定。</p>
<p>通过jdbc日志<code>{resultSet rows 1, build in 0 msec}</code>发现，只返回了一行数据，排除这种可能。</p>
<p>综上所述，基本可以排除锁表的嫌疑。</p>
<h3 id="怀疑事务长时间未结束"><a class="header-anchor" href="#怀疑事务长时间未结束">¶</a>怀疑事务长时间未结束</h3>
<p><code>forUpdateLockAndRun</code>方法中<code>lockExecutor.executeLock(lockData)</code>执行时间非常短（平均只有36ms），也就是说从获取到锁之后到事务结束平均耗时只有36ms。</p>
<p>那么是不是就可以排除事务长时间未结束的嫌疑呢，我们可以本地代码调试验证下：</p>
<ol>
<li>在<code>IEntity newLockData = lockExecutor.executeLock(lockData);</code>添加断点，然后在数据库查询<code>select * from t_wybs_scpz where key='xxx_wybs' for update</code>，发现被锁住不能返回结果，符合预期。</li>
<li>在<code>forUpdateLockAndRun</code>方法执行结束后，然后在数据库查询<code>select * from t_wybs_scpz where key='xxx_wybs' for update</code>，发现被锁住不能返回结果，<strong>不符合预期</strong>。</li>
</ol>
<p>因此可以判断的确是事务长时间未结束导致数据库行锁长时间未释放。</p>
<p><strong>为什么<code>forUpdateLockAndRun</code>方法中事务被commit了，但是事务仍然没有结束呢？</strong></p>
<p>查阅源码发现事务的定义<code>private static TransactionDefinition TRANSACTION_DEFINITION = new DefaultTransactionDefinition();</code>，其中使用事务的传播机制默认使用的<code>PROPAGATION_REQUIRED</code>，通过查阅<code>spring-tx</code>中<code>TransactionDefinition</code>的源码中对其的定义如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Support a current transaction; create a new one if none exists.</span></span><br><span class="line"><span class="comment"> * Analogous to the EJB transaction attribute of the same name.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;This is typically the default setting of a transaction definition,</span></span><br><span class="line"><span class="comment"> * and typically defines a transaction synchronization scope.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">int</span> PROPAGATION_REQUIRED = <span class="number">0</span>;</span><br></pre></td></tr></table></figure>
<p>也就是说<code>forUpdateLockAndRun</code>方法中<code>TransactionStatus transactionStatus = TRANSACTION_MANAGER.getTransaction(TRANSACTION_DEFINITION);</code>开启事务的逻辑为：创建一个事务，如果当前已存在事务则加入到这个事务中。</p>
<p>通过查看<code>forUpdateLockAndRun</code>的上层调用链，发现在上层调用链的入口果然也在事务中：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Transactional</span>(rollbackFor = Exception<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">void</span> <span class="title">saveData</span>() </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 下层调用forUpdateLockAndRun</span></span><br><span class="line">    createWybs()</span><br><span class="line">    </span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>至此终于找到了<code>SELECT FOR UPDATE</code>慢的原因了：由于<code>forUpdateLockAndRun</code>方法中的<code>SELECT FOR UPDATE</code><font color="red">必须要等到外层事务结束后才能释放数据库的行锁</font>，因此高并发下请求<code>forUpdateLockAndRun</code>方法就出现了大量排队的情况。</p>
<h2 id="解决方案"><a class="header-anchor" href="#解决方案">¶</a>解决方案</h2>
<p>将事务定义的传播方式设置为<code>PROPAGATION_REQUIRES_NEW</code>：创建一个新事务，如果当前存在事务，则把当前事务挂起，<code>private static TransactionDefinition TRANSACTION_DEFINITION = new DefaultTransactionDefinition(TransactionDefinition.PROPAGATION_REQUIRES_NEW);</code></p>
<p>调整完成后再次进行压测，结果如下：</p>
<table>
<thead>
<tr>
<th>类名名</th>
<th>方法名</th>
<th>调用次数</th>
<th>avg</th>
<th>min</th>
<th>max</th>
<th>90% pct</th>
<th>95% pct</th>
<th>99% pct</th>
</tr>
</thead>
<tbody>
<tr>
<td>LockUtils</td>
<td>forUpdateLockAndRun</td>
<td>7248</td>
<td>37</td>
<td>14</td>
<td>523</td>
<td>51</td>
<td>106</td>
<td>271</td>
</tr>
<tr>
<td>LockExecutor</td>
<td>executeLock</td>
<td>7248</td>
<td>22</td>
<td>6</td>
<td>251</td>
<td>24</td>
<td>51</td>
<td>107</td>
</tr>
</tbody>
</table>
<p><code>forUpdateLockAndRun</code>的平均耗时从3821ms提升到37ms，性能提升<strong>100倍</strong>。</p>
<p>注意：<strong>将事务定义的传播方式设置为<code>PROPAGATION_REQUIRES_NEW</code>后，锁定的数据修改是单独提交的，如果<code>forUpdateLockAndRun</code>执行成功后（修改已提交到数据库）上层事务处理失败回滚时需要由上层调用方判断<code>forUpdateLockAndRun</code>中修改的数据是否需要回滚，如果需要回滚则需要手动回滚。</strong></p>
<p>如果锁的竞争不是非常大，可以考虑使用乐观锁代替<code>SELECT FOR UPDATE</code>。乐观锁可以通过版本号或时间戳机制来实现并发控制，避免了悲观锁的锁竞争问题。</p>
<blockquote>
<p>这里补充一下Spring的7种事务传播机制：</p>
<ol>
<li>REQUIRED（默认）：如果当前存在事务，则加入该事务；如果当前没有事务，则创建一个新的事务。</li>
<li>SUPPORTS：如果当前存在事务，则加入该事务；如果当前没有事务，则以非事务的方式继续运行。</li>
<li>MANDATORY：如果当前存在事务，则加入该事务；如果当前没有事务，则抛出异常。</li>
<li>REQUIRES_NEW：创建一个新的事务,如果当前存在事务,则把当前事务挂起。</li>
<li>NOT_SUPPORTED：以非事务方式运行，如果当前存在事务，则把当前事务挂起。</li>
<li>NEVER：以非事务方式运行，如果当前存在事务，则抛出异常。</li>
<li>NESTED：如果当前存在事务，则创建一个事务作为当前事务的嵌套事务来运行；如果当前没有事务，则该取值等价于REQUIRED</li>
</ol>
</blockquote>
]]></content>
      <categories>
        <category>性能优化</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>springboot + jpa + h2 单元测试实战</title>
    <url>/unit-test/db-test.html</url>
    <content><![CDATA[<p>最近需要开发一个组件，里面会有数据库操作，但由于该组件是单独开发没有实际的数据库，数据库后期会创建到集成该组件的业务系统里面。所以想到用内存数据来做测试，也避免了拿真实数据库作测试时留下垃圾数据，而且速度还比较快。</p>
<p><img src="https://s2.ax1x.com/2019/07/03/ZtTvnI.png" alt="ZtTvnI.png"></p>
<a id="more"></a>
<p>经历了一顿操作，终于搭好了单元测试环境。</p>
<h1 id="pom文件"><a class="header-anchor" href="#pom文件">¶</a>POM文件</h1>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.projectlombok<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lombok<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-data-jpa<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">&lt;!-- test dependency --&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-test<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.h2database<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>h2<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependencyManagement</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">			<span class="comment">&lt;!-- Import dependency management from Spring Boot --&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-dependencies<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">version</span>&gt;</span>2.1.2.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">type</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">type</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">scope</span>&gt;</span>import<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencyManagement</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h1 id="配置文件application-yml"><a class="header-anchor" href="#配置文件application-yml">¶</a>配置文件application.yml</h1>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">h2:</span></span><br><span class="line">    <span class="attr">console:</span></span><br><span class="line">      <span class="attr">enabled:</span> <span class="literal">true</span></span><br><span class="line">      <span class="attr">path:</span> <span class="string">/console</span></span><br><span class="line">      <span class="attr">settings:</span></span><br><span class="line">        <span class="attr">web-allow-others:</span> <span class="literal">true</span></span><br><span class="line">        <span class="attr">trace:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">dataSource</span></span><br><span class="line">    <span class="attr">platform:</span> <span class="string">h2</span></span><br><span class="line">    <span class="attr">driver-class-name:</span> <span class="string">org.h2.Driver</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">jdbc:h2:mem:test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false;</span></span><br><span class="line">    <span class="attr">username:</span> <span class="string">sa</span></span><br><span class="line">    <span class="attr">password:</span> <span class="number">123456</span></span><br><span class="line">    <span class="attr">schema:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">classpath:sql/create/01.CS_SCHEMA.sql</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">classpath:sql/create/02.CT_TABLE.sql</span></span><br><span class="line">    <span class="attr">data:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">classpath:sql/data/02.I_T_TEST.sql</span></span><br><span class="line">  <span class="attr">jpa:</span></span><br><span class="line">    <span class="attr">show-sql:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">hibernate:</span></span><br><span class="line">      <span class="attr">ddl-auto:</span> <span class="string">none</span></span><br><span class="line">    <span class="attr">database-platform:</span> <span class="string">org.hibernate.dialect.H2Dialect</span></span><br></pre></td></tr></table></figure>
<h1 id="创建do层"><a class="header-anchor" href="#创建do层">¶</a>创建DO层</h1>
<ul>
<li>Entity</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"t_test"</span>, schema = <span class="string">"test"</span>)</span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestDO</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1L</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@Column</span>(name = <span class="string">"id"</span>)</span><br><span class="line">    <span class="keyword">private</span> String id;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// columns定义</span></span><br><span class="line">    <span class="comment">// ···</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>Repository</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Repository</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">TestRepository</span> <span class="keyword">extends</span> <span class="title">JpaRepository</span>&lt;<span class="title">TestDO</span>,<span class="title">String</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>JPA的使用和配置参考淋哥的：<span class="exturl" data-url="aHR0cDovL2FydGVyeS50aHVuaXNvZnQuY29tL3Bvc3RzL2RldGFpbC8wNTFkZjIyMDI0MzY0NTRmYjE0Mzc5ZmUzZTg4YWI3Yg==" title="http://artery.thunisoft.com/posts/detail/051df2202436454fb14379fe3e88ab7b">Spring data jpa使用总结<i class="fa fa-external-link"></i></span></p>
<h1 id="单元测试"><a class="header-anchor" href="#单元测试">¶</a>单元测试</h1>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RunWith</span>(SpringRunner<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">@<span class="title">SpringBootTest</span>(<span class="title">classes</span> </span>=TestApplication<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">TestRepositoryTest</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> TestRepository testRepository;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test_table_should_have_one_record</span><span class="params">()</span></span>&#123;</span><br><span class="line">        Assert.assertEquals(<span class="number">1</span>, testRepository.count());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>TimeLimitTestApplication代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableJpaRepositories</span>(basePackages = <span class="string">"com.teddy.repository"</span>)</span><br><span class="line"><span class="meta">@EntityScan</span>(basePackages = <span class="string">"com.teddy.entity"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestApplication</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="遇到的问题"><a class="header-anchor" href="#遇到的问题">¶</a>遇到的问题</h1>
<ul>
<li>table not found<br>
h2数据库初始化的时候，已经执行了初始化脚本，但是执行单元测试时找不到表，经过排查，发现创建数据库脚本如下：</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span> search_path <span class="keyword">to</span> <span class="keyword">test</span>;</span><br><span class="line"><span class="keyword">commit</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">drop</span> <span class="keyword">table</span> <span class="keyword">if</span> <span class="keyword">exists</span> t_test;</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> t_test(</span><br><span class="line">   ···</span><br><span class="line">);</span><br></pre></td></tr></table></figure>
<p>该脚本在真实数据库中执行没有任何问题，但是在h2数据库中，并不能将表正确的创建到test模式中，而是创建到PUBLIC模式中了，导致找不到表。</p>
<p>修改脚本如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">set search_path to test;</span><br><span class="line">commit;</span><br><span class="line"></span><br><span class="line">drop table <span class="keyword">if</span> exists test.t_test;</span><br><span class="line">create table test.t_test(</span><br><span class="line">   ···</span><br><span class="line">);</span><br></pre></td></tr></table></figure>
<p>即可解决该问题。</p>
<ul>
<li>schema not found<br>
由于JPA不区分大小写，注解里面配置的都会转成小写（可以通过配置：<br>
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl即可解决表名，但是schema都是小写）。而H2数据库是区分大小写的，因此可能导致schema not found。</li>
</ul>
<p>只要统一大小写即可处理。</p>
]]></content>
      <categories>
        <category>单元测试</category>
      </categories>
      <tags>
        <tag>springboot</tag>
        <tag>jpa</tag>
        <tag>h2</tag>
        <tag>单元测试</tag>
      </tags>
  </entry>
  <entry>
    <title>线程池详解</title>
    <url>/mutlithreading/thread-pool.html</url>
    <content><![CDATA[<p>所谓线程池通俗的理解就是有一个池子，里面存放着已经创建好的线程，当有任务提交给线程池执行时，池子中的 某个线程会主动执行该任务。如果池子中的线程数量不够应付数量众多的任务时，则需要自动扩充新的线程到池子中，但是该数量是有限的，就好比池塘的水界线一样。当任务比较少的时候，池子中的线程能够自动回收，释放 资源。为了能够异步地提交任务和缓存未被处理的任务，需要有一个任务队列。</p>
<p><img src="https://s2.ax1x.com/2019/04/13/ALMKeg.png" alt="ALMKeg.png"></p>
<a id="more"></a>
<h1 id="前言"><a class="header-anchor" href="#前言">¶</a>前言</h1>
<p>线程的使用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Thread thread = <span class="keyword">new</span> Thread(()-&gt;&#123;</span><br><span class="line">    System.out.println(<span class="string">"Hello World!"</span>);</span><br><span class="line">&#125;);</span><br><span class="line">thread.start();</span><br></pre></td></tr></table></figure>
<p>缺点：</p>
<ul>
<li>使用一次创建一次</li>
<li>使用之后销毁线程</li>
<li>同时创建大量线程可能导致系统资源耗尽</li>
</ul>
<h1 id="线程池"><a class="header-anchor" href="#线程池">¶</a>线程池</h1>
<p>在面向对象编程中，创建和销毁对象是很费时间的，因为创建一个对象要获取内存资源或者其它更多资源。<br>
在Java中虚拟机将试图跟踪每一个对象，以便能够在对象销毁后进行垃圾回收。所以提高服务程序效率的一个<br>
手段就是尽可能减少创建和销毁对象的次数，特别是一些很耗资源的对象创建和销毁。<br>
如何利用已有对象来服务就是一个解决的关键问题，这也就是&quot;池化资源&quot;技术产生的原因。</p>
<p>线程池是一种多线程处理形式，处理过程中将任务添加到队列，然后在创建线程后自动启动这些任务。</p>
<blockquote>
<p>线程是稀缺资源，使用线程池可以减少创建和销毁线程的次数，每个工作线程都可以重复使用。</p>
<p>可以根据系统的承受能力，调整线程池中工作线程的数量，防止因为消耗过多内存导致服务器崩溃。</p>
</blockquote>
<p>一个线程池包括以下四个基本组成部分：</p>
<ul>
<li>线程池管理器（ThreadPool）：用于创建并管理线程池，包括创建线程池、销毁线程池，添加新任务；</li>
<li>工作线程（PoolWorker）：线程池中线程，在没有任务时处于等待状态，可以循环的执行任务；</li>
<li>任务接口（Task）：每个任务必须实现的接口，以供工作线程调度任务的执行，它主要规定了任务的入口，任务执行完后的收尾工作，任务的执行状态等；</li>
<li>任务队列（taskQueue）：用于存放没有处理的任务。提供一种缓冲机制。</li>
</ul>
<h2 id="线程池的五种状态"><a class="header-anchor" href="#线程池的五种状态">¶</a>线程池的五种状态</h2>
<blockquote>
<p>线程池状态示意图以及五种状态的说明摘自CSDN一只逗比的程序猿</p>
</blockquote>
<p>一共有五种，分别是RUNNING、SHUTDOWN、STOP、TIDYING、TERMINATED</p>
<p>线程池状态切换示意图</p>
<p><span class="exturl" data-url="aHR0cHM6Ly9pbWdjaHIuY29tL2kvQUxNTzBn" title="https://imgchr.com/i/ALMO0g"><img src="https://s2.ax1x.com/2019/04/13/ALMO0g.md.png" alt="ALMO0g.md.png"><i class="fa fa-external-link"></i></span></p>
<ul>
<li>RUNNING<br>
状态说明：线程池处在RUNNING状态时，能够接收新任务，以及对已添加的任务进行处理</li>
</ul>
<p>状态切换：线程池的初始化状态是RUNNING。换句话说，线程池被一旦被创建，就处于RUNNING状态，并且线程池中的任务数为0</p>
<ul>
<li>SHUTDOWN<br>
状态说明：线程池处在SHUTDOWN状态时，不接收新任务，但能处理已添加的任务</li>
</ul>
<p>状态切换：调用线程池的shutdown()接口时，线程池由RUNNING -&gt; SHUTDOWN</p>
<p>注：虽然状态已经不是RUNNING了，但是如果任务队列中还有任务的时候，线程池仍然会继续执行，具体分析请见ThreadPoolExecutor.execute()方法解析</p>
<ul>
<li>STOP<br>
状态说明：线程池处在STOP状态时，不接收新任务，不处理已添加的任务，并且会中断正在处理的任务</li>
</ul>
<p>状态切换：调用线程池的shutdownNow()接口时，线程池由(RUNNING or SHUTDOWN ) -&gt; STOP</p>
<ul>
<li>TIDYING<br>
状态说明：当所有的任务已终止，ctl记录的”任务数量”为0，线程池会变为TIDYING状态。当线程池变为TIDYING状态时，会执行钩子函数terminated()。terminated()在ThreadPoolExecutor类中是空的，若用户想在线程池变为TIDYING时，进行相应的处理；可以通过重载terminated()函数来实现</li>
</ul>
<p>状态切换：当线程池在SHUTDOWN状态下，阻塞队列为空并且线程池中执行的任务也为空时，就会由 SHUTDOWN -&gt; TIDYING。 当线程池在STOP状态下，线程池中执行的任务为空时，就会由STOP -&gt; TIDYING</p>
<ul>
<li>TERMINATED<br>
状态说明：线程池彻底终止，就变成TERMINATED状态</li>
</ul>
<p>状态切换：线程池处在TIDYING状态时，执行完terminated()之后，就会由 TIDYING -&gt; TERMINATED</p>
<p>线程池五种状态的二进制表示</p>
<table>
<thead>
<tr>
<th>线程池状态</th>
<th>二进制</th>
</tr>
</thead>
<tbody>
<tr>
<td>RUNNING</td>
<td>111</td>
</tr>
<tr>
<td>SHUTDOWN</td>
<td>000</td>
</tr>
<tr>
<td>STOP</td>
<td>001</td>
</tr>
<tr>
<td>TIDYING</td>
<td>010</td>
</tr>
<tr>
<td>TERMINATED</td>
<td>011</td>
</tr>
</tbody>
</table>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">COUNT_BITS :29</span><br><span class="line">RUNNING    :11100000 00000000 00000000 00000000</span><br><span class="line">SHUTDOWN   :00000000 00000000 00000000 00000000</span><br><span class="line">STOP       :00100000 00000000 00000000 00000000</span><br><span class="line">TIDYING    :01000000 00000000 00000000 00000000</span><br><span class="line">TERMINATED :01100000 00000000 00000000 00000000</span><br><span class="line">RUNNING    :-536870912</span><br><span class="line">SHUTDOWN   :0</span><br><span class="line">STOP       :536870912</span><br><span class="line">TIDYING    :1073741824</span><br><span class="line">TERMINATED :1610612736</span><br></pre></td></tr></table></figure>
<h2 id="工作线程-poolworker"><a class="header-anchor" href="#工作线程-poolworker">¶</a>工作线程（PoolWorker）</h2>
<p>工作线程是由ThreadPoolExecutor的内部类Worker类实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Class Worker mainly maintains interrupt control state for</span></span><br><span class="line"><span class="comment"> * threads running tasks, along with other minor bookkeeping.</span></span><br><span class="line"><span class="comment"> * This class opportunistically extends AbstractQueuedSynchronizer</span></span><br><span class="line"><span class="comment"> * to simplify acquiring and releasing a lock surrounding each</span></span><br><span class="line"><span class="comment"> * task execution.  This protects against interrupts that are</span></span><br><span class="line"><span class="comment"> * intended to wake up a worker thread waiting for a task from</span></span><br><span class="line"><span class="comment"> * instead interrupting a task being run.  We implement a simple</span></span><br><span class="line"><span class="comment"> * non-reentrant mutual exclusion lock rather than use</span></span><br><span class="line"><span class="comment"> * ReentrantLock because we do not want worker tasks to be able to</span></span><br><span class="line"><span class="comment"> * reacquire the lock when they invoke pool control methods like</span></span><br><span class="line"><span class="comment"> * setCorePoolSize.  Additionally, to suppress interrupts until</span></span><br><span class="line"><span class="comment"> * the thread actually starts running tasks, we initialize lock</span></span><br><span class="line"><span class="comment"> * state to a negative value, and clear it upon start (in</span></span><br><span class="line"><span class="comment"> * runWorker).</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Worker</span></span></span><br><span class="line"><span class="class">    <span class="keyword">extends</span> <span class="title">AbstractQueuedSynchronizer</span></span></span><br><span class="line"><span class="class">    <span class="keyword">implements</span> <span class="title">Runnable</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="comment">/** Thread this worker is running in.  Null if factory fails. */</span></span><br><span class="line">    <span class="keyword">final</span> Thread thread;</span><br><span class="line">    <span class="comment">/** Initial task to run.  Possibly null. */</span></span><br><span class="line">    Runnable firstTask;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Creates with given first task and thread from ThreadFactory.</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> firstTask the first task (null if none)</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    Worker(Runnable firstTask) &#123;</span><br><span class="line">        setState(-<span class="number">1</span>); <span class="comment">// inhibit interrupts until runWorker</span></span><br><span class="line">        <span class="keyword">this</span>.firstTask = firstTask;</span><br><span class="line">        <span class="keyword">this</span>.thread = getThreadFactory().newThread(<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/** Delegates main run loop to outer runWorker  */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        runWorker(<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Worker是实现了Runnable接口，每个Worker中有一个线程属性<code>thread</code>,添加Worker的时候会启动该线程，该线程会循环执行任务，直到线程池停止。</p>
<h2 id="任务接口-task"><a class="header-anchor" href="#任务接口-task">¶</a>任务接口（Task）</h2>
<ul>
<li>Runnable</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * When an object implementing interface &lt;code&gt;Runnable&lt;/code&gt; is used</span></span><br><span class="line"><span class="comment">     * to create a thread, starting the thread causes the object's</span></span><br><span class="line"><span class="comment">     * &lt;code&gt;run&lt;/code&gt; method to be called in that separately executing</span></span><br><span class="line"><span class="comment">     * thread.</span></span><br><span class="line"><span class="comment">     * &lt;p&gt;</span></span><br><span class="line"><span class="comment">     * The general contract of the method &lt;code&gt;run&lt;/code&gt; is that it may</span></span><br><span class="line"><span class="comment">     * take any action whatsoever.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@see</span>     java.lang.Thread#run()</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>任务需要实现Runnable接口，实现抽象方法<code>run</code>,在其中编写具体的任务实现。</p>
<ul>
<li>Callable</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Callable</span>&lt;<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Computes a result, or throws an exception if unable to do so.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> computed result</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> Exception if unable to compute a result</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function">V <span class="title">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>任务实现Callable接口，实现抽象方法<code>call</code>，这个方法有一个返回值，用于获取任务执行的结果。</p>
<p>提交到线程池后，会返回另一个实现了Runnable接口的<code>RunnableFuture</code>,其定义如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">RunnableFuture</span>&lt;<span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">Runnable</span>, <span class="title">Future</span>&lt;<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Sets this Future to the result of its computation</span></span><br><span class="line"><span class="comment">     * unless it has been cancelled.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>RunnableFuture</code>接口继承了<code>Runnable</code>接口和<code>Future</code>接口，可以调用提交到线程池后返回的<code>RunnableFuture</code>的<code>get</code>方法，该方法是阻塞的，直到该任务执行完成。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">get</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException, ExecutionException </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> s = state;</span><br><span class="line">    <span class="keyword">if</span> (s &lt;= COMPLETING)</span><br><span class="line">        s = awaitDone(<span class="keyword">false</span>, <span class="number">0L</span>);</span><br><span class="line">    <span class="keyword">return</span> report(s);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="任务队列"><a class="header-anchor" href="#任务队列">¶</a>任务队列</h2>
<p>任务提交到线程池后，如果没有空闲的线程来执行该任务，则会将其放到任务缓冲队列里面，该队列是一个阻塞队列：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> BlockingQueue&lt;Runnable&gt; workQueue;</span><br></pre></td></tr></table></figure>
<p><code>BlockingQueue</code>4 组不同的方法用于插入、移除以及对队列中的元素进行检查。如果请求的操作不能得到立即执行的话，每个方法的表现也不同。这些方法如下：</p>
<table>
<thead>
<tr>
<th></th>
<th><em>抛出异常</em></th>
<th><em>特殊值</em></th>
<th><em>阻塞</em></th>
<th><em>超时</em></th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>插入</strong></td>
<td><code>add(e)</code></td>
<td><code>offer(e)</code></td>
<td><code>put(e)</code></td>
<td><code>offer(e, time, unit)</code></td>
</tr>
<tr>
<td><strong>移除</strong></td>
<td><code>remove()</code></td>
<td><code>poll()</code></td>
<td><code>take()</code></td>
<td><code>poll(time, unit)</code></td>
</tr>
<tr>
<td><strong>检查</strong></td>
<td><code>element()</code></td>
<td><code>peek()</code></td>
<td><em>不可用</em></td>
<td><em>不可用</em></td>
</tr>
</tbody>
</table>
<p>四组不同的行为方式解释:</p>
<ul>
<li>异常</li>
</ul>
<p>如果试图的操作无法立即执行，抛一个异常。</p>
<ul>
<li>特定值</li>
</ul>
<p>如果试图的操作无法立即执行，返回一个特定的值(常常是 true / false)。</p>
<ul>
<li>阻塞</li>
</ul>
<p>如果试图的操作无法立即执行，该方法调用将会发生阻塞，直到能够执行。</p>
<ul>
<li>超时</li>
</ul>
<p>如果试图的操作无法立即执行，该方法调用将会发生阻塞，直到能够执行，但等待时间不会超过给定值。返回一个特定值以告知该操作是否成功(典型的是 true / false)。</p>
<p>具有以下特点：</p>
<ul>
<li>先进先出（FIFO）</li>
<li>不接受 null 元素</li>
<li>可以是限定容量的</li>
<li>实现主要用于生产者-使用者队列，但它另外还支持 Collection 接口</li>
<li>实现是线程安全的</li>
</ul>
<p><code>BlockingQueue</code>有多种实现，分别满足不同功能的线程池，这里只介绍线程池中常用的队列（其它实现有兴趣的可以深入具体学习）：</p>
<ul>
<li>ArrayBlockingQueue</li>
</ul>
<p>一个由数组支持的有界阻塞队列。此队列按 FIFO（先进先出）原则对元素进行排序。队列的头部是在队列中存在时间最长的元素。队列的尾部 是在队列中存在时间最短的元素。新元素插入到队列的尾部，队列获取操作则是从队列头部开始获得元素。</p>
<ul>
<li>DelayQueue</li>
</ul>
<p>Delayed 元素的一个无界阻塞队列，只有在延迟期满时才能从中提取元素。该队列的头部 是延迟期满后保存时间最长的 Delayed 元素。如果延迟都还没有期满，则队列没有头部，并且 poll 将返回 null。当一个元素的 getDelay(TimeUnit.NANOSECONDS) 方法返回一个小于等于 0 的值时，将发生到期。即使无法使用 take 或 poll 移除未到期的元素，也不会将这些元素作为正常元素对待。</p>
<ul>
<li>LinkedBlockingQueue</li>
</ul>
<p>内部以一个链式结构(链接节点)对其元素进行存储，满足FIFO(先进先出)原则。</p>
<ul>
<li>SynchronousQueue</li>
</ul>
<p>SynchronousQueue 是一个特殊的队列，它的内部同时只能够容纳单个元素。如果该队列已有一元素的话，试图向队列中插入一个新元素的线程将会阻塞，直到另一个线程将该元素从队列中抽走。同样，如果该队列为空，试图向队列中抽取一个元素的线程将会阻塞，直到另一个线程向队列中插入了一条新的元素。</p>
<h2 id="线程池管理器"><a class="header-anchor" href="#线程池管理器">¶</a>线程池管理器</h2>
<h3 id="类图"><a class="header-anchor" href="#类图">¶</a>类图</h3>
<p><img src="https://s2.ax1x.com/2019/03/16/AZCAdH.png" alt="AZCAdH.png"></p>
<ul>
<li>Executor：负责线程的使用与调度的根接口</li>
<li>ExecutorService：Executor的子接口，线程池的主要接口</li>
<li>AbstractExecutorService：实现了ExecutorService接口，基本实现了ExecutorService其中声明的所有方法，另有添加其他方法</li>
<li>ThreadPoolExecutor：继承了AbstractExecutorService，线程池常用实现类</li>
<li>ScheduledExecutorService：继承了ExecutorService，负责线程调度的接口</li>
<li>ScheduledThreadPoolExecutor：继承了ThreadPoolExecutor同时实现了ScheduledExecutorService</li>
</ul>
<h3 id="executors"><a class="header-anchor" href="#executors">¶</a>Executors</h3>
<p>Executors利用工厂模式向我们提供了4种线程池实现方式：</p>
<p><img src="https://s1.ax1x.com/2020/06/14/txXRp9.png" alt="Executors.png"></p>
<ul>
<li>newSingleThreadExecutor</li>
</ul>
<p>创建一个单线程的线程池。这个线程池只有一个线程在工作，也就是相当于单线程串行执行所有任务。如果这个唯一的线程因为异常结束，那么会有一个新的线程来替代它。<br>
此线程池保证所有任务的执行顺序按照任务的提交顺序执行。</p>
<blockquote>
<p>拥有一个存活时间无限长的线程，排队的任务将放入无界队列，适用于一个任务一个任务执行的场景。</p>
</blockquote>
<ul>
<li>newFixedThreadPool</li>
</ul>
<p>创建固定大小的线程池。每次提交一个任务就创建一个线程，直到线程达到线程池的最大大小。<br>
线程池的大小一旦达到最大值就会保持不变，如果某个线程因为执行异常而结束，那么线程池会补充一个新线程。</p>
<blockquote>
<p>每个线程存活的时间无限，适用于执行长期的任务，例如服务器。</p>
</blockquote>
<ul>
<li>newCachedThreadPool</li>
</ul>
<p>创建一个可缓存的线程池。如果线程池的大小超过了处理任务所需要的线程，<br>
那么就会回收部分空闲（60秒不执行任务）的线程，当任务数增加时，此线程池又可以智能的添加新线程来处理任务。<br>
此线程池不会对线程池大小做限制，线程池大小完全依赖于操作系统（或者说JVM）能够创建的最大线程大小。</p>
<blockquote>
<p>可以无限增加线程数，适用于执行很多短期异步的小程序或者负载较轻的服务器。</p>
</blockquote>
<ul>
<li>newScheduledThreadPool</li>
</ul>
<p>创建一个定长线程池，支持定时及周期性任务执行.</p>
<p>!&gt; 阿里巴巴编码规约有一条：</p>
<p>!&gt; 【强制】线程池不允许使用 Executors 去创建，而是通过 ThreadPoolExecutor 的方式，这样的处理方式让写的同学更加明确线程池的运行规则，规避资源耗尽的风险。</p>
<p>大家对线程池的了解吗，<code>newSingleThreadExecutor</code>、<code>newFixedThreadPool</code>、<code>newCachedThreadPool</code>、<code>newScheduledThreadPool</code></p>
<ul>
<li>这些线程池是如何实现的呢？</li>
<li>创建的时候都有哪些参数呢？</li>
</ul>
<h3 id="threadpoolexecutor"><a class="header-anchor" href="#threadpoolexecutor">¶</a>ThreadPoolExecutor</h3>
<h4 id="构造函数"><a class="header-anchor" href="#构造函数">¶</a>构造函数</h4>
<p>先来看一下ThreadPoolExecutor的构造函数吧。</p>
<p><img src="https://s1.ax1x.com/2020/06/14/txO6xI.png" alt="AZHzp4.png"></p>
<p>在这里着重介绍几个参数</p>
<ul>
<li>阻塞队列</li>
</ul>
<p>没有空闲的Worker时，将到达的任务加入队列中。</p>
<pre><code>1.直接传递。SynchronousQueue队列的默认方式，一个存储元素的阻塞队列而是直接投递到线程中。
每一个入队操作必须等到另一个线程调用移除操作，否则入队将一直阻塞。
当处理一些可能有内部依赖的任务时，这种策略避免了加锁操作。
直接传递一般不能限制maximumPoolSizes以避免拒绝 接收新的任务。
如果新增任务的速度大于任务处理的速度就会造成增加无限多的线程的可能性。
    
2.无界队列。如LinkedBlockingQueue，当核心线程正在工作时，使用不用预先定义大小的无界队列将使新到来的任务处理等到中，
所以如果线程数是小于corePoolSize时，将不会创建有入队操作。这种策略将很适合那些相互独立的任务，
如Web服务器。如果新增任务的速度大于任务处理的速度就会造成无界队列一直增长的可能性。
    
3.有界队列。如ArrayBlockingQueue，当定义了maximumPoolSizes时使用有界队列可以预防资源的耗尽，
但是增加了调整和控制队列的难度，队列的大小和线程池的大小是相互影响的，
使用很大的队列和较小的线程池会减少CPU消耗、操作系统资源以及线程上下文开销，但却人为的降低了吞吐量。
如果任务是频繁阻塞型的（I/O），系统是可以把时间片分给多个线程的。而采用较小的队列和较大的线程池，
虽会造成CPU繁忙，但却会遇到调度开销，这也会降低吞吐量。
</code></pre>
<ul>
<li>饱和策略（拒绝接收任务）</li>
</ul>
<p>当Executor调用shutdown方法后或者达到工作队列的最容量时,线程池则已经饱和了，此时则不会接收新的task。但无论是何种情 况，execute方法会调用RejectedExecutionHandler#rejectedExecution方法来执行饱和策略，在线程池内部预定义了几种处理策略：</p>
<pre><code>1.终止执行(AbortPolicy)。默认策略， Executor会抛出一个RejectedExecutionException运行异常到调用者线程来完成终止。

2.调用者线程来运行任务(CallerRunsPolicy)。这种策略会由调用execute方法的线程自身来执行任务，
它提供了一个简单的反馈机制并能降低新任务的提交频率。

3.丢弃策略(DiscardPolicy)。不处理，直接丢弃提交的任务。

4.丢弃队列里最近的一个任务(DiscardOldestPolicy)。如果Executor还未shutdown的话，
则丢弃工作队列的最近的一个任务，然后执行当前任务。
</code></pre>
<h4 id="主要属性"><a class="header-anchor" href="#主要属性">¶</a>主要属性</h4>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * The queue used for holding tasks and handing off to worker</span></span><br><span class="line"><span class="comment"> * threads.  We do not require that workQueue.poll() returning</span></span><br><span class="line"><span class="comment"> * null necessarily means that workQueue.isEmpty(), so rely</span></span><br><span class="line"><span class="comment"> * solely on isEmpty to see if the queue is empty (which we must</span></span><br><span class="line"><span class="comment"> * do for example when deciding whether to transition from</span></span><br><span class="line"><span class="comment"> * SHUTDOWN to TIDYING).  This accommodates special-purpose</span></span><br><span class="line"><span class="comment"> * queues such as DelayQueues for which poll() is allowed to</span></span><br><span class="line"><span class="comment"> * return null even if it may later return non-null when delays</span></span><br><span class="line"><span class="comment"> * expire.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> BlockingQueue&lt;Runnable&gt; workQueue;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Lock held on access to workers set and related bookkeeping.</span></span><br><span class="line"><span class="comment"> * While we could use a concurrent set of some sort, it turns out</span></span><br><span class="line"><span class="comment"> * to be generally preferable to use a lock. Among the reasons is</span></span><br><span class="line"><span class="comment"> * that this serializes interruptIdleWorkers, which avoids</span></span><br><span class="line"><span class="comment"> * unnecessary interrupt storms, especially during shutdown.</span></span><br><span class="line"><span class="comment"> * Otherwise exiting threads would concurrently interrupt those</span></span><br><span class="line"><span class="comment"> * that have not yet interrupted. It also simplifies some of the</span></span><br><span class="line"><span class="comment"> * associated statistics bookkeeping of largestPoolSize etc. We</span></span><br><span class="line"><span class="comment"> * also hold mainLock on shutdown and shutdownNow, for the sake of</span></span><br><span class="line"><span class="comment"> * ensuring workers set is stable while separately checking</span></span><br><span class="line"><span class="comment"> * permission to interrupt and actually interrupting.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Set containing all worker threads in pool. Accessed only when</span></span><br><span class="line"><span class="comment"> * holding mainLock.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> HashSet&lt;Worker&gt; workers = <span class="keyword">new</span> HashSet&lt;Worker&gt;();</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Factory for new threads. All threads are created using this</span></span><br><span class="line"><span class="comment"> * factory (via method addWorker).  All callers must be prepared</span></span><br><span class="line"><span class="comment"> * for addWorker to fail, which may reflect a system or user's</span></span><br><span class="line"><span class="comment"> * policy limiting the number of threads.  Even though it is not</span></span><br><span class="line"><span class="comment"> * treated as an error, failure to create threads may result in</span></span><br><span class="line"><span class="comment"> * new tasks being rejected or existing ones remaining stuck in</span></span><br><span class="line"><span class="comment"> * the queue.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * We go further and preserve pool invariants even in the face of</span></span><br><span class="line"><span class="comment"> * errors such as OutOfMemoryError, that might be thrown while</span></span><br><span class="line"><span class="comment"> * trying to create threads.  Such errors are rather common due to</span></span><br><span class="line"><span class="comment"> * the need to allocate a native stack in Thread.start, and users</span></span><br><span class="line"><span class="comment"> * will want to perform clean pool shutdown to clean up.  There</span></span><br><span class="line"><span class="comment"> * will likely be enough memory available for the cleanup code to</span></span><br><span class="line"><span class="comment"> * complete without encountering yet another OutOfMemoryError.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> ThreadFactory threadFactory;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Handler called when saturated or shutdown in execute.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> RejectedExecutionHandler handler;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Timeout in nanoseconds for idle threads waiting for work.</span></span><br><span class="line"><span class="comment"> * Threads use this timeout when there are more than corePoolSize</span></span><br><span class="line"><span class="comment"> * present or if allowCoreThreadTimeOut. Otherwise they wait</span></span><br><span class="line"><span class="comment"> * forever for new work.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">long</span> keepAliveTime;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Core pool size is the minimum number of workers to keep alive</span></span><br><span class="line"><span class="comment"> * (and not allow to time out etc) unless allowCoreThreadTimeOut</span></span><br><span class="line"><span class="comment"> * is set, in which case the minimum is zero.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> corePoolSize;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Maximum pool size. Note that the actual maximum is internally</span></span><br><span class="line"><span class="comment"> * bounded by CAPACITY.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> maximumPoolSize;</span><br></pre></td></tr></table></figure>
<h4 id="任务的执行"><a class="header-anchor" href="#任务的执行">¶</a>任务的执行</h4>
<p>线程池执行任务的主要方法有：<code>execute</code>、<code>submit</code>，其中submit方法会将传入的Runnable或者Callable封装成RunnableFuture然后调用execute方法，<br>
那么任务具体是如何执行的呢？我的理解大致如下：<br>
<img src="https://s2.ax1x.com/2019/03/22/A8ofeS.png" alt="A8ofeS.png"></p>
<p>其代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable command)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (command == <span class="keyword">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * Proceed in 3 steps:</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * 1. If fewer than corePoolSize threads are running, try to</span></span><br><span class="line"><span class="comment">     * start a new thread with the given command as its first</span></span><br><span class="line"><span class="comment">     * task.  The call to addWorker atomically checks runState and</span></span><br><span class="line"><span class="comment">     * workerCount, and so prevents false alarms that would add</span></span><br><span class="line"><span class="comment">     * threads when it shouldn't, by returning false.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * 2. If a task can be successfully queued, then we still need</span></span><br><span class="line"><span class="comment">     * to double-check whether we should have added a thread</span></span><br><span class="line"><span class="comment">     * (because existing ones died since last checking) or that</span></span><br><span class="line"><span class="comment">     * the pool shut down since entry into this method. So we</span></span><br><span class="line"><span class="comment">     * recheck state and if necessary roll back the enqueuing if</span></span><br><span class="line"><span class="comment">     * stopped, or start a new thread if there are none.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * 3. If we cannot queue task, then we try to add a new</span></span><br><span class="line"><span class="comment">     * thread.  If it fails, we know we are shut down or saturated</span></span><br><span class="line"><span class="comment">     * and so reject the task.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">    <span class="keyword">if</span> (workerCountOf(c) &lt; corePoolSize) &#123;</span><br><span class="line">        <span class="keyword">if</span> (addWorker(command, <span class="keyword">true</span>))</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        c = ctl.get();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (isRunning(c) &amp;&amp; workQueue.offer(command)) &#123;</span><br><span class="line">        <span class="keyword">int</span> recheck = ctl.get();</span><br><span class="line">        <span class="keyword">if</span> (! isRunning(recheck) &amp;&amp; remove(command))</span><br><span class="line">            reject(command);</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (workerCountOf(recheck) == <span class="number">0</span>)</span><br><span class="line">            addWorker(<span class="keyword">null</span>, <span class="keyword">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (!addWorker(command, <span class="keyword">false</span>))</span><br><span class="line">        reject(command);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>execute方法简单总结如下：</p>
<ul>
<li>如果当前线程池里面运行的线程数量小于corePoolSize，则创建新的线程（需要获取全局锁）。</li>
<li>如果当前线程池里面运行的线程数量大于或等于corePoolSize，则将任务加入workQueue中，缓存起来。</li>
<li>如果workQueue已满，但是线程数量小于maximumPoolSize，则继续添加线程（需要再次获取全局锁）。</li>
<li>如果线程数已达到最大线程数了，任务队列也满了，任务将被拒绝，并调用RejectedExecutionHandler的rejectExecution方法。</li>
</ul>
<h4 id="添加工作线程addworker"><a class="header-anchor" href="#添加工作线程addworker">¶</a>添加工作线程addWorker</h4>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 两个参数，firstTask表示需要跑的任务。boolean类型的core参数为true的话表示使用corePoolSize，为false使用maximumPoolSize</span></span><br><span class="line"><span class="comment">// 返回值是boolean类型，true表示新任务被添加了，并且执行了。否则是false</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">addWorker</span><span class="params">(Runnable firstTask, <span class="keyword">boolean</span> core)</span> </span>&#123;</span><br><span class="line">    retry:</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">        <span class="keyword">int</span> rs = runStateOf(c); <span class="comment">// 线程池当前状态</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 这个判断转换成 rs &gt;= SHUTDOWN &amp;&amp; (rs != SHUTDOWN || firstTask != null || workQueue.isEmpty)。 </span></span><br><span class="line">        <span class="comment">// 概括为3个条件：</span></span><br><span class="line">        <span class="comment">// 1. 线程池不在RUNNING状态并且状态是STOP、TIDYING或TERMINATED中的任意一种状态</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 线程池不在RUNNING状态，线程池接受了新的任务 </span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 线程池不在RUNNING状态，阻塞队列为空。  满足这3个条件中的任意一个的话，拒绝执行任务</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (rs &gt;= SHUTDOWN &amp;&amp;</span><br><span class="line">            ! (rs == SHUTDOWN &amp;&amp;</span><br><span class="line">               firstTask == <span class="keyword">null</span> &amp;&amp;</span><br><span class="line">               ! workQueue.isEmpty()))</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">int</span> wc = workerCountOf(c); <span class="comment">// 线程池线程个数</span></span><br><span class="line">            <span class="comment">// 如果线程池线程数量超过线程池最大容量或者线程数量超过了</span></span><br><span class="line">            <span class="comment">// corePoolSize(core参数为true，core参数为false的话判断超过最大大小)</span></span><br><span class="line">            <span class="keyword">if</span> (wc &gt;= CAPACITY ||</span><br><span class="line">                wc &gt;= (core ? corePoolSize : maximumPoolSize)) </span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">false</span>; <span class="comment">// 超过直接返回false</span></span><br><span class="line">            <span class="keyword">if</span> (compareAndIncrementWorkerCount(c)) <span class="comment">// 没有超过各种大小的话，cas操作线程池线程数量+1，cas成功的话跳出循环</span></span><br><span class="line">                <span class="keyword">break</span> retry;</span><br><span class="line">            c = ctl.get();  <span class="comment">// 重新检查状态</span></span><br><span class="line">            <span class="keyword">if</span> (runStateOf(c) != rs) <span class="comment">// 如果状态改变了，重新循环操作</span></span><br><span class="line">                <span class="keyword">continue</span> retry;</span><br><span class="line">            <span class="comment">// else CAS failed due to workerCount change; retry inner loop</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 走到这一步说明cas操作成功了，线程池线程数量+1</span></span><br><span class="line">    <span class="keyword">boolean</span> workerStarted = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">boolean</span> workerAdded = <span class="keyword">false</span>;</span><br><span class="line">    Worker w = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">        w = <span class="keyword">new</span> Worker(firstTask); <span class="comment">// 基于任务firstTask构造worker</span></span><br><span class="line">        <span class="keyword">final</span> Thread t = w.thread; <span class="comment">// 使用Worker的属性thread，这个thread是使用ThreadFactory构造出来的</span></span><br><span class="line">        <span class="keyword">if</span> (t != <span class="keyword">null</span>) &#123; <span class="comment">// ThreadFactory构造出的Thread有可能是null，做个判断</span></span><br><span class="line">            mainLock.lock();  <span class="comment">// 得到线程池的可重入锁</span></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 在锁住之后再重新检测一下状态</span></span><br><span class="line">                <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">                <span class="keyword">int</span> rs = runStateOf(c);</span><br><span class="line">                <span class="comment">// 如果线程池在RUNNING状态或者线程池在SHUTDOWN状态并且任务是个null</span></span><br><span class="line">                <span class="keyword">if</span> (rs &lt; SHUTDOWN ||</span><br><span class="line">                    (rs == SHUTDOWN &amp;&amp; firstTask == <span class="keyword">null</span>)) &#123; </span><br><span class="line">                    <span class="keyword">if</span> (t.isAlive()) <span class="comment">// 判断线程是否还活着，也就是说线程已经启动并且还没死掉</span></span><br><span class="line">                        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalThreadStateException(); <span class="comment">// 如果存在已经启动并且还没死的线程，抛出异常</span></span><br><span class="line">                    workers.add(w); <span class="comment">// worker添加到线程池的workers属性中，是个HashSet</span></span><br><span class="line">                    <span class="keyword">int</span> s = workers.size(); <span class="comment">// 得到目前线程池中的线程个数</span></span><br><span class="line">                    <span class="comment">// 如果线程池中的线程个数超过了线程池中的最大线程数时，更新一下这个最大线程数</span></span><br><span class="line">                    <span class="keyword">if</span> (s &gt; largestPoolSize) </span><br><span class="line">                        largestPoolSize = s;</span><br><span class="line">                    workerAdded = <span class="keyword">true</span>; <span class="comment">// 标识一下Worker已经添加成功</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                mainLock.unlock(); <span class="comment">// 解锁</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (workerAdded) &#123; <span class="comment">// 如果Worker添加成功，运行任务</span></span><br><span class="line">                t.start(); <span class="comment">// 启动线程，启动Worker</span></span><br><span class="line">                workerStarted = <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (! workerStarted) <span class="comment">// 如果任务启动失败，调用addWorkerFailed方法</span></span><br><span class="line">            addWorkerFailed(w);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> workerStarted;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>工作线程添加的方法基本了解了，那么这个Worker是如何运行的呢，又是如何重用，如何执行多个任务的呢？</p>
<h4 id="工作线程的运行"><a class="header-anchor" href="#工作线程的运行">¶</a>工作线程的运行</h4>
<p>Worker在addWorker方法中，当Worker成功添加到workers后，调用Worker.thread启动Worker，在前面<a href="#%E5%B7%A5%E4%BD%9C%E7%BA%BF%E7%A8%8B%EF%BC%88PoolWorker%EF%BC%89">Worker的介绍</a>中了解到run方法直接调用了<br>
ThreadPoolExecutor.runWorker方法具体执行任务，ThreadPoolExecutor.runWorker代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">runWorker</span><span class="params">(Worker w)</span> </span>&#123;</span><br><span class="line">    Thread wt = Thread.currentThread(); <span class="comment">// 得到当前线程</span></span><br><span class="line">    Runnable task = w.firstTask; <span class="comment">// 得到Worker中的任务task，也就是用户传入的task</span></span><br><span class="line">    w.firstTask = <span class="keyword">null</span>; <span class="comment">// 将Worker中的任务置空</span></span><br><span class="line">    w.unlock(); <span class="comment">// allow interrupts。 </span></span><br><span class="line">    <span class="keyword">boolean</span> completedAbruptly = <span class="keyword">true</span>; <span class="comment">// 标识当前Worker异常结束，默认是异常结束</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 如果worker中的任务不为空，执行执行任务</span></span><br><span class="line">        <span class="comment">// 否则使用getTask获得任务。一直循环，除非得到的任务为空才退出</span></span><br><span class="line">        <span class="keyword">while</span> (task != <span class="keyword">null</span> || (task = getTask()) != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 如果拿到了任务，给自己上锁，表示当前Worker已经要开始执行任务了，</span></span><br><span class="line">            <span class="comment">// 已经不是处于闲置Worker(闲置Worker的解释请看下面的线程池关闭)</span></span><br><span class="line">            w.lock();  </span><br><span class="line">            <span class="comment">// 在执行任务之前先做一些处理。 </span></span><br><span class="line">            <span class="comment">// 1. 如果线程池已经处于STOP状态并且当前线程没有被中断，中断线程 </span></span><br><span class="line">            <span class="comment">// 2. 如果线程池还处于RUNNING或SHUTDOWN状态，并且当前线程已经被中断了，</span></span><br><span class="line">            <span class="comment">// 重新检查一下线程池状态，如果处于STOP状态并且没有被中断，那么中断线程</span></span><br><span class="line">            <span class="keyword">if</span> ((runStateAtLeast(ctl.get(), STOP) ||</span><br><span class="line">                 (Thread.interrupted() &amp;&amp;</span><br><span class="line">                  runStateAtLeast(ctl.get(), STOP))) &amp;&amp;</span><br><span class="line">                !wt.isInterrupted())</span><br><span class="line">                wt.interrupt();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 任务执行前需要做什么，ThreadPoolExecutor是个空实现，子类可以自行扩展</span></span><br><span class="line">                beforeExecute(wt, task); </span><br><span class="line">                Throwable thrown = <span class="keyword">null</span>;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">// 真正的开始执行任务，这里run的时候可能会被中断，比如线程池调用了shutdownNow方法</span></span><br><span class="line">                    task.run(); </span><br><span class="line">                &#125; <span class="keyword">catch</span> (RuntimeException x) &#123; <span class="comment">// 任务执行发生的异常全部抛出，不在runWorker中处理</span></span><br><span class="line">                    thrown = x; <span class="keyword">throw</span> x;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Error x) &#123;</span><br><span class="line">                    thrown = x; <span class="keyword">throw</span> x;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Throwable x) &#123;</span><br><span class="line">                    thrown = x; <span class="keyword">throw</span> <span class="keyword">new</span> Error(x);</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    <span class="comment">// 任务执行结束需要做什么，ThreadPoolExecutor是个空实现，子类可以自行扩展</span></span><br><span class="line">                    afterExecute(task, thrown); </span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                task = <span class="keyword">null</span>;</span><br><span class="line">                w.completedTasks++; <span class="comment">// 记录执行任务的个数</span></span><br><span class="line">                w.unlock(); <span class="comment">// 执行完任务之后，解锁，Worker变成闲置Worker，等待执行下一个任务</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        completedAbruptly = <span class="keyword">false</span>; <span class="comment">// 正常结束</span></span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        processWorkerExit(w, completedAbruptly); <span class="comment">// Worker退出时执行</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Worker正常结束或者异常结束时都会调用processWorkerExit方法，当Worker异常结束时（比如执行的任务中抛出了未处理的异常）可能会重新创建一个新的Worker替换上。<br>
processWorkerExit具体实现如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">processWorkerExit</span><span class="params">(Worker w, <span class="keyword">boolean</span> completedAbruptly)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 如果Worker没有正常结束流程调用processWorkerExit方法，worker数量减一。</span></span><br><span class="line">    <span class="comment">// 如果是正常结束的话，在getTask方法里worker数量已经减一了</span></span><br><span class="line">    <span class="keyword">if</span> (completedAbruptly) </span><br><span class="line">        decrementWorkerCount();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">    mainLock.lock(); <span class="comment">// 加锁，防止并发问题</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        completedTaskCount += w.completedTasks; <span class="comment">// 记录总的完成任务数</span></span><br><span class="line">        workers.remove(w); <span class="comment">// 线程池的worker集合删除掉需要回收的Worker</span></span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        mainLock.unlock(); <span class="comment">// 解锁</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    tryTerminate(); <span class="comment">// 尝试结束线程池</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">    <span class="keyword">if</span> (runStateLessThan(c, STOP)) &#123;  <span class="comment">// 如果线程池还未停止，处于RUNNING或者SHUTDOWN状态</span></span><br><span class="line">        <span class="keyword">if</span> (!completedAbruptly) &#123; <span class="comment">// Worker是正常结束流程的话</span></span><br><span class="line">            <span class="keyword">int</span> min = allowCoreThreadTimeOut ? <span class="number">0</span> : corePoolSize; <span class="comment">// 核心线程允许超过空闲时间回收</span></span><br><span class="line">            <span class="keyword">if</span> (min == <span class="number">0</span> &amp;&amp; ! workQueue.isEmpty())</span><br><span class="line">                min = <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">if</span> (workerCountOf(c) &gt;= min) <span class="comment">// 还有在工作的Worker</span></span><br><span class="line">                <span class="keyword">return</span>; <span class="comment">// 不需要新开一个Worker</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 新开一个Worker代替原先的Worker</span></span><br><span class="line">        <span class="comment">// 新开一个Worker有以下几种情况</span></span><br><span class="line">        <span class="comment">// 1. 用户执行的任务发生了异常</span></span><br><span class="line">        <span class="comment">// 2. Worker正常退出，Worker数量比线程池corePoolSize小，阻塞队列不空但是没有任何Worker在工作</span></span><br><span class="line">        addWorker(<span class="keyword">null</span>, <span class="keyword">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="线程池的关闭"><a class="header-anchor" href="#线程池的关闭">¶</a>线程池的关闭</h4>
<p>shutdown方法，关闭线程池，关闭之后阻塞队列里的任务不受影响，会继续被Worker处理，但是新的任务不会被接受，方法实现如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">shutdown</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">    mainLock.lock(); <span class="comment">// 关闭的时候需要加锁，防止并发</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        checkShutdownAccess(); <span class="comment">// 检查关闭线程池的权限</span></span><br><span class="line">        advanceRunState(SHUTDOWN); <span class="comment">// 把线程池状态更新到SHUTDOWN</span></span><br><span class="line">        interruptIdleWorkers(); <span class="comment">// 中断闲置的Worker</span></span><br><span class="line">        onShutdown(); <span class="comment">// 钩子方法，默认不处理。ScheduledThreadPoolExecutor会做一些处理</span></span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        mainLock.unlock(); <span class="comment">// 解锁</span></span><br><span class="line">    &#125;</span><br><span class="line">    tryTerminate(); <span class="comment">// 尝试结束线程池</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>interruptIdleWorkers方法，注意，这个方法打断的是闲置Worker，打断闲置Worker之后，getTask方法会返回null，然后Worker会被回收。那什么是闲置Worker呢？</p>
<p>闲置Worker是这样解释的：Worker运行的时候会去阻塞队列拿数据(getTask方法)，拿的时候如果没有设置超时时间，那么会一直阻塞等待阻塞队列进数据，这样的Worker就被称为闲置Worker。<br>
由于Worker也是一个AQS(AbstractQueuedSynchronizer,详解点击<span class="exturl" data-url="aHR0cHM6Ly93d3cuamlhbnNodS5jb20vcC9kYTlkMDUxZGNjM2Q=" title="https://www.jianshu.com/p/da9d051dcc3d">这里<i class="fa fa-external-link"></i></span>)，在runWorker方法里会有一对lock和unlock操作，这对lock操作是为了确保Worker不是一个闲置Worker。</p>
<p>所以Worker被设计成一个AQS是为了根据Worker的锁来判断是否是闲置线程，是否可以被强制中断。我们来看看它的实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 调用他的一个重载方法，传入了参数false，表示要中断所有的正在运行的闲置Worker，如果为true表示只打断一个闲置Worker</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">interruptIdleWorkers</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    interruptIdleWorkers(<span class="keyword">false</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">interruptIdleWorkers</span><span class="params">(<span class="keyword">boolean</span> onlyOne)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">    mainLock.lock(); <span class="comment">// 中断闲置Worker需要加锁，防止并发</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (Worker w : workers) &#123; </span><br><span class="line">            Thread t = w.thread; <span class="comment">// 拿到worker中的线程</span></span><br><span class="line">            <span class="comment">// Worker中的线程没有被打断并且Worker可以获取锁，这里Worker能获取锁说明Worker是个闲置Worker，</span></span><br><span class="line">            <span class="comment">// 在阻塞队列里拿数据一直被阻塞，没有数据进来。如果没有获取到Worker锁，说明Worker还在执行任务，</span></span><br><span class="line">            <span class="comment">// 不进行中断(shutdown方法不会中断正在执行的任务)</span></span><br><span class="line">            <span class="keyword">if</span> (!t.isInterrupted() &amp;&amp; w.tryLock()) &#123; </span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    t.interrupt();  <span class="comment">// 中断Worker线程</span></span><br><span class="line">                &#125; <span class="keyword">catch</span> (SecurityException ignore) &#123;</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    w.unlock(); <span class="comment">// 释放Worker锁</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (onlyOne) <span class="comment">// 如果只打断1个Worker的话，直接break退出，否则，遍历所有的Worker</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        mainLock.unlock(); <span class="comment">// 解锁</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从上面的代码可以看到，shutdown并不会立即停止线程池（shutdownNow会立即停止线程池），而是先将线程状态置于SHUTDOWN，然后中断闲置的Worker。<br>
然后尝试结束线程池，tryTerminate的实现如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">tryTerminate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">        <span class="comment">// 满足3个条件中的任意一个，不终止线程池</span></span><br><span class="line">        <span class="comment">// 1. 线程池还在运行，不能终止</span></span><br><span class="line">        <span class="comment">// 2. 线程池处于TIDYING或TERMINATED状态，说明已经在关闭了，不允许继续处理</span></span><br><span class="line">        <span class="comment">// 3. 线程池处于SHUTDOWN状态并且阻塞队列不为空，这时候还需要处理阻塞队列的任务，不能终止线程池</span></span><br><span class="line">        <span class="keyword">if</span> (isRunning(c) ||</span><br><span class="line">            runStateAtLeast(c, TIDYING) ||</span><br><span class="line">            (runStateOf(c) == SHUTDOWN &amp;&amp; ! workQueue.isEmpty()))</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        <span class="comment">// 走到这一步说明线程池已经不在运行，阻塞队列已经没有任务，但是还要回收正在工作的Worker</span></span><br><span class="line">        <span class="keyword">if</span> (workerCountOf(c) != <span class="number">0</span>) &#123;</span><br><span class="line">             <span class="comment">// 由于线程池不运行了，调用了线程池的关闭方法</span></span><br><span class="line">             <span class="comment">// 中断闲置Worker，直到回收全部的Worker。这里没有那么暴力，只中断一个，中断之后退出方法，</span></span><br><span class="line">             <span class="comment">// 中断了Worker之后，Worker会回收，然后还是会调用tryTerminate方法，如果还有闲置线程，那么继续中断</span></span><br><span class="line">            interruptIdleWorkers(ONLY_ONE); </span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 走到这里说明worker已经全部回收了，并且线程池已经不在运行，阻塞队列已经没有任务。可以准备结束线程池了</span></span><br><span class="line">        <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">        mainLock.lock(); <span class="comment">// 加锁，防止并发</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (ctl.compareAndSet(c, ctlOf(TIDYING, <span class="number">0</span>))) &#123; <span class="comment">// cas操作，将线程池状态改成TIDYING</span></span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    terminated(); <span class="comment">// 调用terminated方法</span></span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    ctl.set(ctlOf(TERMINATED, <span class="number">0</span>)); <span class="comment">// terminated方法调用完毕之后，状态变为TERMINATED</span></span><br><span class="line">                    termination.signalAll();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            mainLock.unlock(); <span class="comment">// 解锁</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// else retry on failed CAS</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="简单实战"><a class="header-anchor" href="#简单实战">¶</a>简单实战</h2>
<p>好了关于线程池的简单介绍就到这里了，我们接下来看一个小小的例子，顺便了解一下线程池的参数选择。</p>
<p>线程数的选择常见策略如下：</p>
<ul>
<li>CPU密集型任务</li>
</ul>
<p>尽量使用较小的线程池，一般为CPU核心数+1。<br>
因为CPU密集型任务使得CPU使用率很高，若开过多的线程数，只能增加上下文切换的次数，因此会带来额外的开销。</p>
<ul>
<li>IO密集型任务</li>
</ul>
<p>可以使用稍大的线程池，一般为2*CPU核心数。<br>
IO密集型任务CPU使用率并不高，因此可以让CPU在等待IO的时候去处理别的任务，充分利用CPU时间。</p>
<ul>
<li>混合型任务</li>
</ul>
<p>可以将任务分成IO密集型和CPU密集型任务，然后分别用不同的线程池去处理。<br>
只要分完之后两个任务的执行时间相差不大，那么就会比串行执行来的高效。<br>
因为如果划分之后两个任务执行时间相差甚远，那么先执行完的任务就要等后执行完的任务，最终的时间仍然取决于后执行完的任务，而且还要加上任务拆分与合并的开销，得不偿失。</p>
<p>通过前面了解到ThreadPoolExecutor的<a href="#%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0">饱和策略</a>默认选择的是AbortPolicy策略，新来的任务将被丢弃，即使保证任务不丢弃的策略CallerRunsPolicy也是直接调用任务的run方法来实现。</p>
<p>我们如何实现一个满足生产者消费者模型的线程池呢，这里将Worker当做消费者，可以同时处理多个生产者提交的任务，同时要保证生产者过多时任务不被丢弃。具体代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * package com.teddy.thread</span></span><br><span class="line"><span class="comment"> * description: 线程池工具类</span></span><br><span class="line"><span class="comment"> * Copyright 2018 Teddy, Inc. All rights reserved.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> Teddy</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2018-9-16 14:20</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadPoolUtils</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 任务等待队列 容量</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> TASK_QUEUE_SIZE = <span class="number">1000</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 空闲线程存活时间 单位分钟</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> KEEP_ALIVE_TIME = <span class="number">10L</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 任务执行线程池</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> ThreadPoolExecutor threadPool;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="keyword">int</span> corePoolNum = <span class="number">2</span> * Runtime.getRuntime().availableProcessors() + <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">int</span> maximumPoolSize = <span class="number">2</span> * corePoolNum;</span><br><span class="line">        threadPool = <span class="keyword">new</span> ThreadPoolExecutor(</span><br><span class="line">                corePoolNum,</span><br><span class="line">                maximumPoolSize,</span><br><span class="line">                KEEP_ALIVE_TIME,</span><br><span class="line">                TimeUnit.MINUTES,</span><br><span class="line">                <span class="keyword">new</span> ArrayBlockingQueue&lt;&gt;(TASK_QUEUE_SIZE),</span><br><span class="line">                <span class="keyword">new</span> ThreadFactoryBuilder().setNameFormat(<span class="string">"ThreadPoolUtils-%d"</span>).build(), (r, executor) -&gt; &#123;</span><br><span class="line">            <span class="keyword">if</span> (!executor.isShutdown()) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    executor.getQueue().put(r);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                    Thread.currentThread().interrupt();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Description: 执行任务</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> task 任务</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@author</span> teddy</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@date</span> 2018/9/16</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable task)</span> </span>&#123;</span><br><span class="line">        threadPool.execute(task);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Description: 提交任务到线程池</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> task 任务</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> Future&lt;T&gt;</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@author</span> teddy</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@date</span> 2019/3/23</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">Future&lt;T&gt; <span class="title">submit</span><span class="params">(Callable&lt;T&gt; task)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> threadPool.submit(task);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>java多线程</tag>
        <tag>ThreadPool</tag>
      </tags>
  </entry>
  <entry>
    <title>DeepSeek本地部署</title>
    <url>/ai/develop/deepseek-local.html</url>
    <content><![CDATA[<h1 id="deepseek本地部署"><a class="header-anchor" href="#deepseek本地部署">¶</a>DeepSeek本地部署</h1>
<p>作为一款现象级的Ai产品，DeepSeek用户量暴增，服务器又被攻击，使用DeepSeek，经常出现服务器繁忙。</p>
<p>将DeepSeek部署在本地电脑就方便很多，选择对应的模型来下载，1.5b、7b、8b、14b、32b、70b或671b，这里有很多版本可选，模型越大，要求电脑内存、显卡等的配置越高。DeepSeek部署在本地电脑上部署，有些不方便公开的数据，比如实验数据、企业内部数据，可以被本地的大模型安全地使用了。</p>
<h2 id="下载安装-ollama"><a class="header-anchor" href="#下载安装-ollama">¶</a>下载安装 Ollama</h2>
<p>访问Ollama官网：<span class="exturl" data-url="aHR0cHM6Ly9vbGxhbWEuY29tL2Rvd25sb2Fk" title="https://ollama.com/download">https://ollama.com/download<i class="fa fa-external-link"></i></span></p>
<p>选择对应操作系统进行安装，这里以linux为例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">curl -fsSL https://ollama.com/install.sh | sh</span><br></pre></td></tr></table></figure>
<p>如果下载较慢，可以考虑使用docker方式安装：<span class="exturl" data-url="aHR0cHM6Ly9odWIuZG9ja2VyLmNvbS9yL29sbGFtYS9vbGxhbWE=" title="https://hub.docker.com/r/ollama/ollama">https://hub.docker.com/r/ollama/ollama<i class="fa fa-external-link"></i></span></p>
<ul>
<li>Install the NVIDIA Container Toolkit packages</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-get install -y nvidia-container-toolkit</span><br></pre></td></tr></table></figure>
<ul>
<li>Configure Docker to use Nvidia driver</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo nvidia-ctk runtime configure --runtime=docker</span><br><span class="line">sudo systemctl restart docker</span><br></pre></td></tr></table></figure>
<p>启动容器</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -d --gpus=all --restart always -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama</span><br></pre></td></tr></table></figure>
<h2 id="安装deepseek大模型"><a class="header-anchor" href="#安装deepseek大模型">¶</a>安装DeepSeek大模型</h2>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker exec -it ollama ollama run deepseek-r1:8b</span><br></pre></td></tr></table></figure>
<p>模型大小如何选择：</p>
<table>
<thead>
<tr>
<th>模型大小</th>
<th>参数量</th>
<th>显存需求 (GPU)</th>
<th>内存需求</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td>1.5B</td>
<td>15亿</td>
<td>2～4 GB</td>
<td>8 GB</td>
<td>低端设备，轻量推理</td>
</tr>
<tr>
<td>7B</td>
<td>70亿</td>
<td>8～12 GB</td>
<td>16 GB</td>
<td>中端设备，通用推理</td>
</tr>
<tr>
<td>8B</td>
<td>80亿</td>
<td>10～16 GB</td>
<td>16 ~ 32 GB</td>
<td>中高端设备，高性能推理</td>
</tr>
<tr>
<td>14B</td>
<td>140亿</td>
<td>16～24 GB</td>
<td>32 GB</td>
<td>高端设备，高性能推理</td>
</tr>
<tr>
<td>32B</td>
<td>320亿</td>
<td>32～48 GB</td>
<td>64 GB</td>
<td>高端设备，专业推理</td>
</tr>
<tr>
<td>70B</td>
<td>700亿</td>
<td>64 GB+</td>
<td>128 GB</td>
<td>顶级设备，大规模推理</td>
</tr>
<tr>
<td>761B</td>
<td>6710亿</td>
<td>多GPU（80 GB+）</td>
<td>256 GB+</td>
<td>超大规模推理，分布式计算</td>
</tr>
</tbody>
</table>
<p>然后就可以开启对话了：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker exec -it ollama ollama run deepseek-r1:8b</span><br><span class="line">pulling manifest </span><br><span class="line">pulling 6340dc3229b0... 100% ▕█████████████████████ 4.9 GB                         </span><br><span class="line">pulling 369ca498f347... 100% ▕█████████████████████ 387 B                         </span><br><span class="line">pulling 6e4c38e1172f... 100% ▕█████████████████████ 1.1 KB                         </span><br><span class="line">pulling f4d24e9138dd... 100% ▕█████████████████████ 148 B                         </span><br><span class="line">pulling 0cb05c6e4e02... 100% ▕█████████████████████ 487 B                         </span><br><span class="line">verifying sha256 digest </span><br><span class="line">writing manifest </span><br><span class="line">success </span><br><span class="line"><span class="meta">&gt;</span>&gt;&gt; 请详细介绍你的来历</span><br><span class="line">&lt;think&gt;</span><br><span class="line">您好！我是由中国的深度求索（DeepSeek）公司开发的智能助手DeepSeek-R1。如您有任何任何问题，我会尽我所能为您提供帮助。</span><br><span class="line">&lt;/think&gt;</span><br><span class="line"></span><br><span class="line">您好！我是由中国的深度求索（DeepSeek）公司开发的智能助手DeepSeek-R1。如您有任何任何问题，我会尽我所能为您提供帮助。</span><br><span class="line"></span><br><span class="line">Use Ctrl + d or /bye to exit.</span><br></pre></td></tr></table></figure>
<p>后续将介绍如何基于本地模型搭建自己的知识库。</p>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>部署</tag>
      </tags>
  </entry>
  <entry>
    <title>给 Agent 搞个干活“工具人”：一个 HTTP 小服务带来的改变</title>
    <url>/ai/develop/agent-tool-service.html</url>
    <content><![CDATA[<h1 id="给-agent-搞个干活-工具人-：一个-http-小服务带来的改变"><a class="header-anchor" href="#给-agent-搞个干活-工具人-：一个-http-小服务带来的改变">¶</a>给 Agent 搞个干活“工具人”：一个 HTTP 小服务带来的改变</h1>
<p>这篇不是讲“如何零代码搭一个 AI 工作流平台”，也不是讲“某个大模型能力有多牛”。<br>
它讲的是一件更朴素的事：<br>
我为什么宁愿多写一个看起来“很普通”的 HTTP 小服务，也不想再在编排工具里堆一堆复杂节点。</p>
<p>如果你也在做 Agent / 工作流，已经开始被流程图、脚本逻辑和大模型输出共同折磨，这篇可能会对你有点参考价值。</p>
<hr>
<h2 id="一-先把话说白：这个-agent-工具服务具体在干什么？"><a class="header-anchor" href="#一-先把话说白：这个-agent-工具服务具体在干什么？">¶</a>一、先把话说白：这个 Agent 工具服务具体在干什么？</h2>
<p>我这个服务的定位特别简单粗暴：<br>
<strong>给 Agent 和工作流，提供一组“能干具体活”的 HTTP 工具接口。</strong></p>
<p>它大概是在做这几类事情（抽象说法，不贴项目细节）：</p>
<ul>
<li>
<p><strong>一类：复杂一步活“打包”为一次 HTTP 调用</strong></p>
<ul>
<li>上游发来一个结构化请求（JSON），里面写清楚：你想干什么，需要多少条数据，需要什么字段。</li>
<li>服务内部会：
<ul>
<li>按要求调大模型做内容生成或理解；</li>
<li>用普通 Java 代码去重、补字段、格式整理；</li>
<li>把结果导出成文件（比如表格），存到临时存储里；</li>
<li>生成一个下载 token + 一份 Markdown 预览，方便人或其他系统查看。</li>
</ul>
</li>
<li>对调用方来说：
<ul>
<li>只看到“一次请求 → 一个结构化响应”，不需要关心中间调了几次大模型、做了多少后处理。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>另一类：配套的小工具能力</strong></p>
<ul>
<li>自检大模型配置：
<ul>
<li>发一个简单问句，确认 baseUrl / model / apiKey 这些都没问题，把延迟和部分响应内容打出来。</li>
</ul>
</li>
<li>做一些和 Agent 工作流强相关的基础活：
<ul>
<li>日志记录和脱敏；</li>
<li>临时文件存储；</li>
<li>统一的错误结构和返回码。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>用一句话讲，这个服务就在做两件事：</p>
<ol>
<li>帮你跟大模型、文件、复杂逻辑打交道；</li>
<li>把最后的结果通过一个 HTTP 接口，干净地交给上游。</li>
</ol>
<hr>
<h2 id="二-为什么这些逻辑不适合全堆在编排工具-agent-脚本里？"><a class="header-anchor" href="#二-为什么这些逻辑不适合全堆在编排工具-agent-脚本里？">¶</a>二、为什么这些逻辑不适合全堆在编排工具 / Agent 脚本里？</h2>
<p>一开始我也以为：“有了编排平台，很多东西拖一拖节点就完了，何必再写服务？”</p>
<p>实践下来，几个很具体的问题把我劝退了。</p>
<h3 id="1-某些步骤内部太复杂-用节点堆出来-理论可行-实际难维护"><a class="header-anchor" href="#1-某些步骤内部太复杂-用节点堆出来-理论可行-实际难维护">¶</a>1. 某些步骤内部太复杂，用节点堆出来“理论可行、实际难维护”</h3>
<p>想象一个很现实的需求：</p>
<ul>
<li>需要批量生成一批结构化内容（不一定是题库，也可能是某种配置、表格数据等）；</li>
<li>要求数量要对；</li>
<li>内容不能重复；</li>
<li>字段要统一；</li>
<li>结果要导出成 Excel / CSV 给运营同学用。</li>
</ul>
<p>如果你完全在编排工具里做，大概会变成这样：</p>
<ul>
<li>一堆“大模型调用”节点，控制生成数量；</li>
<li>中间插着各种“json 解析”、“分支判断”、“重试”节点；</li>
<li>后面再加一堆“数组处理”、“格式转换”、“上传文件”的节点。</li>
</ul>
<p><strong>能跑起来，但一眼看过去就知道：<br>
一旦需求稍微变形，这条流程图谁都不敢动。</strong></p>
<h3 id="2-需要精细的资源与异常控制-编排层很难做细"><a class="header-anchor" href="#2-需要精细的资源与异常控制-编排层很难做细">¶</a>2. 需要精细的资源与异常控制，编排层很难做细</h3>
<p>比如：</p>
<ul>
<li>大模型调用是 IO 密集 + CPU 也会吃一点，想控制并发度和线程池大小；</li>
<li>某些步骤你希望有明确的“总超时时间”和“每批超时时间”；</li>
<li>解析 JSON 失败时，希望能给出可读的错误信息，而不是一个“未知异常”。</li>
</ul>
<p>这些事情，用一门普通语言（Java/Go/Python 都行）写在服务里，逻辑、日志、测试都有办法做得比较细。<br>
但如果你全靠编排工具，能拼出来，<br>
<strong>但很难做到“这一块就交给一个人负责、写单元测试、看日志、做性能调优”</strong>。</p>
<h3 id="3-逻辑复制粘贴问题：同一套-大模型-解析-清洗-到处都是"><a class="header-anchor" href="#3-逻辑复制粘贴问题：同一套-大模型-解析-清洗-到处都是">¶</a>3. 逻辑复制粘贴问题：同一套“大模型 + 解析 + 清洗”，到处都是</h3>
<p>如果你把所有逻辑写在 Agent 脚本里，会发生什么？</p>
<ul>
<li>每个 Agent 都有一段类似的“模型调用 + 解析 + 清洗 + 导出”逻辑；</li>
<li>需要改格式 / 换模型 / 调整规则时，你得去每个地方改一遍；</li>
<li>很快就没有人弄得清“线上到底在跑哪一版逻辑”。</li>
</ul>
<p>这其实是一个很典型的“应该收敛成服务能力，却被复制成脚本逻辑”的问题。</p>
<p>所以最后的落地选择是：</p>
<blockquote>
<p><strong>把那些“步骤内部很复杂，但对外可以收敛成一个动作”的事情，抽进一个 HTTP 工具服务里。<br>
工作流只需要把它当“黑盒步骤”去调用。</strong></p>
</blockquote>
<hr>
<h2 id="三-设计这个服务时-我只盯着三个特别务实的目标"><a class="header-anchor" href="#三-设计这个服务时-我只盯着三个特别务实的目标">¶</a>三、设计这个服务时，我只盯着三个特别务实的目标</h2>
<p>我没有想着做一个所谓“Agent 能力平台”，只盯着三件很务实的事：</p>
<ol>
<li><strong>单一职责，别搞成另一个大一统平台</strong></li>
<li><strong>可复用，任何会发 HTTP 的东西都能用</strong></li>
<li><strong>好运维，能按正常微服务那一套去部署监控</strong></li>
</ol>
<h3 id="1-单一职责：谁该干啥-就只干那点事"><a class="header-anchor" href="#1-单一职责：谁该干啥-就只干那点事">¶</a>1. 单一职责：谁该干啥，就只干那点事</h3>
<p>在服务内部，我基本按下面这种划分来做：</p>
<ul>
<li>
<p>控制器（Controller）：</p>
<ul>
<li>只做 HTTP 协议相关的事情：参数接收、校验、返回 <code>200/400/500</code> 这种状态；</li>
<li>打一日志：谁调了这个接口，带了什么参数；</li>
<li>捕获异常、包装成统一的错误响应结构。</li>
</ul>
</li>
<li>
<p>业务服务（Service）：</p>
<ul>
<li>聚焦某一类“工具能力”，比如“批量生成结构化内容 + 去重 + 导出文件”；</li>
<li>内部可以包含多次模型调用、各种小算法，但对外就是“一次 HTTP 请求”。</li>
</ul>
</li>
<li>
<p>基础设施（Infra）：</p>
<ul>
<li>LLM 调用适配：
<ul>
<li>屏蔽不同厂商 API 差异；</li>
<li>统一控制超时时间、重试策略；</li>
<li>统一打日志。</li>
</ul>
</li>
<li>临时文件存储：
<ul>
<li>管理文件路径、过期时间、下载 token；</li>
</ul>
</li>
<li>日志配置、OpenAPI 文档、Docker 打包等。</li>
</ul>
</li>
</ul>
<p>这样做的好处是：</p>
<ul>
<li>新需求进来时，大概率只改某个 Service；</li>
<li>控制器层不用被业务细节污染；</li>
<li>和大模型的交互逻辑集中在一处，可调可观测。</li>
</ul>
<h3 id="2-可复用：只要能发-http-就能用"><a class="header-anchor" href="#2-可复用：只要能发-http-就能用">¶</a>2. 可复用：只要能发 HTTP，就能用</h3>
<p>相比起“某个 SDK”或者“某个本地插件”，<strong>HTTP + JSON 是目前最通用的一种能力暴露方式</strong>：</p>
<ul>
<li>后端服务直接用自己熟悉的 HTTP 客户端库就能调用；</li>
<li>前端 / BFF 可以通过网关转发；</li>
<li>传统工作流平台，可以直接加一个 HTTP 节点调用；</li>
<li>命令行、脚本、CI/CD 也都能用 curl 之类调试。</li>
</ul>
<p>这意味着：</p>
<blockquote>
<p>只要我把这套能力打包成 HTTP 接口，就不怎么依赖上游用的是哪一套 Agent 框架或编排平台。</p>
</blockquote>
<h3 id="3-好运维：当成一个普通微服务来管"><a class="header-anchor" href="#3-好运维：当成一个普通微服务来管">¶</a>3. 好运维：当成一个普通微服务来管</h3>
<p>这一点很现实：</p>
<ul>
<li>我们已经有一套成熟的 Spring Boot + Docker 的体系；</li>
<li>监控平台、日志平台、网关、灰度发布都围绕 HTTP 微服务建好的。</li>
</ul>
<p>把工具能力做成 HTTP 服务，最大的好处之一就是：</p>
<ul>
<li>它能直接接入这套现有体系：
<ul>
<li>用容器参数来控制 JVM 内存、GC 策略；</li>
<li>使用统一的日志格式、日志滚动策略；</li>
<li>用健康检查接口接入监控；</li>
<li>用网关来做鉴权、限流、熔断。</li>
</ul>
</li>
</ul>
<p>如果我上来就做一个 MCP 服务，反而会脱离当前这套稳定的基础设施，这其实是我暂时没有这么做的关键原因之一。</p>
<hr>
<h2 id="四-和大模型合作这件事-我在服务里都做了什么"><a class="header-anchor" href="#四-和大模型合作这件事-我在服务里都做了什么">¶</a>四、和大模型合作这件事，我在服务里都做了什么</h2>
<p>说点细节，会更清晰一些。</p>
<h3 id="1-把-prompt-写成-协议说明-不是随便聊两句"><a class="header-anchor" href="#1-把-prompt-写成-协议说明-不是随便聊两句">¶</a>1. 把 Prompt 写成“协议说明”，不是随便聊两句</h3>
<p>比如我要让模型生成一批结构化结果，我不会只说：</p>
<blockquote>
<p>“请帮我生成 XX 条数据，用 JSON 返回。”</p>
</blockquote>
<p>我会在系统提示和用户提示里，把约束写得很“工程化”，类似于：</p>
<ul>
<li>必须返回严格的 JSON 数组；</li>
<li>数组长度必须等于 N，不能多也不能少；</li>
<li>每个元素必须包含哪些字段（字段名要严格一致）；</li>
<li>不允许有额外解释文字、不要加 Markdown 代码块；</li>
<li>某个字段应该包含什么语义上的信息。</li>
</ul>
<p>这样做的目的不是“提高成功率”这么空泛，而是：</p>
<blockquote>
<p>让服务端有足够的信息做<strong>机械的校验</strong>，比如数量、字段名、某些必填字段。</p>
</blockquote>
<h3 id="2-默认认为模型输出-不干净-所以要有清洗流程"><a class="header-anchor" href="#2-默认认为模型输出-不干净-所以要有清洗流程">¶</a>2. 默认认为模型输出“不干净”，所以要有清洗流程</h3>
<p>现实里，模型非常喜欢这样输出：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">好的，下面是为你生成的内容：</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">[</span><br><span class="line">  ...</span><br><span class="line">]</span><br></pre></td></tr></table></figure>
<p>你让它只给 JSON，它依然会在前后加评价、加说明。</p>
<p>在服务里，我会做这几步：</p>
<ol>
<li>从原始文本中“抠出” JSON 数组：
<ul>
<li>找到 <code>[</code> 和 <code>]</code> 的合理区间；</li>
<li>去掉 Markdown 代码块包裹；</li>
<li>去掉 <code>&lt;think&gt;</code> 之类非 JSON 内容（如果模型会加）。</li>
</ul>
</li>
<li>用 JSON 解析器解析成对象列表；</li>
<li>解析失败时，记录原始文本 + 错误堆栈，返回一个结构化的错误响应，而不是直接 500。</li>
</ol>
<p>这件事如果散落在每个 Agent 里，就会到处都是“差不多的正则 + try-catch”；<br>
集中在工具服务里，就变成一个可以单独演进、统一维护的“清洗模块”。</p>
<h3 id="3-把业务规则放在代码里-而不是只写在-prompt-里祈祷"><a class="header-anchor" href="#3-把业务规则放在代码里-而不是只写在-prompt-里祈祷">¶</a>3. 把业务规则放在代码里，而不是只写在 Prompt 里祈祷</h3>
<p>例如，有几个常见的情况：</p>
<ul>
<li>
<p><strong>数量不对</strong></p>
<ul>
<li>说好要 50 条，它给了 47 条；</li>
<li>做法：
<ul>
<li>服务端算一下数量，如果不足，就再触发一次补生成；</li>
<li>最终截断或补齐到需要的数量。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>字段不统一</strong></p>
<ul>
<li>有的元素多一个字段，有的少一个字段，字段顺序不一致；</li>
<li>做法：
<ul>
<li>用第一条记录的字段作为“列模板”；</li>
<li>扫一遍所有记录，把额外字段加到末尾；</li>
<li>对缺少字段的记录，用空值补齐。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>内容重复</strong></p>
<ul>
<li>看起来不同，其实表达的是类似的内容；</li>
<li>最基础的一步，先用一个简单的“签名”（比如基于某个主字段的字符串）做去重，<br>
更复杂的相似度检测可以后面再加。</li>
</ul>
</li>
</ul>
<p>这些逻辑，如果只写在 Prompt 里：“请不要重复、请确保数量正确”，<br>
<strong>你会一遍遍发现现实和你想象的不一样</strong>。<br>
把它收在服务里以后，就变成了可见、可测、可调的“业务规则”。</p>
<hr>
<h2 id="五-为什么我现在选的是-http-接口-而不是直接做-mcp-服务？"><a class="header-anchor" href="#五-为什么我现在选的是-http-接口-而不是直接做-mcp-服务？">¶</a>五、为什么我现在选的是 HTTP 接口，而不是直接做 MCP 服务？</h2>
<p>这部分我尽量讲具体一点，不绕术语。</p>
<h3 id="1-谁来用这个服务？只靠-mcp-生态不够"><a class="header-anchor" href="#1-谁来用这个服务？只靠-mcp-生态不够">¶</a>1. 谁来用这个服务？只靠 MCP 生态不够</h3>
<p>目前实际情况大致是这样：</p>
<ul>
<li>一部分 Agent / 工具确实跑在支持 MCP 的环境里（比如某些编辑器、框架）；</li>
<li>但还有很多调用方非常“传统”：
<ul>
<li>后端 REST 服务；</li>
<li>BFF / 网关；</li>
<li>现有的工作流平台（它只认识 HTTP 节点）；</li>
<li>定时任务、脚本、CI 任务等。</li>
</ul>
</li>
</ul>
<p><strong>如果我一开始就只暴露 MCP 接口，这些调用方几乎都用不上。</strong></p>
<p>而 HTTP + JSON 的好处是：</p>
<ul>
<li>任何环境，只要能发 HTTP 请求，就能用；</li>
<li>调试也简单：curl / Postman / 浏览器都行。</li>
</ul>
<p>所以我的实际选择是：</p>
<blockquote>
<p><strong>把能力的“底层实现”和“统一接口形态”先落在 HTTP 上，<br>
MCP 以后可以作为“这套 HTTP 能力的其中一个适配壳”。</strong></p>
</blockquote>
<p>也就是说，<strong>HTTP 是“内核接口”，MCP 反而是可以在外面再加的一层皮</strong>，这样不会把自己绑死在某个生态里。</p>
<h3 id="2-部署与运维：http-服务可以无缝接入现有体系"><a class="header-anchor" href="#2-部署与运维：http-服务可以无缝接入现有体系">¶</a>2. 部署与运维：HTTP 服务可以无缝接入现有体系</h3>
<p>现在的基础设施大多围绕“HTTP 微服务”建好的：</p>
<ul>
<li>Spring Boot 项目 → 打成 Jar 或 Docker 镜像；</li>
<li>发布到 K8s 或虚机上；</li>
<li>网关 / API 网关 做统一鉴权、路由、限流；</li>
<li>Prometheus / Grafana / ELK 做监控和日志。</li>
</ul>
<p>把工具能力做成 HTTP 服务的结果是：</p>
<ul>
<li>运维侧几乎不需要学习新东西，按“普通微服务”来管就行；</li>
<li>可以很容易插入：
<ul>
<li>请求级别的限流；</li>
<li>鉴权策略；</li>
<li>灰度策略（按一些 Header 或路径分流）。</li>
</ul>
</li>
</ul>
<p>而 MCP 服务的运行模型更多是：</p>
<ul>
<li>被某个宿主应用拉起来；</li>
<li>宿主负责和 MCP 通信、转发请求；</li>
<li>运维、监控、日志更多依赖宿主对 MCP 的支持。</li>
</ul>
<p>在我现有的团队环境里，<strong>HTTP 服务的接入成本要小得多</strong>，这就是为什么我现在先选了 HTTP。</p>
<h3 id="3-安全模型和边界控制更清晰"><a class="header-anchor" href="#3-安全模型和边界控制更清晰">¶</a>3. 安全模型和边界控制更清晰</h3>
<p>HTTP 服务这条路上的安全模型，相对成熟：</p>
<ul>
<li>可以挂在内网，只通过网关或特定服务访问；</li>
<li>可以验 Token / JWT / mTLS 等；</li>
<li>可以做 IP 白名单、Rate Limit、审计日志。</li>
</ul>
<p>而 MCP 更多设计在“本地工具对接 Agent”这条路上，<br>
如果我要把它暴露到跨团队、跨服务的调用场景里，<br>
<strong>安全边界如何划、谁能调、从哪儿调、怎么审计</strong>，要重新设计一套东西。</p>
<p>对我当前的需求来说：</p>
<blockquote>
<p>一个明确的 HTTP 边界 + 网关 + 内网访问控制，就已经能满足“只给可信方调用”的要求了。</p>
</blockquote>
<p>MCP 的协议更偏向“宿主 ↔ 工具”的能力协商，对使用它的 Agent 来说很友好，<br>
但对不支持 MCP 的系统来说，要用就得先引一整套 MCP 客户端逻辑，这对很多传统系统是不现实的。</p>
<p>所以我的顺序是：</p>
<ol>
<li><strong>先用最简单的 HTTP + JSON，把核心能力抽象稳定；</strong></li>
<li>等 MCP 在团队内部的使用场景足够多，再考虑在外面包一层 MCP 适配。</li>
</ol>
<hr>
<h2 id="六-这个-http-工具服务在实际里的用法"><a class="header-anchor" href="#六-这个-http-工具服务在实际里的用法">¶</a>六、这个 HTTP 工具服务在实际里的用法</h2>
<p>说几个已经在用、而且挺顺手的模式。</p>
<h3 id="模式一：工作流里当成-黑盒步骤"><a class="header-anchor" href="#模式一：工作流里当成-黑盒步骤">¶</a>模式一：工作流里当成“黑盒步骤”</h3>
<ul>
<li>上游节点：准备好参数（通常是一个 JSON 对象），填到 HTTP 调用节点里；</li>
<li>调这个服务的某个接口，等待返回结构化结果；</li>
<li>后续节点只处理这些结果，不关心里面调了几次大模型。</li>
</ul>
<p>效果是：</p>
<blockquote>
<p>流程图变短了，<br>
很多原本拆成 7、8 个节点的东西，收敛成一个 HTTP 节点。</p>
</blockquote>
<h3 id="模式二：agent-把-难啃的骨头-一次甩给服务"><a class="header-anchor" href="#模式二：agent-把-难啃的骨头-一次甩给服务">¶</a>模式二：Agent 把“难啃的骨头”一次甩给服务</h3>
<ul>
<li>Agent 负责决定“什么时候需要用这个能力”；</li>
<li>一旦确定要用，就把上下文和配置打包成请求，丢给工具服务；</li>
<li>工具服务负责和大模型、文件系统、规则逻辑打交道，Agent 只看最终结果和状态。</li>
</ul>
<p>效果是：</p>
<blockquote>
<p>Agent 的 Prompt 不用写得特别长，<br>
它只需要知道：“这个接口会帮我完成某类标准化的工作”。</p>
</blockquote>
<h3 id="模式三：纯后端-运营任务也直接用"><a class="header-anchor" href="#模式三：纯后端-运营任务也直接用">¶</a>模式三：纯后端 / 运营任务也直接用</h3>
<p>有些场景甚至不走 Agent：</p>
<ul>
<li>后台管理页直接调用这个服务生成一批结构化结果；</li>
<li>定时任务每天跑一次，用来产出某种报表或模板。</li>
</ul>
<p>这时候，这个工具服务就成了一个<strong>通用的“结构化生成 + 转换”后端能力</strong>，<br>
顺带也减轻了 Agent 那边的负担。</p>
<hr>
<h2 id="七-写在最后：给-agent-找到自己的-工具人"><a class="header-anchor" href="#七-写在最后：给-agent-找到自己的-工具人">¶</a>七、写在最后：给 Agent 找到自己的“工具人”</h2>
<p>回头看，这个小小的 HTTP 工具服务并没有什么“炫技”的地方，更多是一些很朴素的取舍：</p>
<ul>
<li>复杂逻辑不再硬塞进流程图和 Agent 脚本，而是收敛到一个专门干活的服务里；</li>
<li>和大模型打交道的坑（格式不稳、数量不准、内容重复）集中在一处解决，而不是到处复制 paste；</li>
<li>把大家已经熟悉的 HTTP 微服务体系继续用下去，而不是为了“跟风”换一整套栈。</li>
</ul>
<p>如果你现在也在做 Agent / 工作流，已经感觉到编排工具和脚本越来越难 hold 住复杂步骤，<br>
也许可以考虑：先别急着再造一个平台，先像这样抽一个“小工具人服务”出来，把最难啃的那几块活接过去。<br>
等这块基础垫稳了，后面不论是接 MCP、换模型、上新的编排框架，都会轻松很多。</p>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Agent</tag>
        <tag>工具开发</tag>
      </tags>
  </entry>
  <entry>
    <title>基于Dify + DeepSeek部署本地知识库</title>
    <url>/ai/develop/deepseek-rag.html</url>
    <content><![CDATA[<h1 id="基于dify-deepseek部署本地知识库"><a class="header-anchor" href="#基于dify-deepseek部署本地知识库">¶</a>基于Dify + DeepSeek部署本地知识库</h1>
<p>本地部署的最大意义在于利用DeepSeek大模型的能力加上自己的知识库，可以训练出一个符合自己需求的大模型。</p>
<p>今天就来分享下这个搭建过程，使用基于LLM的大模型知识库问答系统Dify，里面集成DeepSeek以及私有知识库，打造一个符合自己需求的RAG应用。</p>
<h2 id="1-安装docker"><a class="header-anchor" href="#1-安装docker">¶</a>1、安装Docker</h2>
<p>安装Docker和Docker-compose，windows系统可以在docker网站 <span class="exturl" data-url="aHR0cHM6Ly93d3cuZG9ja2VyLmNvbS8=" title="https://www.docker.com/">https://www.docker.com/<i class="fa fa-external-link"></i></span> 下载docker desktop。</p>
<p>这里以linux为例，安装docker。</p>
<h3 id="docker安装请参照官网教程"><a class="header-anchor" href="#docker安装请参照官网教程">¶</a>docker安装请参照官网教程</h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span> docker</span><br><span class="line">curl -fsSL https://get.docker.com | bash -s docker --mirror Aliyun</span><br><span class="line">sudo service docker restart</span><br><span class="line">sudo systemctl enable docker</span><br></pre></td></tr></table></figure>
<h3 id="docker-compose"><a class="header-anchor" href="#docker-compose">¶</a>Docker Compose</h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo curl -L "https://github.com/docker/compose/releases/download/v2.24.6/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose</span><br><span class="line">sudo chmod +x /usr/local/bin/docker-compose</span><br><span class="line">sudo ln -s /usr/local/bin/docker-compose /usr/bin/docker-compose</span><br><span class="line">docker-compose version</span><br></pre></td></tr></table></figure>
<h3 id="设置docker镜像源"><a class="header-anchor" href="#设置docker镜像源">¶</a>设置docker镜像源</h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">vi /etc/docker/daemon.json</span><br><span class="line">&#123;"registry-mirrors":[</span><br><span class="line">        "https://registry.docker-cn.com",</span><br><span class="line">        "https://hub-mirror.c.163.com/"</span><br><span class="line"></span><br><span class="line">]&#125;</span><br></pre></td></tr></table></figure>
<p>最后重启doker<br>
systemctl restart docker</p>
<h2 id="2-安装dify"><a class="header-anchor" href="#2-安装dify">¶</a>2、安装Dify</h2>
<p>Dify文档参见：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2xhbmdnZW5pdXMvZGlmeQ==" title="https://github.com/langgenius/dify">https://github.com/langgenius/dify<i class="fa fa-external-link"></i></span></p>
<ol>
<li>clone dify源码：<code>https://github.com/langgenius/dify.git</code></li>
<li>构建docker镜像，启动docker容器</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cd dify</span><br><span class="line">cd docker</span><br><span class="line">cp .env.example .env</span><br><span class="line">docker compose up -d</span><br></pre></td></tr></table></figure>
<ol start="3">
<li>浏览器访问：<span class="exturl" data-url="aHR0cDovL2xvY2FsaG9zdC9pbnN0YWxs" title="http://localhost/install">http://localhost/install<i class="fa fa-external-link"></i></span>，进行初始化</li>
</ol>
<h2 id="3-配置模型"><a class="header-anchor" href="#3-配置模型">¶</a>3、配置模型</h2>
<h3 id="配置llm模型"><a class="header-anchor" href="#配置llm模型">¶</a>配置LLM模型</h3>
<p>之前的文章我们已经介绍了，如何本地部署DeepSeek：<span class="exturl" data-url="aHR0cHM6Ly9ub3RlLnlvdWRhby5jb20vcy9tSldoNHlh" title="https://note.youdao.com/s/mJWh4ya">https://note.youdao.com/s/mJWh4ya<i class="fa fa-external-link"></i></span>，接下来就以本地部署的DeepSeek为例添加到Dify中。</p>
<ol>
<li>进入Dify页面，点击右上角头像-》设置，进入设置页面</li>
<li>选择模型供应商，找到Ollama</li>
<li>添加Ollama模型，如下图所示<br>
<img src="https://mmbiz.qpic.cn/mmbiz_png/14Blum0GwI7zfMWdeXcoUMs1Rv8Qc6atpQQuYgN3MrxnWjaCQfb2CwgLll5oHAsnhagSDfzTpnh7lSEsUvBcDg/640?wx_fmt=png&amp;from=appmsg" alt="DeepSeek模型配置.png">
<ul>
<li>模型类型：LLM</li>
<li>模型名称：下载的模型名称，例如：deepseek-r1:8b</li>
<li>基础URL：Ollama部署的地址，默认端口为11434</li>
</ul>
</li>
</ol>
<h3 id="配置text-embedding模型"><a class="header-anchor" href="#配置text-embedding模型">¶</a>配置Text Embedding模型</h3>
<p>Text Embedding（文本嵌入）模型的核心作用是将文本（单词、句子、段落或文档）转换为稠密向量（即一组数值构成的向量），从而让计算机能够量化、理解和处理文本的语义信息。</p>
<p>Dify将使用Text Embedding模型将用户输入的问题转换为向量，到知识库根据语义快速检索知识。</p>
<p>Text Embedding模型如何选择，以下是主流Text Embedding模型的对比表格，从多个维度对比其优劣，帮助根据场景选择合适模型：</p>
<table>
<thead>
<tr>
<th>模型名称</th>
<th>参数量</th>
<th>上下文长度</th>
<th>多语言支持</th>
<th>特色优势</th>
<th>主要缺点</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>OpenAI text-embedding-3</strong></td>
<td>未知</td>
<td>8192</td>
<td>是</td>
<td>高准确度，OpenAI生态兼容</td>
<td>收费API，隐私数据需谨慎</td>
<td>商业应用，预算充足的项目</td>
</tr>
<tr>
<td><strong>BAAI/bge-large</strong></td>
<td>1.1B</td>
<td>512</td>
<td>是(侧重中英)</td>
<td>中文任务领先，开源可商用</td>
<td>长文本需分段处理</td>
<td>中文搜索、问答系统</td>
</tr>
<tr>
<td><strong>Alibaba-NLP/gte-large</strong></td>
<td>0.6B</td>
<td>512</td>
<td>是</td>
<td>阿里巴巴优化，文档理解强</td>
<td>资源消耗较大</td>
<td>电商、长文档处理</td>
</tr>
<tr>
<td><strong>Google/Gecko</strong></td>
<td>0.3B</td>
<td>1024</td>
<td>是</td>
<td>轻量高效，谷歌搜索优化</td>
<td>精度略低于大模型</td>
<td>移动端、实时检索系统</td>
</tr>
<tr>
<td><strong>sentence-transformers/all-MiniLM</strong></td>
<td>22M</td>
<td>256</td>
<td>是</td>
<td>超轻量级，推理速度快</td>
<td>表达能力有限</td>
<td>边缘设备、低延迟场景</td>
</tr>
<tr>
<td><strong>Cohere/embed-multilingual</strong></td>
<td>0.3B</td>
<td>512</td>
<td>是(100+语言)</td>
<td>多语言均衡表现</td>
<td>英文略逊于专用模型</td>
<td>跨国多语言应用</td>
</tr>
<tr>
<td><strong>MokaAI/m3e-base</strong></td>
<td>0.3B</td>
<td>512</td>
<td>中文优化</td>
<td>中文CL任务专项优化</td>
<td>非中文任务较弱</td>
<td>中文语义相似度计算</td>
</tr>
</tbody>
</table>
<blockquote>
<p>注：最新模型建议查看HuggingFace的MTEB排行榜（<span class="exturl" data-url="aHR0cHM6Ly9odWdnaW5nZmFjZS5jby9zcGFjZXMvbXRlYi9sZWFkZXJib2FyZA==" title="https://huggingface.co/spaces/mteb/leaderboard">https://huggingface.co/spaces/mteb/leaderboard<i class="fa fa-external-link"></i></span> ）获取实时评测数据。实际选择时应通过自己的测试集验证。</p>
</blockquote>
<p>Dify配置Text Embedding模型步骤如下：</p>
<ol>
<li>进入Dify页面，点击右上角头像-》设置，进入设置页面</li>
<li>选择模型供应商，找到Ollama</li>
<li>添加Ollama模型，添加：bge-large
<ul>
<li>模型类型：Text Embedding</li>
<li>模型名称：bge-large</li>
<li>基础URL：Ollama部署的地址，默认端口为11434</li>
</ul>
</li>
</ol>
<h3 id="配置rerank模型"><a class="header-anchor" href="#配置rerank模型">¶</a>配置Rerank模型</h3>
<p>Rerank模型的主要任务是对Text Embedding模型初步筛选出的候选集通过深度语义理解进行重新排序，确保最相关的结果排在最前面。</p>
<p>Ollama中目前没有找到合适的Rerank模型，这里考虑从<strong>HugginFace中下载BAAI/bge-reranker-large</strong>模型，使用<strong>transformers</strong>搭建一个满足<strong>OpenAI-API-compatible</strong>协议类型的Rerank模型，方便加入到Dify中。</p>
<p>安装BAAI/bge-reranker-large模型请参考：<span class="exturl" data-url="aHR0cHM6Ly9ub3RlLnlvdWRhby5jb20vcy80UFFiRll0Wg==" title="https://note.youdao.com/s/4PQbFYtZ">如何从HuggingFace下载并搭建Rerank模型<i class="fa fa-external-link"></i></span></p>
<p>Dify配Rerank模型步骤如下：</p>
<ol>
<li>进入Dify页面，点击右上角头像-》设置，进入设置页面</li>
<li>选择模型供应商，找到OpenAI-API-compatible</li>
<li>添加OpenAI-API-compatible模型，添加：bge-reranker-large
<ul>
<li>模型类型：Rerank</li>
<li>模型名称：bge-reranker-large</li>
<li>基础URL：<span class="exturl" data-url="aHR0cDovL2lw" title="http://ip">http://ip<i class="fa fa-external-link"></i></span>:port/v1</li>
</ul>
</li>
</ol>
<h2 id="4-构建知识库"><a class="header-anchor" href="#4-构建知识库">¶</a>4、构建知识库</h2>
<p>到这里基础的模型已经配置完成，接下来我们开始搭建知识库，进入Dify的知识库页面点击创建知识库，选择文件后进行如下配置：</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/14Blum0GwI7zfMWdeXcoUMs1Rv8Qc6atJm5p7VDNNdyIG2ulqQ6w317ukv9WruaicXlVYKJWkusicAzKAU5O0Rwg/640?wx_fmt=png&amp;from=appmsg" alt="Dify创建知识库"></p>
<p>然后点击保存并处理，等待索引处理完成，即可使用该知识库。</p>
<p>其中选择的模型在知识库检索过程中有不同的作用：</p>
<ul>
<li>Text Embedding模型：
<ul>
<li>用于召回阶段（Retrieval），从海量文档中快速筛选出Top-K（如1000条）候选结果。</li>
<li>通过向量相似度（如余弦相似度）粗筛，保证高召回率（Recall）。</li>
</ul>
</li>
<li>Rerank模型：
<ul>
<li>用于排序阶段（Reranking），对Top-K候选结果精细排序。</li>
<li>通过深度语义理解（如交叉注意力）计算查询-文档对的相关性，提升准确率（Precision）。</li>
</ul>
</li>
</ul>
<blockquote>
<p>示例流程：用户查询 → [Embedding模型] → 召回1000条候选 → [Rerank模型] → 返回Top-10最相关结果</p>
</blockquote>
<h2 id="5-创建聊天应用"><a class="header-anchor" href="#5-创建聊天应用">¶</a>5、创建聊天应用</h2>
<p>进入Dify的工作室，创建空白应用 -》选择Chatflow类型创建应用，进行工作流编排：</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/14Blum0GwI7zfMWdeXcoUMs1Rv8Qc6atO0pNtZIiaERLeOQHpaGtvQ2ibOoYIWib9j5SF5iaFPLGgBA1YNt76V0MLA/640?wx_fmt=png&amp;from=appmsg" alt="Dify创建应用.png"></p>
<blockquote>
<p>tips：知识库中的知识图片最好是具有语义的markdown格式（例如：![用户登陆.png](<span class="exturl" data-url="aHR0cHM6Ly94eHh4" title="https://xxxx">https://xxxx<i class="fa fa-external-link"></i></span>)），这样大模型才能更好的以图文方式回答用户的问题。</p>
</blockquote>
<h2 id="应用调试和发布"><a class="header-anchor" href="#应用调试和发布">¶</a>应用调试和发布</h2>
<p>点击预览按钮可以对编排的工作流进行调试，其中每一步可点开查看具体输入和输出：<br>
<img src="https://mmbiz.qpic.cn/mmbiz_png/14Blum0GwI7zfMWdeXcoUMs1Rv8Qc6atIPW7UcM86n0mA6J0WA3nTDCHzpWME2VWDVmWTEibvGpICibYwFjrwzbw/640?wx_fmt=png&amp;from=appmsg" alt="Dify应用预览.png"></p>
<p>应用调试完成后，点击发布按钮即可进行发布，发布后点击运行可以打开Dify提供的默认聊天页面。Dify提供的聊天页面支持嵌入到其它网站，同时也提供一系列API接口以供开发者深度集成，大家可根据自己需求自由选择。</p>
<p>参考文档：</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2xhbmdnZW5pdXMvZGlmeQ==" title="https://github.com/langgenius/dify">https://github.com/langgenius/dify<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9odWdnaW5nZmFjZS5jby9zcGFjZXMvbXRlYi9sZWFkZXJib2FyZA==" title="https://huggingface.co/spaces/mteb/leaderboard">https://huggingface.co/spaces/mteb/leaderboard<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>RAG</tag>
        <tag>Dify</tag>
      </tags>
  </entry>
  <entry>
    <title>MCP网关</title>
    <url>/ai/mcp/mcp-gateway-design.html</url>
    <content><![CDATA[<h1 id="mcp网关"><a class="header-anchor" href="#mcp网关">¶</a>MCP网关</h1>
<p>类似开源项目：<br>
<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21jcC1lY29zeXN0ZW0vbWNwLWdhdGV3YXk=" title="https://github.com/mcp-ecosystem/mcp-gateway">https://github.com/mcp-ecosystem/mcp-gateway<i class="fa fa-external-link"></i></span></p>
<p>技术选型：</p>
<ul>
<li>后端：
<ul>
<li>SpringBoot 3</li>
<li>spring-ai-mcp-server-webmvc-spring-boot-starter</li>
<li>PostgreSql</li>
<li>MybatisPlus</li>
<li>druid管理数据库连接</li>
<li>flyway管理数据库脚本，脚本放在resources/db目录下，初始化版本为V0.0</li>
</ul>
</li>
<li>前端：
<ul>
<li>vue3</li>
<li>使用vite进行包管理</li>
</ul>
</li>
<li>数据库：脚本放在resources/db目录下，初始化版本为V0.0
<ul>
<li>t_user：存储用户信息</li>
<li>t_mcp_project：存储项目信息</li>
<li>t_mcp_project_user：存储项目中的人员信息，关联t_user</li>
<li>t_mcp_tools：存储代理的tools，关联到每个具体的项目信息</li>
</ul>
</li>
</ul>
<p>后端目录：server<br>
前端目录：web</p>
<p>实现功能：</p>
<ul>
<li>登录页面：输入用户名密码进行登录，支持手动注册用户</li>
</ul>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>MCP</tag>
        <tag>网关</tag>
      </tags>
  </entry>
  <entry>
    <title>如何从HuggingFace下载并搭建Rerank模型</title>
    <url>/ai/develop/rerank-deploy.html</url>
    <content><![CDATA[<h1 id="如何从huggingface下载并搭建rerank模型"><a class="header-anchor" href="#如何从huggingface下载并搭建rerank模型">¶</a>如何从HuggingFace下载并搭建Rerank模型</h1>
<h2 id="什么是rerank模型"><a class="header-anchor" href="#什么是rerank模型">¶</a>什么是Rerank模型</h2>
<p>Rerank模型的主要任务是对Embedding模型初步筛选出的候选集进行重新排序，确保最相关的结果排在最前面。它通常基于更复杂的语义分析，评估候选文档和查询之间的深层次匹配关系。在Rerank阶段，模型会分析查询与候选文档之间的上下文、语义关系等信息。它可以使用诸如BERT/GPT等预训练语言模型来捕捉更细腻的语义和句子间的关系，从而对初步候选文档进行更精确的评分与排序。Rerank模型一般比Embedding模型计算更复杂，通常需要更多的计算资源，因此适合处理Embedding模型初步检索后的数据。</p>
<ul>
<li><strong>初步检索（Embedding模型）</strong>：用户输入查询后，Embedding模型首先将查询和文档表示为向量，然后通过向量相似度计算，快速从大规模数据集中筛选出若干个候选文档或候选答案。</li>
<li><strong>重新排序（Rerank模型）</strong>：在得到初步候选集后，Rerank模型进一步分析这些候选文档或答案与查询之间的精确匹配程度，并根据复杂的语义关系重新打分，对候选集进行排序。</li>
</ul>
<h4 id="使用docker安装"><a class="header-anchor" href="#使用docker安装">¶</a>使用Docker安装</h4>
<p>采用Docker部署模型，Docker调用GPU，需先安装nvidia-container-toolkits</p>
<p>从存储库更新包列表与安装NVIDIA Container Toolkit</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get update</span><br><span class="line">sudo apt-get install -y nvidia-container-toolkit</span><br></pre></td></tr></table></figure>
<p>一键部署BAAI/bge-reranker-large模型。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --gpus all -p 18080:80 -v /reranker:/data --pull always ghcr.io/huggingface/text-embeddings-inference:turing-1.5 --model-id BAAI/bge-reranker-large</span><br></pre></td></tr></table></figure>
<h4 id="直接安装"><a class="header-anchor" href="#直接安装">¶</a>直接安装</h4>
<p>docker镜像源下载比较慢，这里采用python本地安装</p>
<ol>
<li>确保系统已安装 Python 3.8 或更高版本，并安装 pip 包管理工具。</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 检查 Python 版本</span></span><br><span class="line">python3 --version</span><br><span class="line"></span><br><span class="line"><span class="comment"># 更新 pip</span></span><br><span class="line">python3 -m pip install --upgrade pip</span><br></pre></td></tr></table></figure>
<ol start="2">
<li>安装依赖库<br>
BAAI/bge-reranker-large 是一个基于 Transformer 的模型，通常使用 transformers 库加载和运行。安装以下依赖：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install torch transformers</span><br></pre></td></tr></table></figure>
<ul>
<li>torch: PyTorch 是运行模型的基础框架。</li>
<li>transformers: Hugging Face 提供的库，用于加载和运行预训练模型。</li>
</ul>
<ol start="3">
<li>下载模型</li>
</ol>
<p>参考文档：<span class="exturl" data-url="aHR0cHM6Ly9odWdnaW5nZmFjZS5jby9kb2NzL3RyYW5zZm9ybWVycy92NC40OC4yL3poL2luc3RhbGxhdGlvbiMlRTclQTYlQkIlRTclQkElQkYlRTYlQTglQTElRTUlQkMlOEY=" title="https://huggingface.co/docs/transformers/v4.48.2/zh/installation#%E7%A6%BB%E7%BA%BF%E6%A8%A1%E5%BC%8F">https://huggingface.co/docs/transformers/v4.48.2/zh/installation#离线模式<i class="fa fa-external-link"></i></span></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> transformers <span class="keyword">import</span> AutoTokenizer, AutoModelForSequenceClassification</span><br><span class="line"></span><br><span class="line">tokenizer = AutoTokenizer.from_pretrained(<span class="string">"BAAI/bge-reranker-large"</span>)</span><br><span class="line">model = AutoModelForSequenceClassification.from_pretrained(<span class="string">"BAAI/bge-reranker-large"</span>)</span><br><span class="line"></span><br><span class="line">tokenizer.save_pretrained(<span class="string">"/home/develop/ai-llm/bge-reranker-large/bge-reranker-large"</span>)</span><br><span class="line">model.save_pretrained(<span class="string">"/home/develop/ai-llm/bge-reranker-large/bge-reranker-large"</span>)</span><br></pre></td></tr></table></figure>
<ol start="4">
<li>提供API服务</li>
</ol>
<p>如果你不想微调模型，你可以直接安装包，不用finetune依赖：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install -U FlagEmbedding</span><br></pre></td></tr></table></figure>
<p>如果你想微调模型，你可以用finetune依赖安装：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install -U FlagEmbedding[finetune]</span><br></pre></td></tr></table></figure>
<p>使用 FastAPI 部署为服务：<code>pip install fastapi uvicorn pydantic</code></p>
<p>vi <span class="exturl" data-url="aHR0cDovL2JnZS1yZXJhbmtlci1sYXJnZS5weQ==" title="http://bge-reranker-large.py">bge-reranker-large.py<i class="fa fa-external-link"></i></span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">from fastapi import FastAPI</span><br><span class="line">from transformers import AutoModelForSequenceClassification, AutoTokenizer</span><br><span class="line">import torch</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"># 加载下载好的离线模型和分词器</span><br><span class="line">model_name = &quot;/your/path/bge-reranker-large&quot;</span><br><span class="line">tokenizer = AutoTokenizer.from_pretrained(model_name)</span><br><span class="line">model = AutoModelForSequenceClassification.from_pretrained(model_name)</span><br><span class="line"># 如果有GPU</span><br><span class="line">model = model.to(&quot;cuda&quot;)</span><br><span class="line"></span><br><span class="line"># 将模型设置为评估模式</span><br><span class="line">model.eval()</span><br><span class="line"></span><br><span class="line">@app.post(&quot;/rerank&quot;)</span><br><span class="line">def rerank(query: str, document: str):</span><br><span class="line">    inputs = tokenizer(query, document, return_tensors=&quot;pt&quot;, truncation=True, padding=True)</span><br><span class="line">    with torch.no_grad():</span><br><span class="line">        # 如果有GPU</span><br><span class="line">        inputs = &#123;k: v.to(&quot;cuda&quot;) for k, v in inputs.items()&#125;</span><br><span class="line">        outputs = model(**inputs)</span><br><span class="line">        score = outputs.logits.item()</span><br><span class="line">    return &#123;&quot;score&quot;: score&#125;</span><br></pre></td></tr></table></figure>
<p>启动服务：<code>uvicorn bge-reranker-large:app --reload --host 0.0.0.0 --port 5000</code></p>
<blockquote>
<p>参考文档：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0ZsYWdPcGVuL0ZsYWdFbWJlZGRpbmcvdHJlZS9tYXN0ZXIvZXhhbXBsZXMvaW5mZXJlbmNlL3JlcmFua2VyI3VzaW5nLWh1Z2dpbmdmYWNlLXRyYW5zZm9ybWVycw==" title="https://github.com/FlagOpen/FlagEmbedding/tree/master/examples/inference/reranker#using-huggingface-transformers">https://github.com/FlagOpen/FlagEmbedding/tree/master/examples/inference/reranker#using-huggingface-transformers<i class="fa fa-external-link"></i></span></p>
</blockquote>
<p>访问API进行测试：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">curl -X POST &quot;http://127.0.0.1:5000/rerank&quot; -H &quot;Content-Type: application/json&quot; -d &apos;&#123;&quot;query&quot;: &quot;What is the capital of France?&quot;, &quot;document&quot;: &quot;Paris is the capital of France.&quot;&#125;&apos;</span><br></pre></td></tr></table></figure>
<p>产考文档：</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9odWdnaW5nZmFjZS5jby9kb2NzL3RyYW5zZm9ybWVycy92NC40OC4yL3poL2luc3RhbGxhdGlvbiMlRTclQTYlQkIlRTclQkElQkYlRTYlQTglQTElRTUlQkMlOEY=" title="https://huggingface.co/docs/transformers/v4.48.2/zh/installation#%E7%A6%BB%E7%BA%BF%E6%A8%A1%E5%BC%8F">https://huggingface.co/docs/transformers/v4.48.2/zh/installation#离线模式<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0ZsYWdPcGVuL0ZsYWdFbWJlZGRpbmcvdHJlZS9tYXN0ZXIvZXhhbXBsZXMvaW5mZXJlbmNlL3JlcmFua2VyI3VzaW5nLWh1Z2dpbmdmYWNlLXRyYW5zZm9ybWVycw==" title="https://github.com/FlagOpen/FlagEmbedding/tree/master/examples/inference/reranker#using-huggingface-transformers">https://github.com/FlagOpen/FlagEmbedding/tree/master/examples/inference/reranker#using-huggingface-transformers<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>RAG</tag>
        <tag>Rerank</tag>
      </tags>
  </entry>
  <entry>
    <title>Dify知识检索准确性探讨</title>
    <url>/ai/develop/dify-retrieval.html</url>
    <content><![CDATA[<h1 id="dify知识检索准确性探讨"><a class="header-anchor" href="#dify知识检索准确性探讨">¶</a>Dify知识检索准确性探讨</h1>
<h2 id="embedding模型与rerank模型"><a class="header-anchor" href="#embedding模型与rerank模型">¶</a>Embedding模型与Rerank模型</h2>
<p>Rerank模型的主要任务是对Embedding模型初步筛选出的候选集进行重新排序，确保最相关的结果排在最前面。它通常基于更复杂的语义分析，评估候选文档和查询之间的深层次匹配关系。在Rerank阶段，模型会分析查询与候选文档之间的上下文、语义关系等信息。它可以使用诸如BERT/GPT等预训练语言模型来捕捉更细腻的语义和句子间的关系，从而对初步候选文档进行更精确的评分与排序。Rerank模型一般比Embedding模型计算更复杂，通常需要更多的计算资源，因此适合处理Embedding模型初步检索后的数据。</p>
<ul>
<li>
<p>初步检索（Embedding模型）：用户输入查询后，Embedding模型首先将查询和文档表示为向量，然后通过向量相似度计算，快速从大规模数据集中筛选出若干个候选文档或候选答案。</p>
</li>
<li>
<p>重新排序（Rerank模型）：在得到初步候选集后，Rerank模型进一步分析这些候选文档或答案与查询之间的精确匹配程度，并根据复杂的语义关系重新打分，对候选集进行排序。使用Docker安装</p>
</li>
</ul>
<p>常见的向量库（Vector Database）主要用于高效存储和检索高维向量数据（如嵌入向量），广泛应用于语义搜索、推荐系统、AI知识库等场景。以下是主流向量库及其特点，以及 <strong>Dify</strong> 知识库的选择建议：</p>
<h2 id="提升准确性"><a class="header-anchor" href="#提升准确性">¶</a>提升准确性</h2>
<p>提升知识库向量检索的准确率需要从<strong>数据预处理、模型选择、检索策略优化</strong>等多方面入手。以下是系统化的解决方案，结合最新技术实践（如 2025 年主流方法）：</p>
<hr>
<h3 id="数据预处理优化"><a class="header-anchor" href="#数据预处理优化">¶</a><strong>数据预处理优化</strong></h3>
<h4 id="文本清洗与增强"><a class="header-anchor" href="#文本清洗与增强">¶</a><strong>文本清洗与增强</strong></h4>
<table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>效果提升（实测）</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>实体标准化</strong></td>
<td>统一同义词（如&quot;AI&quot;和&quot;人工智能&quot;）、缩写扩展</td>
<td>+5%~8% Recall</td>
</tr>
<tr>
<td><strong>去噪处理</strong></td>
<td>移除HTML标签、特殊符号、停用词（保留领域关键词）</td>
<td>+3%~5% Precision</td>
</tr>
<tr>
<td><strong>领域术语增强</strong></td>
<td>注入领域词典（如医疗术语表），使用LLM生成同义表述</td>
<td>+7%~10% MRR</td>
</tr>
</tbody>
</table>
<h4 id="分块-chunking-策略"><a class="header-anchor" href="#分块-chunking-策略">¶</a><strong>分块（Chunking）策略</strong></h4>
<table>
<thead>
<tr>
<th>策略</th>
<th>适用场景</th>
<th>推荐工具</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>语义分块</strong></td>
<td>长文档（合同/论文）</td>
<td>LlamaIndex <code>SemanticSplitter</code></td>
</tr>
<tr>
<td><strong>滑动窗口重叠</strong></td>
<td>保持上下文连续性</td>
<td>重叠率15%~20%（128token窗口）</td>
</tr>
<tr>
<td><strong>表格/代码特殊处理</strong></td>
<td>结构化数据单独提取</td>
<td><span class="exturl" data-url="aHR0cDovL1Vuc3RydWN0dXJlZC5pbw==" title="http://Unstructured.io">Unstructured.io<i class="fa fa-external-link"></i></span> 或 <code>pdfplumber</code></td>
</tr>
</tbody>
</table>
<hr>
<h3 id="向量模型选型与微调"><a class="header-anchor" href="#向量模型选型与微调">¶</a><strong>向量模型选型与微调</strong></h3>
<h4 id="1-嵌入模型对比-2025主流"><a class="header-anchor" href="#1-嵌入模型对比-2025主流">¶</a>1. <strong>嵌入模型对比（2025主流）</strong></h4>
<table>
<thead>
<tr>
<th>模型</th>
<th>特点</th>
<th>MTEB检索得分（2025）</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>BAAI/bge-v3</strong></td>
<td>支持指令微调，中文优化</td>
<td>85.7</td>
</tr>
<tr>
<td><strong>Cohere Embed v4</strong></td>
<td>多语言检索强，支持1024维稀疏向量</td>
<td>87.2</td>
</tr>
<tr>
<td><strong>OpenAI text-embedding-3-large</strong></td>
<td>上下文窗口8K，价格低</td>
<td>86.9</td>
</tr>
<tr>
<td><strong>DeepSeek-Embed</strong></td>
<td>动态稀疏编码，显存占用减少40%</td>
<td>84.5</td>
</tr>
</tbody>
</table>
<h4 id="2-领域微调方法"><a class="header-anchor" href="#2-领域微调方法">¶</a>2. <strong>领域微调方法</strong></h4>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用LoRA微调示例（基于BGE模型）</span></span><br><span class="line"><span class="keyword">from</span> peft <span class="keyword">import</span> LoraConfig</span><br><span class="line"><span class="keyword">from</span> transformers <span class="keyword">import</span> AutoModel</span><br><span class="line"></span><br><span class="line">model = AutoModel.from_pretrained(<span class="string">"BAAI/bge-v3"</span>)</span><br><span class="line">peft_config = LoraConfig(</span><br><span class="line">    r=<span class="number">8</span>,  <span class="comment"># 秩</span></span><br><span class="line">    target_modules=[<span class="string">"query"</span>, <span class="string">"key"</span>, <span class="string">"value"</span>],</span><br><span class="line">    lora_alpha=<span class="number">16</span>,</span><br><span class="line">    lora_dropout=<span class="number">0.1</span></span><br><span class="line">)</span><br><span class="line">model.add_adapter(peft_config)  <span class="comment"># 添加轻量适配层</span></span><br></pre></td></tr></table></figure>
<ul>
<li><strong>数据量要求</strong>：500<sub>1000条领域QA对即可提升10%</sub>15%效果</li>
<li><strong>技巧</strong>：混合通用数据（如MS MARCO）防止过拟合</li>
</ul>
<hr>
<h3 id="检索流程优化"><a class="header-anchor" href="#检索流程优化">¶</a><strong>检索流程优化</strong></h3>
<h4 id="1-混合检索策略"><a class="header-anchor" href="#1-混合检索策略">¶</a>1. <strong>混合检索策略</strong></h4>
<table>
<thead>
<tr>
<th>方法</th>
<th>实现方式</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>向量+关键词混合</strong></td>
<td>结合BM25与余弦相似度加权（权重0.7:0.3）</td>
<td>精确术语查询</td>
</tr>
<tr>
<td><strong>多向量融合</strong></td>
<td>对标题/正文分别编码后加权</td>
<td>长文档检索</td>
</tr>
<tr>
<td><strong>递归检索</strong></td>
<td>先粗筛（低维向量），再精排（高维）</td>
<td>千万级以上知识库</td>
</tr>
</tbody>
</table>
<h4 id="重排序-rerank"><a class="header-anchor" href="#重排序-rerank">¶</a><strong>重排序（Rerank）</strong></h4>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 使用Cohere Reranker示例</span><br><span class="line">from cohere import Client</span><br><span class="line"></span><br><span class="line">co = Client(&quot;API_KEY&quot;)</span><br><span class="line">results = co.rerank(</span><br><span class="line">    query=&quot;量子计算原理&quot;,</span><br><span class="line">    documents=top_100_candidates,  # 首轮检索结果</span><br><span class="line">    top_n=10,</span><br><span class="line">    model=&quot;rerank-english-v3.0&quot;  # 中文可用rerank-multilingual-v2</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>性能对比</strong>：
<ul>
<li>无Rerank：MRR@10=0.42</li>
<li>加入Rerank：MRR@10=0.61（+45%）</li>
</ul>
</li>
</ul>
<h3 id="后处理与评估"><a class="header-anchor" href="#后处理与评估">¶</a><strong>后处理与评估</strong></h3>
<h4 id="1-动态阈值过滤"><a class="header-anchor" href="#1-动态阈值过滤">¶</a>1. <strong>动态阈值过滤</strong></h4>
<ul>
<li><strong>相似度校准</strong>：统计分布后设定动态阈值（如均值+2σ）</li>
<li><strong>领域适配</strong>：医疗领域阈值通常比通用领域高0.15~0.2</li>
</ul>
<h4 id="2-评估指标优化"><a class="header-anchor" href="#2-评估指标优化">¶</a>2. <strong>评估指标优化</strong></h4>
<table>
<thead>
<tr>
<th>指标</th>
<th>计算方法</th>
<th>目标值（行业基准）</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>MRR@10</strong></td>
<td>首位相关结果排名的倒数均值</td>
<td>&gt;0.65</td>
</tr>
<tr>
<td><strong>Recall@100</strong></td>
<td>前100结果中覆盖真实答案的比例</td>
<td>&gt;0.85</td>
</tr>
<tr>
<td><strong>Precision@5</strong></td>
<td>前5结果中正确结果的比例</td>
<td>&gt;0.75</td>
</tr>
</tbody>
</table>
<h4 id="3-持续学习机制"><a class="header-anchor" href="#3-持续学习机制">¶</a>3. <strong>持续学习机制</strong></h4>
<ul>
<li><strong>反馈闭环</strong>：记录用户点击数据，每周增量训练</li>
<li><strong>A/B测试</strong>：对比新旧模型在相同query下的MRR变化</li>
</ul>
<h2 id="常见向量库分类及对比"><a class="header-anchor" href="#常见向量库分类及对比">¶</a><strong>常见向量库分类及对比</strong></h2>
<h3 id="专用向量数据库"><a class="header-anchor" href="#专用向量数据库">¶</a><strong>专用向量数据库</strong></h3>
<table>
<thead>
<tr>
<th>名称</th>
<th>特点</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Pinecone</strong></td>
<td>全托管云服务，简单易用，支持实时更新和过滤</td>
<td>快速搭建生产级应用，适合中小团队</td>
</tr>
<tr>
<td><strong>Milvus</strong></td>
<td>开源，高性能，支持分布式和多种索引（IVF_FLAT、HNSW等）</td>
<td>大规模数据、高吞吐场景</td>
</tr>
<tr>
<td><strong>Weaviate</strong></td>
<td>开源，支持多模态和语义检索，内置GraphQL接口</td>
<td>复杂查询、多模态搜索</td>
</tr>
<tr>
<td><strong>Qdrant</strong></td>
<td>开源，Rust编写，高性能，支持过滤和稀疏向量</td>
<td>需要低延迟和高并发的场景</td>
</tr>
<tr>
<td><strong>Chroma</strong></td>
<td>轻量级，嵌入式，适合本地开发</td>
<td>原型开发或小规模应用</td>
</tr>
<tr>
<td><strong>Faiss</strong></td>
<td>Meta开源库，本地运行，需自行管理存储</td>
<td>研究或离线批量处理</td>
</tr>
</tbody>
</table>
<h3 id="扩展型数据库-支持向量"><a class="header-anchor" href="#扩展型数据库-支持向量">¶</a><strong>扩展型数据库（支持向量）</strong></h3>
<table>
<thead>
<tr>
<th>名称</th>
<th>特点</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>PostgreSQL + pgvector</strong></td>
<td>传统数据库扩展，支持向量搜索，适合已有PG生态的项目</td>
</tr>
<tr>
<td><strong>Redis + RedisSearch</strong></td>
<td>内存数据库，支持向量检索，低延迟但容量有限</td>
</tr>
<tr>
<td><strong>Elasticsearch</strong></td>
<td>支持向量搜索插件，适合结合全文检索的场景</td>
</tr>
</tbody>
</table>
<h2 id="dify-知识库的向量库选择"><a class="header-anchor" href="#dify-知识库的向量库选择">¶</a><strong>Dify 知识库的向量库选择</strong></h2>
<p>根据 <strong>Dify 官方文档</strong> 和开源代码：</p>
<ul>
<li><strong>默认使用 Chroma</strong>：轻量级，内置集成，适合快速启动和本地开发。</li>
<li><strong>支持切换其他数据库</strong>：如 Milvus、Weaviate、Qdrant 和 pgvector，用户可根据生产需求配置（需自行部署）。</li>
</ul>
<p><strong>推荐场景</strong>：</p>
<ul>
<li>开发测试：直接用 Chroma。</li>
<li>生产环境：选择 Milvus 或 Qdrant（需通过Dify的配置文件中修改向量库连接参数）。</li>
</ul>
<h2 id="常见大模型对比"><a class="header-anchor" href="#常见大模型对比">¶</a>常见大模型对比</h2>
<p>以下是2025年市面上常见大模型的对比表格，涵盖国内外主流模型，从<strong>技术架构、核心能力、适用场景、开源/闭源情况</strong>等维度进行对比：</p>
<h3 id="2025年主流大模型对比表"><a class="header-anchor" href="#2025年主流大模型对比表">¶</a><strong>2025年主流大模型对比表</strong></h3>
<table>
<thead>
<tr>
<th><strong>模型名称</strong></th>
<th><strong>发布机构</strong></th>
<th><strong>参数规模</strong></th>
<th><strong>核心能力</strong></th>
<th><strong>适用场景</strong></th>
<th><strong>开源/闭源</strong></th>
<th><strong>评测表现</strong>（SuperCLUE 2025）</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>GPT-4o</strong></td>
<td>OpenAI</td>
<td>~1.8T</td>
<td>多模态（文本/图像/音频/视频）、超长上下文（100万Token）、复杂推理</td>
<td>科研分析、跨行业决策、全媒体生成</td>
<td>闭源（API）</td>
<td>总分80.4（理科87.3，文科77.1）</td>
</tr>
<tr>
<td><strong>DeepSeek-V3</strong></td>
<td>深度求索</td>
<td>6710亿</td>
<td>低成本训练（仅600万美元）、STEM领域强（代码正确率91%）、长文本处理</td>
<td>学术研究、代码生成、论文写作</td>
<td>开源</td>
<td>总分68.3（理科72.0，文科78.2）</td>
</tr>
<tr>
<td><strong>Gemini 2.0 Ultra</strong></td>
<td>Google DeepMind</td>
<td>1.56T</td>
<td>原生多模态、132种语言实时翻译、超低时延（&lt;20ms）</td>
<td>全球化协作、实时翻译、边缘计算</td>
<td>闭源</td>
<td>总分68.2（理科72.6，文科76.6）</td>
</tr>
<tr>
<td><strong>Claude 3.5 Sonnet</strong></td>
<td>Anthropic</td>
<td>未公开</td>
<td>200K~1M Token上下文、混合推理（快思慢想）、高安全性</td>
<td>法律分析、医疗诊断、合规对话</td>
<td>闭源</td>
<td>总分67.7（理科71.4，文科77.2）</td>
</tr>
<tr>
<td><strong>通义千问 3.0</strong></td>
<td>阿里巴巴</td>
<td>720亿</td>
<td>中文理解领先、电商/金融优化、支持百万级上下文</td>
<td>企业服务、供应链管理、金融客服</td>
<td>部分开源</td>
<td>总分66.2（理科67.4，文科80.0）</td>
</tr>
<tr>
<td><strong>豆包 1.5 Pro</strong></td>
<td>字节跳动</td>
<td>未公开</td>
<td>稀疏MoE架构、低成本高效训练、擅长短视频脚本生成</td>
<td>社交媒体运营、短视频创作</td>
<td>闭源</td>
<td>总分66.5（理科72.3，文科76.6）</td>
</tr>
<tr>
<td><strong>文心一言 4.0</strong></td>
<td>百度</td>
<td>未公开</td>
<td>中文优化（国考题87%正确率）、知识图谱增强、多模态生成</td>
<td>教育、医疗、金融问答</td>
<td>闭源</td>
<td>总分62.2（理科61.4，文科79.5）</td>
</tr>
<tr>
<td><strong>Kimi Chat</strong></td>
<td>月之暗面</td>
<td>未公开</td>
<td>200万汉字长文本处理、法律/科研文档分析</td>
<td>法律合同、学术文献、长文本总结</td>
<td>闭源</td>
<td>总分59.4（理科58.1，文科76.6）</td>
</tr>
<tr>
<td><strong>盘古大模型 5.5</strong></td>
<td>华为</td>
<td>万亿级</td>
<td>工业级多模态（12K图像识别）、复杂推理优化、制造业/气象预测</td>
<td>智能制造、自动驾驶、气象分析</td>
<td>闭源</td>
<td>未上榜（行业定制化强）</td>
</tr>
<tr>
<td><strong>Llama-3.3-70B</strong></td>
<td>Meta</td>
<td>700亿</td>
<td>开源高性能、推理速度提升200%、多语言优化</td>
<td>中小企业定制、学术研究</td>
<td>开源</td>
<td>总分59.4（理科66.4，文科72.9）</td>
</tr>
</tbody>
</table>
<h3 id="关键对比维度补充"><a class="header-anchor" href="#关键对比维度补充">¶</a><strong>关键对比维度补充</strong></h3>
<ol>
<li>
<p><strong>开源 vs 闭源</strong></p>
<ul>
<li><strong>开源（DeepSeek、Llama）</strong>：适合企业私有化部署、学术研究。</li>
<li><strong>闭源（GPT-4o、Claude）</strong>：API调用方便，但依赖厂商服务。</li>
</ul>
</li>
<li>
<p><strong>语言侧重</strong></p>
<ul>
<li><strong>英文优先</strong>：GPT-4o、Gemini、Claude。</li>
<li><strong>中文优化</strong>：通义千问、文心一言、DeepSeek-V3。</li>
</ul>
</li>
<li>
<p><strong>多模态支持</strong></p>
<ul>
<li><strong>最强</strong>：GPT-4o（8K视频生成）、Gemini（实时多语言翻译）、盘古（工业级多模态）。</li>
<li><strong>较弱</strong>：DeepSeek（仅文本/代码）、Kimi（长文本处理突出）。</li>
</ul>
</li>
<li>
<p><strong>成本与部署</strong></p>
<ul>
<li><strong>低成本</strong>：DeepSeek（训练成本仅GPT-4o的1/70）、豆包（稀疏MoE架构高效）。</li>
<li><strong>高成本</strong>：GPT-4o（私有化部署超500万美元/年）、盘古（需华为昇腾芯片支持）。</li>
</ul>
</li>
</ol>
<h3 id="选型建议"><a class="header-anchor" href="#选型建议">¶</a><strong>选型建议</strong></h3>
<ul>
<li><strong>企业通用AI</strong>：GPT-4o（综合最强）、Claude（安全合规）。</li>
<li><strong>中文场景</strong>：通义千问（电商/金融）、文心一言（教育/医疗）。</li>
<li><strong>开源研究</strong>：DeepSeek-V3（STEM强）、Llama-3（社区生态好）。</li>
<li><strong>长文本分析</strong>：Kimi Chat（200万字上下文）、Claude（200K Token）。</li>
</ul>
<p>参考链接：</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9tcC53ZWl4aW4ucXEuY29tL3MvLTdqdnhfOE05bDJ5RHlqbTgwa29zdw==" title="https://mp.weixin.qq.com/s/-7jvx_8M9l2yDyjm80kosw">https://mp.weixin.qq.com/s/-7jvx_8M9l2yDyjm80kosw<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>RAG</tag>
        <tag>Dify</tag>
      </tags>
  </entry>
  <entry>
    <title>MCP简介及其工作原理</title>
    <url>/ai/mcp/mcp-intro.html</url>
    <content><![CDATA[<blockquote>
<p>摘要：Anthropic推出的模型上下文协议（MCP）通过标准化接口实现大语言模型与内外部工具的安全互联。本文详解MCP工作原理，揭示Prompt工程如何成为工具调用的关键枢纽，为AI应用生态提供&quot;即插即用&quot;式扩展能力。</p>
</blockquote>
<h1 id="模型上下文协议-model-context-protocol-mcp"><a class="header-anchor" href="#模型上下文协议-model-context-protocol-mcp">¶</a>模型上下文协议（Model Context Protocol，MCP）</h1>
<p>模型上下文协议（Model Context Protocol，MCP），是由 Anthropic推出的开源协议，旨在实现大语言模型与外部数据源和工具的集成，用来在大模型和数据源之间建立安全双向的连接。</p>
<p>MCP 是一个开放协议，它标准化了应用程序向 LLM 提供上下文的方式。可以将 MCP 视为AI应用的 USB-C 端口。正如USB-C提供了一种标准化的方式将您的设备连接到各种外围设备和配件一样，MCP提供了一种标准化的方式将 AI 模型连接到不同的数据源和工具。</p>
<h2 id="mcp架构"><a class="header-anchor" href="#mcp架构">¶</a>MCP架构</h2>
<p>MCP遵循客户端 - 服务器架构，包含以下几个核心部分：</p>
<ul>
<li>MCP 主机（MCP Hosts）：发起请求的 AI 应用程序，比如聊天机器人、AI 驱动的 IDE 等。</li>
<li>MCP 客户端（MCP Clients）：在主机程序内部，与 MCP 服务器保持 1:1 的连接。</li>
<li>MCP 服务器（MCP Servers）：为 MCP 客户端提供上下文、工具和提示信息。</li>
<li>本地资源（Local Resources）：本地计算机中可供 MCP 服务器安全访问的资源，如文件、数据库。</li>
<li>远程资源（Remote Resources）：MCP 服务器可以连接到的远程资源，如通过 API 提供的数据。</li>
</ul>
<p><img src="https://mmbiz.qpic.cn/mmbiz_jpg/Z6bicxIx5naI1MWDBvOvKpP7WAY0ebsGatkrs3bVbq5L6fJtc84ttRIqibekVSZ4qQSqkMHIu939qvZWcCmmh9eA/640?wx_fmt=other&amp;from=appmsg&amp;tp=wxpic&amp;wxfrom=5&amp;wx_lazy=1" alt="MCP架构"></p>
<h2 id="大模型如何识别并调用mcp"><a class="header-anchor" href="#大模型如何识别并调用mcp">¶</a>大模型如何识别并调用MCP</h2>
<p>LLM（模型）是在什么时候确定使用哪些工具的呢？Anthropic为我们提供了详细的解释，当用户提出一个问题时：</p>
<ul>
<li>客户端（Claude Desktop / Cursor）将问题发送给 LLM。</li>
<li>LLM 分析可用的工具，并决定使用哪一个（或多个），实际上<strong>模型是依靠prompt来识别当前可用的工具有哪些</strong>。</li>
<li>客户端通过 MCP Server 执行所选的工具。</li>
<li>工具的执行结果被送回给 LLM。LLM 结合执行结果，归纳总结后生成自然语言展示给用户！</li>
</ul>
<p><img src="https://mmbiz.qpic.cn/mmbiz_jpg/Z6bicxIx5naI1MWDBvOvKpP7WAY0ebsGaNticc7Noo7wAiaHZCo4AHJeJxdSs5J9hv7D2giaI3UPft6JnYWiaqSeWQg/640?wx_fmt=other&amp;from=appmsg&amp;tp=wxpic&amp;wxfrom=5&amp;wx_lazy=1" alt="MCP工作原理"></p>
<p>我们可以参考MCP官方提供的<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21vZGVsY29udGV4dHByb3RvY29sL3B5dGhvbi1zZGsvYmxvYi9tYWluL2V4YW1wbGVzL2NsaWVudHMvc2ltcGxlLWNoYXRib3QvbWNwX3NpbXBsZV9jaGF0Ym90L21haW4ucHk=" title="https://github.com/modelcontextprotocol/python-sdk/blob/main/examples/clients/simple-chatbot/mcp_simple_chatbot/main.py">python-sdk client example<i class="fa fa-external-link"></i></span>为讲解示例</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">def</span> <span class="title">start</span><span class="params">(self)</span> -&gt; <span class="keyword">None</span>:</span></span><br><span class="line">    <span class="string">"""Main chat session handler."""</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="comment"># 初始化所有的 mcp server</span></span><br><span class="line">        <span class="keyword">for</span> server <span class="keyword">in</span> self.servers:</span><br><span class="line">            <span class="keyword">try</span>:</span><br><span class="line">                <span class="keyword">await</span> server.initialize()</span><br><span class="line">            <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">                logging.error(<span class="string">f"Failed to initialize server: <span class="subst">&#123;e&#125;</span>"</span>)</span><br><span class="line">                <span class="keyword">await</span> self.cleanup_servers()</span><br><span class="line">                <span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 获取所有的 tools 命名为 all_tools</span></span><br><span class="line">        all_tools = []</span><br><span class="line">        <span class="keyword">for</span> server <span class="keyword">in</span> self.servers:</span><br><span class="line">            tools = <span class="keyword">await</span> server.list_tools()</span><br><span class="line">            all_tools.extend(tools)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将所有的 tools 的功能描述格式化成字符串供 LLM 使用</span></span><br><span class="line">        tools_description = <span class="string">"\n"</span>.join([tool.format_for_llm() <span class="keyword">for</span> tool <span class="keyword">in</span> all_tools])</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 询问 LLM（Claude） 应该使用哪些工具。</span></span><br><span class="line">        system_message = (</span><br><span class="line">            <span class="string">"You are a helpful assistant with access to these tools:\n\n"</span></span><br><span class="line">            <span class="string">f"<span class="subst">&#123;tools_description&#125;</span>\n"</span></span><br><span class="line">            <span class="string">"Choose the appropriate tool based on the user's question. "</span></span><br><span class="line">            <span class="string">"If no tool is needed, reply directly.\n\n"</span></span><br><span class="line">            <span class="string">"IMPORTANT: When you need to use a tool, you must ONLY respond with "</span></span><br><span class="line">            <span class="string">"the exact JSON object format below, nothing else:\n"</span></span><br><span class="line">            <span class="string">"&#123;\n"</span></span><br><span class="line">            <span class="string">'    "tool": "tool-name",\n'</span></span><br><span class="line">            <span class="string">'    "arguments": &#123;\n'</span></span><br><span class="line">            <span class="string">'        "argument-name": "value"\n'</span></span><br><span class="line">            <span class="string">"    &#125;\n"</span></span><br><span class="line">            <span class="string">"&#125;\n\n"</span></span><br><span class="line">            <span class="string">"After receiving a tool's response:\n"</span></span><br><span class="line">            <span class="string">"1. Transform the raw data into a natural, conversational response\n"</span></span><br><span class="line">            <span class="string">"2. Keep responses concise but informative\n"</span></span><br><span class="line">            <span class="string">"3. Focus on the most relevant information\n"</span></span><br><span class="line">            <span class="string">"4. Use appropriate context from the user's question\n"</span></span><br><span class="line">            <span class="string">"5. Avoid simply repeating the raw data\n\n"</span></span><br><span class="line">            <span class="string">"Please use only the tools that are explicitly defined above."</span></span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">        messages = [&#123;<span class="string">"role"</span>: <span class="string">"system"</span>, <span class="string">"content"</span>: system_message&#125;]</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            <span class="keyword">try</span>:</span><br><span class="line">                user_input = input(<span class="string">"You: "</span>).strip().lower()</span><br><span class="line">                <span class="keyword">if</span> user_input <span class="keyword">in</span> [<span class="string">"quit"</span>, <span class="string">"exit"</span>]:</span><br><span class="line">                    logging.info(<span class="string">"\nExiting..."</span>)</span><br><span class="line">                    <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">                messages.append(&#123;<span class="string">"role"</span>: <span class="string">"user"</span>, <span class="string">"content"</span>: user_input&#125;)</span><br><span class="line"></span><br><span class="line">                <span class="comment"># 将 system_message 和用户消息输入一起发送给 LLM</span></span><br><span class="line">                llm_response = self.llm_client.get_response(messages)</span><br><span class="line">                logging.info(<span class="string">"\nAssistant: %s"</span>, llm_response)</span><br><span class="line"></span><br><span class="line">                <span class="comment"># 处理 LLM 的输出（如果有 tool call 则执行对应的工具）</span></span><br><span class="line">                result = <span class="keyword">await</span> self.process_llm_response(llm_response)</span><br><span class="line"></span><br><span class="line">                <span class="comment"># 如果 result 与 llm_response 不同，说明执行了 tool call （有额外信息了）</span></span><br><span class="line">                <span class="comment"># 则将 tool call 的结果重新发送给 LLM 进行处理。</span></span><br><span class="line">                <span class="keyword">if</span> result != llm_response:</span><br><span class="line">                    messages.append(&#123;<span class="string">"role"</span>: <span class="string">"assistant"</span>, <span class="string">"content"</span>: llm_response&#125;)</span><br><span class="line">                    messages.append(&#123;<span class="string">"role"</span>: <span class="string">"system"</span>, <span class="string">"content"</span>: result&#125;)</span><br><span class="line"></span><br><span class="line">                    final_response = self.llm_client.get_response(messages)</span><br><span class="line">                    logging.info(<span class="string">"\nFinal response: %s"</span>, final_response)</span><br><span class="line">                    messages.append(</span><br><span class="line">                        &#123;<span class="string">"role"</span>: <span class="string">"assistant"</span>, <span class="string">"content"</span>: final_response&#125;</span><br><span class="line">                    )</span><br><span class="line">                <span class="comment"># 否则代表没有执行 tool call，则直接将 LLM 的输出返回给用户。</span></span><br><span class="line">                <span class="keyword">else</span>:</span><br><span class="line">                    messages.append(&#123;<span class="string">"role"</span>: <span class="string">"assistant"</span>, <span class="string">"content"</span>: llm_response&#125;)</span><br><span class="line"></span><br><span class="line">            <span class="keyword">except</span> KeyboardInterrupt:</span><br><span class="line">                logging.info(<span class="string">"\nExiting..."</span>)</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        <span class="keyword">await</span> self.cleanup_servers()</span><br></pre></td></tr></table></figure>
<p>根据上面的源码分析，可以看出<strong>工具文档至关重要</strong>。模型依赖于工具描述文本来理解和选择适用的工具，这意味着精心编写的工具名称、文档字符串（docstring）以及参数说明显得尤为重要。鉴于MCP的选择机制基于prompt实现，理论上任何模型只要能够提供相应的工具描述就能与MCP兼容使用。</p>
<p>参考文档：</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9tb2RlbGNvbnRleHRwcm90b2NvbC5pby9pbnRyb2R1Y3Rpb24=" title="https://modelcontextprotocol.io/introduction">https://modelcontextprotocol.io/introduction<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21vZGVsY29udGV4dHByb3RvY29sL3B5dGhvbi1zZGsvYmxvYi9tYWluL2V4YW1wbGVzL2NsaWVudHMvc2ltcGxlLWNoYXRib3QvbWNwX3NpbXBsZV9jaGF0Ym90L21haW4ucHk=" title="https://github.com/modelcontextprotocol/python-sdk/blob/main/examples/clients/simple-chatbot/mcp_simple_chatbot/main.py">https://github.com/modelcontextprotocol/python-sdk/blob/main/examples/clients/simple-chatbot/mcp_simple_chatbot/main.py<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>MCP</tag>
      </tags>
  </entry>
  <entry>
    <title>MCP Gateway：零代码改造，把现有 API 发布为MCP服务</title>
    <url>/ai/mcp/mcp-gateway-release.html</url>
    <content><![CDATA[<h1 id="mcp-gateway：零代码改造-把现有-api-发布为mcp服务"><a class="header-anchor" href="#mcp-gateway：零代码改造-把现有-api-发布为mcp服务">¶</a>MCP Gateway：零代码改造，把现有 API 发布为MCP服务</h1>
<blockquote>
<p>上篇讲了 MCP 协议与网关设计思路；本篇介绍落地实现 <strong>MCP Gateway</strong>：导入 OpenAPI/Swagger 文档即可一键发布 MCP 服务，大模型通过标准协议直接调用你的接口，业务侧零侵入。Spring Boot + MCP SDK，GitHub 已开源，欢迎 Star。</p>
</blockquote>
<hr>
<p><img src="/images/McpGateway.gif" alt="MCP Gateway.gif"></p>
<h2 id="一-mcp-gateway-是什么"><a class="header-anchor" href="#一-mcp-gateway-是什么">¶</a>一、MCP Gateway 是什么</h2>
<p><strong>MCP Gateway</strong> 是一个基于 Spring Boot 的 MCP 服务网关，核心做一件事：<strong>把 OpenAPI/Swagger 文档自动转换成 MCP 工具</strong>，让大模型通过标准 MCP 协议（<code>tools/list</code>、<code>tools/call</code>）直接调用你的业务接口，而无需改业务代码。</p>
<p>你可以把它理解成「API 与 MCP 之间的翻译官」：在管理端导入或手动录入 API 文档，配置好转发地址与认证，发布后就会得到一个独立的 MCP Server；客户端（如 Claude Desktop、Cursor、自研 AI 应用）按 MCP 规范连上来，就能把文档里的接口当作「工具」来调用。原有后端保持 REST/OpenAPI 形态不变，零侵入。</p>
<p>项目已在 GitHub 开源，地址：<strong><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2Zhbnpob25nd2VpL21jcC1nYXRld2F5" title="https://github.com/fanzhongwei/mcp-gateway">https://github.com/fanzhongwei/mcp-gateway<i class="fa fa-external-link"></i></span></strong>，欢迎 Star 与使用。</p>
<hr>
<h2 id="二-核心能力与特性"><a class="header-anchor" href="#二-核心能力与特性">¶</a>二、核心能力与特性</h2>
<ul>
<li><strong>API 文档来源灵活</strong>：支持通过 <strong>OpenAPI/Swagger</strong>（URL 或本地上传）导入，也支持在管理端<strong>手动录入</strong>接口信息；录入或导入后统一转换为 MCP 工具定义，配置驱动、零侵入业务服务。（Postman、cURL、Apifox 等导入方式正在开发中。）</li>
<li><strong>标准协议</strong>：完整支持 MCP 的 <code>tools/list</code>、<code>tools/call</code> 等能力，兼容主流 MCP 客户端。</li>
<li><strong>多服务 / 多租户</strong>：按「服务」维度管理多套 API-Docs，每个服务对应一个 MCP Server 端点（如按 <code>serviceId</code> 路由），便于区分业务域或租户。</li>
<li><strong>认证与鉴权</strong>：支持 Bearer Token 等认证方式，网关侧统一校验后再转发到后端，与上篇设计中的「自研传输入口 + 统一鉴权」一致。</li>
<li><strong>技术栈</strong>：Spring Boot 3.x、SpringDoc OpenAPI、官方 MCP Java SDK（协议层复用），便于与现有 Java 技术体系集成。</li>
</ul>
<hr>
<h2 id="三-快速开始"><a class="header-anchor" href="#三-快速开始">¶</a>三、快速开始</h2>
<h3 id="环境要求"><a class="header-anchor" href="#环境要求">¶</a>环境要求</h3>
<ul>
<li>JDK 17+</li>
<li>Maven 3.6+</li>
<li>Node.js 18+（构建前端时需要）</li>
<li>PostgreSQL 14+（用于管理端数据存储）</li>
</ul>
<h3 id="构建与运行"><a class="header-anchor" href="#构建与运行">¶</a>构建与运行</h3>
<p>在项目根目录执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn clean package</span><br></pre></td></tr></table></figure>
<p>然后启动服务：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java -jar mcp-gateway-server/target/mcp-gateway-server.jar</span><br></pre></td></tr></table></figure>
<h3 id="使用流程"><a class="header-anchor" href="#使用流程">¶</a>使用流程</h3>
<p>启动服务后访问管理端，按以下步骤即可从 API 文档发布到在 MCP 客户端里使用。</p>
<h4 id="1-创建业务系统-维护环境"><a class="header-anchor" href="#1-创建业务系统-维护环境">¶</a>1. 创建业务系统、维护环境</h4>
<ul>
<li>在管理端进入<strong>业务系统</strong>管理，新建一个业务系统（例如「订单服务」「用户中心」），用于归类将要暴露为 MCP 的接口。</li>
<li>为每个业务系统维护<strong>环境</strong>（如开发、测试、生产）：在对应业务系统下进入「环境管理」，添加环境并填写<strong>环境名称</strong>、<strong>Base URL</strong>（该环境下 API 的根地址）等。后续导入或录入的接口会按「业务系统 + 环境」维度管理，发布 MCP 时也会按环境转发请求。</li>
</ul>
<h4 id="2-导入-api-文档"><a class="header-anchor" href="#2-导入-api-文档">¶</a>2. 导入 API 文档</h4>
<ul>
<li><strong>方式一：导入文档</strong><br>
在<strong>接口管理</strong>中选择已创建的业务系统及环境，选择「导入」→ 选择 <strong>OpenAPI/Swagger</strong>（支持填写文档 URL 或本地上传），按页面提示完成导入，系统会解析并生成接口列表。</li>
<li><strong>方式二：手动录入</strong><br>
选择「手动录入」，逐项填写接口的路径、方法、摘要、参数（Query/Header/Body）等，保存后同样进入该业务系统下的接口列表。</li>
</ul>
<p>同一业务系统下可同时存在导入与手动录入的接口，可编辑、删除或补充。</p>
<h4 id="3-创建并发布-mcp-server"><a class="header-anchor" href="#3-创建并发布-mcp-server">¶</a>3. 创建并发布 MCP Server</h4>
<ul>
<li>在<strong>MCP 服务</strong>中点击「创建 MCP 服务」，填写服务名称、描述等基本信息。</li>
<li><strong>配置资源组合</strong>：为当前 MCP 服务选择要暴露的「业务系统 + 环境」组合，并勾选该组合下要作为 MCP 工具暴露的接口（可多选）；可为接口或组合设置便于大模型识别的名称。</li>
<li><strong>服务端点</strong>（该 MCP Server 对外提供的 URL 路径）和<strong>访问令牌</strong>（Access Token，客户端调用时需携带）由系统自动生成，不可修改；发布后可在服务详情中查看与复制。</li>
<li>保存后点击<strong>发布</strong>。发布成功后，该 MCP Server 处于运行中状态，即可被 MCP 客户端连接。</li>
</ul>
<h4 id="4-如何在-mcp-客户端上使用"><a class="header-anchor" href="#4-如何在-mcp-客户端上使用">¶</a>4. 如何在 MCP 客户端上使用</h4>
<p>在管理端 <strong>MCP 服务</strong> 列表点击 <strong>查看</strong> 打开服务详情，详情页提供 <strong>访问令牌</strong>、<strong>服务端点</strong>、<strong>客户端配置</strong> 三项复制按钮，复制后填入客户端即可。</p>
<p><strong>配置示例</strong>（将 <code>url</code>、令牌换为详情页复制的值）：</p>
<ul>
<li><strong>Cursor</strong>（<code>mcpServers</code> 下）：</li>
</ul>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">"mcp-gateway-http": &#123;</span><br><span class="line">  "timeout": 60,</span><br><span class="line">  "type": "streamableHttp",</span><br><span class="line">  "url": "https://your-gateway-host/mcp-gateway/your-service-endpoint",</span><br><span class="line">  "headers": &#123; "Authorization": "Bearer your-access-token" &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>Claude Desktop</strong>（<code>claude_desktop_config.json</code> 的 <code>mcp_servers</code> 下）：</li>
</ul>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">"mcp-gateway-http": &#123;</span><br><span class="line">  "url": "https://your-gateway-host/mcp-gateway/your-service-endpoint",</span><br><span class="line">  "api_key": "your-access-token"</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr>
<h2 id="四-适用场景与小结"><a class="header-anchor" href="#四-适用场景与小结">¶</a>四、适用场景与小结</h2>
<h3 id="适用场景"><a class="header-anchor" href="#适用场景">¶</a>适用场景</h3>
<ul>
<li><strong>存量 API 快速接入 MCP</strong>：已有大量 REST/OpenAPI 接口，希望<strong>不改业务代码</strong>就接入 Claude、Cursor 等 MCP 生态，让大模型直接「会调用」你的接口。</li>
<li><strong>多服务 / 多租户统一出口</strong>：需要按业务域或租户暴露多套 API，每个 MCP Server 对应一个端点与令牌，便于隔离与权限控制。</li>
<li><strong>配置驱动、少写适配代码</strong>：希望用导入文档 + 勾选接口的方式把 OpenAPI 转成 MCP 工具，避免为每个接口手写 MCP 适配层。</li>
<li><strong>内部工具与自研 AI 应用</strong>：企业内已有 OpenAPI/Swagger 文档的内部系统，希望通过统一网关暴露给内部 AI 助手或自研 MCP 客户端，便于检索、调用与审计。</li>
<li><strong>多环境与灰度发布</strong>：同一业务系统配置开发/测试/生产等环境，按环境发布不同 MCP 服务或切换 Base URL，方便在 AI 侧做联调与发布验证。</li>
</ul>
<h3 id="小结"><a class="header-anchor" href="#小结">¶</a>小结</h3>
<p>上篇从协议与传输层拆解了「为什么要自研网关、为何推荐无状态」；本篇介绍的 <strong>MCP Gateway</strong> 即是该思路的落地实现：从导入或录入 API 文档、配置资源组合与认证，到发布 MCP Server、在客户端拷贝配置使用，<strong>一条龙完成</strong>，业务侧零侵入，让传统 REST 服务也能无缝对接大模型生态。若你正打算把现有 API 暴露给 Claude、Cursor 或自研 AI 应用，欢迎试用并反馈。</p>
<hr>
<p><strong>项目地址</strong>：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2Zhbnpob25nd2VpL21jcC1nYXRld2F5" title="https://github.com/fanzhongwei/mcp-gateway">https://github.com/fanzhongwei/mcp-gateway<i class="fa fa-external-link"></i></span></p>
<p>欢迎使用、提 Issue 和 PR，如果对你有帮助，也欢迎给个 Star。</p>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>MCP</tag>
        <tag>网关</tag>
      </tags>
  </entry>
  <entry>
    <title>MCP Server开发实践</title>
    <url>/ai/mcp/mcp-server-practice.html</url>
    <content><![CDATA[<h1 id="mcp-server开发实践"><a class="header-anchor" href="#mcp-server开发实践">¶</a>MCP Server开发实践</h1>
<p>上一篇文章我们介绍了MCP的基本原理，但是对于开发者来说更关心如何实现我们的自己的MCP Server，接下来我们将使用MCP提供的java sdk和spring-ai来实现一个MCP Server。</p>
<h2 id="构建spring-boot服务"><a class="header-anchor" href="#构建spring-boot服务">¶</a>构建Spring Boot服务</h2>
<p>技术选型：</p>
<ul>
<li>Spring Boot 3.4.2，因为Spring AI支持的Spring Boot版本为3.4.x</li>
</ul>
<blockquote>
<p>Spring AI supports Spring Boot 3.4.x. When Spring Boot 3.5.x is released, we will support that as well.</p>
</blockquote>
<ul>
<li>JDK 17，Spring Boot3需要使用JDK 17</li>
<li>spring-ai-mcp-server-webmvc-spring-boot-starter 1.0.0-M6，Spring AI支持三种方式提供服务，这里采用<code>webmvc</code>
<ul>
<li>Standard Input/Output (STDIO) - spring-ai-starter-mcp-server</li>
<li>Spring MVC (Server-Sent Events) - spring-ai-starter-mcp-server-webmvc</li>
<li>Spring WebFlux (Reactive SSE) - spring-ai-starter-mcp-server-webflux</li>
</ul>
</li>
<li>httpclient5，将现有服务的HTTP接口暴露为MCP Server</li>
</ul>
<p><code>pom.xml</code>文件示例：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependencyManagement</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-dependencies<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.4.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">type</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">type</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">scope</span>&gt;</span>import<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencyManagement</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- MCP --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.ai<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-ai-mcp-server-webmvc-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0.0-M6<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- Lombok --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.projectlombok<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lombok<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">optional</span>&gt;</span>true<span class="tag">&lt;/<span class="name">optional</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- Apache HttpClient --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.httpcomponents.client5<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>httpclient5<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.httpcomponents.core5<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>httpcore5<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.httpcomponents.core5<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>httpcore5-h2<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- Spring Boot Starter --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h2 id="mcp配置"><a class="header-anchor" href="#mcp配置">¶</a>MCP配置</h2>
<ul>
<li>application.yml</li>
</ul>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">mcp-name</span></span><br><span class="line">  <span class="attr">ai:</span></span><br><span class="line">    <span class="attr">mcp:</span></span><br><span class="line">      <span class="attr">server:</span></span><br><span class="line">        <span class="attr">name:</span> <span class="string">mcp-name</span></span><br><span class="line">        <span class="attr">version:</span> <span class="number">1.0</span><span class="number">.0</span></span><br><span class="line">        <span class="attr">type:</span> <span class="string">SYNC</span></span><br><span class="line">        <span class="attr">sse-endpoint:</span> <span class="string">/sse</span></span><br></pre></td></tr></table></figure>
<ul>
<li>Server实现，这里直接通过转发的方式将现有的HTTP接口暴露为MCP Server</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2025/03/27 14:52</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SmdMcpService</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> RestTemplate restTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;smd.service.url&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> String smdServiceUrl;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Tool</span>(name = <span class="string">"getSmdInfo"</span>, description = <span class="string">"获取表结构信息"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getSmdInfo</span><span class="params">(@ToolParam(description = <span class="string">"业务系统"</span>)</span> String businessSystem,</span></span><br><span class="line"><span class="function">                             @<span class="title">ToolParam</span><span class="params">(description = <span class="string">"表名"</span>)</span> Set&lt;String&gt; tableNames) </span>&#123;</span><br><span class="line">        Map&lt;String, Object&gt; params = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        params.put(<span class="string">"businessSystem"</span>, businessSystem);</span><br><span class="line">        params.put(<span class="string">"tableNames"</span>, tableNames);</span><br><span class="line"></span><br><span class="line">        ResponseEntity&lt;String&gt; response = restTemplate.postForEntity(</span><br><span class="line">                smdServiceUrl + <span class="string">"/mcp/api/getSmdInfo"</span>,</span><br><span class="line">                params,</span><br><span class="line">                String<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">        <span class="keyword">return</span> response.getBody();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Tool</span>(name = <span class="string">"getCRUDCode"</span>, description = <span class="string">"根据表名生成增删改查代码"</span>)</span><br><span class="line">    <span class="keyword">public</span> List&lt;Map&lt;String, Object&gt;&gt; getCRUDByTable(<span class="meta">@ToolParam</span>(description = <span class="string">"业务系统"</span>) String businessSystem,</span><br><span class="line">                                    <span class="meta">@ToolParam</span>(description = <span class="string">"表名"</span>) Set&lt;String&gt; tableNames,</span><br><span class="line">                                    <span class="meta">@ToolParam</span>(description = <span class="string">"模块名，非必填"</span>) String moduleName</span><br><span class="line">                                 ) &#123;</span><br><span class="line">        Map&lt;String, Object&gt; params = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        params.put(<span class="string">"businessSystem"</span>, businessSystem);</span><br><span class="line">        params.put(<span class="string">"tableNames"</span>, tableNames);</span><br><span class="line">        params.put(<span class="string">"moduleName"</span>, moduleName);</span><br><span class="line">        params.put(<span class="string">"author"</span>, <span class="string">"smd-mcp"</span>);</span><br><span class="line"></span><br><span class="line">        HttpEntity&lt;Map&lt;String, Object&gt;&gt; httpEntity =  <span class="keyword">new</span> HttpEntity&lt;&gt;(params);</span><br><span class="line">        ResponseEntity&lt;List&lt;Map&lt;String, Object&gt;&gt;&gt; response = restTemplate.exchange(</span><br><span class="line">                smdServiceUrl + <span class="string">"/mcp/api/crud"</span>,</span><br><span class="line">                HttpMethod.POST,</span><br><span class="line">                httpEntity,</span><br><span class="line">                <span class="keyword">new</span> ParameterizedTypeReference&lt;List&lt;Map&lt;String, Object&gt;&gt;&gt;() &#123;&#125;);</span><br><span class="line">        <span class="keyword">return</span> response.getBody();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>将对应服务暴露为Mcp Tools</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * MCP配置类</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> AI Assistant</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2024/03/21</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">McpConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ToolCallbackProvider <span class="title">smdToolCallbackProvider</span><span class="params">(SmdMcpService smdMcpService, RulesMcpService rulesMcpService)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> MethodToolCallbackProvider.builder()</span><br><span class="line">                .toolObjects(smdMcpService, rulesMcpService)</span><br><span class="line">                .build();</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="测试mcp"><a class="header-anchor" href="#测试mcp">¶</a>测试MCP</h2>
<p>使用支持MCP的客户端进行测试，客户端支持情况可查看：<span class="exturl" data-url="aHR0cHM6Ly9tb2RlbGNvbnRleHRwcm90b2NvbC5pby9jbGllbnRz" title="https://modelcontextprotocol.io/clients">https://modelcontextprotocol.io/clients<i class="fa fa-external-link"></i></span></p>
<p>这里使用Cursor进行测试：</p>
<p>配置<code>mcp.json</code></p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"mcpServers"</span>: &#123;</span><br><span class="line">      <span class="attr">"mcp-name"</span>: &#123;</span><br><span class="line">        <span class="attr">"url"</span>: <span class="string">"http://localhost:8089/sse"</span>,</span><br><span class="line">        <span class="attr">"env"</span>: &#123;</span><br><span class="line">          <span class="attr">"API_KEY"</span>: <span class="string">"value"</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>配置后即可看到MCP Server提供的Tools，如下图所示：<br>
<img src="https://mmbiz.qpic.cn/mmbiz_png/14Blum0GwI7zfMWdeXcoUMs1Rv8Qc6atGoTgk9KpicA9CxVpLI8z2A8FgbFlt6p0GPekfhbxAic6giaqTj6TzeL2A/640?wx_fmt=png&amp;from=appmsg" alt="Cursor MCP配置.png"></p>
<h2 id="mcp思考"><a class="header-anchor" href="#mcp思考">¶</a>MCP思考</h2>
<p>MCP 还处于发展初期，现阶段更重要的是生态构建，基于统一标准下构筑的生态也会正向的促进整个领域的发展。</p>
<p>对于普通开发者我们可以直接使用已有的MCP工具平台：<span class="exturl" data-url="aHR0cHM6Ly9tY3Auc28v" title="https://mcp.so/">https://mcp.so/<i class="fa fa-external-link"></i></span></p>
<p>对于企业，我们可以通过代理的方式将已有HTTP接口暴露为MCP Server：</p>
<ul>
<li>零侵入改造：无需修改原有HTTP服务代码即可获得MCP能力</li>
<li>跨模型兼容：让原本不支持MCP的传统服务获得与大模型生态无缝对接的能力</li>
<li>低成本投入：已有业务接入MCP生态的改造周期大幅缩短</li>
</ul>
<p><strong>后续考虑将其做成MCP代理服务，通过简单配置即可将已有业务转换为MCP Server，为AI智能体打开潘多拉的魔盒</strong></p>
<p>参考文档：</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9tb2RlbGNvbnRleHRwcm90b2NvbC5pby9zZGsvamF2YS9tY3Atc2VydmVy" title="https://modelcontextprotocol.io/sdk/java/mcp-server">https://modelcontextprotocol.io/sdk/java/mcp-server<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9kb2NzLnNwcmluZy5pby9zcHJpbmctYWkvcmVmZXJlbmNlL2FwaS9tY3AvbWNwLXNlcnZlci1ib290LXN0YXJ0ZXItZG9jcy5odG1s" title="https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html">https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9kb2NzLnNwcmluZy5pby9zcHJpbmctYWkvcmVmZXJlbmNlL2dldHRpbmctc3RhcnRlZC5odG1s" title="https://docs.spring.io/spring-ai/reference/getting-started.html">https://docs.spring.io/spring-ai/reference/getting-started.html<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>MCP</tag>
        <tag>实践</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring AI MCP服务内存泄漏排查实录：从堆分析到源码修复</title>
    <url>/ai/mcp/mcp-memory-leak.html</url>
    <content><![CDATA[<h1 id="spring-ai-mcp服务内存泄漏排查实录：从堆分析到源码修复"><a class="header-anchor" href="#spring-ai-mcp服务内存泄漏排查实录：从堆分析到源码修复">¶</a>Spring AI MCP服务内存泄漏排查实录：从堆分析到源码修复</h1>
<blockquote>
<p>Spring AI构建的MCP服务频繁OOM？本文完整记录问题排查全链路：<br>
1️⃣ 通过MAT精准定位WebMvcSseServerTransport中未释放的会话占99.59%内存<br>
2️⃣ 发现SDK 0.7.0版本仅在异常时清理会话的设计缺陷<br>
3️⃣ 升级1.0.0版本后仍存在异步连接残留问题<br>
4️⃣ 最后采用&quot;心跳检测+异常熔断&quot;双保险机制<br>
👉 关键方案：定时发送轻量级消息sendNotification，实现自动回收失效连接，彻底解决内存泄漏。附完整堆分析截图、源码对比！</p>
</blockquote>
<p>上一篇文章我们介绍了如何使用Spring AI快速构建一个MCP Server：<span class="exturl" data-url="aHR0cHM6Ly9tcC53ZWl4aW4ucXEuY29tL3MvQnZuMklWdUFRTnJoc3NpRlNTTVROUT9wb2NfdG9rZW49SEd0bVltaWp5NFpicVJ1My1LRmVENHNNY2xKc2hSYWJMTzBxZGVGZw==" title="https://mp.weixin.qq.com/s/Bvn2IVuAQNrhssiFSSMTNQ?poc_token=HGtmYmijy4ZbqRu3-KFeD4sMclJshRabLO0qdeFg">Spring AI+MCP实战：零代码改造将传统服务接入大模型生态<i class="fa fa-external-link"></i></span>，但是服务启动一段时间后，总是是内存溢出，导致MCP服务时不时就不可用，必须得重启才能解决。</p>
<p>配置java参数当内存溢出时自动转储堆，然后分析堆内存，终于发现了罪魁祸首，接下来就让我们一起来看看罪魁祸首是谁。</p>
<h2 id="分析堆内存"><a class="header-anchor" href="#分析堆内存">¶</a>分析堆内存</h2>
<p>使用<code>MAT（Eclipse Memory Analyzer）</code>打开自动转储的堆文件，加载完成后打开<code>Leak Suspects</code>可以发现内存泄露的可疑点：</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/14Blum0GwI7qnJcG2Fy61JgxGRia35guVicKn8Fp6IwEasDAuhldqkPvWL4HrXz3jRDS41WrFYHbcbGBIiaqI37MA/640?wx_fmt=png&amp;from=appmsg" alt="内存泄露疑点.png"></p>
<p>从上图可以发现，由<code>io.modelcontextprotocol.server.transport.WebMvcSseServerTransport @ 0x700730098</code>对象持有的<code>java.util.concurrent.ConcurrentHashMap$Node[]</code>占用了99.59%的内存。</p>
<p>到这里基本就可以确定内存泄露的罪魁祸首就是<code>WebMvcSseServerTransport</code>，具体是其中的哪个对象呢，让我们继续分析。</p>
<p>点击<code>Eclipse Memory Analyzer</code>上的<code>dominator_tree</code>可以看到堆内存中对象的树形结构信息，这里根据<code>Retained Heap</code>降序排列，可以看到占用内存最多的对象<code>java.util.concurrent.ConcurrentHashMap$Node[]</code>，右键 -》Path To GC Roots -》with all references，可以看到泄露对象到gc roots的路径，可以清晰的看到是被谁持有但一直未释放。</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/14Blum0GwI7qnJcG2Fy61JgxGRia35guVjyjLhlxSicRkso4VVxPup46Id1YP2dHEccRhnuAT2N1EqT27hK536Ug/640?wx_fmt=png&amp;from=appmsg" alt="内存泄露GcRoots.png"></p>
<p>到这里我们知道了是<code>WebMvcSseServerTransport#sessions</code>属性持有了有大量的Map节点，但一直没释放，最终导致JVM内存溢出了。</p>
<blockquote>
<p>MAT工具的文档详见文末的参考链接</p>
</blockquote>
<h2 id="源码分析"><a class="header-anchor" href="#源码分析">¶</a>源码分析</h2>
<p>之前2025年3月份根据官方文档: <span class="exturl" data-url="aHR0cHM6Ly9kb2NzLnNwcmluZy5pby9zcHJpbmctYWkvcmVmZXJlbmNlL2FwaS9tY3AvbWNwLXNlcnZlci1ib290LXN0YXJ0ZXItZG9jcy5odG1s" title="https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html">https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html<i class="fa fa-external-link"></i></span>集成的时候，引入starter为：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.ai<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-ai-mcp-server-webmvc-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0.0-M6<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>其中引入的<code>io.modelcontextprotocol.sdk:mcp-spring-webmvc</code>的版本为0.7.0，<code>WebMvcSseServerTransport</code>的**核心实现（省略部分与本次内存溢出问题无关的代码）**如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class WebMvcSseServerTransport implements ServerMcpTransport &#123;</span><br><span class="line"></span><br><span class="line">    private final ConcurrentHashMap&lt;String, ClientSession&gt; sessions;</span><br><span class="line">    </span><br><span class="line">    private ServerResponse handleSseConnection(ServerRequest request) &#123;</span><br><span class="line">        if (this.isClosing) &#123;</span><br><span class="line">            return ServerResponse.status(HttpStatus.SERVICE_UNAVAILABLE).body(&quot;Server is shutting down&quot;);</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            String sessionId = UUID.randomUUID().toString();</span><br><span class="line">            logger.debug(&quot;Creating new SSE connection for session: &#123;&#125;&quot;, sessionId);</span><br><span class="line"></span><br><span class="line">            try &#123;</span><br><span class="line">                return ServerResponse.sse((sseBuilder) -&gt; &#123;</span><br><span class="line">                    ClientSession session = new ClientSession(sessionId, sseBuilder);</span><br><span class="line">                    this.sessions.put(sessionId, session);</span><br><span class="line"></span><br><span class="line">                    try &#123;</span><br><span class="line">                        session.sseBuilder.id(session.id).event(&quot;endpoint&quot;).data(this.messageEndpoint);</span><br><span class="line">                    &#125; catch (Exception e) &#123;</span><br><span class="line">                        logger.error(&quot;Failed to poll event from session queue: &#123;&#125;&quot;, e.getMessage());</span><br><span class="line">                        sseBuilder.error(e);</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                &#125;);</span><br><span class="line">            &#125; catch (Exception e) &#123;</span><br><span class="line">                logger.error(&quot;Failed to send initial endpoint event to session &#123;&#125;: &#123;&#125;&quot;, sessionId, e.getMessage());</span><br><span class="line">                // 只有出现异常的时候才将session移除</span><br><span class="line">                this.sessions.remove(sessionId);</span><br><span class="line">                return ServerResponse.status(HttpStatus.INTERNAL_SERVER_ERROR).build();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    private static class ClientSession &#123;</span><br><span class="line">        private final String id;</span><br><span class="line">        private final ServerResponse.SseBuilder sseBuilder;</span><br><span class="line"></span><br><span class="line">        ClientSession(String id, ServerResponse.SseBuilder sseBuilder) &#123;</span><br><span class="line">            this.id = id;</span><br><span class="line">            this.sseBuilder = sseBuilder;</span><br><span class="line">            WebMvcSseServerTransport.logger.debug(&quot;Session &#123;&#125; initialized with SSE emitter&quot;, id);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        void close() &#123;</span><br><span class="line">            WebMvcSseServerTransport.logger.debug(&quot;Closing session: &#123;&#125;&quot;, this.id);</span><br><span class="line"></span><br><span class="line">            try &#123;</span><br><span class="line">                // session关闭时，只将sseBuilder设置为完成</span><br><span class="line">                this.sseBuilder.complete();</span><br><span class="line">                WebMvcSseServerTransport.logger.debug(&quot;Successfully completed SSE emitter for session &#123;&#125;&quot;, this.id);</span><br><span class="line">            &#125; catch (Exception e) &#123;</span><br><span class="line">                WebMvcSseServerTransport.logger.warn(&quot;Failed to complete SSE emitter for session &#123;&#125;: &#123;&#125;&quot;, this.id, e.getMessage());</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从上面代码可以发现，在MCP的ClientSession关闭时，只是将sseBuilder设置为完成；仅当<code>handleSseConnection</code>中出现异常时才会将ClientSession从sessions中移除，估计是想客户端一直复用这个连接吧。</p>
<p>那么正常情况下，这个session就会一直存在于<code>WebMvcSseServerTransport#sessions</code>属性中，而<code>WebMvcSseServerTransport</code>对象在MCP服务运行时会一直存活，因此一段时间后MCP服务就会因为<code>WebMvcSseServerTransport#sessions</code>属性内存泄露最终导致jvm的内存溢出。</p>
<h2 id="sdk升级"><a class="header-anchor" href="#sdk升级">¶</a>SDK升级</h2>
<p>经过上面的源码分析，我们知道了内存泄露的具体原因是<code>WebMvcSseServerTransport#sessions</code>的ClientSession一直在增长，因此只需要在ClientSession完成或异常的时候将其从sessions中移除即可。</p>
<p>经查看最新的官方文档: <span class="exturl" data-url="aHR0cHM6Ly9kb2NzLnNwcmluZy5pby9zcHJpbmctYWkvcmVmZXJlbmNlL2FwaS9tY3AvbWNwLXNlcnZlci1ib290LXN0YXJ0ZXItZG9jcy5odG1s" title="https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html">https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html<i class="fa fa-external-link"></i></span>，其中对于<code>MCP Server Boot Starter</code>已经做了升级，升级到<code>1.0.0</code>版本后，可以看到最新的版本是由<code>WebMvcSseServerTransportProvider</code>来管理see请求的，处理sse请求的源码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> ServerResponse <span class="title">handleSseConnection</span><span class="params">(ServerRequest request)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.isClosing) &#123;</span><br><span class="line">        <span class="keyword">return</span> ServerResponse.status(HttpStatus.SERVICE_UNAVAILABLE).body(<span class="string">"Server is shutting down"</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        String sessionId = UUID.randomUUID().toString();</span><br><span class="line">        logger.debug(<span class="string">"Creating new SSE connection for session: &#123;&#125;"</span>, sessionId);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> ServerResponse.sse((sseBuilder) -&gt; &#123;</span><br><span class="line">                sseBuilder.onComplete(() -&gt; &#123;</span><br><span class="line">                    logger.debug(<span class="string">"SSE connection completed for session: &#123;&#125;"</span>, sessionId);</span><br><span class="line">                    <span class="keyword">this</span>.sessions.remove(sessionId);</span><br><span class="line">                &#125;);</span><br><span class="line">                sseBuilder.onTimeout(() -&gt; &#123;</span><br><span class="line">                    logger.debug(<span class="string">"SSE connection timed out for session: &#123;&#125;"</span>, sessionId);</span><br><span class="line">                    <span class="keyword">this</span>.sessions.remove(sessionId);</span><br><span class="line">                &#125;);</span><br><span class="line">                WebMvcMcpSessionTransport sessionTransport = <span class="keyword">new</span> WebMvcMcpSessionTransport(sessionId, sseBuilder);</span><br><span class="line">                McpServerSession session = <span class="keyword">this</span>.sessionFactory.create(sessionTransport);</span><br><span class="line">                <span class="keyword">this</span>.sessions.put(sessionId, session);</span><br><span class="line"></span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    sseBuilder.id(sessionId).event(<span class="string">"endpoint"</span>).data(<span class="keyword">this</span>.baseUrl + <span class="keyword">this</span>.messageEndpoint + <span class="string">"?sessionId="</span> + sessionId);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                    logger.error(<span class="string">"Failed to send initial endpoint event: &#123;&#125;"</span>, e.getMessage());</span><br><span class="line">                    sseBuilder.error(e);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">            &#125;, Duration.ZERO);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            logger.error(<span class="string">"Failed to send initial endpoint event to session &#123;&#125;: &#123;&#125;"</span>, sessionId, e.getMessage());</span><br><span class="line">            <span class="keyword">this</span>.sessions.remove(sessionId);</span><br><span class="line">            <span class="keyword">return</span> ServerResponse.status(HttpStatus.INTERNAL_SERVER_ERROR).build();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其中可以看到，在请求完成、超时和异常情况下都会将session移除，这样应该就能解决内存溢出问题了。</p>
<p><strong>问题到这里真的解决了吗？</strong></p>
<h2 id="解决方案"><a class="header-anchor" href="#解决方案">¶</a>解决方案</h2>
<p>经过验证，发现升级后的SDK里面<code>WebMvcSseServerTransport#sessions</code>中存放的<code>McpServerSession</code>还是会一直存在，并没有移除。</p>
<p>可能是异步请求的请求，Cursor之类的客户端在创建MCP连接后，即使Cursor关闭后也没有主动去<strong>告诉服务端断开连接</strong>，也就是不会触发<code>onComplete</code>、<code>onTimeout</code>方法去将session移除。</p>
<p>于是，我们可以定时去检测<code>WebMvcSseServerTransport#sessions</code>中的<code>McpServerSession</code>是否还存活，如果客户端已经把连接关闭了，那么就将session移除。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * session管理，避免内存溢出</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2025/07/01 16:18</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableScheduling</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">McpSessionConfig</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> WebMvcSseServerTransportProvider sseServerTransportProvider;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;mcp.session.health-check-enabled:true&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">boolean</span> healthCheckEnabled;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;mcp.session.health-check-interval:1800000&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">long</span> healthCheckInterval;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;mcp.session.health-check-timeout:5000&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">long</span> healthCheckTimeout;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 定时任务：定期执行session存活检测</span></span><br><span class="line"><span class="comment">     * 从sseServerTransportProvider中获取sessions，遍历检测session是否存活，</span></span><br><span class="line"><span class="comment">     * 使用sendNotification进行检测，检测失败的session需要自动移除</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Scheduled</span>(fixedRateString = <span class="string">"$&#123;mcp.session.health-check-interval:1800000&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">checkSessionHealth</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 检查是否启用健康检测</span></span><br><span class="line">        <span class="keyword">if</span> (!healthCheckEnabled) &#123;</span><br><span class="line">            log.debug(<span class="string">"MCP session健康检测已禁用"</span>);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            log.info(<span class="string">"开始执行MCP session存活检测任务"</span>);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 获取所有活跃的sessions</span></span><br><span class="line">            Map&lt;String, McpServerSession&gt; sessionsMap = (Map&lt;String, McpServerSession&gt;) getFieldValue(sseServerTransportProvider, <span class="string">"sessions"</span>);</span><br><span class="line">            <span class="keyword">if</span> (<span class="keyword">null</span> == sessionsMap || sessionsMap.isEmpty()) &#123;</span><br><span class="line">                log.info(<span class="string">"当前没有活跃的MCP sessions"</span>);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line">            log.info(<span class="string">"检测到 &#123;&#125; 个活跃sessions，开始进行存活检测"</span>, sessionsMap.size());</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 遍历检测每个session的存活状态</span></span><br><span class="line">            List&lt;CompletableFuture&lt;Void&gt;&gt; futures = <span class="keyword">new</span> CopyOnWriteArrayList&lt;&gt;();</span><br><span class="line">            <span class="keyword">for</span> (McpServerSession session : sessionsMap.values()) &#123;</span><br><span class="line">                <span class="comment">// 使用sendNotification进行检测，getCurrentTime是一个MCP的Tool方法</span></span><br><span class="line">                <span class="comment">// 发送一个轻量级的ping消息来检测连接是否有效</span></span><br><span class="line">                Mono&lt;Void&gt; mono = session.sendNotification(<span class="string">"getCurrentTime"</span>);</span><br><span class="line">                futures.add(mono.toFuture());</span><br><span class="line">            &#125;</span><br><span class="line">            CompletableFuture.allOf(futures.toArray(<span class="keyword">new</span> CompletableFuture[<span class="number">0</span>])).join();</span><br><span class="line">            log.info(<span class="string">"MCP session存活检测任务执行完成，剩余session数量：&#123;&#125;"</span>, sessionsMap.size());</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            log.error(<span class="string">"执行MCP session存活检测任务时发生异常"</span>, e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> Object <span class="title">getFieldValue</span><span class="params">(Object obj, String fieldName)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Field field = FieldUtils.getField(obj.getClass(), fieldName, <span class="keyword">true</span>);</span><br><span class="line">            field.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">            <span class="keyword">return</span> field.get(obj);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            log.error(<span class="string">"获取字段值时发生异常"</span>, e);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>发送检测消息后<code>session.sendNotification(&quot;getCurrentTime&quot;)</code>，Tomcat中间件会检测到该sse连接是否还存活，如果连接已断开会有如下异常信息(省略部分堆栈)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2025-07-02 14:01:31.064 [http-nio-8089-exec-13] ERROR o.a.c.c.C.[.[.[.[dispatcherServlet] - Servlet.service() for servlet [dispatcherServlet] threw exception</span><br><span class="line">java.io.IOException: 断开的管道</span><br><span class="line">	at java.base/sun.nio.ch.FileDispatcherImpl.write0(Native Method)</span><br><span class="line">	at java.base/sun.nio.ch.SocketDispatcher.write(SocketDispatcher.java:62)</span><br><span class="line">	at java.base/sun.nio.ch.IOUtil.writeFromNativeBuffer(IOUtil.java:132)</span><br><span class="line">	at java.base/sun.nio.ch.IOUtil.write(IOUtil.java:97)</span><br><span class="line">	at java.base/sun.nio.ch.IOUtil.write(IOUtil.java:53)</span><br><span class="line">	at java.base/sun.nio.ch.SocketChannelImpl.write(SocketChannelImpl.java:532)</span><br><span class="line">	...</span><br><span class="line">	at io.modelcontextprotocol.server.transport.WebMvcSseServerTransportProvider$WebMvcMcpSessionTransport.lambda$sendMessage$0(WebMvcSseServerTransportProvider.java:364)</span><br><span class="line">	at reactor.core.publisher.MonoRunnable.subscribe(MonoRunnable.java:49)</span><br><span class="line">	at reactor.core.publisher.Mono.subscribe(Mono.java:4576)</span><br><span class="line">	at reactor.core.publisher.Mono.subscribeWith(Mono.java:4641)</span><br><span class="line">	at reactor.core.publisher.Mono.toFuture(Mono.java:5153)</span><br><span class="line">	at com.teddy.smd.mcp.config.McpSessionConfig.checkSessionHealth(McpSessionConfig.java:74)</span><br></pre></td></tr></table></figure>
<p>Tomcat中间件在<code>org.apache.catalina.core.AsyncContextImpl#doInternalDispatch</code>中检测到异常后，会将连接设置为完成：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doInternalDispatch</span><span class="params">()</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (log.isTraceEnabled()) &#123;</span><br><span class="line">        <span class="keyword">this</span>.logDebug(<span class="string">"intDispatch"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Runnable runnable = <span class="keyword">this</span>.dispatch;</span><br><span class="line">        <span class="keyword">this</span>.dispatch = <span class="keyword">null</span>;</span><br><span class="line">        runnable.run();</span><br><span class="line">        <span class="keyword">if</span> (!<span class="keyword">this</span>.request.isAsync()) &#123;</span><br><span class="line">            <span class="keyword">this</span>.fireOnComplete();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">catch</span> (RuntimeException x) &#123;</span><br><span class="line">        AtomicBoolean result = <span class="keyword">new</span> AtomicBoolean();</span><br><span class="line">        <span class="keyword">this</span>.request.getCoyoteRequest().action(ActionCode.IS_IO_ALLOWED, result);</span><br><span class="line">        <span class="keyword">if</span> (!result.get()) &#123;</span><br><span class="line">            <span class="comment">// 将连接设置为完成</span></span><br><span class="line">            <span class="keyword">this</span>.fireOnComplete();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (x.getCause() <span class="keyword">instanceof</span> ServletException) &#123;</span><br><span class="line">            <span class="keyword">throw</span> (ServletException)x.getCause();</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (x.getCause() <span class="keyword">instanceof</span> IOException) &#123;</span><br><span class="line">            <span class="keyword">throw</span> (IOException)x.getCause();</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> ServletException(x);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>连接设置为完成后，最终会触发<code>WebMvcSseServerTransport#handleSseConnection</code>方法中的<code>sseBuilder.onComplete</code>回调中将session进行移除</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">sseBuilder.onComplete(() -&gt; &#123;</span><br><span class="line">	logger.debug(<span class="string">"SSE connection completed for session: &#123;&#125;"</span>, sessionId);</span><br><span class="line">	sessions.remove(sessionId);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>检测的日志输出情况如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2025-07-02 14:01:31.053 [scheduling-1] INFO  c.t.smd.mcp.config.McpSessionConfig - 检测到 15 个活跃sessions，开始进行存活检测</span><br><span class="line">2025-07-02 14:01:31.062 [scheduling-1] ERROR i.m.s.t.WebMvcSseServerTransportProvider - Failed to send message to session a9af3b98-35fc-4769-b84f-fedbdc384977: ServletOutputStream failed to flush: java.io.IOException: 断开的管道</span><br><span class="line">2025-07-02 14:01:31.063 [scheduling-1] ERROR i.m.s.t.WebMvcSseServerTransportProvider - Failed to send message to session 7b88ebdc-abd3-41fe-be98-75ed217dcfe6: ServletOutputStream failed to flush: java.io.IOException: 断开的管道</span><br><span class="line">...</span><br><span class="line">2025-07-02 14:01:31.063 [scheduling-1] INFO  c.t.smd.mcp.config.McpSessionConfig - MCP session存活检测任务执行完成，剩余session数量：4</span><br></pre></td></tr></table></figure>
<p>到这里终于完美解决MCP服务因为连接泄露导致的内存溢出问题。</p>
<p>参考链接：</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9kb2NzLnNwcmluZy5pby9zcHJpbmctYWkvcmVmZXJlbmNlL2FwaS9tY3AvbWNwLXNlcnZlci1ib290LXN0YXJ0ZXItZG9jcy5odG1s" title="https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html">https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html<i class="fa fa-external-link"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9oZWxwLmVjbGlwc2Uub3JnL2xhdGVzdC9pbmRleC5qc3A/dG9waWM9L29yZy5lY2xpcHNlLm1hdC51aS5oZWxwL3dlbGNvbWUuaHRtbA==" title="https://help.eclipse.org/latest/index.jsp?topic=/org.eclipse.mat.ui.help/welcome.html">https://help.eclipse.org/latest/index.jsp?topic=/org.eclipse.mat.ui.help/welcome.html<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>性能优化</tag>
        <tag>MCP</tag>
      </tags>
  </entry>
  <entry>
    <title>深入拆解MCP协议到MCP网关设计思路</title>
    <url>/ai/mcp/mcp-protocol-gateway.html</url>
    <content><![CDATA[<h1 id="深入拆解mcp协议到mcp网关设计思路"><a class="header-anchor" href="#深入拆解mcp协议到mcp网关设计思路">¶</a>深入拆解MCP协议到MCP网关设计思路</h1>
<blockquote>
<p>本文介绍自研 MCP 网关的研发思路：在网关侧如何实现 MCP 的两种传输形态（有状态 SSE 与无状态 HTTP），如何借鉴官方 Java SDK 的传输层实现，以及为何推荐采用“自研 Controller + 复用 SDK 协议层”而非直接使用 SDK 的 Servlet 提供能力。</p>
</blockquote>
<hr>
<p>之前的文章 <span class="exturl" data-url="aHR0cHM6Ly9tcC53ZWl4aW4ucXEuY29tL3MvQnZuMklWdUFRTnJoc3NpRlNTTVROUQ==" title="https://mp.weixin.qq.com/s/Bvn2IVuAQNrhssiFSSMTNQ">Spring AI+MCP实战：零代码改造将传统服务接入大模型生态<i class="fa fa-external-link"></i></span> 介绍了如何使用Spring AI简单几行代码将传统服务接入大模型，但有没有更简单的方式统一管理业务接口然后组合各个业务系统接口并一键发布为MCP服务呢，接下来我们就从MCP协议入手介绍MCP网关的设计思路。</p>
<h2 id="一-mcp-协议与传输层简述"><a class="header-anchor" href="#一-mcp-协议与传输层简述">¶</a>一、MCP 协议与传输层简述</h2>
<p><strong>MCP（Model Context Protocol）</strong> 是一套用于客户端与“模型上下文”服务之间通信的协议，基于 <strong>JSON-RPC 2.0</strong>：请求（Request）需要响应，通知（Notification）不需要响应。传输层负责把 JSON-RPC 消息在 HTTP 上承载，常见有三种形态：</p>
<ol>
<li><strong>有状态 SSE（Server-Sent Events）</strong>：先通过 GET 建立一条长连接（SSE 流），服务端返回一个“消息端点” URL，客户端后续通过 POST 到该端点发送消息；服务端通过 SSE 向客户端推送响应或通知。</li>
<li><strong>无状态 Streamable HTTP</strong>：不维护会话，每次请求都是独立的 POST；若为 Request 则响应 200 + JSON，若为 Notification 则响应 202 Accepted。规范要求 <code>Accept</code> 头同时包含 <code>application/json</code> 和 <code>text/event-stream</code>。</li>
<li><strong>Streamable HTTP（单端点 + Session）</strong>：同一 URL 支持 GET / POST / DELETE。POST 的 <code>initialize</code> 会创建 Session 并返回 <code>MCP-Session-Id</code>；后续 POST 带该头处理消息，GET 带该头可建立 SSE 流（支持 <code>Last-Event-ID</code> 恢复）；DELETE 用于结束 Session。</li>
</ol>
<h3 id="1-1-mcp-消息中的-method"><a class="header-anchor" href="#1-1-mcp-消息中的-method">¶</a>1.1 MCP 消息中的 method</h3>
<p>每条 JSON-RPC 消息都带有 <code>method</code> 字段，用于标识请求或通知的类型。下表以 <strong>2025-11-25</strong> 规范为准：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21vZGVsY29udGV4dHByb3RvY29sL21vZGVsY29udGV4dHByb3RvY29sL2Jsb2IvbWFpbi9zY2hlbWEvMjAyNS0xMS0yNS9zY2hlbWEuanNvbg==" title="https://github.com/modelcontextprotocol/modelcontextprotocol/blob/main/schema/2025-11-25/schema.json">https://github.com/modelcontextprotocol/modelcontextprotocol/blob/main/schema/2025-11-25/schema.json<i class="fa fa-external-link"></i></span>。</p>
<p><strong>客户端 → 服务端（Request，需响应）</strong></p>
<table>
<thead>
<tr>
<th>method</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>initialize</code></td>
<td>建连时首条请求，协商协议版本与能力</td>
</tr>
<tr>
<td><code>ping</code></td>
<td>保活探测</td>
</tr>
<tr>
<td><code>resources/list</code></td>
<td>列出服务端可读资源（支持分页 cursor）</td>
</tr>
<tr>
<td><code>resources/templates/list</code></td>
<td>列出资源模板</td>
</tr>
<tr>
<td><code>resources/read</code></td>
<td>按 URI 读取资源内容</td>
</tr>
<tr>
<td><code>resources/subscribe</code></td>
<td>订阅资源变更通知</td>
</tr>
<tr>
<td><code>resources/unsubscribe</code></td>
<td>取消资源订阅</td>
</tr>
<tr>
<td><code>prompts/list</code></td>
<td>列出提示词/模板</td>
</tr>
<tr>
<td><code>prompts/get</code></td>
<td>获取指定提示词内容</td>
</tr>
<tr>
<td><code>tools/list</code></td>
<td>列出服务端提供的工具</td>
</tr>
<tr>
<td><code>tools/call</code></td>
<td>调用指定工具（可带 task 参数做异步任务）</td>
</tr>
<tr>
<td><code>tasks/get</code></td>
<td>查询任务状态</td>
</tr>
<tr>
<td><code>tasks/result</code></td>
<td>获取任务结果负载</td>
</tr>
<tr>
<td><code>tasks/cancel</code></td>
<td>取消任务（替代对任务的 notifications/cancelled）</td>
</tr>
<tr>
<td><code>tasks/list</code></td>
<td>分页列出任务</td>
</tr>
<tr>
<td><code>logging/setLevel</code></td>
<td>设置服务端日志级别</td>
</tr>
<tr>
<td><code>completion/complete</code></td>
<td>请求补全建议（如提示词/资源模板参数补全）</td>
</tr>
</tbody>
</table>
<p><strong>客户端 → 服务端（Notification，无响应）</strong></p>
<table>
<thead>
<tr>
<th>method</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>notifications/initialized</code></td>
<td>初始化完成后通知服务端</td>
</tr>
<tr>
<td><code>notifications/cancelled</code></td>
<td>取消某条已发出的请求（不含 task，task 用 tasks/cancel）</td>
</tr>
<tr>
<td><code>notifications/progress</code></td>
<td>进度通知（与请求中的 progressToken 关联）</td>
</tr>
<tr>
<td><code>notifications/tasks/status</code></td>
<td>任务状态变更通知（可选）</td>
</tr>
<tr>
<td><code>notifications/roots/list_changed</code></td>
<td>客户端根目录列表变更通知</td>
</tr>
</tbody>
</table>
<p><strong>服务端 → 客户端（Request，需响应）</strong></p>
<table>
<thead>
<tr>
<th>method</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>ping</code></td>
<td>保活探测</td>
</tr>
<tr>
<td><code>roots/list</code></td>
<td>向客户端请求根目录/根 URI 列表</td>
</tr>
<tr>
<td><code>sampling/createMessage</code></td>
<td>请求客户端代为调用 LLM 生成消息（可带 task 做异步）</td>
</tr>
<tr>
<td><code>elicitation/create</code></td>
<td>服务端通过客户端向用户征集信息（表单或 URL）</td>
</tr>
<tr>
<td><code>tasks/get</code></td>
<td>查询任务状态</td>
</tr>
<tr>
<td><code>tasks/result</code></td>
<td>获取任务结果负载</td>
</tr>
<tr>
<td><code>tasks/cancel</code></td>
<td>取消任务</td>
</tr>
<tr>
<td><code>tasks/list</code></td>
<td>分页列出任务</td>
</tr>
</tbody>
</table>
<p><strong>服务端 → 客户端（Notification，无响应）</strong></p>
<table>
<thead>
<tr>
<th>method</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>notifications/message</code></td>
<td>日志消息（logging）</td>
</tr>
<tr>
<td><code>notifications/progress</code></td>
<td>进度通知</td>
</tr>
<tr>
<td><code>notifications/cancelled</code></td>
<td>取消某条请求（不含 task）</td>
</tr>
<tr>
<td><code>notifications/resources/list_changed</code></td>
<td>资源列表变更</td>
</tr>
<tr>
<td><code>notifications/resources/updated</code></td>
<td>某资源内容更新（需先 subscribe）</td>
</tr>
<tr>
<td><code>notifications/prompts/list_changed</code></td>
<td>提示词列表变更</td>
</tr>
<tr>
<td><code>notifications/tools/list_changed</code></td>
<td>工具列表变更</td>
</tr>
<tr>
<td><code>notifications/tasks/status</code></td>
<td>任务状态变更通知（可选）</td>
</tr>
<tr>
<td><code>notifications/elicitation/complete</code></td>
<td>服务端告知某次 elicitation 已完成</td>
</tr>
</tbody>
</table>
<p>网关在实现时只需按 JSON-RPC 解析 <code>method</code> 并路由到对应Method的Handler实现；协议层可复用 SDK 的 <code>McpSchema</code> 与 Session/StatelessHandler，无需为每个 method 单独实现传输逻辑。</p>
<hr>
<h2 id="二-有状态-sse-传输的研发思路"><a class="header-anchor" href="#二-有状态-sse-传输的研发思路">¶</a>二、有状态 SSE 传输的研发思路</h2>
<h3 id="2-1-整体流程"><a class="header-anchor" href="#2-1-整体流程">¶</a>2.1 整体流程</h3>
<p>网关研发时，有状态 SSE 采用“建连”与“发消息”分离的两个端点：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Client as 客户端</span><br><span class="line">    participant Gateway as 网关 Controller</span><br><span class="line">    participant Session as MCP 会话</span><br><span class="line"></span><br><span class="line">    Client-&gt;&gt;Gateway: GET /&#123;serviceId&#125;/sse  Accept: text/event-stream  Authorization: Bearer token</span><br><span class="line">    Gateway-&gt;&gt;Gateway: 校验 token、服务、协议版本</span><br><span class="line">    Gateway-&gt;&gt;Session: 创建 Session + Transport(SseEmitter) 存入 sessions[sessionId]</span><br><span class="line">    Gateway--&gt;&gt;Client: 200 + SSE 流  event: endpoint  data: messageEndpoint?sessionId=xxx</span><br><span class="line"></span><br><span class="line">    Client-&gt;&gt;Gateway: POST messageEndpoint  Body: JSON-RPC Request/Notification  sessionId 在 query</span><br><span class="line">    Gateway-&gt;&gt;Session: sessions.get(sessionId)  session.handle(message)</span><br><span class="line">    Session--&gt;&gt;Gateway: Transport.sendMessage(response)  SseEmitter.send(event message)</span><br><span class="line">    Gateway--&gt;&gt;Client: SSE event: message  data: JSON-RPC 响应</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>GET</strong>：只负责建连与下发“消息端点”；Session 在服务端用 <code>sessionId</code> 标识，并在内存中维护。</li>
<li><strong>POST 到消息端点</strong>：从 query 取 <code>sessionId</code>，找到对应 Session，反序列化 body 为 JSON-RPC 消息，交给 <code>session.handle(message)</code>；若有响应，通过同一 Session 的 Transport（例如封装了 <code>SseEmitter</code>）以 SSE 的 <code>message</code> 事件推回客户端。</li>
</ul>
<h3 id="2-2-sdk-实现与核心代码：httpservletsseservertransportprovider"><a class="header-anchor" href="#2-2-sdk-实现与核心代码：httpservletsseservertransportprovider">¶</a>2.2 SDK 实现与核心代码：HttpServletSseServerTransportProvider</h3>
<p>研发时参考官方 Java SDK 的 <code>HttpServletSseServerTransportProvider</code>（<code>mcp-core</code>）：SDK 用同一 Servlet 通过 <strong>URI 区分</strong> 建连与消息端点，单端点、单 Session 工厂，不区分服务/租户、不做 Bearer 校验。网关在实现时沿用该思路，并在入口层增加按 <code>serviceId</code> 和 token 校验。SDK 核心逻辑如下，供对照。</p>
<p><strong>建连（GET）</strong> — <code>doGet</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ... </span>&#123;</span><br><span class="line">    <span class="comment">// ... URI 校验、isClosing 等</span></span><br><span class="line">    response.setContentType(<span class="string">"text/event-stream"</span>);</span><br><span class="line">    response.setCharacterEncoding(UTF_8);</span><br><span class="line">    response.setHeader(<span class="string">"Cache-Control"</span>, <span class="string">"no-cache"</span>);</span><br><span class="line">    response.setHeader(<span class="string">"Connection"</span>, <span class="string">"keep-alive"</span>);</span><br><span class="line"></span><br><span class="line">    String sessionId = UUID.randomUUID().toString();</span><br><span class="line">    AsyncContext asyncContext = request.startAsync();</span><br><span class="line">    asyncContext.setTimeout(<span class="number">0</span>);</span><br><span class="line">    PrintWriter writer = response.getWriter();</span><br><span class="line"></span><br><span class="line">    HttpServletMcpSessionTransport sessionTransport = <span class="keyword">new</span> HttpServletMcpSessionTransport(sessionId, asyncContext, writer);</span><br><span class="line">    McpServerSession session = sessionFactory.create(sessionTransport);</span><br><span class="line">    <span class="keyword">this</span>.sessions.put(sessionId, session);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">this</span>.sendEvent(writer, ENDPOINT_EVENT_TYPE, buildEndpointUrl(sessionId));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> String <span class="title">buildEndpointUrl</span><span class="params">(String sessionId)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.baseUrl + <span class="keyword">this</span>.messageEndpoint + <span class="string">"?sessionId="</span> + sessionId;  <span class="comment">// 或 baseUrl 末尾去斜杠拼接</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>收消息（POST messageEndpoint）</strong> — <code>doPost</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doPost</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ... </span>&#123;</span><br><span class="line">    <span class="comment">// ... isClosing、URI 校验</span></span><br><span class="line">    String sessionId = request.getParameter(<span class="string">"sessionId"</span>);</span><br><span class="line">    <span class="keyword">if</span> (sessionId == <span class="keyword">null</span>) &#123; <span class="comment">/* 400 + McpError */</span> <span class="keyword">return</span>; &#125;</span><br><span class="line"></span><br><span class="line">    McpServerSession session = sessions.get(sessionId);</span><br><span class="line">    <span class="keyword">if</span> (session == <span class="keyword">null</span>) &#123; <span class="comment">/* 404 + McpError */</span> <span class="keyword">return</span>; &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 读取 body</span></span><br><span class="line">    StringBuilder body = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">    <span class="keyword">while</span> ((line = reader.readLine()) != <span class="keyword">null</span>) &#123; body.append(line); &#125;</span><br><span class="line"></span><br><span class="line">    McpTransportContext transportContext = <span class="keyword">this</span>.contextExtractor.extract(request);</span><br><span class="line">    McpSchema.JSONRPCMessage message = McpSchema.deserializeJsonRpcMessage(jsonMapper, body.toString());</span><br><span class="line"></span><br><span class="line">    session.handle(message).contextWrite(ctx -&gt; ctx.put(McpTransportContext.KEY, transportContext)).block();</span><br><span class="line">    response.setStatus(HttpServletResponse.SC_OK);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>Transport 职责</strong> — 内部类 <code>HttpServletMcpSessionTransport</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Mono&lt;Void&gt; <span class="title">sendMessage</span><span class="params">(McpSchema.JSONRPCMessage message)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> Mono.fromRunnable(() -&gt; &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            String jsonText = jsonMapper.writeValueAsString(message);</span><br><span class="line">            sendEvent(writer, MESSAGE_EVENT_TYPE, jsonText);  <span class="comment">// event: message \n data: &lt;json&gt;</span></span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            sessions.remove(sessionId);</span><br><span class="line">            asyncContext.complete();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">sendEvent</span><span class="params">(PrintWriter writer, String eventType, String data)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    writer.write(<span class="string">"event: "</span> + eventType + <span class="string">"\n"</span>);</span><br><span class="line">    writer.write(<span class="string">"data: "</span> + data + <span class="string">"\n\n"</span>);</span><br><span class="line">    writer.flush();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Mono&lt;Void&gt; <span class="title">closeGracefully</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> Mono.fromRunnable(() -&gt; &#123;</span><br><span class="line">        sessions.remove(sessionId);</span><br><span class="line">        asyncContext.complete();</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>研发思路：协议层（Session、JSON-RPC 路由）与传输层（SSE、HTTP）分离——Session 与业务逻辑复用 SDK 的 <code>McpServerSession</code>，网关只实现“按 serviceId + token 校验”的入口与基于 Spring <code>SseEmitter</code> 的 Transport，不重复造轮子。</p>
<p><strong>SDK 另一种有状态实现</strong>：<code>HttpServletStreamableServerTransportProvider</code> 采用 Streamable HTTP 单端点 + Session：同一 URL 支持 GET / POST / DELETE；POST 的 <code>initialize</code> 创建 Session 并返回 <code>MCP-Session-Id</code>，后续 POST 带 <code>mcp-session-id</code> 头处理消息，GET 带该头可建立 SSE 流（支持 <code>Last-Event-ID</code> 恢复），DELETE 结束 Session。仍是单端点、单工厂、无按服务/租户的路由与鉴权。</p>
<hr>
<h2 id="三-无状态传输的研发思路"><a class="header-anchor" href="#三-无状态传输的研发思路">¶</a>三、无状态传输的研发思路</h2>
<h3 id="3-1-整体流程"><a class="header-anchor" href="#3-1-整体流程">¶</a>3.1 整体流程</h3>
<p>网关采用无状态模式：不维护 Session，每次 POST 独立处理：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Client as 客户端</span><br><span class="line">    participant Controller as 无状态 Controller</span><br><span class="line">    participant Handler as StatelessHandler</span><br><span class="line"></span><br><span class="line">    Client-&gt;&gt;Controller: GET /&#123;serviceId&#125;/stateless</span><br><span class="line">    Controller--&gt;&gt;Client: 405 Method Not Allowed</span><br><span class="line"></span><br><span class="line">    Client-&gt;&gt;Controller: POST /&#123;serviceId&#125;/stateless  Accept+Authorization  Body: JSON-RPC Request 或 Notification</span><br><span class="line">    Controller-&gt;&gt;Controller: 校验 token、服务、Accept 反序列化 Body</span><br><span class="line">    alt Request</span><br><span class="line">        Controller-&gt;&gt;Handler: handler.handleRequest(...)</span><br><span class="line">        Handler--&gt;&gt;Controller: 200 + JSON 响应</span><br><span class="line">    else Notification</span><br><span class="line">        Controller-&gt;&gt;Handler: handler.handleNotification(...)</span><br><span class="line">        Handler--&gt;&gt;Controller: 202 Accepted</span><br><span class="line">    end</span><br><span class="line">    Controller--&gt;&gt;Client: 200 + JSON 或 202</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>GET</strong>：直接返回 405，表示该端点不支持“建连”。</li>
<li><strong>POST</strong>：
<ul>
<li>校验 <code>Accept</code> 必须同时包含 <code>application/json</code> 和 <code>text/event-stream</code>（与规范一致）。</li>
<li>若为 <strong>Request</strong>：调用无状态 Handler 的 <code>handleRequest</code>，将返回的 <code>JSONRPCResponse</code> 序列化后 200 写出。</li>
<li>若为 <strong>Notification</strong>：调用 <code>handleNotification</code>，返回 202 Accepted，无 body。</li>
</ul>
</li>
</ul>
<p>无状态不保存任何会话，无需 Session ID 与内存 Map，便于网关水平扩展，是自研网关的推荐默认形态。</p>
<h3 id="3-2-sdk-实现与核心代码：httpservletstatelessservertransport"><a class="header-anchor" href="#3-2-sdk-实现与核心代码：httpservletstatelessservertransport">¶</a>3.2 SDK 实现与核心代码：HttpServletStatelessServerTransport</h3>
<p>研发时参考官方 Java SDK 的 <code>HttpServletStatelessServerTransport</code>（<code>mcp-core</code>）：SDK 仅处理 POST、GET 返回 405，端点固定、无 <code>serviceId</code> 路由、无内置认证。网关在实现时沿用该逻辑，在入口增加按 <code>serviceId</code> 和 token 校验。SDK 核心逻辑如下，供对照。</p>
<p><strong>GET</strong> — <code>doGet</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ... </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!requestURI.endsWith(mcpEndpoint)) &#123;</span><br><span class="line">        response.sendError(HttpServletResponse.SC_NOT_FOUND);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>POST</strong> — <code>doPost</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doPost</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> ... </span>&#123;</span><br><span class="line">    <span class="comment">// ... URI、isClosing 校验</span></span><br><span class="line">    McpTransportContext transportContext = <span class="keyword">this</span>.contextExtractor.extract(request);</span><br><span class="line"></span><br><span class="line">    String accept = request.getHeader(ACCEPT);</span><br><span class="line">    <span class="keyword">if</span> (accept == <span class="keyword">null</span> || !(accept.contains(APPLICATION_JSON) &amp;&amp; accept.contains(TEXT_EVENT_STREAM))) &#123;</span><br><span class="line">        <span class="keyword">this</span>.responseError(response, HttpServletResponse.SC_BAD_REQUEST,</span><br><span class="line">            <span class="keyword">new</span> McpError(<span class="string">"Both application/json and text/event-stream required in Accept header"</span>));</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 读取 body</span></span><br><span class="line">    StringBuilder body = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">    <span class="keyword">while</span> ((line = reader.readLine()) != <span class="keyword">null</span>) &#123; body.append(line); &#125;</span><br><span class="line">    McpSchema.JSONRPCMessage message = McpSchema.deserializeJsonRpcMessage(jsonMapper, body.toString());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (message <span class="keyword">instanceof</span> McpSchema.JSONRPCRequest jsonrpcRequest) &#123;</span><br><span class="line">        McpSchema.JSONRPCResponse jsonrpcResponse = <span class="keyword">this</span>.mcpHandler</span><br><span class="line">            .handleRequest(transportContext, jsonrpcRequest)</span><br><span class="line">            .contextWrite(ctx -&gt; ctx.put(McpTransportContext.KEY, transportContext))</span><br><span class="line">            .block();</span><br><span class="line">        response.setContentType(APPLICATION_JSON);</span><br><span class="line">        response.setCharacterEncoding(UTF_8);</span><br><span class="line">        response.setStatus(HttpServletResponse.SC_OK);</span><br><span class="line">        String jsonResponseText = jsonMapper.writeValueAsString(jsonrpcResponse);</span><br><span class="line">        response.getWriter().write(jsonResponseText);</span><br><span class="line">        response.getWriter().flush();</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (message <span class="keyword">instanceof</span> McpSchema.JSONRPCNotification jsonrpcNotification) &#123;</span><br><span class="line">        <span class="keyword">this</span>.mcpHandler.handleNotification(transportContext, jsonrpcNotification)</span><br><span class="line">            .contextWrite(ctx -&gt; ctx.put(McpTransportContext.KEY, transportContext))</span><br><span class="line">            .block();</span><br><span class="line">        response.setStatus(HttpServletResponse.SC_ACCEPTED);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">this</span>.responseError(response, HttpServletResponse.SC_BAD_REQUEST,</span><br><span class="line">            <span class="keyword">new</span> McpError(<span class="string">"The server accepts either requests or notifications"</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>研发思路：无状态 Controller 不持有 Session，只做“路由 + 鉴权 + 反序列化/序列化 + 调用 Handler”，与 SDK 的 <code>McpStatelessServerHandler</code> 接口对齐，协议层完全复用 SDK。</p>
<hr>
<h2 id="四-网关研发中的选型思路：为何自研传输入口而非直接用-sdk"><a class="header-anchor" href="#四-网关研发中的选型思路：为何自研传输入口而非直接用-sdk">¶</a>四、网关研发中的选型思路：为何自研传输入口而非直接用 SDK</h2>
<h3 id="4-1-研发时的业务诉求"><a class="header-anchor" href="#4-1-研发时的业务诉求">¶</a>4.1 研发时的业务诉求</h3>
<ul>
<li><strong>多服务/多租户</strong>：网关设计上需要按 <code>serviceId</code> 路由到不同 MCP 服务（不同配置、不同工具集），即 URL 形如 <code>/mcp/service/{serviceId}/sse</code> 或 <code>.../stateless</code>。SDK 的 Servlet 是“单端点、单工厂”，无法在同一个 Servlet 里根据 path 动态选择不同 Session 工厂或 Handler，因此传输入口需要自研。</li>
<li><strong>统一认证与鉴权</strong>：研发时要求在处理 MCP 前完成校验：校验 Bearer Token、校验该 Token 是否可访问指定 <code>serviceId</code>（例如服务是否存在、是否已发布）。SDK 的 Transport 层不负责认证；若用 Filter 做，需要把“服务信息”等注入请求再在 Session/Handler 里使用。与“先验服务再建 Session/调 Handler”的流程更契合的做法是：在自研 Controller 里先校验，再创建或调用对应工厂/Handler。</li>
<li><strong>与现有技术栈一致</strong>：网关基于 Spring MVC（RestController、统一异常、Swagger 等），希望 MCP 端点也是普通 Controller 方法，便于监控、限流、日志。SDK 提供的是独立 <code>@WebServlet</code>，直接挂载则路由、文档、异常处理都要额外适配；自研 Controller 可完全按现有规范实现。</li>
</ul>
<h3 id="4-2-sdk-的适用边界-为何不能直接当网关用"><a class="header-anchor" href="#4-2-sdk-的适用边界-为何不能直接当网关用">¶</a>4.2 SDK 的适用边界（为何不能直接当网关用）</h3>
<ul>
<li><strong>单服务、单端点</strong>：SDK 的 Builder 配置的是“一个 baseUrl、一个 messageEndpoint、一个 sseEndpoint”或“一个 mcpEndpoint”，没有“路径中的 serviceId”概念，无法满足多服务网关的路由需求。</li>
<li><strong>认证与上下文</strong>：通过 <code>McpTransportContextExtractor</code> 可以从 <code>HttpServletRequest</code> 里取上下文，但“校验 token、查库校验服务”仍要在 Servlet 外自己做（例如 Filter 或包装一层），且 Session 工厂/Handler 需根据“当前请求属于哪个服务”来创建，这部分 SDK 不提供。</li>
<li><strong>会话存储</strong>：SSE/Streamable 的 Session 存在进程内 Map 中，多实例部署时需自行解决会话亲和或共享存储；无状态则无此问题。网关研发时采用“无状态优先、有状态 SSE 不作为默认推荐”的策略，在扩展性与功能之间做权衡。</li>
</ul>
<h3 id="4-3-研发选型对比"><a class="header-anchor" href="#4-3-研发选型对比">¶</a>4.3 研发选型对比</h3>
<table>
<thead>
<tr>
<th>维度</th>
<th>直接使用 SDK Servlet</th>
<th>自研网关</th>
</tr>
</thead>
<tbody>
<tr>
<td>路由</td>
<td>单端点，难以按 serviceId 拆成多服务</td>
<td><code>/mcp/service/{serviceId}/sse</code> 或 <code>.../stateless</code></td>
</tr>
<tr>
<td>认证与鉴权</td>
<td>需在 Filter 或外层实现，与 Session 解耦</td>
<td>Controller 内统一：token + service 校验后再建 Session/调 Handler</td>
</tr>
<tr>
<td>会话存储</td>
<td>进程内 Map，多实例需自行考虑</td>
<td>有状态 SSE 同样用内存 Map；无状态不存会话，易扩展</td>
</tr>
<tr>
<td>协议版本</td>
<td>随 SDK 固定</td>
<td>可在 Controller 中支持多版本（如 2025-11-25）</td>
</tr>
<tr>
<td>与 Spring 集成</td>
<td>需额外配置 Servlet、文档、异常</td>
<td>与现有 RestController、Swagger、全局异常一致</td>
</tr>
<tr>
<td>维护成本</td>
<td>低，跟随 SDK 升级即可</td>
<td>需随 MCP 规范与 SDK 接口演进做少量适配</td>
</tr>
</tbody>
</table>
<p>结论：自研网关的价值在于 <strong>按服务路由、统一认证和与 Spring 体系一致</strong>；代价是 <strong>需要自行维护传输层与协议版本的兼容</strong>。协议层仍大量复用 SDK：协议解析（<code>McpSchema</code>）、Session 逻辑（<code>McpServerSession</code> / <code>McpStreamableServerSession</code>）、无状态处理（<code>McpStatelessServerHandler</code>）、JSON 映射（<code>McpJsonMapper</code>）等，网关研发只做“换一层 HTTP 入口和鉴权”。</p>
<hr>
<h2 id="五-有状态与无状态对比及选型建议"><a class="header-anchor" href="#五-有状态与无状态对比及选型建议">¶</a>五、有状态与无状态对比及选型建议</h2>
<h3 id="5-1-有状态-sse-与无状态实现方式对比"><a class="header-anchor" href="#5-1-有状态-sse-与无状态实现方式对比">¶</a>5.1 有状态 SSE 与无状态实现方式对比</h3>
<table>
<thead>
<tr>
<th>维度</th>
<th>有状态 SSE 实现</th>
<th>无状态实现</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>会话</strong></td>
<td>服务端维护 Session（内存 Map），按 sessionId 关联请求与响应</td>
<td>无 Session，每次 POST 独立，请求与响应一一对应</td>
</tr>
<tr>
<td><strong>扩展性</strong></td>
<td>多实例需会话亲和或共享 Session 存储，扩展复杂</td>
<td>天然无状态，可任意水平扩展，无需亲和</td>
</tr>
<tr>
<td><strong>资源占用</strong></td>
<td>长连接与 Session 占用内存和连接数，连接断开需清理</td>
<td>请求结束即释放，无长连接与 Session 占用</td>
</tr>
<tr>
<td><strong>服务端推送</strong></td>
<td>支持通过 SSE 主动向客户端推送通知</td>
<td>不支持服务端主动推送，仅请求-响应</td>
</tr>
<tr>
<td><strong>实现复杂度</strong></td>
<td>GET 建连 + POST 消息端点 + Session 管理 + Transport 封装</td>
<td>仅 POST 单端点 + 鉴权 + 调用 Handler，逻辑简单</td>
</tr>
<tr>
<td><strong>适用场景</strong></td>
<td>必须“服务端主动推送”时才考虑</td>
<td>绝大多数 MCP 调用（工具列表、工具调用等）</td>
</tr>
</tbody>
</table>
<p><strong>有状态 SSE 优势</strong>：支持服务端通过 SSE 长连接主动推送消息（如进度、通知）。<br>
<strong>有状态 SSE 劣势</strong>：会话在内存、多实例不共享，扩容需会话亲和或共享存储；长连接与 Session 增加资源压力和运维复杂度；实现与排查问题都更复杂。</p>
<p><strong>无状态优势</strong>：实现简单、易水平扩展、无 Session 与长连接占用、与现有 HTTP 运维体系一致；协议层直接复用 SDK 的 <code>McpStatelessServerHandler</code>。<br>
<strong>无状态劣势</strong>：无法实现“服务端主动推送”，仅适合请求-响应模式（而多数 MCP 能力正是此类）。</p>
<h3 id="5-2-选型建议：推荐采用无状态"><a class="header-anchor" href="#5-2-选型建议：推荐采用无状态">¶</a>5.2 选型建议：推荐采用无状态</h3>
<p>综合优劣势与典型 MCP 使用场景（如 <code>initialize</code>、<code>tools/list</code>、<code>tools/call</code> 等均为请求-响应），<strong>推荐自研网关默认采用无状态实现方案</strong>，理由如下：</p>
<ol>
<li><strong>场景匹配</strong>：绝大部分 MCP 交互是“客户端发请求、服务端返结果”，不需要服务端主动推送，无状态即可满足。</li>
<li><strong>扩展与运维</strong>：无状态便于水平扩容、无需会话亲和或共享存储，部署与故障转移更简单。</li>
<li><strong>实现与维护成本</strong>：单端点 + 鉴权 + Handler，逻辑清晰，与 SDK 的 <code>HttpServletStatelessServerTransport</code> 行为一致，易于维护。</li>
<li><strong>有状态按需使用</strong>：仅在业务明确需要“服务端主动推送”时再启用有状态 SSE，并接受其扩展与运维成本。</li>
</ol>
<p>实现要点：网关在 Controller 中按 <code>serviceId</code> 路由与 token 校验后，直接复用 SDK 的 <code>McpStatelessServerHandler</code>；有状态 SSE 可作为可选能力保留，但不作为默认推荐。</p>
<h3 id="5-3-小结"><a class="header-anchor" href="#5-3-小结">¶</a>5.3 小结</h3>
<ul>
<li><strong>有状态 SSE</strong>：适合必须“服务端通过长连接主动推送”的场景；需维护 GET 建连、message 端点、Session 及基于 <code>SseEmitter</code> 的 Transport，多实例需考虑会话亲和或共享存储，<strong>建议仅在确有推送需求时使用</strong>。</li>
<li><strong>无状态</strong>：每次请求独立、无 Session，易水平扩展，与 SDK 的 <code>HttpServletStatelessServerTransport</code> 行为对齐；网关增加 serviceId 路由与认证后复用 <code>McpStatelessServerHandler</code>。<strong>推荐作为自研网关的默认实现方案。</strong></li>
<li><strong>研发思路</strong>：自研网关只做“路由 + 认证 + 与 Spring 的集成”，协议层依托 SDK；在“多服务、统一鉴权、RestController 风格”下，自研传输入口更贴合需求。</li>
</ul>
<p>以上即为自研 MCP 网关的研发思路说明：两种传输形态的对比与选型、<strong>推荐采用无状态</strong>作为默认方案，以及如何参考 SDK 实现自研传输入口。后续再结合openapi等格式的接口定义，通过简单配置即可将已有业务接口通过MCP网关发布为MCP服务，让原本不支持MCP的传统服务获得与大模型生态无缝对接的能力。</p>
<p>参考文档：</p>
<ul>
<li>MCP JSON-RPC消息格式：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21vZGVsY29udGV4dHByb3RvY29sL21vZGVsY29udGV4dHByb3RvY29sL2Jsb2IvbWFpbi9zY2hlbWEvMjAyNS0xMS0yNS9zY2hlbWEuanNvbg==" title="https://github.com/modelcontextprotocol/modelcontextprotocol/blob/main/schema/2025-11-25/schema.json">https://github.com/modelcontextprotocol/modelcontextprotocol/blob/main/schema/2025-11-25/schema.json<i class="fa fa-external-link"></i></span></li>
<li>MCP JAVA SDK：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21vZGVsY29udGV4dHByb3RvY29sL2phdmEtc2Rr" title="https://github.com/modelcontextprotocol/java-sdk">https://github.com/modelcontextprotocol/java-sdk<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>MCP</tag>
        <tag>网关</tag>
      </tags>
  </entry>
</search>
